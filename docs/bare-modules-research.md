# Bare modules research dossier

Generated by `scripts/research-bare-modules.ts` from each repo's `package.json` + README via the GitHub API. Scope: every holepunch repo matching bare-* that is a library or build-tool module (native-integration kits and example repos excluded). Do not hand-edit — re-run the script to refresh.

**167** modules · **36** full-page candidates · **102** missing from catalog · **76** native addons · **3** without a published package.json.

## Summary

| Module | Stars | Type | min Bare | Bare deps | Inbound | API methods | In catalog | Disposition |
| --- | --: | --- | --- | --: | --: | --: | :-: | --- |
| [bare-fs](https://github.com/holepunchto/bare-fs) | 26 | native | >=1.16.0 | 4 | 23 | 199 | ✓ | **page** |
| [bare-ffmpeg](https://github.com/holepunchto/bare-ffmpeg) | 13 | native | — | 0 | 1 | 0 | ✓ | catalog row |
| [bare-module](https://github.com/holepunchto/bare-module) | 12 | native | >=1.23.0 <1.29.0 | 5 | 8 | 41 | ✓ | **page** |
| [bare-media](https://github.com/holepunchto/bare-media) | 11 | pure-JS | — | 14 | 0 | 0 | ✓ | catalog row |
| [bare-node](https://github.com/holepunchto/bare-node) | 11 | pure-JS | — | 0 | 0 | 0 | ✗ | catalog row |
| [bare-runtime](https://github.com/holepunchto/bare-runtime) | 10 | pure-JS · tool | — | 5 | 0 | 2 | ✓ | catalog row |
| [bare-worker](https://github.com/holepunchto/bare-worker) | 10 | pure-JS | — | 4 | 1 | 0 | ✓ | catalog row |
| [bare-tcp](https://github.com/holepunchto/bare-tcp) | 9 | native | >=1.16.0 | 3 | 4 | 40 | ✓ | **page** |
| [bare-events](https://github.com/holepunchto/bare-events) | 9 | pure-JS | — | 0 | 26 | 0 | ✓ | catalog row |
| [bare-fetch](https://github.com/holepunchto/bare-fetch) | 9 | pure-JS | — | 7 | 3 | 43 | ✓ | **page** |
| [bare-repl](https://github.com/holepunchto/bare-repl) | 9 | native | — | 8 | 1 | 3 | ✓ | catalog row |
| [bare-module-lexer](https://github.com/holepunchto/bare-module-lexer) | 8 | native | — | 0 | 3 | 2 | ✓ | catalog row |
| [bare-rpc](https://github.com/holepunchto/bare-rpc) | 8 | pure-JS | — | 1 | 0 | 18 | ✓ | **page** |
| [bare-os](https://github.com/holepunchto/bare-os) | 8 | native | >=1.14.0 | 0 | 18 | 43 | ✓ | **page** |
| [bare-ws](https://github.com/holepunchto/bare-ws) | 8 | pure-JS | — | 5 | 2 | 0 | ✓ | catalog row |
| [bare-pack](https://github.com/holepunchto/bare-pack) | 7 | pure-JS · tool | — | 5 | 5 | 1 | ✓ | catalog row |
| [bare-channel](https://github.com/holepunchto/bare-channel) | 7 | native | >=1.7.0 | 3 | 1 | 19 | ✓ | **page** |
| [bare-make](https://github.com/holepunchto/bare-make) | 7 | pure-JS · tool | — | 5 | 1 | 4 | ✓ | **page** |
| [bare-realm](https://github.com/holepunchto/bare-realm) | 7 | native | >=1.5.0 | 0 | 1 | 3 | ✓ | catalog row |
| [bare-stream](https://github.com/holepunchto/bare-stream) | 6 | pure-JS | — | 0 | 28 | 66 | ✓ | **page** |
| [bare-pipe](https://github.com/holepunchto/bare-pipe) | 6 | native | >=1.16.0 | 2 | 6 | 26 | ✓ | **page** |
| [bare-crypto](https://github.com/holepunchto/bare-crypto) | 6 | native | — | 2 | 2 | 36 | ✓ | **page** |
| [bare-native](https://github.com/holepunchto/bare-native) | 6 | pure-JS | — | 8 | 0 | 0 | ✓ | catalog row |
| [bare-build](https://github.com/holepunchto/bare-build) | 6 | pure-JS · tool | — | 27 | 0 | 1 | ✓ | catalog row |
| [bare-bundle](https://github.com/holepunchto/bare-bundle) | 6 | pure-JS · tool | — | 0 | 8 | 0 | ✓ | catalog row |
| [bare-subprocess](https://github.com/holepunchto/bare-subprocess) | 5 | native | >=1.7.0 | 7 | 9 | 32 | ✓ | **page** |
| [bare-tty](https://github.com/holepunchto/bare-tty) | 5 | native | >=1.16.0 | 3 | 4 | 0 | ✓ | catalog row |
| [bare-http1](https://github.com/holepunchto/bare-http1) | 5 | pure-JS | — | 4 | 6 | 0 | ✓ | catalog row |
| [bare-buffer](https://github.com/holepunchto/bare-buffer) | 5 | native | >=1.20.0 | 0 | 3 | 0 | ✓ | catalog row |
| [bare-process](https://github.com/holepunchto/bare-process) | 5 | pure-JS | — | 8 | 8 | 0 | ✓ | catalog row |
| [bare-signals](https://github.com/holepunchto/bare-signals) | 5 | native | >=1.7.0 | 2 | 2 | 0 | ✓ | catalog row |
| [bare-atomics](https://github.com/holepunchto/bare-atomics) | 5 | native | >=1.2.0 | 0 | 0 | 27 | ✓ | **page** |
| [bare-assert](https://github.com/holepunchto/bare-assert) | 5 | pure-JS | — | 1 | 5 | 0 | ✓ | catalog row |
| [bare-ansi-escapes](https://github.com/holepunchto/bare-ansi-escapes) | 5 | pure-JS | — | 1 | 3 | 0 | ✓ | catalog row |
| [bare-abort](https://github.com/holepunchto/bare-abort) | 5 | native | — | 0 | 1 | 0 | ✓ | catalog row |
| [bare-thread](https://github.com/holepunchto/bare-thread) | 4 | native | — | 4 | 1 | 0 | ✓ | catalog row |
| [bare-sidecar](https://github.com/holepunchto/bare-sidecar) | 4 | pure-JS | — | 8 | 0 | 6 | ✗ | **page** |
| [bare-tui](https://github.com/holepunchto/bare-tui) | 4 | pure-JS | — | 2 | 3 | 0 | ✓ | catalog row |
| [bare-net](https://github.com/holepunchto/bare-net) | 4 | pure-JS | — | 4 | 2 | 0 | ✓ | catalog row |
| [bare-path](https://github.com/holepunchto/bare-path) | 4 | pure-JS | — | 1 | 24 | 0 | ✓ | catalog row |
| [bare-png](https://github.com/holepunchto/bare-png) | 4 | native | — | 0 | 1 | 0 | ✓ | catalog row |
| [bare-link](https://github.com/holepunchto/bare-link) | 4 | pure-JS | — | 8 | 1 | 1 | ✗ | catalog row |
| [bare-addon](https://github.com/holepunchto/bare-addon) | 4 | native | — | 0 | 0 | 0 | ✗ | catalog row |
| [bare-https](https://github.com/holepunchto/bare-https) | 4 | pure-JS | — | 3 | 4 | 0 | ✓ | catalog row |
| [bare-readline](https://github.com/holepunchto/bare-readline) | 4 | pure-JS | — | 2 | 2 | 0 | ✓ | catalog row |
| [bare-pack-drive](https://github.com/holepunchto/bare-pack-drive) | 4 | pure-JS | — | 1 | 1 | 1 | ✗ | catalog row |
| [bare-encoding](https://github.com/holepunchto/bare-encoding) | 4 | pure-JS | — | 1 | 1 | 0 | ✓ | catalog row |
| [bare-dns](https://github.com/holepunchto/bare-dns) | 4 | native | >=1.7.0 | 0 | 2 | 0 | ✓ | catalog row |
| [bare-diagnostics-channel](https://github.com/holepunchto/bare-diagnostics-channel) | 4 | pure-JS | — | 0 | 1 | 0 | ✗ | catalog row |
| [bare-bundle-compile](https://github.com/holepunchto/bare-bundle-compile) | 4 | pure-JS | — | 0 | 0 | 1 | ✗ | catalog row |
| [bare-rust](https://github.com/holepunchto/bare-rust) | 4 | pure-JS | — | 0 | 0 | 0 | ✗ | catalog row |
| [bare-inspector](https://github.com/holepunchto/bare-inspector) | 3 | native | >=1.25.0 <1.29.0 | 5 | 2 | 8 | ✓ | **page** |
| [bare-tui-form](https://github.com/holepunchto/bare-tui-form) | 3 | pure-JS | — | 1 | 1 | 0 | ✗ | catalog row |
| [bare-module-traverse](https://github.com/holepunchto/bare-module-traverse) | 3 | pure-JS | — | 3 | 8 | 14 | ✗ | **page** |
| [bare-sdl](https://github.com/holepunchto/bare-sdl) | 3 | native | >=1.14.0 | 0 | 0 | 20 | ✗ | **page** |
| [bare-url](https://github.com/holepunchto/bare-url) | 3 | native | — | 1 | 17 | 31 | ✓ | **page** |
| [bare-console](https://github.com/holepunchto/bare-console) | 3 | pure-JS | — | 5 | 1 | 15 | ✓ | **page** |
| [bare-rpc-swift](https://github.com/holepunchto/bare-rpc-swift) | 3 | pure-JS | — | 0 | 0 | 0 | ✗ | catalog row |
| [bare-gif](https://github.com/holepunchto/bare-gif) | 3 | native | — | 0 | 1 | 0 | ✗ | catalog row |
| [bare-jpeg](https://github.com/holepunchto/bare-jpeg) | 3 | native | — | 0 | 1 | 0 | ✗ | catalog row |
| [bare-heif](https://github.com/holepunchto/bare-heif) | 3 | native | — | 0 | 1 | 0 | ✗ | catalog row |
| [bare-tiff](https://github.com/holepunchto/bare-tiff) | 3 | native | — | 0 | 1 | 0 | ✗ | catalog row |
| [bare-node-runtime](https://github.com/holepunchto/bare-node-runtime) | 3 | pure-JS | — | 39 | 0 | 0 | ✗ | catalog row |
| [bare-distributable](https://github.com/holepunchto/bare-distributable) | 3 | pure-JS | — | 0 | 0 | 0 | ✗ | catalog row |
| [bare-webp](https://github.com/holepunchto/bare-webp) | 3 | native | — | 0 | 1 | 0 | ✗ | catalog row |
| [bare-vm](https://github.com/holepunchto/bare-vm) | 3 | pure-JS | — | 1 | 1 | 0 | ✗ | catalog row |
| [bare-type](https://github.com/holepunchto/bare-type) | 3 | native | >=1.2.0 | 0 | 4 | 0 | ✓ | catalog row |
| [bare-timers](https://github.com/holepunchto/bare-timers) | 3 | native | >=1.7.0 | 0 | 1 | 0 | ✓ | catalog row |
| [bare-run](https://github.com/holepunchto/bare-run) | 3 | pure-JS | >=1.16.0 | 12 | 0 | 1 | ✗ | catalog row |
| [bare-logger](https://github.com/holepunchto/bare-logger) | 3 | native | — | 1 | 3 | 0 | ✓ | catalog row |
| [bare-inspect](https://github.com/holepunchto/bare-inspect) | 3 | native | >=1.18.0 | 2 | 4 | 0 | ✓ | catalog row |
| [bare-image-resample](https://github.com/holepunchto/bare-image-resample) | 3 | native | — | 0 | 1 | 0 | ✗ | catalog row |
| [bare-hrtime](https://github.com/holepunchto/bare-hrtime) | 3 | native | — | 0 | 2 | 0 | ✓ | catalog row |
| [bare-env](https://github.com/holepunchto/bare-env) | 3 | pure-JS | — | 1 | 7 | 0 | ✓ | catalog row |
| [bare-dgram](https://github.com/holepunchto/bare-dgram) | 3 | pure-JS | — | 1 | 2 | 0 | ✓ | catalog row |
| [bare-boot](https://github.com/holepunchto/bare-boot) | 3 | pure-JS | — | 4 | 0 | 0 | ✗ | catalog row |
| [bare-async-hooks](https://github.com/holepunchto/bare-async-hooks) | 3 | pure-JS | — | 0 | 1 | 0 | ✗ | catalog row |
| [bare-broadcast-channel](https://github.com/holepunchto/bare-broadcast-channel) | 2 | native | — | 3 | 0 | 19 | ✗ | **page** |
| [bare-structured-clone](https://github.com/holepunchto/bare-structured-clone) | 2 | native | >=1.2.0 | 3 | 4 | 12 | ✓ | **page** |
| [bare-tls](https://github.com/holepunchto/bare-tls) | 2 | native | >=1.7.0 | 2 | 2 | 18 | ✓ | **page** |
| [bare-app-kit](https://github.com/holepunchto/bare-app-kit) | 2 | native | — | 1 | 1 | 0 | ✗ | catalog row |
| [bare-gtk](https://github.com/holepunchto/bare-gtk) | 2 | native | — | 0 | 2 | 0 | ✗ | catalog row |
| [bare-union-bundle](https://github.com/holepunchto/bare-union-bundle) | 2 | pure-JS | — | 4 | 0 | 5 | ✗ | **page** |
| [bare-abort-controller](https://github.com/holepunchto/bare-abort-controller) | 2 | pure-JS | — | 1 | 1 | 0 | ✗ | catalog row |
| [bare-form-data](https://github.com/holepunchto/bare-form-data) | 2 | pure-JS | — | 2 | 1 | 22 | ✓ | **page** |
| [bare-module-resolve](https://github.com/holepunchto/bare-module-resolve) | 2 | pure-JS | — | 1 | 7 | 16 | ✗ | **page** |
| [bare-zmq](https://github.com/holepunchto/bare-zmq) | 2 | native | — | 2 | 0 | 0 | ✓ | catalog row |
| [bare-which](https://github.com/holepunchto/bare-which) | 2 | pure-JS | — | 3 | 1 | 0 | ✗ | catalog row |
| [bare-querystring](https://github.com/holepunchto/bare-querystring) | 2 | pure-JS | — | 0 | 1 | 0 | ✗ | catalog row |
| [bare-performance](https://github.com/holepunchto/bare-performance) | 2 | native | >=1.27.0 | 0 | 2 | 0 | ✓ | catalog row |
| [bare-debug-log](https://github.com/holepunchto/bare-debug-log) | 2 | pure-JS | — | 1 | 1 | 0 | ✗ | catalog row |
| [bare-daemon](https://github.com/holepunchto/bare-daemon) | 2 | native | — | 2 | 0 | 0 | ✓ | catalog row |
| [bare-bundle-id](https://github.com/holepunchto/bare-bundle-id) | 2 | pure-JS | — | 0 | 4 | 1 | ✗ | catalog row |
| [bare-addon-resolve](https://github.com/holepunchto/bare-addon-resolve) | 2 | pure-JS | — | 2 | 2 | 11 | ✗ | **page** |
| [bare-addon-jstl](https://github.com/holepunchto/bare-addon-jstl) | 2 | native | — | 0 | 0 | 0 | ✗ | catalog row |
| [bare-kit-swift](https://github.com/holepunchto/bare-kit-swift) | 2 | pure-JS | — | 0 | 0 | 0 | ✗ | catalog row |
| [bare-prebuild](https://github.com/holepunchto/bare-prebuild) | 2 | pure-JS | — | 6 | 0 | 1 | ✗ | catalog row |
| [bare-expo-hrpc-demo](https://github.com/holepunchto/bare-expo-hrpc-demo) | 2 | pure-JS | — | 1 | 0 | 0 | ✗ | catalog row |
| [bare-xdiff](https://github.com/holepunchto/bare-xdiff) | 2 | native | — | 0 | 0 | 2 | ✗ | catalog row |
| [bare-delta](https://github.com/holepunchto/bare-delta) | 2 | native | — | 0 | 0 | 0 | ✗ | catalog row |
| [bare-stow](https://github.com/holepunchto/bare-stow) | 1 | pure-JS | — | 9 | 0 | 2 | ✗ | catalog row |
| [bare-macos](https://github.com/holepunchto/bare-macos) | 1 | pure-JS | — | 1 | 0 | 0 | ✗ | catalog row |
| [bare-bluetooth-android](https://github.com/holepunchto/bare-bluetooth-android) | 1 | native | — | 2 | 0 | 103 | ✗ | **page** |
| [bare-type-stripper](https://github.com/holepunchto/bare-type-stripper) | 1 | native | — | 0 | 1 | 3 | ✗ | catalog row |
| [bare-ipc](https://github.com/holepunchto/bare-ipc) | 1 | pure-JS | — | 3 | 0 | 11 | ✓ | **page** |
| [bare-bluetooth-apple](https://github.com/holepunchto/bare-bluetooth-apple) | 1 | native | — | 2 | 0 | 83 | ✗ | **page** |
| [bare-unpack](https://github.com/holepunchto/bare-unpack) | 1 | pure-JS | — | 3 | 2 | 1 | ✓ | catalog row |
| [bare-rpc-golang](https://github.com/holepunchto/bare-rpc-golang) | 1 | pure-JS | — | 0 | 0 | 0 | ✗ | catalog row |
| [bare-http-parser](https://github.com/holepunchto/bare-http-parser) | 1 | pure-JS | — | 0 | 1 | 0 | ✗ | catalog row |
| [bare-zlib](https://github.com/holepunchto/bare-zlib) | 1 | native | — | 1 | 3 | 0 | ✓ | catalog row |
| [bare-ndk](https://github.com/holepunchto/bare-ndk) | 1 | native | — | 0 | 1 | 0 | ✗ | catalog row |
| [bare-apk](https://github.com/holepunchto/bare-apk) | 1 | pure-JS | — | 5 | 0 | 4 | ✗ | **page** |
| [bare-sqlite-vector](https://github.com/holepunchto/bare-sqlite-vector) | 1 | native | — | 1 | 0 | 1 | ✗ | catalog row |
| [bare-sqlite](https://github.com/holepunchto/bare-sqlite) | 1 | native | — | 0 | 2 | 21 | ✗ | **page** |
| [bare-exif](https://github.com/holepunchto/bare-exif) | 1 | native | — | 0 | 1 | 0 | ✗ | catalog row |
| [bare-walk-handles](https://github.com/holepunchto/bare-walk-handles) | 1 | native | — | 0 | 0 | 0 | ✗ | catalog row |
| [bare-utils](https://github.com/holepunchto/bare-utils) | 1 | pure-JS | — | 6 | 3 | 0 | ✗ | catalog row |
| [bare-tpl](https://github.com/holepunchto/bare-tpl) | 1 | pure-JS | — | 0 | 1 | 1 | ✗ | catalog row |
| [bare-system-logger](https://github.com/holepunchto/bare-system-logger) | 1 | native | — | 1 | 1 | 0 | ✗ | catalog row |
| [bare-storage](https://github.com/holepunchto/bare-storage) | 1 | native | — | 5 | 0 | 2 | ✗ | catalog row |
| [bare-stdio](https://github.com/holepunchto/bare-stdio) | 1 | native | — | 3 | 1 | 3 | ✗ | catalog row |
| [bare-open](https://github.com/holepunchto/bare-open) | 1 | native | — | 0 | 0 | 1 | ✗ | catalog row |
| [bare-md4c](https://github.com/holepunchto/bare-md4c) | 1 | native | — | 0 | 0 | 0 | ✗ | catalog row |
| [bare-intl](https://github.com/holepunchto/bare-intl) | 1 | pure-JS | — | 0 | 0 | 0 | ✗ | catalog row |
| [bare-format](https://github.com/holepunchto/bare-format) | 1 | pure-JS | — | 1 | 3 | 0 | ✓ | catalog row |
| [bare-compat-napi](https://github.com/holepunchto/bare-compat-napi) | 1 | pure-JS | — | 0 | 1 | 0 | ✗ | catalog row |
| [bare-bundle-evaluate](https://github.com/holepunchto/bare-bundle-evaluate) | 1 | pure-JS | — | 5 | 0 | 1 | ✗ | catalog row |
| [bare-addon-rust](https://github.com/holepunchto/bare-addon-rust) | 1 | native | — | 0 | 0 | 0 | ✗ | catalog row |
| [bare-addon-java](https://github.com/holepunchto/bare-addon-java) | 1 | native | — | 0 | 0 | 0 | ✗ | catalog row |
| [bare-v8-to-istanbul](https://github.com/holepunchto/bare-v8-to-istanbul) | 1 | pure-JS | — | 7 | 1 | 0 | ✗ | catalog row |
| [bare-app-image](https://github.com/holepunchto/bare-app-image) | 1 | pure-JS | — | 3 | 0 | 1 | ✗ | catalog row |
| [bare-ffmpeg-encodings](https://github.com/holepunchto/bare-ffmpeg-encodings) | 1 | pure-JS | — | 0 | 0 | 0 | ✗ | catalog row |
| [bare-node-fetch](https://github.com/holepunchto/bare-node-fetch) | 1 | pure-JS | — | 1 | 0 | 0 | ✗ | catalog row |
| [bare-translations](https://github.com/holepunchto/bare-translations) | 0 | pure-JS | — | 0 | 0 | 0 | ✗ | catalog row |
| [bare-headers](https://github.com/holepunchto/bare-headers) | 0 | pure-JS | — | 0 | 0 | 0 | ✗ | catalog row |
| [bare-stow-target-pear-runtime](https://github.com/holepunchto/bare-stow-target-pear-runtime) | 0 | pure-JS | — | 0 | 0 | 0 | ✗ | catalog row |
| [bare-stow-target-react-native](https://github.com/holepunchto/bare-stow-target-react-native) | 0 | pure-JS | — | 0 | 0 | 0 | ✗ | catalog row |
| [bare-switch-core](https://github.com/holepunchto/bare-switch-core) | 0 | pure-JS | — | 0 | 1 | 0 | ✗ | catalog row |
| [bare-tui-paparam](https://github.com/holepunchto/bare-tui-paparam) | 0 | pure-JS | — | 3 | 0 | 0 | ✗ | catalog row |
| [bare-tui-markdown](https://github.com/holepunchto/bare-tui-markdown) | 0 | pure-JS | — | 1 | 1 | 0 | ✗ | catalog row |
| [bare-tui-paparam-ai](https://github.com/holepunchto/bare-tui-paparam-ai) | 0 | pure-JS | — | 0 | 0 | 0 | ✗ | catalog row |
| [bare-ico](https://github.com/holepunchto/bare-ico) | 0 | native | — | 0 | 1 | 0 | ✗ | catalog row |
| [bare-bmp](https://github.com/holepunchto/bare-bmp) | 0 | native | — | 0 | 1 | 0 | ✗ | catalog row |
| [bare-web-kit-gtk](https://github.com/holepunchto/bare-web-kit-gtk) | 0 | native | — | 1 | 1 | 0 | ✗ | catalog row |
| [bare-ui-kit](https://github.com/holepunchto/bare-ui-kit) | 0 | native | — | 1 | 1 | 0 | ✗ | catalog row |
| [bare-win-ui](https://github.com/holepunchto/bare-win-ui) | 0 | native | — | 1 | 1 | 0 | ✗ | catalog row |
| [bare-collabora](https://github.com/holepunchto/bare-collabora) | 0 | native | — | 3 | 0 | 2 | ✗ | catalog row |
| [bare-prom-client](https://github.com/holepunchto/bare-prom-client) | 0 | pure-JS | — | 9 | 0 | 12 | ✗ | **page** |
| [bare-hc](https://github.com/holepunchto/bare-hc) | 0 | native | — | 0 | 0 | 0 | ✗ | catalog row |
| [bare-mdns-discovery](https://github.com/holepunchto/bare-mdns-discovery) | 0 | pure-JS | — | 1 | 0 | 7 | ✗ | **page** |
| [bare-navigation-android](https://github.com/holepunchto/bare-navigation-android) | 0 | native | — | 1 | 0 | 0 | ✗ | catalog row |
| [bare-wifi-android](https://github.com/holepunchto/bare-wifi-android) | 0 | native | — | 0 | 0 | 0 | ✗ | catalog row |
| [bare-lief](https://github.com/holepunchto/bare-lief) | 0 | native | — | 1 | 2 | 0 | ✗ | catalog row |
| [bare-cov](https://github.com/holepunchto/bare-cov) | 0 | pure-JS | — | 6 | 0 | 0 | ✗ | catalog row |
| [bare-posix](https://github.com/holepunchto/bare-posix) | 0 | native | — | 0 | 1 | 11 | ✗ | **page** |
| [bare-mime](https://github.com/holepunchto/bare-mime) | 0 | pure-JS | — | 0 | 1 | 5 | ✗ | **page** |
| [bare-v8](https://github.com/holepunchto/bare-v8) | 0 | native | — | 0 | 2 | 0 | ✗ | catalog row |
| [bare-web-kit](https://github.com/holepunchto/bare-web-kit) | 0 | native | — | 1 | 1 | 0 | ✗ | catalog row |
| [bare-string-decoder](https://github.com/holepunchto/bare-string-decoder) | 0 | pure-JS | — | 0 | 1 | 0 | ✗ | catalog row |
| [bare-semver](https://github.com/holepunchto/bare-semver) | 0 | pure-JS | — | 0 | 4 | 23 | ✓ | **page** |
| [bare-queue-microtask](https://github.com/holepunchto/bare-queue-microtask) | 0 | pure-JS | — | 0 | 0 | 0 | ✓ | catalog row |
| [bare-file-logger](https://github.com/holepunchto/bare-file-logger) | 0 | pure-JS | — | 2 | 0 | 3 | ✗ | catalog row |
| [bare-sidecar-bundle](https://github.com/holepunchto/bare-sidecar-bundle) | 0 | pure-JS | — | 3 | 0 | 0 | ✗ | catalog row |
| [bare-svg](https://github.com/holepunchto/bare-svg) | 0 | native | — | 0 | 1 | 0 | ✗ | catalog row |
| [bare-inspector-gc](https://github.com/holepunchto/bare-inspector-gc) | 0 | pure-JS | >=1.2.0 | 0 | 0 | 0 | ✗ | catalog row |
| [bare-tap](https://github.com/holepunchto/bare-tap) | 0 | pure-JS | — | 1 | 0 | 0 | ✗ | catalog row |
| [bare-punycode](https://github.com/holepunchto/bare-punycode) | 0 | pure-JS | — | 0 | 1 | 0 | ✗ | catalog row |

## Full-page candidates

Modules with a substantive API surface (≥4 documented methods) that warrant a dedicated reference page:

- **bare-fs** — 199 methods · Native file system operations for Bare
- **bare-module** — 41 methods · Module support for JavaScript
- **bare-tcp** — 40 methods · Native TCP sockets for JavaScript
- **bare-fetch** — 43 methods · WHATWG Fetch implementation for Bare
- **bare-rpc** — 18 methods · librpc ABI compatible RPC for Bare
- **bare-os** — 43 methods · Operating system utilities for Bare
- **bare-channel** — 19 methods · Inter-thread messaging for JavaScript
- **bare-make** — 4 methods · Opinionated build system generator based on CMake
- **bare-stream** — 66 methods · Streaming data for JavaScript
- **bare-pipe** — 26 methods · Native I/O pipes for JavaScript
- **bare-crypto** — 36 methods · Cryptographic primitives for JavaScript
- **bare-subprocess** — 32 methods · Native process spawning for JavaScript
- **bare-atomics** — 27 methods · Native synchronization primitives for JavaScript
- **bare-sidecar** — 6 methods · Start and manage Bare sidecar processes from Node.js and Electron
- **bare-inspector** — 8 methods · V8 inspector support for Bare
- **bare-module-traverse** — 14 methods · Low-level module graph traversal for Bare
- **bare-sdl** — 20 methods · Bare binding for SDL
- **bare-url** — 31 methods · WHATWG URL implementation for JavaScript
- **bare-console** — 15 methods · WHATWG debugging console for JavaScript
- **bare-broadcast-channel** — 19 methods · Multi-producer, multi-consumer inter-thread broadcast messaging for JavaScript
- **bare-structured-clone** — 12 methods · Structured cloning algorithm for JavaScript
- **bare-tls** — 18 methods · Transport Layer Security (TLS) streams for JavaScript
- **bare-union-bundle** — 5 methods · Union bundle
- **bare-form-data** — 22 methods · Form data support for Bare
- **bare-module-resolve** — 16 methods · Low-level module resolution algorithm for Bare
- **bare-addon-resolve** — 11 methods · Low-level addon resolution algorithm for Bare
- **bare-bluetooth-android** — 103 methods · Android Bluetooth bindings for Bare
- **bare-ipc** — 11 methods · Lightweight pipe-based IPC for Bare
- **bare-bluetooth-apple** — 83 methods · CoreBluetooth bindings for Bare
- **bare-apk** — 4 methods · APK packaging tools for Bare
- **bare-sqlite** — 21 methods · SQLite bindings for Bare
- **bare-prom-client** — 12 methods · Client for prometheus
- **bare-mdns-discovery** — 7 methods · bare-mdns-discovery
- **bare-posix** — 11 methods · POSIX-specific bindings for Bare
- **bare-mime** — 5 methods · MIME type parsing for Bare
- **bare-semver** — 23 methods · Minimal semantic versioning library for Bare

## Catalog corrections (recommendations)

### Missing from the catalog

- **bare-node** (11★) — Compatibility modules for Node.js builtins in Bare
- **bare-sidecar** (4★) — Start and manage Bare sidecar processes from Node.js and Electron
- **bare-link** (4★) — Native addon linker for Bare
- **bare-addon** (4★) — Example Bare addon
- **bare-pack-drive** (4★) — Pack drives to Bare bundles
- **bare-diagnostics-channel** (4★) — Create and publish data to named channels for diagnostics purposes
- **bare-bundle-compile** (4★) — Compile a bundle of CommonJS modules to a single module
- **bare-rust** (4★) — Rust and Bare, sitting in a tree
- **bare-tui-form** (3★) — A declarative form builder for bare-tui
- **bare-module-traverse** (3★) — Low-level module graph traversal for Bare
- **bare-sdl** (3★) — Bare binding for SDL
- **bare-rpc-swift** (3★) — https://github.com/holepunchto/librpc ABI compatible RPC for Swift 
- **bare-gif** (3★) — GIF support for Bare
- **bare-jpeg** (3★) — JPEG support for Bare
- **bare-heif** (3★) — HEIF support for Bare
- **bare-tiff** (3★) — TIFF support for Bare
- **bare-node-runtime** (3★) — Compatibility layer for Node.js builtins and globals in Bare
- **bare-distributable** (3★) — Template repository for creating custom Bare distributables
- **bare-webp** (3★) — WebP support for Bare
- **bare-vm** (3★) — Isolated JavaScript contexts for Bare
- **bare-run** (3★) — Cross-platform script runner for Bare
- **bare-image-resample** (3★) — Image resampling support for Bare
- **bare-boot** (3★) — Boot drives in Bare
- **bare-async-hooks** (3★) — `async_hooks` shim for Bare
- **bare-broadcast-channel** (2★) — Multi-producer, multi-consumer inter-thread broadcast messaging for JavaScript
- **bare-app-kit** (2★) — AppKit bindings and runtime for Bare
- **bare-gtk** (2★) — GTK bindings and runtime for Bare
- **bare-union-bundle** (2★) — Union bundle
- **bare-abort-controller** (2★) — Abort controller support for Bare
- **bare-module-resolve** (2★) — Low-level module resolution algorithm for Bare
- **bare-which** (2★) — Find a program in $PATH
- **bare-querystring** (2★) — URL query strings utilities
- **bare-debug-log** (2★) — Simple debug log for JavaScript
- **bare-bundle-id** (2★) — Construct a unique ID for a bundle
- **bare-addon-resolve** (2★) — Low-level addon resolution algorithm for Bare
- **bare-addon-jstl** (2★) — Example Bare addon
- **bare-kit-swift** (2★) — Swift extensions for Bare Kit
- **bare-prebuild** (2★) — Tool for recursively prebuilding installed native addons from source
- **bare-expo-hrpc-demo** (2★) — Example of embedding Bare in an Expo application
- **bare-xdiff** (2★) — libxdiff bindings for Bare
- **bare-delta** (2★) — Binary patch handling for Bare
- **bare-stow** (1★) — Module bundling and harness generation for Bare
- **bare-macos** (1★) — Example of embedding Bare in a macOS application
- **bare-bluetooth-android** (1★) — Android Bluetooth bindings for Bare
- **bare-type-stripper** (1★) — Heuristic lexer for stripping TypeScript type syntax to produce plain JavaScript
- **bare-bluetooth-apple** (1★) — CoreBluetooth bindings for Bare
- **bare-rpc-golang** (1★) — Golang RPC client and server compatible with the bare RPC protocol
- **bare-http-parser** (1★) — Streaming HTTP request and response parser for Bare
- **bare-ndk** (1★) — Android NDK bindings and runtime for Bare
- **bare-apk** (1★) — APK packaging tools for Bare
- **bare-sqlite-vector** (1★) — SQLite Vector bindings for Bare
- **bare-sqlite** (1★) — SQLite bindings for Bare
- **bare-exif** (1★) — EXIF support for Bare
- **bare-walk-handles** (1★) — Walk the event loop handles of the Bare process
- **bare-utils** (1★) — Node.js-compatible utility functions for Bare
- **bare-tpl** (1★) — Simple template string based templating for Bare
- **bare-system-logger** (1★) — System logger for Bare
- **bare-storage** (1★) — Minimal, cross-platform directory locator for Bare
- **bare-stdio** (1★) — Standard input/output streams for Bare
- **bare-open** (1★) — Cross-platform application launcher for Bare
- **bare-md4c** (1★) — MD4C bindings, markdown push parser
- **bare-intl** (1★) — ECMAScript Internationalization API for Bare
- **bare-compat-napi** (1★) — Bare compatibility headers for Node-API
- **bare-bundle-evaluate** (1★) — Evaluate a bundle of CommonJS modules across JavaScript runtimes
- **bare-addon-rust** (1★) — Example Bare addon
- **bare-addon-java** (1★) — Example Bare addon
- **bare-v8-to-istanbul** (1★) — Wrapper for v8-to-istanbul that works with Bare
- **bare-app-image** (1★) — AppImage packaging tools for Bare
- **bare-ffmpeg-encodings** (1★) — Serialize ffmpeg primitives
- **bare-node-fetch** (1★) — Cross runtime fetch module
- **bare-translations** (0★) — 
- **bare-headers** (0★) — Development headers for Bare
- **bare-stow-target-pear-runtime** (0★) — Pear Runtime target for bare-stow
- **bare-stow-target-react-native** (0★) — React Native target for bare-stow
- **bare-switch-core** (0★) — Shared peer-to-peer switch core behind the bare example apps (bare-macos, bare-ios)
- **bare-tui-paparam** (0★) — menu/form helper for any paparam CLI
- **bare-tui-markdown** (0★) — A limited markdown renderer for bare-tui
- **bare-tui-paparam-ai** (0★) — AI assisted paparam upgrade
- **bare-ico** (0★) — ICO support for Bare
- **bare-bmp** (0★) — Native BMP codec for Bare
- **bare-web-kit-gtk** (0★) — WebKitGTK bindings for Bare
- **bare-ui-kit** (0★) — UIKit bindings and runtime for Bare
- **bare-win-ui** (0★) — WinUI bindings and runtime for Bare
- **bare-collabora** (0★) — Collabora bindings for Bare
- **bare-prom-client** (0★) — Client for prometheus
- **bare-hc** (0★) — Bare bindings for libhc
- **bare-mdns-discovery** (0★) — bare-mdns-discovery
- **bare-navigation-android** (0★) — Android back gesture/button handler for the Bare runtime
- **bare-wifi-android** (0★) — Android WifiManager bindings for the Bare runtime — multicast lock and WiFi interface IP
- **bare-lief** (0★) — LIEF bindings for Bare
- **bare-cov** (0★) — Generate coverage reports using Bare and Node.js
- **bare-posix** (0★) — POSIX-specific bindings for Bare
- **bare-mime** (0★) — MIME type parsing for Bare
- **bare-v8** (0★) — V8 utilities for Bare
- **bare-web-kit** (0★) — WebKit bindings for Bare
- **bare-string-decoder** (0★) — `string_decoder` shim for Bare
- **bare-file-logger** (0★) — File system logger for Bare
- **bare-sidecar-bundle** (0★) — Bare bundler optimised for sidecars to be used in pear-runtime
- **bare-svg** (0★) — SVG support for Bare
- **bare-inspector-gc** (0★) — manually trigger garbage collection via inspector protocol
- **bare-tap** (0★) — Minimal TAP library for Bare
- **bare-punycode** (0★) — Punycode support for Bare

### Description differs from package.json

- **bare-fs**
  - catalog: Native file system for JavaScript — [reference](/reference/bare/modules/bare-fs)
  - package.json: Native file system operations for Bare
- **bare-tcp**
  - catalog: Native TCP sockets for JavaScript — [reference](/reference/bare/modules/bare-tcp)
  - package.json: Native TCP sockets for JavaScript
- **bare-fetch**
  - catalog: WHATWG Fetch implementation for Bare — [reference](/reference/bare/modules/bare-fetch)
  - package.json: WHATWG Fetch implementation for Bare
- **bare-rpc**
  - catalog: [librpc](https://github.com/holepunchto/librpc) ABI compatible RPC for Bare — [reference](/reference/bare/modules/bare-rpc)
  - package.json: librpc ABI compatible RPC for Bare
- **bare-os**
  - catalog: Operating system utilities for JavaScript — [reference](/reference/bare/modules/bare-os)
  - package.json: Operating system utilities for Bare
- **bare-pack**
  - catalog: Bundle packing for Bare—traverse a module graph into an embeddable bundle
  - package.json: Bundle packing for Bare
- **bare-make**
  - catalog: Opinionated CMake-based build system generator for native addons
  - package.json: Opinionated build system generator based on CMake
- **bare-stream**
  - catalog: Streaming data for JavaScript — [reference](/reference/bare/modules/bare-stream)
  - package.json: Streaming data for JavaScript
- **bare-crypto**
  - catalog: Cryptographic primitives for JavaScript — [reference](/reference/bare/modules/bare-crypto)
  - package.json: Cryptographic primitives for JavaScript
- **bare-build**
  - catalog: Package a Bare app as native bundles or standalone executables
  - package.json: Application builder for Bare
- **bare-subprocess**
  - catalog: Native process spawning for JavaScript — [reference](/reference/bare/modules/bare-subprocess)
  - package.json: Native process spawning for JavaScript
- **bare-http1**
  - catalog: HTTP/1 library for JavaScript
  - package.json: Native HTTP/1 library for JavaScript
- **bare-timers**
  - catalog: Native timers for JavaScript
  - package.json: Native timers for Javascript
- **bare-logger**
  - catalog: Low-level logger for Bare with system log integration
  - package.json: Low-level logger for Bare
- **bare-zmq**
  - catalog: Low-level ZeroMQ bindings for JavaScript
  - package.json: ZeroMQ bindings for JavaScript

### Native addons (verify platform support against prebuilds/CI)

`bare-fs`, `bare-ffmpeg`, `bare-module`, `bare-tcp`, `bare-repl`, `bare-module-lexer`, `bare-os`, `bare-channel`, `bare-realm`, `bare-pipe`, `bare-crypto`, `bare-subprocess`, `bare-tty`, `bare-buffer`, `bare-signals`, `bare-atomics`, `bare-abort`, `bare-thread`, `bare-png`, `bare-addon`, `bare-dns`, `bare-inspector`, `bare-sdl`, `bare-url`, `bare-gif`, `bare-jpeg`, `bare-heif`, `bare-tiff`, `bare-webp`, `bare-type`, `bare-timers`, `bare-logger`, `bare-inspect`, `bare-image-resample`, `bare-hrtime`, `bare-broadcast-channel`, `bare-structured-clone`, `bare-tls`, `bare-app-kit`, `bare-gtk`, `bare-zmq`, `bare-performance`, `bare-daemon`, `bare-addon-jstl`, `bare-xdiff`, `bare-delta`, `bare-bluetooth-android`, `bare-type-stripper`, `bare-bluetooth-apple`, `bare-zlib`, `bare-ndk`, `bare-sqlite-vector`, `bare-sqlite`, `bare-exif`, `bare-walk-handles`, `bare-system-logger`, `bare-storage`, `bare-stdio`, `bare-open`, `bare-md4c`, `bare-addon-rust`, `bare-addon-java`, `bare-ico`, `bare-bmp`, `bare-web-kit-gtk`, `bare-ui-kit`, `bare-win-ui`, `bare-collabora`, `bare-hc`, `bare-navigation-android`, `bare-wifi-android`, `bare-lief`, `bare-posix`, `bare-v8`, `bare-web-kit`, `bare-svg`

## Per-module notes

### bare-fs

Native file system operations for Bare

- repo: https://github.com/holepunchto/bare-fs · 26★ · last push 2026-06-02 · JavaScript
- version: 4.7.2 · native addon · min Bare: >=1.16.0 · types: yes · prebuilds: yes
- node-compat: yes · bare deps: bare-events, bare-path, bare-stream, bare-url · inbound deps: 23
- disposition: **full-page candidate**

<details><summary>API section (README)</summary>

#### `const fd = await fs.open(filepath[, flags[, mode]])`

Open a file, returning a file descriptor. `flags` defaults to `'r'` and `mode` defaults to `0o666`. `flags` may be a string such as `'r'`, `'w'`, `'a'`, `'r+'`, etc., or a numeric combination of `fs.constants` flags.

#### `fs.open(filepath[, flags[, mode]], callback)`

Callback version of `fs.open()`.

#### `const fd = fs.openSync(filepath[, flags[, mode]])`

Synchronous version of `fs.open()`.

#### `await fs.close(fd)`

Close a file descriptor.

#### `fs.close(fd, callback)`

Callback version of `fs.close()`.

#### `fs.closeSync(fd)`

Synchronous version of `fs.close()`.

#### `await fs.access(filepath[, mode])`

Check whether the file at `filepath` is accessible. `mode` defaults to `fs.constants.F_OK`.

#### `fs.access(filepath[, mode], callback)`

Callback version of `fs.access()`.

#### `fs.accessSync(filepath[, mode])`

Synchronous version of `fs.access()`.

#### `const exists = await fs.exists(filepath)`

Check whether a file exists at `filepath`. Returns `true` if the file is accessible, `false` otherwise.

#### `fs.exists(filepath, callback)`

Callback version of `fs.exists()`.

#### `const exists = fs.existsSync(filepath)`

Synchronous version of `fs.exists()`.

#### `const bytesRead = await fs.read(fd, buffer[, offset[, len[, pos]]])`


</details>

### bare-ffmpeg

Low-level FFmpeg bindings for Bare

- repo: https://github.com/holepunchto/bare-ffmpeg · 13★ · last push 2026-06-13 · JavaScript
- version: 1.2.3 · native addon · min Bare: — · types: no · prebuilds: yes
- node-compat: no · bare deps: none · inbound deps: 1
- disposition: **catalog-row-only**

<details><summary>API section (README)</summary>

Complete API documentation for all components is available in the `/docs` directory:

### Core Components

- [IOContext](docs/io-context.md) - Input/output context for media files with streaming support
- [Dictionary](docs/dictionary.md) - Key-value pairs for FFmpeg options

### Codecs & Streams

- [Codec](docs/codec.md) - Access to FFmpeg codecs
- [Encoder](docs/encoder.md) - Find and access encoders by name or codec
- [Decoder](docs/decoder.md) - Find and access decoders by name or codec
- [CodecContext](docs/codec-context.md) - Encoding/decoding functionality
- [CodecParameters](docs/codec-parameters.md) - Codec parameter configuration
- [Stream](docs/stream.md) - Media stream information and operations

### Formats

- [InputFormat](docs/input-format.md) - Input format specification
- [OutputFormat](docs/output-format.md) - Output format specification
- [FormatContext](docs/format-context.md) - Base class for media file handling
- [InputFormatContext](docs/input-format-context.md) - Reading media files
- [OutputFormatContext](docs/output-format-context.md) - Writing media files

### Data Structures

- [Frame](docs/frame.md) - Decoded audio/video data
- [Packet](docs/packet.md) - Encoded audio/video data
- [SideData](docs/side-data.md) - Packet side data and metadata
- [Image](docs/image.md) - Raw pixel data management
- [Rational](docs/rational.md) - Rational number (fraction) representation

### Processing

- [Scaler](docs/scaler.md) - Video scaling and pixel format conversion
- [Resampler](docs/resampler.md) - Audio resampling and format conversion
- [Filter](docs/filter.md) - FFmpeg filter access
- [FilterGraph](docs/filter-graph.md) - Filter chain management
- [FilterContext](docs/filter-context.md) - Filter instance representation
- [FilterInOut](docs/filter-in-out.md) - Filter input/output pads
- [AudioFIFO](docs/audio-fifo.md) - Audio sample buffering

### Hardware Acceleration

- [HWDeviceContext](docs/hw-device-context.md) - Hardware device context for acceleration
- [HWFramesContext](docs/hw-frames-context.md) - Hardware frame pool management
- [HWFramesConstraints](docs/hw-frames-constraints.md) - Hardware capability information

### Utilities


</details>

### bare-module

Module support for JavaScript

- repo: https://github.com/holepunchto/bare-module · 12★ · last push 2026-06-16 · JavaScript
- version: 6.2.1 · native addon · min Bare: >=1.23.0 <1.29.0 · types: yes · prebuilds: yes
- node-compat: no · bare deps: bare-bundle, bare-module-lexer, bare-module-resolve, bare-path, bare-url · inbound deps: 8
- disposition: **full-page candidate**

<details><summary>API section (README)</summary>

#### `Module.constants.states`

The flags for the current state of a module.

| Constant      | Description                                  |
| :------------ | :------------------------------------------- |
| `EVALUATED`   | The module has been evaluated.               |
| `SYNTHESIZED` | The module named exports have been detected. |

#### `Module.constants.types`

| Constant | Description                                                                  |
| :------- | :--------------------------------------------------------------------------- |
| `SCRIPT` | The module is a CommonJS module.                                             |
| `MODULE` | The module is a ECMAScript module.                                           |
| `JSON`   | The module is a JSON file.                                                   |
| `BUNDLE` | The module is a [`bare-bundle`](https://github.com/holepunchto/bare-bundle). |
| `ADDON`  | The module is a native addon.                                                |
| `BINARY` | The module is a binary file.                                                 |
| `TEXT`   | The module is a text file.                                                   |

#### `Module.protocol`

The default `ModuleProtocol` class for resolving, reading and loading modules. See [Protocols](#protocols) for usage.

#### `Module.cache`

The global cache of loaded modules.

> [!WARNING]
> The cache may contain `Module` instances from several independent versions of `bare-module` as the cache is inherited through the module graph.

#### `const url = Module.resolve(specifier, parentURL[, options])`

Resolve the module `specifier` relative to the `parentURL`. `specifier` is a string and `parentURL` is a WHATWG `URL`.

Options include:

```js
options = {
  // Whether the module is called via `import` or `import()`.
  isImport: false,
  // The referring module.
  referrer: null,
  // The type of the module. See Module.constants.types for possible values. The
  // default is the equivalent constant of the `attributes`'s `type` property.
  type,
  // A list of file extensions to look for. The default is based on the `type`
  // option.
  extensions: [],

</details>

### bare-media

A set of media APIs for Bare

- repo: https://github.com/holepunchto/bare-media · 11★ · last push 2026-06-16 · JavaScript
- version: 2.9.0 · pure-JS · min Bare: — · types: no · prebuilds: no
- node-compat: no · bare deps: bare-bmp, bare-exif, bare-fetch, bare-ffmpeg, bare-fs, bare-gif, bare-heif, bare-ico, bare-image-resample, bare-jpeg, bare-png, bare-svg, bare-tiff, bare-webp · inbound deps: 0
- disposition: **catalog-row-only**

### bare-node

Compatibility modules for Node.js builtins in Bare

- repo: https://github.com/holepunchto/bare-node · 11★ · last push 2026-05-26 · JavaScript
- version: — · pure-JS · min Bare: — · types: no · prebuilds: no
- node-compat: yes · bare deps: none · inbound deps: 0
- disposition: **catalog-row-only** · ⚠ missing from catalog

### bare-runtime

Prebuilt Bare binaries for macOS, iOS, Linux, Android, and Windows

- repo: https://github.com/holepunchto/bare-runtime · 10★ · last push 2026-06-13 · JavaScript
- version: 1.28.7 · pure-JS · min Bare: — · types: no · prebuilds: no
- node-compat: no · bare deps: bare-fs, bare-os, bare-path, bare-process, bare-subprocess · inbound deps: 0
- disposition: **catalog-row-only**

<details><summary>API section (README)</summary>

> [!WARNING]
> These functions are currently subject to change between releases and are not by covered Semantic Versioning. The version of the prebuilds strictly match the corresponding Bare version. If your code depends on the APIs make sure to specify a fixed version (`1.2.3`) when declaring the module dependency.

#### `const prebuild = runtime([referrer][, options])`

Get the path to the Bare binary prebuild for the current platform. Will throw an error if no matching binary prebuild is found.

Options include:

```js
options = {
  platform: process.platform,
  arch: process.arch
}
```

### Spawning

The `bare-runtime/spawn` module provides a convenient way to spawn the Bare binary prebuilds as child processes, adding appropriate executable permissions as required.

#### `const process = spawn([referrer][, options])`

Options include:

```js
options = {
  platform: process.platform,
  arch: process.arch,
  args: process.argv.slice(2)
}
```

</details>

### bare-worker

Higher-level worker threads for JavaScript

- repo: https://github.com/holepunchto/bare-worker · 10★ · last push 2026-06-11 · JavaScript
- version: 4.1.9 · pure-JS · min Bare: — · types: no · prebuilds: no
- node-compat: no · bare deps: bare-channel, bare-events, bare-module, bare-thread · inbound deps: 1
- disposition: **catalog-row-only**

### bare-tcp

Native TCP sockets for JavaScript

- repo: https://github.com/holepunchto/bare-tcp · 9★ · last push 2026-06-11 · JavaScript
- version: 2.5.1 · native addon · min Bare: >=1.16.0 · types: yes · prebuilds: yes
- node-compat: no · bare deps: bare-dns, bare-events, bare-stream · inbound deps: 4
- disposition: **full-page candidate**

<details><summary>API section (README)</summary>

#### `const socket = new tcp.Socket([options])`

Create a new TCP socket.

Options include:

```js
options = {
  readBufferSize: 65536,
  allowHalfOpen: true,
  eagerOpen: true
}
```

#### `socket.connecting`

Whether the socket is currently connecting.

#### `socket.pending`

Whether the socket has not yet connected.

#### `socket.timeout`

The timeout in milliseconds, or `undefined` if no timeout is set.

#### `socket.readyState`

The current state of the socket. Either `'open'` or `'opening'`.

#### `socket.localAddress`

The local IP address of the socket, if connected.

#### `socket.localFamily`

The local IP family (`'IPv4'` or `'IPv6'`), if connected.

#### `socket.localPort`

The local port of the socket, if connected.

#### `socket.remoteAddress`

The remote IP address of the socket, if connected.

#### `socket.remoteFamily`

The remote IP family (`'IPv4'` or `'IPv6'`), if connected.


</details>

### bare-events

Event emitters for JavaScript

- repo: https://github.com/holepunchto/bare-events · 9★ · last push 2026-06-02 · JavaScript
- version: 2.9.1 · pure-JS · min Bare: — · types: yes · prebuilds: no
- node-compat: no · bare deps: none · inbound deps: 26
- disposition: **catalog-row-only**

### bare-fetch

WHATWG Fetch implementation for Bare

- repo: https://github.com/holepunchto/bare-fetch · 9★ · last push 2026-05-05 · JavaScript
- version: 3.0.1 · pure-JS · min Bare: — · types: yes · prebuilds: no
- node-compat: no · bare deps: bare-form-data, bare-http1, bare-https, bare-mime, bare-stream, bare-url, bare-zlib · inbound deps: 3
- disposition: **full-page candidate**

<details><summary>API section (README)</summary>

#### `const response = await fetch(input[, init])`

Perform an HTTP or HTTPS request. `input` may be a URL string, a `URL` object, or a `Request` object. `init` is an optional options object.

Options include:

```js
init = {
  body: null,
  method: 'GET',
  headers: new Headers(),
  signal: null,
  agent: null
}
```

Redirects are followed automatically up to a maximum of 20. When crossing origins, the `authorization` header is removed. If `signal` is provided, the request can be aborted using an `AbortController`.

#### `fetch.Request`

The `Request` class. See below.

#### `fetch.Response`

The `Response` class. See below.

#### `fetch.Headers`

The `Headers` class. See below.

### `Request`

#### `const request = new Request(input[, init])`

Create a new request. `input` may be a URL string, a `URL` object, or another `Request` object. `init` is an optional options object, identical to the one accepted by `fetch()`.

#### `request.url`

The request URL as a string.

#### `request.method`

The request method. Standard methods (`GET`, `POST`, `PUT`, `DELETE`, `HEAD`, `OPTIONS`) are uppercased automatically.

#### `request.headers`

The request headers as a `Headers` object.

#### `request.signal`


</details>

### bare-repl

Read-Evaluate-Print-Loop environment for JavaScript

- repo: https://github.com/holepunchto/bare-repl · 9★ · last push 2026-04-23 · JavaScript
- version: 6.0.2 · native addon · min Bare: — · types: no · prebuilds: yes
- node-compat: no · bare deps: bare-inspect, bare-module, bare-os, bare-path, bare-pipe, bare-readline, bare-stream, bare-tty · inbound deps: 1
- disposition: **catalog-row-only**

<details><summary>API section (README)</summary>

#### `const repl = start([options])`

Options include:

```js
{
  prompt: string, // sets repl prompt
  useColors: boolean, // toogles ANSI style codes to colorize the output
  writer: (message) => message, // converts repl output
  input: stream, // sets repl input stream
  output: stream, // sets repl output stream
  eval: (cmd) => {} // sets eval function
}
```

#### `repl.defineCommand(keyword, { help, action })`

Define a repl command keyword.

```js
repl.defineCommand('greet', {
  help: 'Greetings',
  action: () => console.log('hello')
})
```

```sh
bare-repl
> .greet
hello
```

#### `repl.context`

Set execution context.

```js
repl.context.f = () => console.log('Hello from context')
```

```js
bare-repl
> f()
Hello from context
```

</details>

### bare-module-lexer

Heuristic lexer for detecting imports and exports in JavaScript modules

- repo: https://github.com/holepunchto/bare-module-lexer · 8★ · last push 2026-06-11 · C
- version: 1.5.2 · native addon · min Bare: — · types: yes · prebuilds: yes
- node-compat: no · bare deps: none · inbound deps: 3
- disposition: **catalog-row-only**

<details><summary>API section (README)</summary>

#### `const { imports, exports } = lex(source[, encoding][, options])`

`imports` is an array of objects with the following shape:

```js
imports = {
  specifier: 'string',
  type: number,
  names: ['string'],
  position: [
    number, // Import start
    number, // Specifier start
    number // Specifier end
  ]
}
```

`exports` is an array of objects with the following shape:

```js
exports = {
  name: 'string',
  position: [
    number, // Export start
    number, // Name start
    number // Name end
  ]
}
```

Options are reserved.

#### `lex.constants`

| Constant   | Description                                                                                                                                                                                                              |
| :--------- | :----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `REQUIRE`  | CommonJS `require()`.                                                                                                                                                                                                    |
| `IMPORT`   | ES module `import`.                                                                                                                                                                                                      |
| `DYNAMIC`  | ES module `import()` if `IMPORT` is set.                                                                                                                                                                                 |
| `ADDON`    | CommonJS `require.addon()` if `REQUIRE` is set, or ES module `import.meta.addon()` if `IMPORT` is set.                                                                                                                   |
| `ASSET`    | CommonJS `require.asset()` if `REQUIRE` is set, or ES module `import.meta.asset()` if `IMPORT` is set.                                                                                                                   |
| `RESOLVE`  | CommonJS `require.resolve()` or `require.addon.resolve()` if `REQUIRE` and optionally `ADDON` are set, or ES module `import.meta.resolve()` or `import.meta.addon.resolve()` if `IMPORT` and optionally `ADDON` are set. |
| `REEXPORT` | Re-export of a CommonJS `require()` if `REQUIRE` is set, or ES module `export from` if `IMPORT` is set.                                                                                                                  |

</details>

### bare-rpc

librpc ABI compatible RPC for Bare

- repo: https://github.com/holepunchto/bare-rpc · 8★ · last push 2026-06-08 · JavaScript
- version: 1.3.3 · pure-JS · min Bare: — · types: yes · prebuilds: no
- node-compat: no · bare deps: bare-stream · inbound deps: 0
- disposition: **full-page candidate**

<details><summary>API section (README)</summary>

### `RPC`

#### `const rpc = new RPC(stream[, onrequest])`

Create an RPC instance using a duplex `stream`. `onrequest` is an optional callback run when a remote request is received. This is where processing and responding to an RPC request happens. `onrequest` receives a `RPCIncomingRequest` as an argument, for example:

```js
const rpc = new RPC(stream, (req) => {
  if (req.command === 42) {
    req.reply('pong')
  }
})
```

See [`RPCIncomingRequest`](#rpcincomingrequest) for properties and methods for processing the request.

`onrequest` can also be a [`RPCCommandRouter`](#rpccommandrouter).

#### `const req = rpc.request(command)`

Create a request for `command`. `command` is a unique number that should be used to differentiate different requests on the remote end. Returns a `RPCOutgoingRequest`.

### `RPCOutgoingRequest`

#### `req.command`

The command that the request was created with. A command is a unique number.

#### `req.sent`

A boolean for whether the request has been sent.

#### `req.received`

A boolean for whether the request has received a reply.

#### `req.send([data[, encoding]])`

Send the request with the provided `data`. `data` can be a buffer or a string which will be encoded using `encoding`.

`.send()` can only be called once per request.

#### `const data = await req.reply([encoding])`

Await the reply from the remote end to the request. `encoding` can be defined for decoding the response `data` buffer back into a string.

#### `const stream = req.createRequestStream([options])`

Create a [`Writable`](https://github.com/mafintosh/streamx#writable-stream) stream for sending data with the request.


</details>

### bare-os

Operating system utilities for Bare

- repo: https://github.com/holepunchto/bare-os · 8★ · last push 2026-05-07 · C
- version: 3.9.1 · native addon · min Bare: >=1.14.0 · types: yes · prebuilds: yes
- node-compat: yes · bare deps: none · inbound deps: 18
- disposition: **full-page candidate**

<details><summary>API section (README)</summary>

#### `os.constants`

An object containing the following properties:

- `signals` - Signal constants such as `SIGTERM` and `SIGKILL`.
- `errnos` - Error number constants.
- `priority` - Process priority constants.

These are also available as a separate module:

```js
const constants = require('bare-os/constants')
```

#### `os.EOL`

The platform-specific end-of-line marker. `'\r\n'` on Windows, `'\n'` everywhere else.

#### `os.devNull`

The platform-specific path to the null device. `'\\\\.\\nul'` on Windows, `'/dev/null'` everywhere else.

#### `const p = os.platform()`

Returns the operating system platform as a string. Possible values include `'android'`, `'darwin'`, `'ios'`, `'linux'`, and `'win32'`.

#### `const a = os.arch()`

Returns the CPU architecture as a string. Possible values include `'arm'`, `'arm64'`, `'ia32'`, and `'x64'`.

#### `const t = os.type()`

Returns the operating system name as returned by `uname(3)`.

#### `const v = os.version()`

Returns the operating system version.

#### `const r = os.release()`

Returns the operating system release.

#### `const m = os.machine()`

Returns the machine type as a string.

#### `const p = os.execPath()`

Returns the absolute path of the executable that started the process.


</details>

### bare-ws

WebSocket library for JavaScript

- repo: https://github.com/holepunchto/bare-ws · 8★ · last push 2026-04-28 · JavaScript
- version: 3.0.0 · pure-JS · min Bare: — · types: yes · prebuilds: no
- node-compat: no · bare deps: bare-crypto, bare-events, bare-http1, bare-https, bare-stream · inbound deps: 2
- disposition: **catalog-row-only**

### bare-pack

Bundle packing for Bare

- repo: https://github.com/holepunchto/bare-pack · 7★ · last push 2026-06-12 · JavaScript
- version: 2.1.3 · pure-JS · min Bare: — · types: yes · prebuilds: no
- node-compat: no · bare deps: bare-bundle, bare-bundle-id, bare-fs, bare-module-traverse, bare-path · inbound deps: 5
- disposition: **catalog-row-only**

<details><summary>API section (README)</summary>

#### `const bundle = await pack(url[, options], readModule[, listPrefix[, writeFile]])`

Bundle the module graph rooted at `url`, which must be a WHATWG `URL` instance. `readModule` is called with a `URL` instance for every module to be read and must either return the module source, if it exists, or `null`. `listPrefix` is called with a `URL` instance of every prefix to be listed and must yield `URL` instances that have the specified `URL` as a prefix. If not provided, prefixes won't be bundled. `writeFile` is called for every addon or asset that should be offloaded rather than embedded; see [Offloading](#offloading) below. When `writeFile` is provided, `listPrefix` must be passed positionally (or as `null`).

Options include:

```js
options = {
  concurrency: 0,
  base: null,
  offload: false
}
```

`base`, if set, must be a WHATWG `URL` instance (or string) indicating where the bundle will be deployed. The resulting bundle is unmounted relative to `base` so that all keys and resolutions become relative paths.

`offload` controls whether addons and assets are written to the bundle or routed to `writeFile`. Pass `true` to offload both, or an object such as `{ addons: true }` or `{ assets: true }` to offload only one.

Options supported by <https://github.com/holepunchto/bare-module-traverse> may also be specified.

##### Aliases

To bundle source files with extensions that aren't natively recognized, use the `aliases` option from <https://github.com/holepunchto/bare-module-traverse> to map them to a supported extension. The aliased extension is used for module type detection, so `readModule` must return source compatible with that type. Aliased modules are stored in the bundle with the aliased extension, and resolutions to them are rewritten to match, so the example below stores `file:///foo.js` and `file:///bar.js`.

```js
function readModule(url) {
  if (url.href === 'file:///foo.ts') {
    return "const bar = require('./bar.ts')"
  }

  if (url.href === 'file:///bar.ts') {
    return 'module.exports = 42'
  }

  return null
}

const bundle = await pack(new URL('file:///foo.ts'), { aliases: { '.ts': '.js' } }, readModule)
```

##### Offloading

To keep addons and assets out of the bundle, set `offload` to `true` (or `{ addons: true }` / `{ assets: true }` for a single kind) and provide a `writeFile` callback. Each offloaded file is passed to `writeFile` instead of being embedded and is omitted from `bundle.addons` and `bundle.assets`.

`writeFile` receives the file's `URL` and source. If it returns a string, that string replaces the file's resolution in the bundle's imports map. Otherwise, when `base` is set, the resolution defaults to `'/../' + <path-relative-to-base>`, which resolves to a sibling of the bundle.

```js
function writeFile(url, source) {
  // Persist `source` for `url`, e.g. to disk next to the bundle.
}

</details>

### bare-channel

Inter-thread messaging for JavaScript

- repo: https://github.com/holepunchto/bare-channel · 7★ · last push 2026-06-05 · C
- version: 5.2.4 · native addon · min Bare: >=1.7.0 · types: no · prebuilds: yes
- node-compat: no · bare deps: bare-events, bare-stream, bare-structured-clone · inbound deps: 1
- disposition: **full-page candidate**

<details><summary>API section (README)</summary>

#### `const channel = new Channel([options])`

Create a new channel. A channel is a shared rendezvous point between threads; both ends acquire a `Port` by calling `channel.connect()` to send and receive messages.

`options` include:

```js
options = {
  handle,
  interfaces: []
}
```

`handle` is an existing `SharedArrayBuffer` returned by `channel.handle` on another thread. When provided, the new channel is wired up to the same underlying channel. If omitted, a fresh channel is created.

`interfaces` is an array of constructors with `bare-structured-clone` serialize and/or transfer symbols. Instances of these types may be passed to `port.write()` and will be reconstructed on the receiving side. See <https://github.com/holepunchto/bare-structured-clone> for the symbols and conventions.

#### `channel.handle`

The underlying `SharedArrayBuffer` for the channel. Pass this across thread boundaries (e.g. via `Bare.Thread`'s `data` option) and reconstruct the channel on the other side with `Channel.from(handle)`.

#### `channel.interfaces`

The array of constructors passed to the constructor.

#### `const port = channel.connect()`

Connect a new `Port` to the channel. A channel supports two connected ports; messages written on one port appear in the read queue of the other.

#### `const channel = Channel.from(handle[, options])`

Static helper for constructing a channel from an existing `handle`. Equivalent to `new Channel({ ...options, handle })`.

#### `const data = await port.read()`

Read the next value from the port. Resolves with `null` once the remote side has ended and the queue is drained.

#### `const data = port.readSync()`

Synchronous variant of `port.read()` that blocks the current thread until a value is available or the remote side has ended. Returns `null` when the remote side has ended and the queue is drained.

#### `const flushed = await port.write(value[, options])`

Write `value` to the port. Resolves to `true` once the value has been accepted into the channel, or `false` if the remote side has ended.

`options` include:

```js
options = {
  transfer: []

</details>

### bare-make

Opinionated build system generator based on CMake

- repo: https://github.com/holepunchto/bare-make · 7★ · last push 2026-06-04 · JavaScript
- version: 1.8.0 · pure-JS · min Bare: — · types: yes · prebuilds: no
- node-compat: no · bare deps: bare-env, bare-fs, bare-os, bare-path, bare-process · inbound deps: 1
- disposition: **full-page candidate**

<details><summary>API section (README)</summary>

#### `await generate([options])`

Options include:

```js
options = {
  source: '.',
  build: 'build',
  platform: os.platform(),
  arch: os.arch(),
  simulator: false,
  environment,
  cache: true,
  preset,
  sanitize,
  fuzz,
  debug,
  withDebugSymbols,
  withMinimalSize,
  define,
  cwd: path.resolve('.'),
  color: false,
  verbose: false,
  stdio
}
```

#### `await build([options])`

Options include:

```js
options = {
  build: 'build',
  target,
  clean: false,
  parallel,
  preset,
  cwd: path.resolve('.'),
  verbose: false,
  stdio
}
```

#### `await install([options])`

Options include:

```js
options = {

</details>

### bare-realm

Realm support for Bare

- repo: https://github.com/holepunchto/bare-realm · 7★ · last push 2026-04-23 · C
- version: 2.0.1 · native addon · min Bare: >=1.5.0 · types: yes · prebuilds: yes
- node-compat: no · bare deps: none · inbound deps: 1
- disposition: **catalog-row-only**

<details><summary>API section (README)</summary>

#### `const realm = new Realm()`

#### `realm.destroy()`

#### `const result = realm.evaluate(code[, options])`

Options include:

```js
{
  filename: '<anonymous>',
  offset: 0
}
```

</details>

### bare-stream

Streaming data for JavaScript

- repo: https://github.com/holepunchto/bare-stream · 6★ · last push 2026-06-12 · JavaScript
- version: 2.13.3 · pure-JS · min Bare: — · types: yes · prebuilds: no
- node-compat: yes · bare deps: none · inbound deps: 28
- disposition: **full-page candidate**

<details><summary>API section (README)</summary>

The classic stream classes and helpers are exported from `bare-stream` and follow the Node.js `stream` API. See <https://nodejs.org/api/stream.html> and <https://github.com/mafintosh/streamx> for the complete behavior; the additions and differences specific to this module are documented below.

#### `const stream = new Readable([options])`

A readable stream. Options include:

```js
options = {
  eagerOpen: false,
  highWaterMark: 16384,
  encoding: null,
  signal: null,
  open(cb) {},
  read(size) {},
  predestroy() {},
  destroy(err, cb) {}
}
```

If `encoding` is set, emitted data is decoded to strings using that encoding. If `signal` is an `AbortSignal`, the stream is destroyed when the signal aborts.

#### `stream.closed`

`true` when the stream is no longer readable.

#### `stream.errored`

The error the stream was destroyed with, or `null`.

#### `stream.push(data[, encoding])`

Push `data` into the stream's internal buffer. If `data` is a string, it is encoded to a `Buffer` using `encoding`, defaulting to `'utf8'`. Push `null` to signal the end of the stream.

#### `stream.unshift(data[, encoding])`

Like `stream.push()` but prepends `data` to the internal buffer so it is read before any already buffered data.

#### `Readable.from(data[, options])`

Create a readable stream from `data`, which may be a value, an array of values, or an async iterable.

#### `Readable.isBackpressured(stream)`

#### `Readable.isPaused(stream)`

#### `Readable.fromWeb(readableStream[, options])`

Convert a web `ReadableStream` into a `Readable`. Options include:

```js

</details>

### bare-pipe

Native I/O pipes for JavaScript

- repo: https://github.com/holepunchto/bare-pipe · 6★ · last push 2026-06-11 · JavaScript
- version: 4.2.2 · native addon · min Bare: >=1.16.0 · types: yes · prebuilds: yes
- node-compat: no · bare deps: bare-events, bare-stream · inbound deps: 6
- disposition: **full-page candidate**

<details><summary>API section (README)</summary>

#### `const pipe = new Pipe([path][, options])`

Create a new pipe. If `path` is a number, it is treated as a file descriptor to open. If it is a string, it is treated as a path to connect to.

Options include:

```js
options = {
  readBufferSize: 65536,
  allowHalfOpen: true,
  eagerOpen: true,
  ipc: false
}
```

Set `ipc: true` to enable handle passing over the pipe. See [IPC handle passing](#ipc-handle-passing).

#### `pipe.connecting`

Whether the pipe is currently connecting.

#### `pipe.pending`

Whether the pipe has not yet connected.

#### `pipe.readyState`

The current state of the pipe. One of `'open'`, `'readOnly'`, `'writeOnly'`, or `'opening'`.

#### `pipe.open(fd[, options][, onconnect])`

Open the pipe on the given file descriptor.

#### `pipe.connect(path[, options][, onconnect])`

Connect the pipe to `path`. `onconnect` is called when the connection is established.

#### `pipe.write(chunk[, encoding][, handle][, cb])`

Write `chunk` to the pipe. If `handle` is given and the pipe was created with `ipc: true`, the handle is transferred to the receiver alongside the chunk. `handle` must implement the [`IPCAcceptable`](#ipc-handle-passing) protocol.

#### `pipe.accept(target)`

Accept a pending handle into `target`. `target` must implement the [`IPCAcceptable`](#ipc-handle-passing) protocol. Call this synchronously from the `'handle'` event listener. Throws `INVALID_IPC_TARGET` if `target` does not implement the protocol.

#### `pipe.ref()`

Ref the pipe, preventing the process from exiting.

#### `pipe.unref()`

</details>

### bare-crypto

Cryptographic primitives for JavaScript

- repo: https://github.com/holepunchto/bare-crypto · 6★ · last push 2026-06-09 · JavaScript
- version: 1.15.3 · native addon · min Bare: — · types: yes · prebuilds: yes
- node-compat: no · bare deps: bare-assert, bare-stream · inbound deps: 2
- disposition: **full-page candidate**

<details><summary>API section (README)</summary>

#### `const hash = createHash(algorithm[, options])`

Create a new `Hash` instance with the specified `algorithm`. `algorithm` may be a string (e.g. `'sha256'`, `'sha-256'`) or a numeric constant from `constants.hash`. The `options` are forwarded to the `Transform` constructor from `bare-stream` (<https://github.com/holepunchto/bare-stream>).

### `class Hash`

Stream-based hash. Extends `Transform`.

#### `hash.update(data[, encoding])`

Push `data` into the hash. If `data` is a string, it is decoded using `encoding` (defaults to `'utf8'`). Returns the same `hash` for chaining. Throws once `digest()` has been called.

#### `const digest = hash.digest([encoding])`

Finalize the hash and return the digest. If `encoding` is provided (and is not `'buffer'`), the digest is returned as a string in that encoding; otherwise as a `Buffer`. Further calls to `update()` or `digest()` throw.

#### `const hmac = createHmac(algorithm, key[, options])`

Create a new `Hmac` instance using `algorithm` and `key`. `key` may be a string or `ArrayBufferView`. If `key` is a string, an `encoding` option (defaults to `'utf8'`) controls how it is decoded. The `options` are also forwarded to `Transform`.

### `class Hmac`

Stream-based HMAC. Extends `Transform`.

#### `hmac.update(data[, encoding])`

Push `data` into the HMAC. Same semantics as `hash.update()`.

#### `const digest = hmac.digest([encoding])`

Finalize the HMAC. Same semantics as `hash.digest()`.

#### `const cipher = createCipheriv(algorithm, key, iv[, options])`

Create a new `Cipheriv` instance using `algorithm`, `key`, and `iv` (initialization vector / nonce). `key` and `iv` must match the algorithm's required lengths. For AEAD algorithms (e.g. `AES128GCM`, `CHACHA20POLY1305`), the `options` may include an `authTagLength` (defaults to `16`).

Options include:

```js
options = {
  encoding: 'utf8',
  authTagLength: 16
}
```

`authTagLength` must be one of `12`, `14`, or `16`. The `options` are also forwarded to `Transform`.

### `class Cipheriv`

Stream-based encryption. Extends `Transform`.

</details>

### bare-native

Native application development framework for Bare

- repo: https://github.com/holepunchto/bare-native · 6★ · last push 2026-06-01 · JavaScript
- version: 0.2.0 · pure-JS · min Bare: — · types: no · prebuilds: no
- node-compat: no · bare deps: bare-app-kit, bare-events, bare-gtk, bare-ndk, bare-ui-kit, bare-web-kit, bare-web-kit-gtk, bare-win-ui · inbound deps: 0
- disposition: **catalog-row-only**

### bare-build

Application builder for Bare

- repo: https://github.com/holepunchto/bare-build · 6★ · last push 2026-06-01 · JavaScript
- version: 1.0.1 · pure-JS · min Bare: — · types: no · prebuilds: no
- node-compat: no · bare deps: bare-build-android-arm, bare-build-android-arm64, bare-build-android-ia32, bare-build-android-x64, bare-build-darwin-arm64, bare-build-darwin-x64, bare-build-ios-arm64, bare-build-ios-arm64-simulator, bare-build-ios-x64-simulator, bare-build-linux-arm64, bare-build-linux-x64, bare-build-win32-arm64, bare-build-win32-x64, bare-bundle-id, bare-fs, bare-lief, bare-link, bare-module-resolve, bare-module-traverse, bare-os, bare-pack, bare-path, bare-semver, bare-subprocess, bare-tpl, bare-unpack, bare-url · inbound deps: 0
- disposition: **catalog-row-only**

<details><summary>API section (README)</summary>

#### `for await (const resource of build(entry[, preflight][, options]))`

Options include:

```js
options = {
  name: pkg.name,
  version: pkg.version,
  author: pkg.author,
  description: pkg.description,
  icon,
  assetCatalog,
  iconName: 'AppIcon',
  identifier,
  infoPlist,
  androidManifest,
  appxManifest,
  desktopEntry,
  categories: ['Utility'],
  resources,
  compatibility,
  minimumVersion,
  targetVersion,
  language,
  base: '.',
  hosts: [],
  out: '.',
  runtime,
  standalone: false,
  package: false,
  sign: false,

  // Apple signing options
  identity: 'Apple Development',
  applicationIdentity: identity,
  installerIdentity: identity,
  provisioningProfile,
  keychain,
  entitlements,
  hardenedRuntime: false,

  // Windows signing options
  subject,
  subjectName,
  thumbprint,

  // Linux signing options
  key,

  // Android signing options

</details>

### bare-bundle

Application bundle format for JavaScript

- repo: https://github.com/holepunchto/bare-bundle · 6★ · last push 2026-04-23 · JavaScript
- version: 1.10.0 · pure-JS · min Bare: — · types: yes · prebuilds: no
- node-compat: no · bare deps: none · inbound deps: 8
- disposition: **catalog-row-only**

### bare-subprocess

Native process spawning for JavaScript

- repo: https://github.com/holepunchto/bare-subprocess · 5★ · last push 2026-06-13 · JavaScript
- version: 6.1.0 · native addon · min Bare: >=1.7.0 · types: yes · prebuilds: yes
- node-compat: no · bare deps: bare-env, bare-events, bare-os, bare-pipe, bare-structured-clone, bare-tcp, bare-url · inbound deps: 9
- disposition: **full-page candidate**

<details><summary>API section (README)</summary>

#### `const subprocess = spawn(file[, args][, options])`

Spawn `file` as a new subprocess with the given `args`. Returns a `Subprocess` instance. `args` may be `null` or omitted to spawn with no arguments. If `args` is omitted, the second argument is treated as `options`.

Options include:

```js
options = {
  cwd: os.cwd(),
  env: process.env,
  stdio: [],
  shell: false,
  detached: false,
  uid: -1,
  gid: -1,
  windowsHide: false,
  windowsVerbatimArguments: false,
  serialization: 'json'
}
```

`stdio` may be an array of slot descriptors or a single string applied to all of `stdin`, `stdout`, and `stderr`. Each slot may be one of:

| Value          | Description                                                                          |
| -------------- | ------------------------------------------------------------------------------------ |
| `'pipe'`       | Open a pipe between parent and child.                                                |
| `'overlapped'` | Like `'pipe'` but opens the pipe in overlapped mode on Windows.                      |
| `'inherit'`    | Inherit the parent's corresponding file descriptor (or `'ignore'` for fds beyond 2). |
| `'ignore'`     | Do not open the file descriptor in the child.                                        |
| `'ipc'`        | Open an IPC channel between parent and child. At most one slot may use this.         |

`serialization` selects how IPC messages are framed:

| Mode         | Description                                                                                           |
| ------------ | ----------------------------------------------------------------------------------------------------- |
| `'json'`     | Newline-delimited JSON. Only JSON-serializable values are supported. Default.                         |
| `'advanced'` | Length-prefixed structured clone, via `bare-structured-clone`. Supports `Date`, `Map`, `Buffer`, etc. |
| `'binary'`   | Raw pipe with no framing. `subprocess.channel` is `undefined`; use `subprocess.stdio[fd]` directly.   |

`shell` may be a string identifying the shell to use, or `true` to use the platform default (`/bin/sh`, `/system/bin/sh` on Android, or `cmd.exe` on Windows).

#### `const result = spawnSync(file[, args][, options])`

Synchronously spawn `file` and wait for it to exit. Returns an object:

```js
result = {
  pid,
  status,
  signal,

</details>

### bare-tty

Native TTY streams for JavaScript

- repo: https://github.com/holepunchto/bare-tty · 5★ · last push 2026-06-11 · C
- version: 5.1.1 · native addon · min Bare: >=1.16.0 · types: yes · prebuilds: yes
- node-compat: no · bare deps: bare-events, bare-signals, bare-stream · inbound deps: 4
- disposition: **catalog-row-only**

### bare-http1

Native HTTP/1 library for JavaScript

- repo: https://github.com/holepunchto/bare-http1 · 5★ · last push 2026-06-04 · JavaScript
- version: 4.5.7 · pure-JS · min Bare: — · types: yes · prebuilds: no
- node-compat: no · bare deps: bare-events, bare-http-parser, bare-stream, bare-tcp · inbound deps: 6
- disposition: **catalog-row-only**

### bare-buffer

Native buffers for JavaScript

- repo: https://github.com/holepunchto/bare-buffer · 5★ · last push 2026-06-01 · JavaScript
- version: 3.6.1 · native addon · min Bare: >=1.20.0 · types: yes · prebuilds: yes
- node-compat: no · bare deps: none · inbound deps: 3
- disposition: **catalog-row-only**

### bare-process

Node.js-compatible process control for Bare

- repo: https://github.com/holepunchto/bare-process · 5★ · last push 2026-05-01 · JavaScript
- version: 4.4.1 · pure-JS · min Bare: — · types: yes · prebuilds: no
- node-compat: yes · bare deps: bare-abort, bare-env, bare-events, bare-hrtime, bare-os, bare-posix, bare-signals, bare-stdio · inbound deps: 8
- disposition: **catalog-row-only**

### bare-signals

Native signal handling for JavaScript

- repo: https://github.com/holepunchto/bare-signals · 5★ · last push 2026-04-23 · C
- version: 4.2.0 · native addon · min Bare: >=1.7.0 · types: yes · prebuilds: yes
- node-compat: no · bare deps: bare-events, bare-os · inbound deps: 2
- disposition: **catalog-row-only**

### bare-atomics

Native synchronization primitives for JavaScript

- repo: https://github.com/holepunchto/bare-atomics · 5★ · last push 2026-04-23 · C
- version: 3.0.7 · native addon · min Bare: >=1.2.0 · types: yes · prebuilds: yes
- node-compat: no · bare deps: none · inbound deps: 0
- disposition: **full-page candidate**

<details><summary>API section (README)</summary>

### Mutex

#### `const mutex = new Mutex([options])`

Options include:

```js
{
  recursive: false
}
```

#### `const mutex = Mutex.from(handle[, options])`

Options are the same as `new Mutex()`.

#### `mutex.handle`

A `SharedArrayBuffer` with the underlying mutex handle.

#### `mutex.held`

Whether or not the current thread currently holds the mutex.

#### `mutex.lock()`

#### `const success = mutex.tryLock()`

#### `mutex.unlock()`

#### `mutex.destroy()`

### Semaphore

#### `const semaphore = new Semaphore(value)`

#### `const semaphore = Semaphore.from(handle)`

#### `semaphore.handle`

A `SharedArrayBuffer` with the underlying semaphore handle.

#### `semaphore.wait()`

#### `const success = semaphore.tryWait()`

#### `semaphore.post()`

#### `semaphore.destroy()`


</details>

### bare-assert

Assertion library for JavaScript

- repo: https://github.com/holepunchto/bare-assert · 5★ · last push 2026-04-23 · JavaScript
- version: 1.2.0 · pure-JS · min Bare: — · types: yes · prebuilds: no
- node-compat: no · bare deps: bare-inspect · inbound deps: 5
- disposition: **catalog-row-only**

### bare-ansi-escapes

Parse and produce ANSI escape sequences

- repo: https://github.com/holepunchto/bare-ansi-escapes · 5★ · last push 2026-04-23 · JavaScript
- version: 2.2.3 · pure-JS · min Bare: — · types: yes · prebuilds: no
- node-compat: no · bare deps: bare-stream · inbound deps: 3
- disposition: **catalog-row-only**

### bare-abort

Cause abnormal program termination and generate a crash report

- repo: https://github.com/holepunchto/bare-abort · 5★ · last push 2026-04-23 · C
- version: 2.0.13 · native addon · min Bare: — · types: yes · prebuilds: yes
- node-compat: no · bare deps: none · inbound deps: 1
- disposition: **catalog-row-only**

### bare-thread

Thread support for Bare

- repo: https://github.com/holepunchto/bare-thread · 4★ · last push 2026-06-15 · C
- version: 1.2.3 · native addon · min Bare: — · types: no · prebuilds: yes
- node-compat: no · bare deps: bare-bundle, bare-module-resolve, bare-module-traverse, bare-url · inbound deps: 1
- disposition: **catalog-row-only**

### bare-sidecar

Start and manage Bare sidecar processes from Node.js and Electron

- repo: https://github.com/holepunchto/bare-sidecar · 4★ · last push 2026-06-12 · JavaScript
- version: 0.5.0 · pure-JS · min Bare: — · types: yes · prebuilds: yes
- node-compat: yes · bare deps: bare-fs, bare-module, bare-os, bare-path, bare-pipe, bare-stream, bare-subprocess, bare-url · inbound deps: 0
- disposition: **full-page candidate** · ⚠ missing from catalog

<details><summary>API section (README)</summary>

#### `const sidecar = new Sidecar(entry[, args][, options])`

Spawn a bundled Bare runtime running `entry` and return a `Sidecar`. `entry` is the path to the module to run, typically resolved with `require.resolve()`. `args` is an array of additional command line arguments passed to the process. `options` is reserved for future use.

`Sidecar` extends a duplex stream. Writing to the stream sends data to the sidecar over its IPC channel, and data received from the sidecar over the same channel is emitted as `'data'` events. Destroying the stream kills the sidecar process.

The correct prebuilt Bare binary for the current platform and architecture is selected automatically and made executable on first use.

#### `sidecar.stdin`

The writable standard input stream of the underlying process.

#### `sidecar.stdout`

The readable standard output stream of the underlying process.

#### `sidecar.stderr`

The readable standard error stream of the underlying process.

#### `event: 'exit'`

Emitted when the sidecar process exits, with the arguments `code` and `status`. `code` is the exit code, or `null` if the process was terminated by a signal. `status` is the signal name that terminated the process, or `null` if it exited normally.

#### `event: 'close'`

Emitted after the sidecar process has exited and the stream has been destroyed.

</details>

### bare-tui

A little TUI framework for Bare, based on bubbletea

- repo: https://github.com/holepunchto/bare-tui · 4★ · last push 2026-06-11 · JavaScript
- version: 0.0.3 · pure-JS · min Bare: — · types: yes · prebuilds: no
- node-compat: yes · bare deps: bare-ansi-escapes, bare-tty · inbound deps: 3
- disposition: **catalog-row-only**

### bare-net

TCP and IPC servers and clients for JavaScript

- repo: https://github.com/holepunchto/bare-net · 4★ · last push 2026-06-02 · JavaScript
- version: 2.3.2 · pure-JS · min Bare: — · types: yes · prebuilds: no
- node-compat: no · bare deps: bare-events, bare-pipe, bare-stream, bare-tcp · inbound deps: 2
- disposition: **catalog-row-only**

### bare-path

Path manipulation library for JavaScript

- repo: https://github.com/holepunchto/bare-path · 4★ · last push 2026-06-01 · JavaScript
- version: 3.0.1 · pure-JS · min Bare: — · types: yes · prebuilds: no
- node-compat: no · bare deps: bare-os · inbound deps: 24
- disposition: **catalog-row-only**

### bare-png

PNG support for Bare

- repo: https://github.com/holepunchto/bare-png · 4★ · last push 2026-05-29 · C
- version: 1.0.6 · native addon · min Bare: — · types: no · prebuilds: yes
- node-compat: no · bare deps: none · inbound deps: 1
- disposition: **catalog-row-only**

### bare-link

Native addon linker for Bare

- repo: https://github.com/holepunchto/bare-link · 4★ · last push 2026-05-29 · JavaScript
- version: 3.2.2 · pure-JS · min Bare: — · types: no · prebuilds: no
- node-compat: no · bare deps: bare-fs, bare-lief, bare-module-resolve, bare-os, bare-path, bare-semver, bare-subprocess, bare-url · inbound deps: 1
- disposition: **catalog-row-only** · ⚠ missing from catalog

<details><summary>API section (README)</summary>

#### `for await (const resource of link([base][, options]))`

Options include:

```js
options = {
  hosts: [],
  out: '.',
  preset,
  sign: false,

  // Apple signing options
  identity: 'Apple Development',
  keychain,

  // Windows signing options
  subject,
  subjectName,
  thumbprint
}
```

</details>

### bare-addon

Example Bare addon

- repo: https://github.com/holepunchto/bare-addon · 4★ · last push 2026-05-11 · C
- version: 1.0.0 · native addon · min Bare: — · types: no · prebuilds: yes
- node-compat: no · bare deps: none · inbound deps: 0
- disposition: **catalog-row-only** · ⚠ missing from catalog

### bare-https

HTTPS library for JavaScript

- repo: https://github.com/holepunchto/bare-https · 4★ · last push 2026-04-28 · JavaScript
- version: 3.0.0 · pure-JS · min Bare: — · types: yes · prebuilds: no
- node-compat: no · bare deps: bare-http1, bare-tcp, bare-tls · inbound deps: 4
- disposition: **catalog-row-only**

### bare-readline

Line editing for interactive CLIs with command history

- repo: https://github.com/holepunchto/bare-readline · 4★ · last push 2026-04-23 · JavaScript
- version: 1.3.1 · pure-JS · min Bare: — · types: yes · prebuilds: no
- node-compat: no · bare deps: bare-ansi-escapes, bare-stream · inbound deps: 2
- disposition: **catalog-row-only**

### bare-pack-drive

Pack drives to Bare bundles

- repo: https://github.com/holepunchto/bare-pack-drive · 4★ · last push 2026-04-23 · JavaScript
- version: 2.0.0 · pure-JS · min Bare: — · types: no · prebuilds: no
- node-compat: no · bare deps: bare-pack · inbound deps: 1
- disposition: **catalog-row-only** · ⚠ missing from catalog

<details><summary>API section (README)</summary>

#### `const bundle = await pack(drive[, entry][, options])`

Options supported by <https://github.com/holepunchto/bare-pack> may be specified.

</details>

### bare-encoding

WHATWG text encoding interfaces for JavaScript

- repo: https://github.com/holepunchto/bare-encoding · 4★ · last push 2026-04-23 · JavaScript
- version: 1.0.3 · pure-JS · min Bare: — · types: yes · prebuilds: no
- node-compat: no · bare deps: bare-stream · inbound deps: 1
- disposition: **catalog-row-only**

### bare-dns

Domain name resolution for JavaScript

- repo: https://github.com/holepunchto/bare-dns · 4★ · last push 2026-04-23 · C
- version: 2.1.4 · native addon · min Bare: >=1.7.0 · types: yes · prebuilds: yes
- node-compat: no · bare deps: none · inbound deps: 2
- disposition: **catalog-row-only**

### bare-diagnostics-channel

Create and publish data to named channels for diagnostics purposes

- repo: https://github.com/holepunchto/bare-diagnostics-channel · 4★ · last push 2026-04-23 · JavaScript
- version: 1.1.0 · pure-JS · min Bare: — · types: no · prebuilds: no
- node-compat: no · bare deps: none · inbound deps: 1
- disposition: **catalog-row-only** · ⚠ missing from catalog

### bare-bundle-compile

Compile a bundle of CommonJS modules to a single module

- repo: https://github.com/holepunchto/bare-bundle-compile · 4★ · last push 2026-04-23 · JavaScript
- version: 1.2.2 · pure-JS · min Bare: — · types: no · prebuilds: no
- node-compat: no · bare deps: none · inbound deps: 0
- disposition: **catalog-row-only** · ⚠ missing from catalog

<details><summary>API section (README)</summary>

#### `const code = compile(bundle)`

</details>

### bare-rust

Rust and Bare, sitting in a tree

- repo: https://github.com/holepunchto/bare-rust · 4★ · last push 2025-12-08 · Rust
- version: — · pure-JS · min Bare: — · types: no · prebuilds: no
- node-compat: no · bare deps: none · inbound deps: 0
- disposition: **catalog-row-only** · ⚠ missing from catalog

### bare-inspector

V8 inspector support for Bare

- repo: https://github.com/holepunchto/bare-inspector · 3★ · last push 2026-06-16 · JavaScript
- version: 6.0.2 · native addon · min Bare: >=1.25.0 <1.29.0 · types: yes · prebuilds: yes
- node-compat: no · bare deps: bare-events, bare-http1, bare-stream, bare-url, bare-ws · inbound deps: 2
- disposition: **full-page candidate**

<details><summary>API section (README)</summary>

#### `const session = new Session()`

#### `session.connect()`

#### `session.post()`

#### `session.destroy()`

#### `session.on('<inspector-protocol-method>', message)`

#### `session.on('inspectorNotification', message)`

#### `const snapshot = new HeapSnapshot(session)`

#### `for await (const chunk of snapshot)`

</details>

### bare-tui-form

A declarative form builder for bare-tui

- repo: https://github.com/holepunchto/bare-tui-form · 3★ · last push 2026-06-12 · JavaScript
- version: 0.0.2 · pure-JS · min Bare: — · types: no · prebuilds: no
- node-compat: no · bare deps: bare-tui · inbound deps: 1
- disposition: **catalog-row-only** · ⚠ missing from catalog

### bare-module-traverse

Low-level module graph traversal for Bare

- repo: https://github.com/holepunchto/bare-module-traverse · 3★ · last push 2026-06-10 · JavaScript
- version: 2.1.2 · pure-JS · min Bare: — · types: yes · prebuilds: no
- node-compat: no · bare deps: bare-addon-resolve, bare-module-lexer, bare-module-resolve · inbound deps: 8
- disposition: **full-page candidate** · ⚠ missing from catalog

<details><summary>API section (README)</summary>

#### `const dependencies = traverse(url[, options], readModule[, listPrefix])`

Traverse the module graph rooted at `url`, which must be a WHATWG `URL` instance. `readModule` is called with a `URL` instance for every module to be read and must either return the module source, if it exists, or `null`. `listPrefix` is called with a `URL` instance of every prefix to be listed and must yield `URL` instances that have the specified `URL` as a prefix. If not provided, prefixes won't be traversed. If `readModule` returns a promise or `listPrefix` returns a promise generator, synchronous iteration is not supported.

Options include:

```js
options = {
  defaultType: constants.SCRIPT,
  aliases: {
    // Map an extension to a supported extension, e.g. `'.ts': '.js'`. The
    // aliased extension is used for module type detection, so `readModule()`
    // must return source compatible with that type. Aliased modules are also
    // emitted with the aliased extension, and resolutions to them are
    // rewritten to match, so `'.ts': '.js'` yields a `file:///foo.js`
    // dependency rather than `file:///foo.ts`.
  },
  resolve: resolve.default
}
```

Options supported by <https://github.com/holepunchto/bare-module-resolve> and <https://github.com/holepunchto/bare-addon-resolve> may also be specified.

#### `for (const dependency of dependencies)`

Synchronously iterate the module graph. Each yielded dependency has the following shape:

```js
dependency = {
  url: URL,
  source: 'string' | Buffer, // Source as returned by `readModule()`
  imports: {
    // See https://github.com/holepunchto/bare-module#imports
  },
  lexer: {
    imports: [
      // See https://github.com/holepunchto/bare-module-lexer#api
    ]
  }
}
```

#### `for await (const dependency of dependencies)`

Asynchronously iterate the module graph. If `readModule` returns a promise or `listPrefix` returns a promise generator, these will be awaited. The same comments as `for (const dependency of dependencies)` apply.

### Resolution

Module and addon resolution is configurable by providing a resolver function. A resolver function is a generator function that yields values matching the shapes defined by <https://github.com/holepunchto/bare-module-resolve#algorithm>. Several resolvers are provided out of the box to support the most common use cases.


</details>

### bare-sdl

Bare binding for SDL

- repo: https://github.com/holepunchto/bare-sdl · 3★ · last push 2026-06-05 · JavaScript
- version: 1.0.0-5 · native addon · min Bare: >=1.14.0 · types: no · prebuilds: yes
- node-compat: no · bare deps: none · inbound deps: 0
- disposition: **full-page candidate** · ⚠ missing from catalog

<details><summary>API section (README)</summary>

### `Window`

The `Window` API provides functionality to create SDL windows.

```js
const window = new sdl.Window(title, width, height[, flags])
```

Parameters:

- `title` (`string`): The window title
- `width` (`number`): The window width in pixels
- `height` (`number`): The window height in pixels
- `flags` (`number`, optional): Window creation flags. Defaults to 0

Available flags are exposed through the `constants` object. Common flags include:

- `SDL_WINDOW_FULLSCREEN`: Fullscreen window
- `SDL_WINDOW_BORDERLESS`: Borderless window
- `SDL_WINDOW_RESIZABLE`: Resizable window
- `SDL_WINDOW_VULKAN`: Window usable with Vulkan
- `SDL_WINDOW_METAL`: Window usable with Metal

**Returns**: A new `sdl.Window` instance

#### Methods

##### `Window.destroy()`

Destroy `Window` and associated resources.

**Returns**: `void`

Example:

```js
const window = new sdl.Window('My Window', 800, 600)
window.destroy()
```

### `Renderer`

The `Renderer` API provides functionality to render graphics using SDL.

```js
const renderer = new sdl.Renderer(window)
```

Parameters:


</details>

### bare-url

WHATWG URL implementation for JavaScript

- repo: https://github.com/holepunchto/bare-url · 3★ · last push 2026-06-04 · JavaScript
- version: 2.4.5 · native addon · min Bare: — · types: yes · prebuilds: yes
- node-compat: no · bare deps: bare-path · inbound deps: 17
- disposition: **full-page candidate**

<details><summary>API section (README)</summary>

#### `const url = new URL(input[, base])`

Parse `input` as a URL. If `base` is provided, `input` is resolved relative to `base`. Throws if `input` is not a valid URL.

#### `url.href`

The full serialized URL string. Setting this property reparses the URL.

#### `url.protocol`

The URL scheme followed by `':'`, e.g. `'https:'`.

#### `url.username`

The username portion of the URL, or an empty string.

#### `url.password`

The password portion of the URL, or an empty string.

#### `url.host`

The hostname and port, e.g. `'example.com:8080'`.

#### `url.hostname`

The hostname without the port.

#### `url.port`

The port as a string, or an empty string if not present.

#### `url.pathname`

The path portion of the URL.

#### `url.search`

The query string including the leading `'?'`, or an empty string.

#### `url.searchParams`

A `URLSearchParams` object for the query string. Mutations to the params are reflected in the URL.

#### `url.hash`

The fragment including the leading `'#'`, or an empty string.

#### `url.toString()`


</details>

### bare-console

WHATWG debugging console for JavaScript

- repo: https://github.com/holepunchto/bare-console · 3★ · last push 2026-06-04 · JavaScript
- version: 6.2.0 · pure-JS · min Bare: — · types: yes · prebuilds: no
- node-compat: no · bare deps: bare-format, bare-hrtime, bare-logger, bare-system-logger, bare-type · inbound deps: 1
- disposition: **full-page candidate**

<details><summary>API section (README)</summary>

#### `const console = new Console([log])`

Construct a new `Console`. By default, output is written through `bare-logger` (<https://github.com/holepunchto/bare-logger>), or `bare-system-logger` (<https://github.com/holepunchto/bare-system-logger>) on Android.

Pass a custom `log` object to redirect output:

```js
const Console = require('bare-console')
const FileLog = require('bare-file-logger')

const console = new Console(new FileLog('my-logs.txt'))
```

The `log` object implements the methods used by the console operations being called:

```js
log = {
  debug(...data),
  info(...data),
  warn(...data),
  error(...data),
  clear(),
  format(...data)
}
```

A minimal backend only needs the subset it intends to support; for example, `info` is sufficient for `console.log()`.

#### `console.log(...data)`

#### `console.info(...data)`

#### `console.debug(...data)`

#### `console.warn(...data)`

#### `console.error(...data)`

Forward `data` to the corresponding method on the backend. `console.log()` is an alias for `console.info()`.

#### `console.clear()`

Forward to `log.clear()`.

#### `console.time([label])`

Start a timer identified by `label` (default `'default'`). Warns if a timer with the same label is already running.

#### `console.timeLog([label[, ...data]])`


</details>

### bare-rpc-swift

https://github.com/holepunchto/librpc ABI compatible RPC for Swift 

- repo: https://github.com/holepunchto/bare-rpc-swift · 3★ · last push 2026-05-30 · Swift
- version: — · pure-JS · min Bare: — · types: no · prebuilds: no
- node-compat: no · bare deps: none · inbound deps: 0
- disposition: **catalog-row-only** · ⚠ missing from catalog

### bare-gif

GIF support for Bare

- repo: https://github.com/holepunchto/bare-gif · 3★ · last push 2026-05-29 · C
- version: 1.1.4 · native addon · min Bare: — · types: no · prebuilds: yes
- node-compat: no · bare deps: none · inbound deps: 1
- disposition: **catalog-row-only** · ⚠ missing from catalog

### bare-jpeg

JPEG support for Bare

- repo: https://github.com/holepunchto/bare-jpeg · 3★ · last push 2026-05-29 · C
- version: 1.1.2 · native addon · min Bare: — · types: no · prebuilds: yes
- node-compat: no · bare deps: none · inbound deps: 1
- disposition: **catalog-row-only** · ⚠ missing from catalog

### bare-heif

HEIF support for Bare

- repo: https://github.com/holepunchto/bare-heif · 3★ · last push 2026-05-29 · C
- version: 1.0.11 · native addon · min Bare: — · types: no · prebuilds: yes
- node-compat: no · bare deps: none · inbound deps: 1
- disposition: **catalog-row-only** · ⚠ missing from catalog

### bare-tiff

TIFF support for Bare

- repo: https://github.com/holepunchto/bare-tiff · 3★ · last push 2026-05-29 · C
- version: 1.0.4 · native addon · min Bare: — · types: no · prebuilds: yes
- node-compat: no · bare deps: none · inbound deps: 1
- disposition: **catalog-row-only** · ⚠ missing from catalog

### bare-node-runtime

Compatibility layer for Node.js builtins and globals in Bare

- repo: https://github.com/holepunchto/bare-node-runtime · 3★ · last push 2026-05-26 · JavaScript
- version: 1.4.0 · pure-JS · min Bare: — · types: no · prebuilds: no
- node-compat: yes · bare deps: bare-abort-controller, bare-assert, bare-async-hooks, bare-buffer, bare-console, bare-crypto, bare-dgram, bare-diagnostics-channel, bare-dns, bare-events, bare-fetch, bare-fs, bare-http1, bare-https, bare-inspector, bare-module, bare-net, bare-os, bare-path, bare-performance, bare-process, bare-punycode, bare-querystring, bare-readline, bare-repl, bare-sqlite, bare-stream, bare-string-decoder, bare-subprocess, bare-timers, bare-tls, bare-tty, bare-url, bare-utils, bare-v8, bare-vm, bare-worker, bare-ws, bare-zlib · inbound deps: 0
- disposition: **catalog-row-only** · ⚠ missing from catalog

### bare-distributable

Template repository for creating custom Bare distributables

- repo: https://github.com/holepunchto/bare-distributable · 3★ · last push 2026-05-12 · C
- version: — · pure-JS · min Bare: — · types: no · prebuilds: no
- node-compat: no · bare deps: none · inbound deps: 0
- disposition: **catalog-row-only** · ⚠ missing from catalog

### bare-webp

WebP support for Bare

- repo: https://github.com/holepunchto/bare-webp · 3★ · last push 2026-04-23 · C
- version: 1.3.0 · native addon · min Bare: — · types: no · prebuilds: yes
- node-compat: no · bare deps: none · inbound deps: 1
- disposition: **catalog-row-only** · ⚠ missing from catalog

### bare-vm

Isolated JavaScript contexts for Bare

- repo: https://github.com/holepunchto/bare-vm · 3★ · last push 2026-04-23 · JavaScript
- version: 1.0.1 · pure-JS · min Bare: — · types: yes · prebuilds: no
- node-compat: no · bare deps: bare-realm · inbound deps: 1
- disposition: **catalog-row-only** · ⚠ missing from catalog

### bare-type

Cross-realm type predicates for Bare

- repo: https://github.com/holepunchto/bare-type · 3★ · last push 2026-04-23 · JavaScript
- version: 1.1.0 · native addon · min Bare: >=1.2.0 · types: yes · prebuilds: yes
- node-compat: no · bare deps: none · inbound deps: 4
- disposition: **catalog-row-only**

### bare-timers

Native timers for Javascript

- repo: https://github.com/holepunchto/bare-timers · 3★ · last push 2026-04-23 · JavaScript
- version: 3.2.1 · native addon · min Bare: >=1.7.0 · types: yes · prebuilds: yes
- node-compat: no · bare deps: none · inbound deps: 1
- disposition: **catalog-row-only**

### bare-run

Cross-platform script runner for Bare

- repo: https://github.com/holepunchto/bare-run · 3★ · last push 2026-04-23 · JavaScript
- version: 0.1.0 · pure-JS · min Bare: >=1.16.0 · types: no · prebuilds: yes
- node-compat: no · bare deps: bare-bundle, bare-bundle-id, bare-fs, bare-module, bare-module-traverse, bare-os, bare-pack, bare-path, bare-subprocess, bare-unpack, bare-url, bare-which · inbound deps: 0
- disposition: **catalog-row-only** · ⚠ missing from catalog

<details><summary>API section (README)</summary>

#### `const result = await run(entry[, options])`

Run a JavaScript module at the specified entry point. The function will automatically detect the platform and architecture if not specified, and handle the execution environment setup.

Parameters:

- `entry` (string) - The entry point file path
- `options` (object, optional) - Configuration options

Options include:

```js
options = {
  base: '.',
  host,
  device
}
```

The function returns a Promise that resolves when the bundle execution completes.

### Supported platforms

- **Android**: Runs on Android devices and emulators via ADB
- **iOS**: Runs on iOS simulators and devices via Xcode tools
- **Desktop**: Runs on Windows, macOS, and Linux natively

</details>

### bare-logger

Low-level logger for Bare

- repo: https://github.com/holepunchto/bare-logger · 3★ · last push 2026-04-23 · C
- version: 2.0.3 · native addon · min Bare: — · types: yes · prebuilds: yes
- node-compat: no · bare deps: bare-format · inbound deps: 3
- disposition: **catalog-row-only**

### bare-inspect

Inspect objects as strings for debugging

- repo: https://github.com/holepunchto/bare-inspect · 3★ · last push 2026-04-23 · JavaScript
- version: 3.1.4 · native addon · min Bare: >=1.18.0 · types: yes · prebuilds: yes
- node-compat: no · bare deps: bare-ansi-escapes, bare-type · inbound deps: 4
- disposition: **catalog-row-only**

### bare-image-resample

Image resampling support for Bare

- repo: https://github.com/holepunchto/bare-image-resample · 3★ · last push 2026-04-23 · C
- version: 1.0.2 · native addon · min Bare: — · types: no · prebuilds: yes
- node-compat: no · bare deps: none · inbound deps: 1
- disposition: **catalog-row-only** · ⚠ missing from catalog

### bare-hrtime

High-resolution timers for JavaScript

- repo: https://github.com/holepunchto/bare-hrtime · 3★ · last push 2026-04-23 · C
- version: 2.1.1 · native addon · min Bare: — · types: yes · prebuilds: yes
- node-compat: no · bare deps: none · inbound deps: 2
- disposition: **catalog-row-only**

### bare-env

Environment variable support for JavaScript

- repo: https://github.com/holepunchto/bare-env · 3★ · last push 2026-04-23 · JavaScript
- version: 3.0.0 · pure-JS · min Bare: — · types: yes · prebuilds: no
- node-compat: no · bare deps: bare-os · inbound deps: 7
- disposition: **catalog-row-only**

### bare-dgram

Native UDP for JavaScript

- repo: https://github.com/holepunchto/bare-dgram · 3★ · last push 2026-04-23 · JavaScript
- version: 1.0.1 · pure-JS · min Bare: — · types: yes · prebuilds: no
- node-compat: no · bare deps: bare-events · inbound deps: 2
- disposition: **catalog-row-only**

### bare-boot

Boot drives in Bare

- repo: https://github.com/holepunchto/bare-boot · 3★ · last push 2026-04-23 · JavaScript
- version: 4.0.0 · pure-JS · min Bare: — · types: no · prebuilds: no
- node-compat: no · bare deps: bare-module, bare-module-traverse, bare-pack-drive, bare-path · inbound deps: 0
- disposition: **catalog-row-only** · ⚠ missing from catalog

### bare-async-hooks

`async_hooks` shim for Bare

- repo: https://github.com/holepunchto/bare-async-hooks · 3★ · last push 2026-03-06 · JavaScript
- version: 0.0.0 · pure-JS · min Bare: — · types: no · prebuilds: no
- node-compat: no · bare deps: none · inbound deps: 1
- disposition: **catalog-row-only** · ⚠ missing from catalog

### bare-broadcast-channel

Multi-producer, multi-consumer inter-thread broadcast messaging for JavaScript

- repo: https://github.com/holepunchto/bare-broadcast-channel · 2★ · last push 2026-06-05 · C
- version: 0.1.0 · native addon · min Bare: — · types: yes · prebuilds: yes
- node-compat: no · bare deps: bare-events, bare-stream, bare-structured-clone · inbound deps: 0
- disposition: **full-page candidate** · ⚠ missing from catalog

<details><summary>API section (README)</summary>

#### `const channel = new BroadcastChannel([options])`

Create a new broadcast channel. The channel is backed by a `SharedArrayBuffer` exposed as `channel.handle` that can be passed to other threads to share the channel across them.

Options include:

```js
options = {
  handle,
  interfaces: []
}
```

If `handle` is provided, the channel is restored from that existing `SharedArrayBuffer` rather than allocating a fresh one. `interfaces` is a list of serializable or transferable constructors used by <https://github.com/holepunchto/bare-structured-clone> when encoding values.

#### `BroadcastChannel.MAX_PORTS`

Maximum number of ports that may connect to a single channel over its lifetime.

#### `const channel = BroadcastChannel.from(handle[, options])`

Restore a channel from its `SharedArrayBuffer` `handle`. `options` accepts the same fields as the constructor, except for `handle`.

#### `channel.handle`

The `SharedArrayBuffer` backing the channel. Pass this to other threads to share the channel.

#### `channel.interfaces`

The serializable and transferable interfaces registered on the channel.

#### `const port = channel.connect()`

Connect a new port to the channel. Throws if `MAX_PORTS` has been reached. Port slots are not reused, so the cap applies to the total number of connections over the channel's lifetime, not the number of concurrently connected ports.

### Port

A `Port` is an `EventEmitter` and is both iterable (`for ... of`) and async iterable (`for await ... of`).

#### `port.peers`

The current number of connected peer ports, excluding `port` itself.

#### `const flushed = await port.write(value[, options])`

Broadcast `value` to every currently connected peer. Resolves to `true` once `value` has been pushed to every peer's queue, or `false` if `port` is closed. If any peer's queue is full, the write waits for it to drain before resolving.

Options include:

```js

</details>

### bare-structured-clone

Structured cloning algorithm for JavaScript

- repo: https://github.com/holepunchto/bare-structured-clone · 2★ · last push 2026-06-04 · JavaScript
- version: 1.5.5 · native addon · min Bare: >=1.2.0 · types: yes · prebuilds: yes
- node-compat: no · bare deps: bare-buffer, bare-type, bare-url · inbound deps: 4
- disposition: **full-page candidate**

<details><summary>API section (README)</summary>

#### `const copy = structuredClone(value[, options])`

Clone `value` by serializing and then deserializing it. `options` may include a `transfer` list and an `interfaces` list:

```js
options = {
  transfer: [],
  interfaces: []
}
```

`transfer` is an array of `ArrayBuffer` and `Transferable` objects whose ownership is transferred to the clone. After transfer, the original objects are detached. `interfaces` is an array of `Serializable` and `Transferable` constructors that may appear in `value`; each constructor must be present at both serialize and deserialize time.

#### `const serialized = serialize(value[, forStorage[, interfaces]])`

Serialize `value` to a `SerializedValue` describing its structure. If `forStorage` is `true`, the value is being serialized for persistent storage; types that cannot be persisted (such as `SharedArrayBuffer`) will throw.

#### `const serialized = serializeWithTransfer(value[, transferList[, interfaces]])`

Like `serialize()` but additionally detaches the objects in `transferList` and embeds their backing stores in the resulting `SerializedTransfer`.

#### `const value = deserialize(serialized[, interfaces])`

Deserialize a value previously produced by `serialize()`.

#### `const value = deserializeWithTransfer(serialized[, interfaces])`

Deserialize a value previously produced by `serializeWithTransfer()`, attaching the transferred backing stores to new objects.

#### `preencode(state, serialized)`

#### `encode(state, serialized)`

#### `const serialized = decode(state)`

The module is itself a compact-encoding codec for `SerializedValue` and `SerializedTransfer`, prefixing the payload with an ABI version header. Use it with `compact-encoding` to encode a serialized value to a buffer:

```js
const c = require('compact-encoding')
const structuredClone = require('bare-structured-clone')
const { serialize, deserialize } = structuredClone

const buffer = c.encode(structuredClone, serialize({ hello: 'world' }))
const value = deserialize(c.decode(structuredClone, buffer))
```

#### `constants`

Numeric tags used in serialized values. Also available as `require('bare-structured-clone/constants')`.


</details>

### bare-tls

Transport Layer Security (TLS) streams for JavaScript

- repo: https://github.com/holepunchto/bare-tls · 2★ · last push 2026-06-04 · JavaScript
- version: 3.1.7 · native addon · min Bare: >=1.7.0 · types: yes · prebuilds: yes
- node-compat: no · bare deps: bare-net, bare-stream · inbound deps: 2
- disposition: **full-page candidate**

<details><summary>API section (README)</summary>

#### `const socket = new tls.Socket(stream[, options])`

Wraps an existing duplex `stream` with TLS. The underlying `stream` handles transport; the TLS socket handles encryption and decryption.

Options include:

```js
options = {
  isServer: false,
  cert: null,
  key: null,
  host: null,
  rejectUnauthorized: true,
  ca: null,
  alpnProtocols: null,
  eagerOpen: true,
  allowHalfOpen: true,
  readBufferSize: 65536
}
```

`isServer` controls whether the socket acts as a TLS server or client. If `true`, `cert` and `key` must be provided.

`cert` and `key` are `Buffer`s containing PEM-encoded certificate and private key data, respectively.

`host` enables hostname verification against the server certificate. For DNS names it is also sent as the SNI (Server Name Indication) extension; for IP literals it is matched against the certificate's IP SANs and SNI is suppressed per RFC 6066. Required for client sockets unless `rejectUnauthorized` is `false`.

`rejectUnauthorized` controls whether the client rejects connections when certificate verification fails. Defaults to `true`.

`ca` is a `Buffer` containing one or more PEM-encoded CA certificates. When provided, only these CAs are used for verification instead of the bundled Mozilla root certificates.

`alpnProtocols` is an array of ALPN protocol name strings, ordered by preference.

#### `socket.socket`

The underlying duplex stream.

#### `socket.encrypted`

Always `true`.

#### `socket.alpnProtocol`

The negotiated ALPN protocol as a string, or `null` if no protocol was negotiated.

#### `event: 'connect'`

Emitted when the TLS handshake completes.

#### `const server = tls.createServer([options][, onconnection])`

</details>

### bare-app-kit

AppKit bindings and runtime for Bare

- repo: https://github.com/holepunchto/bare-app-kit · 2★ · last push 2026-05-29 · Objective-C
- version: 0.2.0 · native addon · min Bare: — · types: no · prebuilds: yes
- node-compat: no · bare deps: bare-events · inbound deps: 1
- disposition: **catalog-row-only** · ⚠ missing from catalog

### bare-gtk

GTK bindings and runtime for Bare

- repo: https://github.com/holepunchto/bare-gtk · 2★ · last push 2026-05-29 · C
- version: 0.2.0 · native addon · min Bare: — · types: no · prebuilds: yes
- node-compat: no · bare deps: none · inbound deps: 2
- disposition: **catalog-row-only** · ⚠ missing from catalog

### bare-union-bundle

Union bundle

- repo: https://github.com/holepunchto/bare-union-bundle · 2★ · last push 2026-05-26 · JavaScript
- version: 1.1.1 · pure-JS · min Bare: — · types: no · prebuilds: no
- node-compat: no · bare deps: bare-bundle, bare-fs, bare-module, bare-module-traverse · inbound deps: 0
- disposition: **full-page candidate** · ⚠ missing from catalog

<details><summary>API section (README)</summary>

#### `const b = new UnionBundle(bundles)`

Create a union of the `bundles`, an array of [Bundles](https://github.com/holepunchto/bare-bundle).

#### `const b = UnionBundle.require(files)`

Shortcut for creating a `UnionBundle` from file paths (`files`) automatically reading them as Bundles.

#### `const layer = await b.add(root, entrypoint, opts = {})`

Create a new layer bundle for an `entrypoint` skipping modules referenced in the existing bundles in the `UnionBundle`. `root` is a `URL` for the root of the `entrypoint`, aka it contains the `node_modules` directory where dependencies are installed.

`opts` includes:

```
{
  skipModules: true // skip bundling dependencies in `node_modules` in `root`
}
```

A layer bundle can be loaded by creating the union with the previous layer bundles via the `UnionBundle` constructor and loading via the union's `load()`. For example:

```js
// Creating
const b = UnionBundle.require('./0.bundle', './1.bundle', './2.bundle')

const layer = b.add(new URL('file://root-of-project'), 'entrypoint.js')
await fs.writeFile('./3.bundle', layer.toBuffer())

// Loading
const b = UnionBundle.require('./0.bundle', './1.bundle', './2.bundle', './3.bundle')
const mod = b.load(new URL('file://root-of-project'), 'entrypoint.js')
```

#### `const bundle = b.checkout(index)`

Create a Bundle resolving all dependencies at the provided `index`.

Example:

```
const past = UnionBundle.require('./0.bundle', './1.bundle', './2.bundle')
const mod1 = past.checkout(1) // returns a resolved bundle combining `1.bundle` & `0.bundle`
```

#### `const mod = b.load(root, entrypoint, index, opts = {})`

Load `entrypoint` (relative to `root`) from the bundle at the provided checkout `index`.

`opts` includes:

</details>

### bare-abort-controller

Abort controller support for Bare

- repo: https://github.com/holepunchto/bare-abort-controller · 2★ · last push 2026-05-13 · JavaScript
- version: 1.1.2 · pure-JS · min Bare: — · types: yes · prebuilds: no
- node-compat: no · bare deps: bare-events · inbound deps: 1
- disposition: **catalog-row-only** · ⚠ missing from catalog

### bare-form-data

Form data support for Bare

- repo: https://github.com/holepunchto/bare-form-data · 2★ · last push 2026-05-07 · JavaScript
- version: 1.2.2 · pure-JS · min Bare: — · types: no · prebuilds: no
- node-compat: no · bare deps: bare-buffer, bare-stream · inbound deps: 1
- disposition: **full-page candidate**

<details><summary>API section (README)</summary>

#### `const formData = new FormData()`

Create a new `FormData` instance.

#### `formData.append(name, value[, filename])`

Append a new entry with the given `name` and `value`. If `value` is not a string and not a `File`, it will be wrapped in a `File` with the given `filename` or `'blob'` as the default name.

#### `formData.set(name, value[, filename])`

Set the entry with the given `name` to `value`, replacing any existing entries with the same name.

#### `formData.delete(name)`

Remove all entries with the given `name`.

#### `formData.get(name)`

Return the first value associated with `name`, or `null` if no entry exists.

#### `formData.getAll(name)`

Return an array of all values associated with `name`.

#### `formData.has(name)`

Return `true` if an entry with the given `name` exists.

#### `const blob = new Blob(parts[, options])`

Create a new `Blob` from an array of `parts`, which may be strings, `Blob` instances, buffers, or typed arrays.

Options include:

```js
options = {
  type
}
```

#### `blob.size`

The size of the blob in bytes.

#### `blob.type`

The MIME type of the blob.

#### `blob.stream()`


</details>

### bare-module-resolve

Low-level module resolution algorithm for Bare

- repo: https://github.com/holepunchto/bare-module-resolve · 2★ · last push 2026-04-30 · JavaScript
- version: 1.12.2 · pure-JS · min Bare: — · types: yes · prebuilds: no
- node-compat: no · bare deps: bare-semver · inbound deps: 7
- disposition: **full-page candidate** · ⚠ missing from catalog

<details><summary>API section (README)</summary>

#### `const resolver = resolve(specifier, parentURL[, options][, readPackage])`

Resolve `specifier` relative to `parentURL`, which must be a WHATWG `URL` instance. `readPackage` is called with a `URL` instance for every package manifest to be read and must either return the parsed JSON package manifest, if it exists, or `null`. If `readPackage` returns a promise, synchronous iteration is not supported.

Options include:

```js
options = {
  // A default "imports" map to apply to all specifiers. Follows the same
  // syntax and rules as the "imports" property defined in `package.json`.
  imports,
  // A list of builtin module specifiers. If matched, the protocol of the
  // resolved URL will be `builtinProtocol`.
  builtins: [],
  // The protocol to use for resolved builtin module specifiers.
  builtinProtocol: 'builtin:',
  // A list of module specifiers whose resolution should be deferred. If matched,
  // the protocol of the resolved URL will be `deferredProtocol`.
  defer: [],
  // The protocol to use for resolved deferred module specifiers.
  deferredProtocol: 'deferred:',
  // The supported import conditions. "default" is always recognized.
  conditions: [],
  // An array reference which will contain the matched conditions when yielding
  // resolutions.
  matchedConditions: [],
  // An array reference which will contain the matched targets when yielding
  // remapped specifiers., such as those from resolution or import maps.
  matchedTargets: [],
  // The supported engine versions.
  engines: {},
  // The file extensions to look for. Must be provided to support extensionless
  // specifier resolution and directory support, such as resolving './foo' to
  // './foo.js' or './foo/index.js'.
  extensions: [],
  // A map of preresolved imports with keys being serialized parent URLs and
  // values being "imports" maps.
  resolutions
}
```

#### `for (const resolution of resolver)`

Synchronously iterate the module resolution candidates. The resolved module is the first candidate that exists, either as a file on a file system, a resource at a URL, or something else entirely.

#### `for await (const resolution of resolver)`

Asynchronously iterate the module resolution candidates. If `readPackage` returns promises, these will be awaited. The same comments as `for (const resolution of resolver)` apply.

### Algorithm

</details>

### bare-zmq

ZeroMQ bindings for JavaScript

- repo: https://github.com/holepunchto/bare-zmq · 2★ · last push 2026-04-23 · C
- version: 1.2.5 · native addon · min Bare: — · types: no · prebuilds: yes
- node-compat: no · bare deps: bare-events, bare-stream · inbound deps: 0
- disposition: **catalog-row-only**

### bare-which

Find a program in $PATH

- repo: https://github.com/holepunchto/bare-which · 2★ · last push 2026-04-23 · JavaScript
- version: 2.0.0 · pure-JS · min Bare: — · types: no · prebuilds: no
- node-compat: no · bare deps: bare-fs, bare-path, bare-process · inbound deps: 1
- disposition: **catalog-row-only** · ⚠ missing from catalog

### bare-querystring

URL query strings utilities

- repo: https://github.com/holepunchto/bare-querystring · 2★ · last push 2026-04-23 · JavaScript
- version: 1.1.0 · pure-JS · min Bare: — · types: yes · prebuilds: no
- node-compat: no · bare deps: none · inbound deps: 1
- disposition: **catalog-row-only** · ⚠ missing from catalog

### bare-performance

Performance monitoring for Bare

- repo: https://github.com/holepunchto/bare-performance · 2★ · last push 2026-04-23 · JavaScript
- version: 2.0.0 · native addon · min Bare: >=1.27.0 · types: no · prebuilds: yes
- node-compat: no · bare deps: none · inbound deps: 2
- disposition: **catalog-row-only**

<details><summary>API section (README)</summary>

Supported APIs:

- [`performance.now()`](https://nodejs.org/api/perf_hooks.html#performancenow)
- [`performance.eventLoopUtilization()`](https://nodejs.org/api/perf_hooks.html#performanceeventlooputilizationutilization1-utilization2)
- [`performance.nodeTiming`](https://nodejs.org/api/perf_hooks.html#performancenodetiming)
- [`performance.timeOrigin`](https://nodejs.org/api/perf_hooks.html#performancetimeorigin)

</details>

### bare-debug-log

Simple debug log for JavaScript

- repo: https://github.com/holepunchto/bare-debug-log · 2★ · last push 2026-04-23 · JavaScript
- version: 2.0.0 · pure-JS · min Bare: — · types: no · prebuilds: no
- node-compat: no · bare deps: bare-os · inbound deps: 1
- disposition: **catalog-row-only** · ⚠ missing from catalog

### bare-daemon

Create and manage daemon processes in JavaScript

- repo: https://github.com/holepunchto/bare-daemon · 2★ · last push 2026-04-23 · C
- version: 1.2.4 · native addon · min Bare: — · types: no · prebuilds: yes
- node-compat: no · bare deps: bare-env, bare-os · inbound deps: 0
- disposition: **catalog-row-only**

### bare-bundle-id

Construct a unique ID for a bundle

- repo: https://github.com/holepunchto/bare-bundle-id · 2★ · last push 2026-04-23 · JavaScript
- version: 1.0.2 · pure-JS · min Bare: — · types: yes · prebuilds: no
- node-compat: no · bare deps: none · inbound deps: 4
- disposition: **catalog-row-only** · ⚠ missing from catalog

<details><summary>API section (README)</summary>

#### `const buffer = id(bundle)`

</details>

### bare-addon-resolve

Low-level addon resolution algorithm for Bare

- repo: https://github.com/holepunchto/bare-addon-resolve · 2★ · last push 2026-04-23 · JavaScript
- version: 1.10.0 · pure-JS · min Bare: — · types: yes · prebuilds: no
- node-compat: no · bare deps: bare-module-resolve, bare-semver · inbound deps: 2
- disposition: **full-page candidate** · ⚠ missing from catalog

<details><summary>API section (README)</summary>

#### `const resolver = resolve(specifier, parentURL[, options][, readPackage])`

Resolve `specifier` relative to `parentURL`, which must be a WHATWG `URL` instance. `readPackage` is called with a `URL` instance for every package manifest to be read and must either return the parsed JSON package manifest, if it exists, or `null`. If `readPackage` returns a promise, synchronous iteration is not supported.

Options include:

```js
options = {
  // A list of builtin addon specifiers. If matched, the protocol of the
  // resolved URL will be `builtinProtocol`.
  builtins: [],
  // The protocol to use for resolved builtin addon specifiers.
  builtinProtocol: 'builtin:',
  // Whether or not addons linked ahead-of-time should be resolved.
  linked: true,
  // The protocol to use for addons linked ahead-of-time.
  linkedProtocol: 'linked:',
  // The supported import conditions. "default" is always recognized.
  conditions: [],
  // An array reference which will contain the matched conditions when yielding
  // resolutions.
  matchedConditions: [],
  // The `<platform>-<arch>` combinations to look for when resolving dynamic
  // addons. If empty, only builtin specifiers can be resolved. In Bare,
  // pass `[Bare.Addon.host]`.
  hosts: [],
  // The file extensions to look for when resolving dynamic addons.
  extensions: [],
  // A map of preresolved imports with keys being serialized directory URLs and
  // values being "imports" maps.
  resolutions
}
```

#### `for (const resolution of resolver)`

Synchronously iterate the addon resolution candidates. The resolved addon is the first candidate that exists as a file on the file system.

#### `for await (const resolution of resolver)`

Asynchronously iterate the addon resolution candidates. If `readPackage` returns promises, these will be awaited. The same comments as `for (const resolution of resolver)` apply.

### Algorithm

The following generator functions implement the resolution algorithm. The yielded values have the following shape:

**Package manifest**

```js
next.value = {

</details>

### bare-addon-jstl

Example Bare addon

- repo: https://github.com/holepunchto/bare-addon-jstl · 2★ · last push 2026-04-23 · C++
- version: 1.0.0 · native addon · min Bare: — · types: no · prebuilds: yes
- node-compat: no · bare deps: none · inbound deps: 0
- disposition: **catalog-row-only** · ⚠ missing from catalog

### bare-kit-swift

Swift extensions for Bare Kit

- repo: https://github.com/holepunchto/bare-kit-swift · 2★ · last push 2026-03-20 · Swift
- version: — · pure-JS · min Bare: — · types: no · prebuilds: no
- node-compat: no · bare deps: none · inbound deps: 0
- disposition: **catalog-row-only** · ⚠ missing from catalog

### bare-prebuild

Tool for recursively prebuilding installed native addons from source

- repo: https://github.com/holepunchto/bare-prebuild · 2★ · last push 2026-03-06 · JavaScript
- version: 1.1.2 · pure-JS · min Bare: — · types: no · prebuilds: no
- node-compat: no · bare deps: bare-fs, bare-make, bare-os, bare-path, bare-process, bare-subprocess · inbound deps: 0
- disposition: **catalog-row-only** · ⚠ missing from catalog

<details><summary>API section (README)</summary>

#### `await prebuild([base][, options])`

Options include:

```js
{
  platform: os.platform(),
  arch: os.arch(),
  simulator: false,
  environment: null,
  sanitize,
  debug,
  define,
  verbose: false,
  stdio
}
```

</details>

### bare-expo-hrpc-demo

Example of embedding Bare in an Expo application

- repo: https://github.com/holepunchto/bare-expo-hrpc-demo · 2★ · last push 2025-10-07 · JavaScript
- version: — · pure-JS · min Bare: — · types: no · prebuilds: no
- node-compat: no · bare deps: bare-stream · inbound deps: 0
- disposition: **catalog-row-only** · ⚠ missing from catalog

### bare-xdiff

libxdiff bindings for Bare

- repo: https://github.com/holepunchto/bare-xdiff · 2★ · last push 2025-09-09 · JavaScript
- version: 2.0.1 · native addon · min Bare: — · types: no · prebuilds: yes
- node-compat: no · bare deps: none · inbound deps: 0
- disposition: **catalog-row-only** · ⚠ missing from catalog

<details><summary>API section (README)</summary>

### `diff(a, b[, options])`

Generates a unified diff patch from two buffer inputs.

- `a` - Original data (Uint8Array)
- `b` - Modified data (Uint8Array)
- `options` - Optional diff options

Returns a `Promise<Uint8Array>` containing the diff patch.

#### Options

- `ignoreWhitespace` - Ignore all whitespace differences
- `ignoreWhitespaceChange` - Ignore changes in amount of whitespace
- `ignoreWhitespaceAtEol` - Ignore whitespace at end of line
- `ignoreBlankLines` - Ignore blank line changes
- `algorithm` - Diff algorithm: `'minimal'`, `'patience'`, or `'histogram'`

### `merge(ancestor, ours, theirs[, options])`

Performs a three-way merge of buffers.

- `ancestor` - Original/ancestor data (Uint8Array)
- `ours` - Our changes data (Uint8Array)
- `theirs` - Their changes data (Uint8Array)
- `options` - Optional merge options

Returns a `Promise<{conflict: boolean, output: Uint8Array}>` containing conflict status and merged data.

#### Options

- `level` - Merge level: `'minimal'`, `'eager'`, `'zealous'`, or `'zealous_alnum'`
- `favor` - Conflict resolution: `'ours'`, `'theirs'`, or `'union'` 
- `style` - Output style: `'normal'`, `'diff3'`, or `'zealous_diff3'`
- `markerSize` - Conflict marker size (default: 7)

### `diffSync(a, b[, options])`

Synchronous version of `diff()`. Returns a `Uint8Array` directly.

### `mergeSync(ancestor, ours, theirs[, options])`

Synchronous version of `merge()`. Returns a `{conflict: boolean, output: Uint8Array}` directly.

</details>

### bare-delta

Binary patch handling for Bare

- repo: https://github.com/holepunchto/bare-delta · 2★ · last push 2025-09-06 · C
- version: 1.1.0 · native addon · min Bare: — · types: no · prebuilds: yes
- node-compat: no · bare deps: none · inbound deps: 0
- disposition: **catalog-row-only** · ⚠ missing from catalog

<details><summary>API section (README)</summary>

### `create(original, modified[, options])`

Creates a binary patch between two buffers.

- `original` - Original data (Buffer or Uint8Array)
- `modified` - Modified data (Buffer or Uint8Array)
- `options` - Optional creation options
  - `hashWindowSize` - Hash window size (must be power of 2, default: 16)
  - `searchDepth` - Maximum search depth for matches (default: 250)
  - `compressed` - Whether to compress the patch (default: false)

Returns a `Promise<Buffer>` containing the patch.

### `apply(original, patch)`

Applies a binary patch to reconstruct the modified data. Automatically detects if the patch is compressed.

- `original` - Original data (Buffer or Uint8Array)
- `patch` - Patch created by `create()` (Buffer or Uint8Array)

Returns a `Promise<Buffer>` containing the result.

### `applyBatch(original, patches)`

Applies multiple binary patches sequentially to reconstruct the final result. Automatically detects if each patch is compressed.

- `original` - Original data (Buffer or Uint8Array)
- `patches` - Array of patches to apply in order (Array of Buffer or Uint8Array)

Returns a `Promise<Buffer>` containing the result.

### `createSync(original, modified[, options])`

Synchronous version of `create()`. Returns a `Buffer` directly.

- `original` - Original data (Buffer or Uint8Array)
- `modified` - Modified data (Buffer or Uint8Array)
- `options` - Optional creation options
  - `hashWindowSize` - Hash window size (must be power of 2, default: 16)
  - `searchDepth` - Maximum search depth for matches (default: 250)
  - `compressed` - Whether to compress the patch (default: false)

### `applySync(original, patch)`

Synchronous version of `apply()`. Returns a `Buffer` directly. Automatically detects if the patch is compressed.

- `original` - Original data (Buffer or Uint8Array)
- `patch` - Patch created by `create()` (Buffer or Uint8Array)

### `applyBatchSync(original, patches)`

</details>

### bare-stow

Module bundling and harness generation for Bare

- repo: https://github.com/holepunchto/bare-stow · 1★ · last push 2026-06-16 · JavaScript
- version: 0.1.1 · pure-JS · min Bare: — · types: yes · prebuilds: no
- node-compat: no · bare deps: bare-bundle, bare-bundle-id, bare-fs, bare-module-lexer, bare-module-traverse, bare-pack, bare-path, bare-stream, bare-type-stripper · inbound deps: 0
- disposition: **catalog-row-only** · ⚠ missing from catalog

<details><summary>API section (README)</summary>

#### `const artifacts = stow(entry, target, out[, opts])`

Bundle the module graph rooted at `entry` for a `target` runtime and write the resulting artifacts to disk at and alongside `out`. Returns an async generator that yields `{ url }` objects as each artifact is written, allowing callers to observe progress.

`entry` is a `URL` (or `URL`-coercible string) pointing at the entry module. `target` selects the target runtime, given either as the built-in name `'bare-sidecar'` or as a [target provider](#targets-and-rpc-providers) object. Other targets ship as their own packages; require one and pass it as the provider. The target determines the harness format, bundle extension, host triples, and whether assets and addons are linked into the bundle or offloaded as sibling files. `out` is the output `URL` of the harness; the bundle is written next to it with the target's extension.

The harness artifacts are yielded first: The harness itself at `out`, followed by a TypeScript declaration (`.d.ts`) alongside it so hosts importing the harness are typed. The bundle is yielded next, then any offloaded assets and native addons when the target supports offloading.

Options include:

```js
opts = {
  client,
  server,
  base,
  hosts,
  resolveTarget,
  resolveRPC
}
```

- `client`: The RPC library to wire into the harness as a client, given either as a built-in name (`'bare-rpc'`) or as an [RPC provider](#targets-and-rpc-providers) object.
- `server`: The RPC library to wire into the bundle entry shim as a server, given either as a built-in name (`'bare-rpc'`) or as an [RPC provider](#targets-and-rpc-providers) object.
- `base`: The base `URL` of the module graph. Defaults to the directory containing `entry`.
- `hosts`: An array of host triples to build for. Must be a subset of the host triples supported by the target; passing a host the target does not support throws. Defaults to all host triples supported by the target.
- `resolveTarget`: A function mapping a target name to a [target provider](#targets-and-rpc-providers) object, called when `target` is a name not in the built-in registry (which only knows `'bare-sidecar'`).
- `resolveRPC`: A function mapping an RPC library name to an [RPC provider](#targets-and-rpc-providers) object, called when `client` or `server` is a name not in the built-in registry (which only knows `'bare-rpc'`).

Any additional options are forwarded to `bare-pack`. See <https://github.com/holepunchto/bare-pack> for the full set, including `builtins`, `imports`, `defer`, and `resolve`.

#### TypeScript

TypeScript modules are supported out of the box: `.ts`, `.mts`, and `.cts` sources are stripped of their type syntax with [`bare-type-stripper`](https://github.com/holepunchto/bare-type-stripper) as they are read, and their extensions are aliased to `.js`, `.mjs`, and `.cjs` respectively for module-type detection. The stripper only erases types, so the module system follows the aliased JavaScript extension. Because stripping is purely lexical, non-erasable constructs (`enum`, `namespace` with a body, parameter properties) throw, and JSX (`.tsx`) is not supported.

### Targets and RPC providers

The harness and RPC code generators are decoupled from the bundler: `target`, `client`, and `server` each accept a provider object directly, so generators can live outside `bare-stow`. Passing a name instead resolves it through `resolveTarget` / `resolveRPC`, which default to the built-in registries.

Both kinds of provider return an ordered array of artifacts. An artifact is `{ extension?, source }`:

```js
artifacts = [
  { source }, // The primary output.
  { extension, source } // A companion, named after the primary plus this extension.
]
```

A target provider describes the runtime profile it owns and generates the harness that boots the bundle from the host:

```js

</details>

### bare-macos

Example of embedding Bare in a macOS application

- repo: https://github.com/holepunchto/bare-macos · 1★ · last push 2026-06-15 · Swift
- version: 1.0.0 · pure-JS · min Bare: — · types: no · prebuilds: no
- node-compat: no · bare deps: bare-switch-core · inbound deps: 0
- disposition: **catalog-row-only** · ⚠ missing from catalog

### bare-bluetooth-android

Android Bluetooth bindings for Bare

- repo: https://github.com/holepunchto/bare-bluetooth-android · 1★ · last push 2026-06-13 · C++
- version: 0.1.1 · native addon · min Bare: — · types: yes · prebuilds: yes
- node-compat: no · bare deps: bare-events, bare-stream · inbound deps: 0
- disposition: **full-page candidate** · ⚠ missing from catalog

<details><summary>API section (README)</summary>

#### `const central = new Central()`

Create a new BLE central manager for scanning and connecting to peripherals.

#### `central.state`

The current Bluetooth adapter state. One of `'off'`, `'turningOn'`, `'on'`, or `'turningOff'`.

#### `central.startScan(serviceUUIDs[, opts])`

Start scanning for peripherals advertising the given `serviceUUIDs`. Pass `null` to scan for all peripherals.

Options include:

```js
opts = {
  scanMode: null
}
```

Set `scanMode` to one of `Central.SCAN_MODE_OPPORTUNISTIC`, `Central.SCAN_MODE_LOW_POWER`, `Central.SCAN_MODE_BALANCED`, or `Central.SCAN_MODE_LOW_LATENCY`.

#### `central.stopScan()`

Stop scanning for peripherals.

#### `central.connect(peripheral)`

Connect to a discovered `peripheral`.

#### `central.disconnect(peripheral)`

Disconnect from a connected `peripheral`.

#### `central.destroy()`

Destroy the central manager, disconnecting all connected peripherals.

#### `event: 'stateChange'`

Emitted when the Bluetooth adapter state changes. The listener receives the new `state` string.

#### `event: 'discover'`

Emits when a peripheral is discovered during scanning. The listener receives a `Peripheral` instance. Its `scanResult` is a `ScanResult` exposing `scanResult.device` (a `Device` with `address` and `name`), `scanResult.rssi`, and `scanResult.scanRecord` (a `ScanRecord` with `serviceData`, or `null` when the advertisement has no scan record). The peripheral also exposes `id`, `name`, `rssi`, and `serviceData` derived from the scan result.

#### `event: 'connect'`

Emitted when a connection to a peripheral is established. The listener receives a `Peripheral` instance.


</details>

### bare-type-stripper

Heuristic lexer for stripping TypeScript type syntax to produce plain JavaScript

- repo: https://github.com/holepunchto/bare-type-stripper · 1★ · last push 2026-06-11 · C
- version: 0.1.2 · native addon · min Bare: — · types: yes · prebuilds: yes
- node-compat: no · bare deps: none · inbound deps: 1
- disposition: **catalog-row-only** · ⚠ missing from catalog

<details><summary>API section (README)</summary>

#### `const output = strip(source[, encoding][, options])`

Strip TypeScript-only syntax from `source` and return plain JavaScript as a `Buffer`. Stripped regions are replaced with spaces (newlines preserved) so the output has the same byte length as the input, keeping stack traces and source positions aligned.

`source` may be a string or a `Buffer`. When a string, `encoding` selects the character encoding (defaults to `'utf8'`).

Throws a `SyntaxError` when the source contains non-erasable TypeScript syntax (see below).

Options are reserved.

#### `const ranges = strip.lex(source[, encoding][, options])`

Return the raw `[start, end, flags?]` ranges that `strip()` would erase, without applying them. The optional third element is a bitmap of flags from `strip.constants` that controls how a single byte in the range is rewritten (instead of being blanked to a space).

```js
strip.lex('const x: number = 1')
// [ [ 7, 16 ] ]
```

#### `strip.constants`

| Constant | Description                                                                                                                                                                                                                                                                                           |
| :------- | :---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `SEMI`   | The first byte of the range becomes `;` instead of a space. Used on whole-statement strips (`type`, `interface`, `declare`, `import type`, `export type/interface`) and on the first stripped modifier of a class member to keep ASI from folding the preceding statement or field into what follows. |
| `PAREN`  | The first byte of the range becomes `)` instead of a space. Used together with a wider strip range to relocate an arrow function's closing `)` down to the line where `=>` lives, when the (now stripped) return-type annotation spanned newlines.                                                    |
| `ERROR`  | The range marks non-erasable TypeScript syntax that cannot be stripped to valid JavaScript. `strip()` throws a `SyntaxError` when a lexed range carries this flag; `lex()` returns the range so callers can implement their own handling.                                                             |

</details>

### bare-ipc

Lightweight pipe-based IPC for Bare

- repo: https://github.com/holepunchto/bare-ipc · 1★ · last push 2026-06-11 · JavaScript
- version: 1.1.2 · pure-JS · min Bare: — · types: yes · prebuilds: no
- node-compat: no · bare deps: bare-pipe, bare-stream, bare-structured-clone · inbound deps: 0
- disposition: **full-page candidate**

<details><summary>API section (README)</summary>

### `IPC`

#### `const [portA, portB] = IPC.open()`

Returns a pair of connected `IPCPort`s for constructing the IPC duplex stream based on `bare-pipe`. Each port is transferable and can be sent to another thread or process before being connected.

#### `const ipc = new IPC(port)`

Returns a duplex stream using the provided `port`. See `bare-stream`'s `Duplex` (<https://github.com/holepunchto/bare-stream>) for the duplex stream API.

Errors emitted by the underlying incoming or outgoing pipes are propagated to the stream as `error` events, after which the stream is destroyed.

#### `ipc.incoming`

The underlying `bare-pipe` `Pipe` used for reading. Read-only.

#### `ipc.outgoing`

The underlying `bare-pipe` `Pipe` used for writing. Read-only.

#### `ipc.ref()`

Increase the reference count for the IPC to keep the event loop alive.

A common pattern is to `ipc.ref()` on `Bare.on('resume')` and `ipc.unref()` on suspend like so:

```js
Bare.on('suspend', () => ipc.unref()).on('resume', () => ipc.ref())
```

#### `ipc.unref()`

Decrease the reference count for the IPC to allow the event loop to exit.

See `ipc.ref()` for the common pattern to keep the event loop alive.

### `IPCPort`

#### `const port = new IPCPort(incoming, outgoing)`

Constructs a port from a pair of file handles. The arguments are:

- `incoming` is the read file handle.
- `outgoing` is the write file handle.

#### `port.connect()`

Returns an `IPC` connected to the `port` and marks the `port` as detached.

#### `port.incoming`

</details>

### bare-bluetooth-apple

CoreBluetooth bindings for Bare

- repo: https://github.com/holepunchto/bare-bluetooth-apple · 1★ · last push 2026-06-10 · Objective-C++
- version: 0.2.2 · native addon · min Bare: — · types: yes · prebuilds: yes
- node-compat: no · bare deps: bare-events, bare-stream · inbound deps: 0
- disposition: **full-page candidate** · ⚠ missing from catalog

<details><summary>API section (README)</summary>

#### `const central = new Central()`

Create a new BLE central manager. The central scans for and connects to peripherals.

#### `central.state`

The current Bluetooth state. One of `'unknown'`, `'resetting'`, `'unsupported'`, `'unauthorized'`, `'poweredOff'`, or `'poweredOn'`.

#### `central.startScan([serviceUUIDs])`

Start scanning for peripherals. If `serviceUUIDs` is provided, only peripherals advertising those services will be discovered.

#### `central.stopScan()`

Stop scanning for peripherals.

#### `central.connect(peripheral)`

Connect to a discovered `peripheral`.

#### `central.disconnect(peripheral)`

Disconnect from a connected `peripheral`.

#### `central.destroy()`

Destroy the central manager and release all resources.

#### `event: 'stateChange'`

Emitted with `state` when the Bluetooth state changes.

#### `event: 'discover'`

Emitted when a peripheral is discovered during scanning. The listener receives a `peripheral` object.

The `peripheral` object has `id`, `name`, `rssi`, and `serviceData` properties. `rssi` is the signal strength reported with the most recent advertisement packet. `serviceData` is an object mapping service UUIDs to `Uint8Array` data, or `null` if no service data was advertised in this packet.

The same `peripheral` reference is reused across discover events for a given `id`; its `rssi` and `serviceData` are updated in place to reflect the latest packet.

#### `event: 'connect'`

Emitted with `peripheral` when a connection to a peripheral is established. The `peripheral` is a `Peripheral` instance.

#### `event: 'disconnect'`

Emitted with `peripheral` and `error` when a peripheral disconnects.

#### `event: 'connectFail'`


</details>

### bare-unpack

Bundle unpacking for Bare

- repo: https://github.com/holepunchto/bare-unpack · 1★ · last push 2026-06-05 · JavaScript
- version: 1.1.3 · pure-JS · min Bare: — · types: no · prebuilds: no
- node-compat: no · bare deps: bare-bundle, bare-fs, bare-path · inbound deps: 2
- disposition: **catalog-row-only**

<details><summary>API section (README)</summary>

#### `const repacked = await unpack(bundle[, options], writeFile)`

Unpack `bundle`, writing either all or parts of its files to disk or elsewhere. `writeFile` is called with the key of every file to write and must return the path to which the file corresponding to the key is written. Any files not written elsewhere will be repacked into a new bundle, which is returned to the caller, and any references to files written outside the bundle will be rewritten.

Options include:

```js
options = {
  files: true,
  addons: files,
  assets: files,
  concurrency: 0
}
```

</details>

### bare-rpc-golang

Golang RPC client and server compatible with the bare RPC protocol

- repo: https://github.com/holepunchto/bare-rpc-golang · 1★ · last push 2026-06-04 · Go
- version: — · pure-JS · min Bare: — · types: no · prebuilds: no
- node-compat: no · bare deps: none · inbound deps: 0
- disposition: **catalog-row-only** · ⚠ missing from catalog

<details><summary>API section (README)</summary>

### RPC

```go
// Create new RPC instance
rpc := bare_rpc.NewRPC(stream io.ReadWriter)

// Make a request and wait for response
data, err := rpc.Request(command uint, data []byte) ([]byte, error)

// Send one-way event (request with ID 0)
err := rpc.Event(command uint, data []byte) error

// Send response to a request
err := rpc.Reply(id uint, data []byte) error

// Send error response
err := rpc.ReplyError(id uint, err error) error

// Listen for incoming messages
err := rpc.Listen(onRequest func(req *Request)) error

// Build a request that can attach streams
req := rpc.NewRequest(command uint) *OutgoingRequest
req.Send(data []byte) error                       // unary payload
data, err := req.Reply() ([]byte, error)          // await unary response
ws := req.CreateRequestStream() *OutgoingStream   // io.WriteCloser (-> handler)
rs := req.CreateResponseStream() *IncomingStream  // io.ReadCloser  (<- handler)
```

On the handler side, the `*Request` passed to `Listen` exposes the mirror
methods: `CreateRequestStream() *IncomingStream` (read what the initiator sends)
and `CreateResponseStream() *OutgoingStream` (write the response stream).

### Listen

```go
err := rpc.Listen(func(req *Request) {
	// Reply to this request
	err := req.Reply([]byte("Hello javascript!"))
})
```

</details>

### bare-http-parser

Streaming HTTP request and response parser for Bare

- repo: https://github.com/holepunchto/bare-http-parser · 1★ · last push 2026-06-04 · JavaScript
- version: 1.1.5 · pure-JS · min Bare: — · types: no · prebuilds: no
- node-compat: no · bare deps: none · inbound deps: 1
- disposition: **catalog-row-only** · ⚠ missing from catalog

### bare-zlib

Stream-based zlib bindings for JavaScript

- repo: https://github.com/holepunchto/bare-zlib · 1★ · last push 2026-06-02 · JavaScript
- version: 1.4.0 · native addon · min Bare: — · types: yes · prebuilds: yes
- node-compat: no · bare deps: bare-stream · inbound deps: 3
- disposition: **catalog-row-only**

### bare-ndk

Android NDK bindings and runtime for Bare

- repo: https://github.com/holepunchto/bare-ndk · 1★ · last push 2026-05-29 · C++
- version: 0.2.0 · native addon · min Bare: — · types: no · prebuilds: yes
- node-compat: no · bare deps: none · inbound deps: 1
- disposition: **catalog-row-only** · ⚠ missing from catalog

### bare-apk

APK packaging tools for Bare

- repo: https://github.com/holepunchto/bare-apk · 1★ · last push 2026-05-28 · JavaScript
- version: 0.1.3 · pure-JS · min Bare: — · types: no · prebuilds: yes
- node-compat: no · bare deps: bare-env, bare-fs, bare-os, bare-path, bare-subprocess · inbound deps: 0
- disposition: **full-page candidate** · ⚠ missing from catalog

<details><summary>API section (README)</summary>

#### `await createAppBundle(manifest, out[, options])`

Options include:

```js
options = {
  targetSDK: DEFAULT_TARGET_SDK,
  // Additional files and directories to include in uncompressed format
  include: [],
  // Resource directory to compile and compress
  resources
}
```

#### `await createAPKSet(bundle, out[, options])`

Options include:

```js
options = {
  universal: false,
  archive: true,
  sign: false,
  keystore,
  keystoreKey,
  keystorePassword
}
```

#### `await createAPK(bundle, out[, options])`

Options include:

```js
options = {
  sign: false,
  keystore,
  keystoreKey,
  keystorePassword
}
```

#### `constants`

| Constant              | Description                                |
| :-------------------- | :----------------------------------------- |
| `ANDROID_HOME`        | The Android SDK root directory.            |
| `DEFAULT_MINIMUM_SDK` | The default minimum Android SDK API level. |
| `DEFAULT_TARGET_SDK`  | The default target Android SDK API level.  |

</details>

### bare-sqlite-vector

SQLite Vector bindings for Bare

- repo: https://github.com/holepunchto/bare-sqlite-vector · 1★ · last push 2026-05-26 · JavaScript
- version: 0.1.1 · native addon · min Bare: — · types: yes · prebuilds: yes
- node-compat: no · bare deps: bare-sqlite · inbound deps: 0
- disposition: **catalog-row-only** · ⚠ missing from catalog

<details><summary>API section (README)</summary>

#### `vector.register(db)`

Register the vector extension's SQL functions and virtual table modules on the given `bare-sqlite` connection. `db` must be an open `DatabaseSync` instance from `bare-sqlite`.

Registration is per-connection; each new `DatabaseSync` that needs vector functions must call `register` again. The call is idempotent: Invoking it more than once on the same connection is a no-op.

Throws `DATABASE_NOT_OPEN` if `db` has been closed.

</details>

### bare-sqlite

SQLite bindings for Bare

- repo: https://github.com/holepunchto/bare-sqlite · 1★ · last push 2026-05-26 · JavaScript
- version: 0.1.4 · native addon · min Bare: — · types: yes · prebuilds: yes
- node-compat: no · bare deps: none · inbound deps: 2
- disposition: **full-page candidate** · ⚠ missing from catalog

<details><summary>API section (README)</summary>

#### `const db = new DatabaseSync(location[, options])`

Open a SQLite database. `location` is a path to a database file, or `':memory:'` for an in-memory database.

Options include:

```js
options = {
  open: true,
  readOnly: false,
  enableForeignKeyConstraints: true,
  enableDoubleQuotedStringLiterals: false,
  allowExtension: false,
  timeout: 0
}
```

`open` controls whether the database is opened during construction. With `open: false`, the instance is initialised but the underlying connection is deferred until `db.open()` is called.

`readOnly` opens the database in read-only mode.

`enableForeignKeyConstraints` controls whether foreign key constraints are enforced.

`enableDoubleQuotedStringLiterals` controls whether double-quoted strings are interpreted as string literals (rather than identifiers) in DML and DDL.

`allowExtension` is the master switch for `db.loadExtension()` and `db.enableLoadExtension()`. When `false` (the default), both methods throw. When `true`, extension loading via the C API is enabled; the SQL `load_extension()` function remains disabled.

`timeout` is the busy timeout in milliseconds. When non-zero, SQLite waits up to that long for a lock before returning a `BUSY` error.

#### `db.isOpen`

`true` if the database is currently open, `false` otherwise.

#### `db.open()`

Open the database. Throws if already open. Useful when the database was constructed with `open: false`.

#### `db.close()`

Close the database. Throws if not open. Prepared statements that are still reachable from JavaScript remain valid until they are finalized; the underlying connection is released once the last statement is gone.

#### `db.exec(sql)`

Execute one or more SQL statements without returning rows. `sql` may contain multiple statements separated by `;`.

#### `const stmt = db.prepare(sql)`

Compile `sql` into a prepared statement. The returned `StatementSync` can be reused with different parameter values.

#### `db.loadExtension(path[, entryPoint])`

</details>

### bare-exif

EXIF support for Bare

- repo: https://github.com/holepunchto/bare-exif · 1★ · last push 2026-05-25 · C
- version: 1.1.0 · native addon · min Bare: — · types: no · prebuilds: yes
- node-compat: no · bare deps: none · inbound deps: 1
- disposition: **catalog-row-only** · ⚠ missing from catalog

### bare-walk-handles

Walk the event loop handles of the Bare process

- repo: https://github.com/holepunchto/bare-walk-handles · 1★ · last push 2026-04-23 · C
- version: 2.0.10 · native addon · min Bare: — · types: no · prebuilds: yes
- node-compat: no · bare deps: none · inbound deps: 0
- disposition: **catalog-row-only** · ⚠ missing from catalog

### bare-utils

Node.js-compatible utility functions for Bare

- repo: https://github.com/holepunchto/bare-utils · 1★ · last push 2026-04-23 · JavaScript
- version: 1.6.0 · pure-JS · min Bare: — · types: no · prebuilds: no
- node-compat: yes · bare deps: bare-debug-log, bare-encoding, bare-format, bare-inspect, bare-stylize, bare-type · inbound deps: 3
- disposition: **catalog-row-only** · ⚠ missing from catalog

### bare-tpl

Simple template string based templating for Bare

- repo: https://github.com/holepunchto/bare-tpl · 1★ · last push 2026-04-23 · JavaScript
- version: 1.0.1 · pure-JS · min Bare: — · types: no · prebuilds: no
- node-compat: no · bare deps: none · inbound deps: 1
- disposition: **catalog-row-only** · ⚠ missing from catalog

<details><summary>API section (README)</summary>

#### `const result = tpl.render(template, variables)`

Render a `template` string using the provided `variables`. The template uses JavaScript template literal syntax (`${...}`), with each key in `variables` available as a local variable within the template. Expressions are supported, so templates may contain arbitrary JavaScript within `${...}` delimiters.

`template` can be a string or any object with a `.toString()` method. `variables` must be an object whose keys become the variable names available in the template.

</details>

### bare-system-logger

System logger for Bare

- repo: https://github.com/holepunchto/bare-system-logger · 1★ · last push 2026-04-23 · C
- version: 1.0.3 · native addon · min Bare: — · types: yes · prebuilds: yes
- node-compat: no · bare deps: bare-logger · inbound deps: 1
- disposition: **catalog-row-only** · ⚠ missing from catalog

### bare-storage

Minimal, cross-platform directory locator for Bare

- repo: https://github.com/holepunchto/bare-storage · 1★ · last push 2026-04-23 · JavaScript
- version: 1.1.0 · native addon · min Bare: — · types: no · prebuilds: yes
- node-compat: no · bare deps: bare-compat-napi, bare-env, bare-fs, bare-os, bare-path · inbound deps: 0
- disposition: **catalog-row-only** · ⚠ missing from catalog

<details><summary>API section (README)</summary>

#### `const path = dir.persistent()`

Data that should survive restarts and OS cleanup.

#### `const path = dir.ephemeral()`

Cache or temporary data that the OS can wipe.

</details>

### bare-stdio

Standard input/output streams for Bare

- repo: https://github.com/holepunchto/bare-stdio · 1★ · last push 2026-04-23 · JavaScript
- version: 1.0.2 · native addon · min Bare: — · types: yes · prebuilds: yes
- node-compat: no · bare deps: bare-fs, bare-pipe, bare-tty · inbound deps: 1
- disposition: **catalog-row-only** · ⚠ missing from catalog

<details><summary>API section (README)</summary>

#### `stdio.in`

A readable stream for standard input (`fd 0`). Returns a `bare-tty` `ReadStream` if stdin is a terminal, a `bare-pipe` `Pipe` if it's a pipe, otherwise a `bare-fs` `ReadStream`.

#### `stdio.out`

A writable stream for standard output (`fd 1`). Returns a `bare-tty` `WriteStream` if stdout is a terminal, a `bare-pipe` `Pipe` if it's a pipe, otherwise a `bare-fs` `WriteStream`.

#### `stdio.err`

A writable stream for standard error (`fd 2`). Returns a `bare-tty` `WriteStream` if stderr is a terminal, a `bare-pipe` `Pipe` if it's a pipe, otherwise a `bare-fs` `WriteStream`.

</details>

### bare-open

Cross-platform application launcher for Bare

- repo: https://github.com/holepunchto/bare-open · 1★ · last push 2026-04-23 · C
- version: 1.0.3 · native addon · min Bare: — · types: no · prebuilds: yes
- node-compat: no · bare deps: none · inbound deps: 0
- disposition: **catalog-row-only** · ⚠ missing from catalog

<details><summary>API section (README)</summary>

#### `open(app[, argument])`

</details>

### bare-md4c

MD4C bindings, markdown push parser

- repo: https://github.com/holepunchto/bare-md4c · 1★ · last push 2026-04-23 · C++
- version: 1.0.0 · native addon · min Bare: — · types: no · prebuilds: yes
- node-compat: no · bare deps: none · inbound deps: 0
- disposition: **catalog-row-only** · ⚠ missing from catalog

<details><summary>API section (README)</summary>

```js
import md4c from 'bare-md4c'
```

### `md4c.toHTML(input, flags, htmlFlags)`

Convert markdown to html

Example:

```js
const html = md4c.toHTML(mdText)
```

Parameters:

- `input` - markdown string
- `flags` - any combination of `md4c.constants.flags`, default: `DIALECT_GITHUB`
- `htmlFlags` - any combination of `md4c.constants.htmlFlags`, default: none

### `md4c.parse(input, onparse, flags, logDebug = false)`

Parse text `input`.

- `flags` - see exported `md4c.constants.flags`, default: `DIALECT_GITHUB`
- `logDebug` enable md4c's debug logger

Example `callback`:

```js
function onpush(event, tag, text, details) {
  const { events, block, span, text } = md4c.constants

  console.log(
    event, // value in `events`
    tag, // value in `block|span|text`
    text, // the message (`undefined` on non `events.TEXT` events)
    details // tag details (header level, link href)
  )

  return 0 // continue. `-1` to abort parsing
}
```

</details>

### bare-intl

ECMAScript Internationalization API for Bare

- repo: https://github.com/holepunchto/bare-intl · 1★ · last push 2026-04-23 · JavaScript
- version: 0.0.0 · pure-JS · min Bare: — · types: no · prebuilds: no
- node-compat: no · bare deps: none · inbound deps: 0
- disposition: **catalog-row-only** · ⚠ missing from catalog

### bare-format

String formatting for JavaScript

- repo: https://github.com/holepunchto/bare-format · 1★ · last push 2026-04-23 · JavaScript
- version: 1.0.2 · pure-JS · min Bare: — · types: yes · prebuilds: no
- node-compat: no · bare deps: bare-inspect · inbound deps: 3
- disposition: **catalog-row-only**

### bare-compat-napi

Bare compatibility headers for Node-API

- repo: https://github.com/holepunchto/bare-compat-napi · 1★ · last push 2026-04-23 · C
- version: 1.3.9 · pure-JS · min Bare: — · types: no · prebuilds: no
- node-compat: no · bare deps: none · inbound deps: 1
- disposition: **catalog-row-only** · ⚠ missing from catalog

### bare-bundle-evaluate

Evaluate a bundle of CommonJS modules across JavaScript runtimes

- repo: https://github.com/holepunchto/bare-bundle-evaluate · 1★ · last push 2026-04-23 · JavaScript
- version: 2.0.0 · pure-JS · min Bare: — · types: no · prebuilds: no
- node-compat: no · bare deps: bare-addon-resolve, bare-fs, bare-module-resolve, bare-path, bare-url · inbound deps: 0
- disposition: **catalog-row-only** · ⚠ missing from catalog

<details><summary>API section (README)</summary>

#### `const module = evaluate(bundle[, runtime][, builtinRequire])`

</details>

### bare-addon-rust

Example Bare addon

- repo: https://github.com/holepunchto/bare-addon-rust · 1★ · last push 2026-04-23 · CMake
- version: 1.0.0 · native addon · min Bare: — · types: no · prebuilds: yes
- node-compat: no · bare deps: none · inbound deps: 0
- disposition: **catalog-row-only** · ⚠ missing from catalog

### bare-addon-java

Example Bare addon

- repo: https://github.com/holepunchto/bare-addon-java · 1★ · last push 2026-04-23 · CMake
- version: 1.0.0 · native addon · min Bare: — · types: no · prebuilds: yes
- node-compat: no · bare deps: none · inbound deps: 0
- disposition: **catalog-row-only** · ⚠ missing from catalog

### bare-v8-to-istanbul

Wrapper for v8-to-istanbul that works with Bare

- repo: https://github.com/holepunchto/bare-v8-to-istanbul · 1★ · last push 2025-12-17 · JavaScript
- version: 1.0.3 · pure-JS · min Bare: — · types: no · prebuilds: no
- node-compat: no · bare deps: bare-assert, bare-fs, bare-module, bare-path, bare-process, bare-url, bare-utils · inbound deps: 1
- disposition: **catalog-row-only** · ⚠ missing from catalog

### bare-app-image

AppImage packaging tools for Bare

- repo: https://github.com/holepunchto/bare-app-image · 1★ · last push 2025-12-17 · JavaScript
- version: 0.1.1 · pure-JS · min Bare: — · types: no · prebuilds: yes
- node-compat: no · bare deps: bare-fs, bare-path, bare-subprocess · inbound deps: 0
- disposition: **catalog-row-only** · ⚠ missing from catalog

<details><summary>API section (README)</summary>

#### `await createAppImage(source[, destination][, options])`

Options include:

```js
options = {
  compression,
  sign,
  key
}
```

</details>

### bare-ffmpeg-encodings

Serialize ffmpeg primitives

- repo: https://github.com/holepunchto/bare-ffmpeg-encodings · 1★ · last push 2025-09-26 · JavaScript
- version: 1.2.1 · pure-JS · min Bare: — · types: no · prebuilds: no
- node-compat: no · bare deps: none · inbound deps: 0
- disposition: **catalog-row-only** · ⚠ missing from catalog

### bare-node-fetch

Cross runtime fetch module

- repo: https://github.com/holepunchto/bare-node-fetch · 1★ · last push 2025-04-08 · JavaScript
- version: 1.0.0 · pure-JS · min Bare: — · types: no · prebuilds: no
- node-compat: no · bare deps: bare-fetch · inbound deps: 0
- disposition: **catalog-row-only** · ⚠ missing from catalog

### bare-translations



- repo: https://github.com/holepunchto/bare-translations · 0★ · last push 2026-06-16 · JavaScript
- version: 1.1.2 · pure-JS · min Bare: — · types: no · prebuilds: no
- node-compat: no · bare deps: none · inbound deps: 0
- disposition: **catalog-row-only** · ⚠ missing from catalog

<details><summary>API section (README)</summary>

### loadModel({ from, to, model? })

Load a model into memory. Returns `{ modelId, modelType }` when the model is ready.

| Property | Type   | Default      | Description                  |
| -------- | ------ | ------------ | ---------------------------- |
| `from`   | string |              | Language to translate from   |
| `to`     | string |              | Language to translate to     |
| `model`  | string | `'BERGAMOT'` | (Optional) Translation model |

### translate({ modelId, modelType, text, from, to })

Translate text using a loaded model.

| Property    | Type   | Description                    |
| ----------- | ------ | ------------------------------ |
| `modelId`   | string | ID returned by `loadModel()`   |
| `modelType` | string | Type returned by `loadModel()` |
| `text`      | string | Text to translate              |
| `from`      | string | Source language code           |
| `to`        | string | Target language code           |

Returns `{ result, from, language }` where `language` is the target language code.

> When calling `translate()` directly (without a worker), `from` can be omitted and the source language will be auto-detected.

### translateStream({ modelId, modelType, text, from, to })

Streaming version of `translate()`. Same parameters. Returns a readable stream that emits `{ result, language }` chunks as translation tokens arrive.

### detectLanguage({ text })

Detect the language of a string.

| Property | Type   | Description    |
| -------- | ------ | -------------- |
| `text`   | string | Text to detect |

Returns `{ code, language }` where `code` is a BCP-47 language code (e.g. `'en'`, `'fr'`) and `language` is the full language name.

### unloadModel({ modelId, clearStorage? })

Unload a model from memory.

| Property       | Type    | Default | Description                                            |
| -------------- | ------- | ------- | ------------------------------------------------------ |
| `modelId`      | string  |         | ID of the model to unload                              |
| `clearStorage` | boolean | `false` | (Optional) Also clear downloaded model files from disk |

### WorkerClient.isModelSupported(model)

</details>

### bare-headers

Development headers for Bare

- repo: https://github.com/holepunchto/bare-headers · 0★ · last push 2026-06-16 · CMake
- version: 1.28.7 · pure-JS · min Bare: — · types: no · prebuilds: no
- node-compat: no · bare deps: none · inbound deps: 0
- disposition: **catalog-row-only** · ⚠ missing from catalog

### bare-stow-target-pear-runtime

Pear Runtime target for bare-stow

- repo: https://github.com/holepunchto/bare-stow-target-pear-runtime · 0★ · last push 2026-06-15 · JavaScript
- version: 0.1.0 · pure-JS · min Bare: — · types: no · prebuilds: no
- node-compat: no · bare deps: none · inbound deps: 0
- disposition: **catalog-row-only** · ⚠ missing from catalog

### bare-stow-target-react-native

React Native target for bare-stow

- repo: https://github.com/holepunchto/bare-stow-target-react-native · 0★ · last push 2026-06-15 · JavaScript
- version: 0.1.0 · pure-JS · min Bare: — · types: no · prebuilds: no
- node-compat: no · bare deps: none · inbound deps: 0
- disposition: **catalog-row-only** · ⚠ missing from catalog

### bare-switch-core

Shared peer-to-peer switch core behind the bare example apps (bare-macos, bare-ios)

- repo: https://github.com/holepunchto/bare-switch-core · 0★ · last push 2026-06-15 · JavaScript
- version: 1.0.1 · pure-JS · min Bare: — · types: no · prebuilds: no
- node-compat: no · bare deps: none · inbound deps: 1
- disposition: **catalog-row-only** · ⚠ missing from catalog

### bare-tui-paparam

menu/form helper for any paparam CLI

- repo: https://github.com/holepunchto/bare-tui-paparam · 0★ · last push 2026-06-12 · JavaScript
- version: 0.0.1 · pure-JS · min Bare: — · types: no · prebuilds: no
- node-compat: no · bare deps: bare-tui, bare-tui-form, bare-tui-markdown · inbound deps: 0
- disposition: **catalog-row-only** · ⚠ missing from catalog

### bare-tui-markdown

A limited markdown renderer for bare-tui

- repo: https://github.com/holepunchto/bare-tui-markdown · 0★ · last push 2026-06-12 · JavaScript
- version: 0.0.1 · pure-JS · min Bare: — · types: no · prebuilds: no
- node-compat: no · bare deps: bare-tui · inbound deps: 1
- disposition: **catalog-row-only** · ⚠ missing from catalog

<details><summary>API section (README)</summary>

```js
md.render(text, { width = 80, theme })      // → string  (lines joined with '\n')
md.renderLines(text, { width = 80, theme }) // → string[]  (one styled row per line) — primary
md.defaultTheme                              // the theme, to spread/extend
md.mergeTheme(partial)                       // → a full theme over the defaults
```

`renderLines` is the primary entry for a TUI that owns its own scrollback (it indents each row itself). `width` is the content width you have available; the renderer never returns a row wider than it (except an unbreakable word or a code line, which overflow rather than split).

### Theme

A small bag of block style functions, inline colour attributes the renderer composes per word, and literal markers — shallow-merged over the defaults:

```js
md.render(text, { theme: { bullet: '* ', inline: { codeFg: 'magenta' } } })
```

</details>

### bare-tui-paparam-ai

AI assisted paparam upgrade

- repo: https://github.com/holepunchto/bare-tui-paparam-ai · 0★ · last push 2026-06-12 · —
- version: — · pure-JS · min Bare: — · types: no · prebuilds: no
- node-compat: no · bare deps: none · inbound deps: 0
- disposition: **catalog-row-only** · ⚠ missing from catalog

### bare-ico

ICO support for Bare

- repo: https://github.com/holepunchto/bare-ico · 0★ · last push 2026-05-29 · C
- version: 1.0.1 · native addon · min Bare: — · types: no · prebuilds: yes
- node-compat: no · bare deps: none · inbound deps: 1
- disposition: **catalog-row-only** · ⚠ missing from catalog

### bare-bmp

Native BMP codec for Bare

- repo: https://github.com/holepunchto/bare-bmp · 0★ · last push 2026-05-29 · C
- version: 1.0.1 · native addon · min Bare: — · types: no · prebuilds: yes
- node-compat: no · bare deps: none · inbound deps: 1
- disposition: **catalog-row-only** · ⚠ missing from catalog

### bare-web-kit-gtk

WebKitGTK bindings for Bare

- repo: https://github.com/holepunchto/bare-web-kit-gtk · 0★ · last push 2026-05-29 · C
- version: 0.2.0 · native addon · min Bare: — · types: no · prebuilds: yes
- node-compat: no · bare deps: bare-gtk · inbound deps: 1
- disposition: **catalog-row-only** · ⚠ missing from catalog

### bare-ui-kit

UIKit bindings and runtime for Bare

- repo: https://github.com/holepunchto/bare-ui-kit · 0★ · last push 2026-05-29 · Objective-C
- version: 0.2.0 · native addon · min Bare: — · types: no · prebuilds: yes
- node-compat: no · bare deps: bare-events · inbound deps: 1
- disposition: **catalog-row-only** · ⚠ missing from catalog

### bare-win-ui

WinUI bindings and runtime for Bare

- repo: https://github.com/holepunchto/bare-win-ui · 0★ · last push 2026-05-29 · C++
- version: 0.2.0 · native addon · min Bare: — · types: no · prebuilds: yes
- node-compat: no · bare deps: bare-events · inbound deps: 1
- disposition: **catalog-row-only** · ⚠ missing from catalog

### bare-collabora

Collabora bindings for Bare

- repo: https://github.com/holepunchto/bare-collabora · 0★ · last push 2026-05-28 · CMake
- version: 0.1.1 · native addon · min Bare: — · types: yes · prebuilds: yes
- node-compat: no · bare deps: bare-env, bare-path, bare-url · inbound deps: 0
- disposition: **catalog-row-only** · ⚠ missing from catalog

<details><summary>API section (README)</summary>

#### `const document = new Document(url)`

Loads the document at `url`. The `url` is a local file path or a `file:` URL pointing to a document in any format supported by Collabora. Throws if the document cannot be opened.

#### `document.saveAs(url[, format[, options]])`

Saves `document` to `url` in the given `format`. If `format` is omitted, it is inferred from the extension of `url`. `options` is a comma-separated string of filter options forwarded to Collabora; see the Collabora documentation for the filters available for a given format. Throws if the document cannot be saved.

</details>

### bare-prom-client

Client for prometheus

- repo: https://github.com/holepunchto/bare-prom-client · 0★ · last push 2026-05-27 · JavaScript
- version: 15.1.9 · pure-JS · min Bare: — · types: yes · prebuilds: no
- node-compat: yes · bare deps: bare-fs, bare-http1, bare-https, bare-performance, bare-process, bare-url, bare-utils, bare-v8, bare-zlib · inbound deps: 0
- disposition: **full-page candidate** · ⚠ missing from catalog

<details><summary>API section (README)</summary>

### Default metrics

There are some default metrics recommended by Prometheus
[itself](https://prometheus.io/docs/instrumenting/writing_clientlibs/#standard-and-runtime-collectors).
To collect these, call `collectDefaultMetrics`. In addition, some
Node.js-specific metrics are included, such as event loop lag, active handles,
GC and Node.js version. See [lib/metrics](lib/metrics) for a list of all
metrics.

NOTE: Some of the metrics, concerning File Descriptors and Memory, are only
available on Linux.

`collectDefaultMetrics` optionally accepts a config object with following entries:

- `prefix` an optional prefix for metric names. Default: no prefix.
- `register` to which registry the metrics should be registered. Default: the global default registry.
- `gcDurationBuckets` with custom buckets for GC duration histogram. Default buckets of GC duration histogram are `[0.001, 0.01, 0.1, 1, 2, 5]` (in seconds).
- `eventLoopMonitoringPrecision` with sampling rate in milliseconds. Must be greater than zero. Default: 10.

To register metrics to another registry, pass it in as `register`:

```js
const client = require('prom-client');
const collectDefaultMetrics = client.collectDefaultMetrics;
const Registry = client.Registry;
const register = new Registry();
collectDefaultMetrics({ register });
```

To use custom buckets for GC duration histogram, pass it in as `gcDurationBuckets`:

```js
const client = require('prom-client');
const collectDefaultMetrics = client.collectDefaultMetrics;
collectDefaultMetrics({ gcDurationBuckets: [0.1, 0.2, 0.3] });
```

To prefix metric names with your own arbitrary string, pass in a `prefix`:

```js
const client = require('prom-client');
const collectDefaultMetrics = client.collectDefaultMetrics;
const prefix = 'my_application_';
collectDefaultMetrics({ prefix });
```

To apply generic labels to all default metrics, pass an object to the `labels` property (useful if you're working in a clustered environment):

```js
const client = require('prom-client');

</details>

### bare-hc

Bare bindings for libhc

- repo: https://github.com/holepunchto/bare-hc · 0★ · last push 2026-05-25 · C
- version: 0.0.0 · native addon · min Bare: — · types: no · prebuilds: yes
- node-compat: no · bare deps: none · inbound deps: 0
- disposition: **catalog-row-only** · ⚠ missing from catalog

### bare-mdns-discovery

bare-mdns-discovery

- repo: https://github.com/holepunchto/bare-mdns-discovery · 0★ · last push 2026-05-25 · JavaScript
- version: 0.2.3 · pure-JS · min Bare: — · types: yes · prebuilds: no
- node-compat: no · bare deps: bare-dgram · inbound deps: 0
- disposition: **full-page candidate** · ⚠ missing from catalog

<details><summary>API section (README)</summary>

### `new MDNS(opts?)`

Create a low-level mDNS instance.

Options:

- `debug` (boolean): Enable debug logging. Default: `false`
- `iface` (string): IPv4 address of the network interface to join the multicast group on. Required on Android — without this, `addMembership` falls back to the OS default interface which may not be the WiFi interface, causing responses to be silently dropped. Pass the WiFi IP (e.g. from `bare-wifi-android`'s `getWifiIP()`).

#### `mdns.ready()`

Wait for the socket to be bound. Returns a Promise.

#### `mdns.query(name, type?)`

Send an mDNS query.

- `name` (string): Service name, e.g. `'_http._tcp.local'`
- `type` (number): Record type. Default: `TYPE.PTR` (12)

#### `mdns.close()`

Close the socket. Returns a Promise.

#### Event: `'records'`

Emitted when records are received.

```javascript
mdns.on('records', (records, rinfo) => {
  // records: Array of parsed DNS records
  // rinfo: { address, port, family, size }
})
```

### `new Discovery(opts?)`

High-level service discovery. Extends `MDNS`.

Options:

- `service` (string): Service type without prefix/suffix, e.g. `'http'`, `'googlecast'`
- `debug` (boolean): Enable debug logging. Default: `false`

#### `discovery.discover(timeout?)`

Discover services on the network. Returns a Promise resolving to an array of services.

- `timeout` (number): Discovery timeout in seconds. Default: `10`


</details>

### bare-navigation-android

Android back gesture/button handler for the Bare runtime

- repo: https://github.com/holepunchto/bare-navigation-android · 0★ · last push 2026-05-25 · C++
- version: 0.1.0 · native addon · min Bare: — · types: no · prebuilds: yes
- node-compat: no · bare deps: bare-events · inbound deps: 0
- disposition: **catalog-row-only** · ⚠ missing from catalog

<details><summary>API section (README)</summary>

### `BackHandler.on('back', listener)` / `BackHandler.addListener('back', listener)`

Register a listener for the Android back gesture/button. The first call suppresses the OS default behaviour for the lifetime of the process.

### `BackHandler.off('back', listener)` / `BackHandler.removeListener('back', listener)`

Unregister a listener.

### `BackHandler.close()`

Finish the Android Activity. This is the only way to close the app once a back listener is active, since the OS default is suppressed.

</details>

### bare-wifi-android

Android WifiManager bindings for the Bare runtime — multicast lock and WiFi interface IP

- repo: https://github.com/holepunchto/bare-wifi-android · 0★ · last push 2026-05-25 · C++
- version: 0.1.5 · native addon · min Bare: — · types: no · prebuilds: yes
- node-compat: no · bare deps: none · inbound deps: 0
- disposition: **catalog-row-only** · ⚠ missing from catalog

<details><summary>API section (README)</summary>

### `acquireMulticastLock([tag])`

Acquires a `WifiManager.MulticastLock` with the given tag string (default `"bare-mdns"`). Idempotent — acquiring a second lock releases the previous one. No-op on non-Android.

### `releaseMulticastLock()`

Releases the currently held lock. No-op if no lock is held or on non-Android.

### `getWifiIP()`

Returns the IPv4 address of the `wlan0` WiFi interface as a string, or `null` if not connected. Requires NDK API 24+.

</details>

### bare-lief

LIEF bindings for Bare

- repo: https://github.com/holepunchto/bare-lief · 0★ · last push 2026-05-11 · C++
- version: 0.2.4 · native addon · min Bare: — · types: no · prebuilds: yes
- node-compat: no · bare deps: bare-assert · inbound deps: 2
- disposition: **catalog-row-only** · ⚠ missing from catalog

### bare-cov

Generate coverage reports using Bare and Node.js

- repo: https://github.com/holepunchto/bare-cov · 0★ · last push 2026-05-04 · JavaScript
- version: 1.2.2 · pure-JS · min Bare: — · types: no · prebuilds: no
- node-compat: yes · bare deps: bare-fs, bare-inspector, bare-path, bare-process, bare-url, bare-v8-to-istanbul · inbound deps: 0
- disposition: **catalog-row-only** · ⚠ missing from catalog

### bare-posix

POSIX-specific bindings for Bare

- repo: https://github.com/holepunchto/bare-posix · 0★ · last push 2026-04-30 · C
- version: 1.0.1 · native addon · min Bare: — · types: yes · prebuilds: yes
- node-compat: no · bare deps: none · inbound deps: 1
- disposition: **full-page candidate** · ⚠ missing from catalog

<details><summary>API section (README)</summary>

#### `posix.getgid()`

#### `posix.setgid(id)`

#### `posix.getegid()`

#### `posix.setegid(id)`

#### `posix.getuid()`

#### `posix.setuid(id)`

#### `posix.geteuid()`

#### `posix.seteuid(id)`

#### `posix.getgroups()`

#### `posix.getgrnam(name)`

#### `posix.getpwnam(name)`

</details>

### bare-mime

MIME type parsing for Bare

- repo: https://github.com/holepunchto/bare-mime · 0★ · last push 2026-04-24 · JavaScript
- version: 1.0.0 · pure-JS · min Bare: — · types: no · prebuilds: no
- node-compat: no · bare deps: none · inbound deps: 1
- disposition: **full-page candidate** · ⚠ missing from catalog

<details><summary>API section (README)</summary>

#### `const mime = new MIME(type, subtype[, parameters])`

Construct a new MIME type record with the given `type` and `subtype` strings. `parameters` is an optional `Map` of parameter name-value pairs, defaulting to an empty `Map`.

#### `mime.type`

The type of the MIME type record, e.g. `'text'`.

#### `mime.subtype`

The subtype of the MIME type record, e.g. `'plain'`.

#### `mime.parameters`

A `Map` of parameters associated with the MIME type record.

#### `const mime = MIME.parse(input)`

Parse a MIME type string per <https://mimesniff.spec.whatwg.org/#parse-a-mime-type>. Returns a `MIME` instance on success or `null` on failure. The `type` and `subtype` are lowercased and leading/trailing HTTP whitespace is stripped. Parameters are parsed from the input, with duplicate parameter names ignored in favor of the first occurrence.

</details>

### bare-v8

V8 utilities for Bare

- repo: https://github.com/holepunchto/bare-v8 · 0★ · last push 2026-04-23 · C
- version: 1.0.1 · native addon · min Bare: — · types: no · prebuilds: yes
- node-compat: no · bare deps: none · inbound deps: 2
- disposition: **catalog-row-only** · ⚠ missing from catalog

### bare-web-kit

WebKit bindings for Bare

- repo: https://github.com/holepunchto/bare-web-kit · 0★ · last push 2026-04-23 · Objective-C
- version: 0.1.2 · native addon · min Bare: — · types: no · prebuilds: yes
- node-compat: no · bare deps: bare-events · inbound deps: 1
- disposition: **catalog-row-only** · ⚠ missing from catalog

### bare-string-decoder

`string_decoder` shim for Bare

- repo: https://github.com/holepunchto/bare-string-decoder · 0★ · last push 2026-04-23 · JavaScript
- version: 1.0.0 · pure-JS · min Bare: — · types: yes · prebuilds: no
- node-compat: no · bare deps: none · inbound deps: 1
- disposition: **catalog-row-only** · ⚠ missing from catalog

### bare-semver

Minimal semantic versioning library for Bare

- repo: https://github.com/holepunchto/bare-semver · 0★ · last push 2026-04-23 · JavaScript
- version: 1.0.3 · pure-JS · min Bare: — · types: yes · prebuilds: no
- node-compat: no · bare deps: none · inbound deps: 4
- disposition: **full-page candidate**

<details><summary>API section (README)</summary>

#### `const satisfied = semver.satisfies(version, range)`

Test whether `version` satisfies `range`. Both `version` and `range` may be strings, in which case they will be parsed.

#### `semver.constants`

An object containing the comparison operator constants:

```js
constants = {
  EQ: 1,
  LT: 2,
  LTE: 3,
  GT: 4,
  GTE: 5
}
```

#### `semver.errors`

The `SemVerError` class. Thrown when parsing invalid versions or ranges.

#### `const version = new semver.Version(major, minor, patch[, options])`

Create a new version with the given `major`, `minor`, and `patch` components.

Options include:

```js
options = {
  prerelease: [],
  build: []
}
```

#### `version.major`

The major version number.

#### `version.minor`

The minor version number.

#### `version.patch`

The patch version number.

#### `version.prerelease`

An array of prerelease tags.

</details>

### bare-queue-microtask

Microtask queuing for Bare

- repo: https://github.com/holepunchto/bare-queue-microtask · 0★ · last push 2026-04-23 · JavaScript
- version: 1.0.0 · pure-JS · min Bare: — · types: no · prebuilds: no
- node-compat: no · bare deps: none · inbound deps: 0
- disposition: **catalog-row-only**

### bare-file-logger

File system logger for Bare

- repo: https://github.com/holepunchto/bare-file-logger · 0★ · last push 2026-04-23 · JavaScript
- version: 1.2.1 · pure-JS · min Bare: — · types: yes · prebuilds: no
- node-compat: no · bare deps: bare-fs, bare-logger · inbound deps: 0
- disposition: **catalog-row-only** · ⚠ missing from catalog

<details><summary>API section (README)</summary>

#### `const log = new FileLog(path[, options])`

Options include:

```js
options = {
  // Maximum allowed byte size of the log file. This is a hint and not a hard
  // limit; the logger will do its best to keep the file size within the limit,
  // but provides no guarantees.
  maxSize: 0,

  // A function called when the file size reaches `maxSize`. It receives the
  // current file path and should return a new path to rename the file to, or a
  // falsy value to do nothing. After a successful rename, a new empty log file
  // is opened at the original path.
  rotate: null,

  // The interval in milliseconds at which the file size is checked against
  // `maxSize` for rotation. Only active when both `maxSize` and `rotate` are
  // set. The check starts after the first write.
  rotateInterval: 2000
}
```

#### Rotation

```js
const log = new FileLog('app.log', {
  maxSize: 1024 * 1024, // 1 MB
  rotate(filePath) {
    return filePath + '.' + Date.now()
  }
})
```

When the log file reaches `maxSize`, the `rotate` function is called. If it returns a path, the current file is renamed to that path and a fresh log file is opened. If it returns a falsy value, no action is taken.

#### `log.on('rotate', (path, dest) => {})`

Emitted after a successful rotation. `path` is the original log file path (now empty and ready for new writes) and `dest` is the path the previous log was renamed to.

</details>

### bare-sidecar-bundle

Bare bundler optimised for sidecars to be used in pear-runtime

- repo: https://github.com/holepunchto/bare-sidecar-bundle · 0★ · last push 2026-03-12 · JavaScript
- version: 1.0.0 · pure-JS · min Bare: — · types: no · prebuilds: no
- node-compat: no · bare deps: bare-bundle, bare-module-traverse, bare-pack · inbound deps: 0
- disposition: **catalog-row-only** · ⚠ missing from catalog

### bare-svg

SVG support for Bare

- repo: https://github.com/holepunchto/bare-svg · 0★ · last push 2026-01-29 · C
- version: 1.0.1 · native addon · min Bare: — · types: no · prebuilds: yes
- node-compat: no · bare deps: none · inbound deps: 1
- disposition: **catalog-row-only** · ⚠ missing from catalog

### bare-inspector-gc

manually trigger garbage collection via inspector protocol

- repo: https://github.com/holepunchto/bare-inspector-gc · 0★ · last push 2026-01-27 · JavaScript
- version: 1.0.0 · pure-JS · min Bare: >=1.2.0 · types: no · prebuilds: no
- node-compat: no · bare deps: none · inbound deps: 0
- disposition: **catalog-row-only** · ⚠ missing from catalog

### bare-tap

Minimal TAP library for Bare

- repo: https://github.com/holepunchto/bare-tap · 0★ · last push 2025-12-05 · JavaScript
- version: 1.0.0 · pure-JS · min Bare: — · types: no · prebuilds: no
- node-compat: no · bare deps: bare-assert · inbound deps: 0
- disposition: **catalog-row-only** · ⚠ missing from catalog

### bare-punycode

Punycode support for Bare

- repo: https://github.com/holepunchto/bare-punycode · 0★ · last push 2025-10-22 · JavaScript
- version: 0.0.0 · pure-JS · min Bare: — · types: no · prebuilds: no
- node-compat: no · bare deps: none · inbound deps: 1
- disposition: **catalog-row-only** · ⚠ missing from catalog
