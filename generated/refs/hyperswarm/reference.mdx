---
title: "Hyperswarm"
description: "A distributed networking stack for connecting peers"
docType: reference
schemaType: APIReference
---

{/* Generated by scripts/generate-refs.ts from holepunchto/hyperswarm@v4.17.0 (88b2c706d5). Do not edit by hand. */}

## Installation

```
npm install hyperswarm
```

## Usage

```js
const Hyperswarm = require('hyperswarm')

const swarm1 = new Hyperswarm()
const swarm2 = new Hyperswarm()

swarm1.on('connection', (conn, info) => {
  // swarm1 will receive server connections
  conn.write('this is a server connection')
  conn.end()
})

swarm2.on('connection', (conn, info) => {
  conn.on('data', (data) => console.log('client got message:', data.toString()))
})

const topic = Buffer.alloc(32).fill('hello world') // A topic must be 32 bytes
const discovery = swarm1.join(topic, { server: true, client: false })
await discovery.flushed() // Waits for the topic to be fully announced on the DHT

swarm2.join(topic, { server: false, client: true })
await swarm2.flush() // Waits for the swarm to connect to pending peers.

// After this point, both client and server should have connections
```

## API Reference

### Constructor

#### `const swarm = new Hyperswarm(opts = {})`

Construct a new Hyperswarm instance.

**Parameters:**

- `opts` _(optional)_ — `opts` can include:

[Source](https://github.com/holepunchto/hyperswarm/blob/v4.17.0/index.js#L24)

### Methods

#### `const discovery = swarm.status(topic)`

Get the [`PeerDiscovery`](https://github.com/holepunchto/hyperswarm/blob/v3/README.md#peerdiscovery-api) object associated with the topic, if it exists.

**Parameters:**

- `key` — Get the [`PeerDiscovery`](https://github.com/holepunchto/hyperswarm/blob/v3/README.md#peerdiscovery-api) object associated with the topic, if it exists.

[Source](https://github.com/holepunchto/hyperswarm/blob/v4.17.0/index.js#L487)

#### `await swarm.listen()`

Explicitly start listening for incoming connections. This will be called internally after the first `join`, so it rarely needs to be called manually.

[Source](https://github.com/holepunchto/hyperswarm/blob/v4.17.0/index.js#L491)

#### `const discovery = swarm.join(topic, opts = {})`

Start discovering and connecting to peers sharing a common topic. As new peers are connected to, they will be emitted from the swarm as `connection` events.

**Parameters:**

- `topic` — Start discovering and connecting to peers sharing a common topic.
- `opts` _(optional)_ — `topic` must be a 32-byte Buffer `opts` can include:

**Returns:** a [`PeerDiscovery`](https://github.com/holepunchto/hyperswarm/blob/v3/README.md#peerdiscovery-api) object.

[Source](https://github.com/holepunchto/hyperswarm/blob/v4.17.0/index.js#L501)

#### `await swarm.leave(topic)`

Stop discovering peers for the given topic.

**Parameters:**

- `topic` — Stop discovering peers for the given topic.

[Source](https://github.com/holepunchto/hyperswarm/blob/v4.17.0/index.js#L525)

#### `swarm.joinPeer(noisePublicKey)`

Establish a direct connection to a known peer.

**Parameters:**

- `publicKey` — `noisePublicKey` must be a 32-byte Buffer

[Source](https://github.com/holepunchto/hyperswarm/blob/v4.17.0/index.js#L543)

#### `swarm.leavePeer(noisePublicKey)`

Stop attempting direct connections to a known peer.

**Parameters:**

- `publicKey` — `noisePublicKey` must be a 32-byte Buffer

[Source](https://github.com/holepunchto/hyperswarm/blob/v4.17.0/index.js#L556)

#### `await swarm.flush()`

Wait for any pending DHT announces, and for the swarm to connect to any pending peers (peers that have been discovered, but are still in the queue awaiting processing).

[Source](https://github.com/holepunchto/hyperswarm/blob/v4.17.0/index.js#L567)

#### `await discovery.destroy()`

Stop discovering peers for the given topic.

[Source](https://github.com/holepunchto/hyperswarm/blob/v4.17.0/index.js#L588)

#### `await swarm.suspend({ log: () => {} })`

Suspend the swarm disconnecting all peers, suspends server listening and stops discovery of new peers. Useful for suspending when the runtime suspends to pause networking.

[Source](https://github.com/holepunchto/hyperswarm/blob/v4.17.0/index.js#L606)

#### `await swarm.resume({ log: () => {} })`

Resume a suspended swarm refreshing discovery of new peers and servers. Useful for reannouncing to the DHT and reconnecting to peers when the runtime resumes.

[Source](https://github.com/holepunchto/hyperswarm/blob/v4.17.0/index.js#L642)

#### `peerInfo.topics`

An Array of topics that this Peer is associated with -- `topics` will only be updated when the Peer is in client mode.

[Source](https://github.com/holepunchto/hyperswarm/blob/v4.17.0/index.js#L659)

#### `await discovery.flushed()`

Wait until the topic has been fully announced to the DHT. This method is only relevant in server mode. When `flushed()` has completed, the server will be available to the network.

#### `await discovery.refresh({ client, server })`

Update the `PeerDiscovery` configuration, optionally toggling client and server modes. This will also trigger an immediate re-announce of the topic, when the `PeerDiscovery` is in server mode.

#### `peerInfo.publicKey`

The peer's Noise public key.

#### `peerInfo.prioritized`

If true, the swarm will rapidly attempt to reconnect to this peer.

#### `peerInfo.ban(banStatus = false)`

Ban or unban the peer. Banning will prevent any future reconnection attempts, but it will not close any existing connections.

### Properties

#### `swarm.dht`

A [`hyperdht`](https://github.com/holepunchto/hyperdht) instance. Useful if you want lower-level control over Hyperswarm's networking.

[Source](https://github.com/holepunchto/hyperswarm/blob/v4.17.0/index.js#L38)

#### `swarm.connecting`

Number that indicates connections in progress.

[Source](https://github.com/holepunchto/hyperswarm/blob/v4.17.0/index.js#L64)

#### `swarm.connections`

A set of all active client/server connections.

[Source](https://github.com/holepunchto/hyperswarm/blob/v4.17.0/index.js#L65)

#### `swarm.peers`

A Map containing all connected peers, of the form: `(Noise public key hex string) -> PeerInfo object`

[Source](https://github.com/holepunchto/hyperswarm/blob/v4.17.0/index.js#L66)

### Events

#### `swarm.on('connection', (socket, peerInfo) => {})`

Emitted whenever the swarm connects to a new peer.

[Source](https://github.com/holepunchto/hyperswarm/blob/v4.17.0/index.js#L244)

#### `swarm.on('update', () => {})`

Emitted when internal values are changed, useful for user interfaces.

[Source](https://github.com/holepunchto/hyperswarm/blob/v4.17.0/index.js#L247)

#### `swarm.on('ban', peerInfo, err)`

Emitted when a peer gets banned. `err` is an error object describing the reason for the ban (e.g. firewalled).

**Parameters:**

- `err` — `err` is an error object describing the reason for the ban (e.g.

[Source](https://github.com/holepunchto/hyperswarm/blob/v4.17.0/index.js#L484)

{/* 17 member(s) found in source but not documented upstream are omitted; see the improvement plan. Re-run with --full to include them. */}
