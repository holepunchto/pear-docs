---
title: "Hyperdrive"
description: "Secure real-time distributed filesystem built on Hypercore-based storage."
docType: reference
schemaType: APIReference
---

<Status level="stable" />

`Hyperdrive` stores filesystem metadata in a [Hyperbee](/reference/building-blocks/hyperbee) and file contents in a blob store, typically backed by one [Corestore](/reference/helpers/corestore). Use this page for the instance surface; use the linked how-to guides for end-to-end replication flows.

<Callout type="info">
For when to choose Hyperdrive over Hyperblobs or raw Hypercore, see [From append-only logs to files](/explanation/from-logs-to-files).
</Callout>

## Install

```sh
npm i hyperdrive
```

## Quickstart

```js
import Corestore from 'corestore'
import Hyperdrive from 'hyperdrive'

const store = new Corestore('./storage')
const drive = new Hyperdrive(store)

await drive.ready()

await drive.put('/hello.txt', Buffer.from('hello world'), {
  metadata: { type: 'text/plain' }
})

const buffer = await drive.get('/hello.txt')
const entry = await drive.entry('/hello.txt')

console.log(buffer.toString())
console.log(entry?.value.metadata)

await drive.close()
```

## API Reference

### Constructor and lifecycle

#### `const drive = new Hyperdrive(store, [key])`

[API definition on GitHub](https://github.com/holepunchto/hyperdrive/blob/v13.3.2/index.js#L41)

Creates a new Hyperdrive instance. `store` must be an instance of `Corestore`.

| Parameter | Type | Default | Description |
| --- | --- | --- | --- |
| `corestore` | [`Corestore`](/reference/helpers/corestore) | — |  |
| `key` | [`Buffer`](https://nodejs.org/api/buffer.html#class-buffer) | — | Public key of an existing drive to load. May be omitted, passing |
| `opts` | [`DriveOptions`](#driveoptions) | `{}` | Drive options. |

```js
const Corestore = require('corestore')

const store = new Corestore('./storage')
const drive = new Hyperdrive(store)
```

#### `await drive.ready()`

Waits until internal state is loaded.

#### `await drive.close()`

Fully close this drive, including its underlying Hypercore backed datastructures.

#### `await drive.purge()`

[API definition on GitHub](https://github.com/holepunchto/hyperdrive/blob/v13.3.2/index.js#L622)

Purge both cores (db and blobs) from your storage, completely removing all the drive's data.

- Returns: [`Promise<void>`](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Promise) — Resolves once both cores are purged from storage.

```js
await drive.purge()
```

### Drive properties

#### `drive.corestore`

[API definition on GitHub](https://github.com/holepunchto/hyperdrive/blob/v13.3.2/index.js#L53)

The Corestore instance used as storage.

- Returns: [`Corestore`](/reference/helpers/corestore)

#### `drive.db`

[API definition on GitHub](https://github.com/holepunchto/hyperdrive/blob/v13.3.2/index.js#L58)

The underlying Hyperbee backing the drive file structure.

- Returns: [`Hyperbee`](/reference/building-blocks/hyperbee)

#### `drive.core`

[API definition on GitHub](https://github.com/holepunchto/hyperdrive/blob/v13.3.2/index.js#L63)

The Hypercore used for `drive.db`.

- Returns: [`Hypercore`](/reference/building-blocks/hypercore)

#### `drive.id`

[API definition on GitHub](https://github.com/holepunchto/hyperdrive/blob/v13.3.2/index.js#L124)

String containing the id (z-base-32 of the public key) identifying this drive.

- Returns: `string`

#### `drive.key`

[API definition on GitHub](https://github.com/holepunchto/hyperdrive/blob/v13.3.2/index.js#L132)

The public key of the Hypercore backing the drive.

- Returns: [`Buffer`](https://nodejs.org/api/buffer.html#class-buffer)

#### `drive.discoveryKey`

[API definition on GitHub](https://github.com/holepunchto/hyperdrive/blob/v13.3.2/index.js#L141)

The hash of the public key of the Hypercore backing the drive.

- Returns: [`Buffer`](https://nodejs.org/api/buffer.html#class-buffer)

#### `drive.contentKey`

[API definition on GitHub](https://github.com/holepunchto/hyperdrive/blob/v13.3.2/index.js#L151)

The public key of the [Hyperblobs](https://github.com/holepunchto/hyperblobs) instance holding blobs associated with entries in the drive.

- Returns: [`Buffer`](https://nodejs.org/api/buffer.html#class-buffer)

#### `drive.writable`

[API definition on GitHub](https://github.com/holepunchto/hyperdrive/blob/v13.3.2/index.js#L168)

Boolean indicating if we can write or delete data in this drive.

- Returns: `boolean`

#### `drive.readable`

[API definition on GitHub](https://github.com/holepunchto/hyperdrive/blob/v13.3.2/index.js#L177)

Boolean indicating if we can read from this drive. After closing the drive this will be `false`.

- Returns: `boolean`

#### `drive.version`

[API definition on GitHub](https://github.com/holepunchto/hyperdrive/blob/v13.3.2/index.js#L160)

Number that indicates how many modifications were made, useful as a version identifier.

- Returns: `number`

#### `drive.supportsMetadata`

[API definition on GitHub](https://github.com/holepunchto/hyperdrive/blob/v13.3.2/index.js#L69)

Boolean indicating if the drive handles or not metadata. Always `true`.

- Returns: `boolean`

### Files

#### `await drive.put(path, buffer, [options])`

[API definition on GitHub](https://github.com/holepunchto/hyperdrive/blob/v13.3.2/index.js#L530)

Creates a file at `path` in the drive. `options` are the same as in `createWriteStream`.

| Parameter | Type | Default | Description |
| --- | --- | --- | --- |
| `name` | `string` | — | Path to write the file at. |
| `buf` | [`Buffer`](https://nodejs.org/api/buffer.html#class-buffer) | — | Blob contents to store. |
| `options` | [`WriteOptions`](#writeoptions) | `{}` |  |

- Returns: [`Promise<void>`](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Promise) — Resolves once the file has been written.

```js
await drive.put('/blob.txt', Buffer.from('example'))
```

#### `const buffer = await drive.get(path, [options])`

[API definition on GitHub](https://github.com/holepunchto/hyperdrive/blob/v13.3.2/index.js#L498)

It also returns `null` for symbolic links.

| Parameter | Type | Default | Description |
| --- | --- | --- | --- |
| `name` | `string` | — | Path of the file to read. |
| `opts` | [`GetOptions`](#getoptions) | — | `options` |

- Returns: [`Promise<Buffer>`](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Promise) — the blob at `path` in the drive.

| Option | Default | Description |
| --- | --- | --- |
| `wait` | `true` | Wait for block to be downloaded |
| `timeout` | `0` | Wait at max some milliseconds (0 means no timeout) |

#### `const entry = await drive.entry(path, [options])`

[API definition on GitHub](https://github.com/holepunchto/hyperdrive/blob/v13.3.2/index.js#L673)

`options` include:

| Parameter | Type | Default | Description |
| --- | --- | --- | --- |
| `name` | `string` | — | Path of the entry to read. |
| `opts` | [`EntryOptions`](#entryoptions) | — |  |

- Returns: [`Promise<object>`](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Promise) — the entry at `path` in the drive.

| Option | Default | Description |
| --- | --- | --- |
| `seq` | `Number` |  |
| `key` | `String` |  |
| `value` | — |  |

#### `const exists = await drive.exists(path)`

[API definition on GitHub](https://github.com/holepunchto/hyperdrive/blob/v13.3.2/index.js#L699)

Checks whether an entry exists at `path`.

| Parameter | Type | Default | Description |
| --- | --- | --- | --- |
| `name` | `string` | — | Path to check. |

- Returns: [`Promise<boolean>`](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Promise) — `true` if the entry at `path` does exists, otherwise `false`.

```js
const exists = await drive.exists('/blob.txt')
```

#### `await drive.has(path)`

[API definition on GitHub](https://github.com/holepunchto/hyperdrive/blob/v13.3.2/index.js#L837)

Checks if path is saved to local store already.

| Parameter | Type | Default | Description |
| --- | --- | --- | --- |
| `path` | `string` | — | of the file or folder to check. |

- Returns: [`Promise<boolean>`](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Promise) — `true` if the blob (or every blob under a folder) is stored locally.

```js
const exists = await drive.has('/blob.txt')
```

#### `await drive.del(path)`

[API definition on GitHub](https://github.com/holepunchto/hyperdrive/blob/v13.3.2/index.js#L547)

Deletes the file at `path` from the drive.

| Parameter | Type | Default | Description |
| --- | --- | --- | --- |
| `name` | `string` | — | Path of the file to delete. |

- Returns: [`Promise<void>`](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Promise) — Resolves once the entry has been deleted.

```js
await drive.del('/blob.txt')
```

#### `const cleared = await drive.clear(path, [options])`

[API definition on GitHub](https://github.com/holepunchto/hyperdrive/blob/v13.3.2/index.js#L579)

Deletes the blob from storage to free up space, but the file structure reference is kept.

| Parameter | Type | Default | Description |
| --- | --- | --- | --- |
| `name` | `string` | — | Path of the file whose blob should be cleared. |
| `opts` | [`ClearOptions`](#clearoptions) | — | `options` |

- Returns: [`Promise<object>`](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Promise) — A `{ blocks }` object when `diff` is enabled, otherwise `null`.

| Option | Default | Description |
| --- | --- | --- |
| `diff` | `false` | Returned `cleared` bytes object is null unless you enable this |

#### `const cleared = await drive.clearAll([options])`

[API definition on GitHub](https://github.com/holepunchto/hyperdrive/blob/v13.3.2/index.js#L605)

Deletes all the blobs from storage to free up space, similar to how `drive.clear()` works.

| Parameter | Type | Default | Description |
| --- | --- | --- | --- |
| `opts` | [`ClearOptions`](#clearoptions) | — | `options` |

- Returns: [`Promise<object>`](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Promise) — A `{ blocks }` object when `diff` is enabled, otherwise `null`.

| Option | Default | Description |
| --- | --- | --- |
| `diff` | `false` | Returned `cleared` bytes object is null unless you enable this |

#### `await drive.symlink(path, linkname)`

[API definition on GitHub](https://github.com/holepunchto/hyperdrive/blob/v13.3.2/index.js#L649)

Creates an entry in drive at `path` that points to the entry at `linkname`.

| Parameter | Type | Default | Description |
| --- | --- | --- | --- |
| `name` | `string` | — | Path of the symlink to create. |
| `dst` | `string` | — | Path the symlink points to. |
| `options` | [`SymlinkOptions`](#symlinkoptions) | `{}` | Symlink options. |

- Returns: [`Promise<void>`](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Promise) — Resolves once the symlink entry has been created.

```js
await drive.symlink('/images/logo.shortcut', '/images/logo.png')
```

#### `const rs = drive.createReadStream(path, [options])`

[API definition on GitHub](https://github.com/holepunchto/hyperdrive/blob/v13.3.2/index.js#L908)

`options` include:

| Parameter | Type | Default | Description |
| --- | --- | --- | --- |
| `name` | `string` | — | Path of the file to read. |
| `opts` | `object` | — |  |

- Returns: [`Readable`](https://nodejs.org/api/stream.html#class-streamreadable) — a stream to read out the blob stored in the drive at `path`.

| Option | Default | Description |
| --- | --- | --- |
| `start` | `Number` | `start` and `end` are inclusive |
| `end` | `Number` |  |
| `length` | `Number` | `length` overrides `end`, they're not meant to be used together |
| `wait` | `true` | Wait for blocks to be downloaded |
| `timeout` | `0` | Wait at max some milliseconds (0 means no timeout) |

#### `const ws = drive.createWriteStream(path, [options])`

[API definition on GitHub](https://github.com/holepunchto/hyperdrive/blob/v13.3.2/index.js#L968)

Stream a blob into the drive at `path`.

| Parameter | Type | Default | Description |
| --- | --- | --- | --- |
| `name` | `string` | — | Path of the file to write. |
| `options` | [`WriteOptions`](#writeoptions) | `{}` | include |

- Returns: [`Writable`](https://nodejs.org/api/stream.html#class-streamwritable) — A writable stream; data written is stored as the file's blob.

| Option | Default | Description |
| --- | --- | --- |
| `executable` | `Boolean` |  |
| `metadata` | `null` | Extended file information i.e. arbitrary JSON value |

### Directories and listings

#### `const stream = drive.list(folder, [options])`

[API definition on GitHub](https://github.com/holepunchto/hyperdrive/blob/v13.3.2/index.js#L863)

`options` include:

| Parameter | Type | Default | Description |
| --- | --- | --- | --- |
| `folder` | `string` | — | Path prefix to list. Defaults to the whole drive. |
| `opts` | `object` | `{}` |  |

- Returns: [`Readable`](https://nodejs.org/api/stream.html#class-streamreadable) — a stream of all entries in the drive at paths prefixed with `folder`.

| Option | Default | Description |
| --- | --- | --- |
| `recursive` | — | Whether to descend into all subfolders or not |
| `ignore` | — | Ignore files and folders by name |
| `wait` | `true` | Wait for block to be downloaded. |

#### `const stream = drive.readdir(folder, [options])`

[API definition on GitHub](https://github.com/holepunchto/hyperdrive/blob/v13.3.2/index.js#L882)

`options` include:

| Parameter | Type | Default | Description |
| --- | --- | --- | --- |
| `folder` | `string` | — | Path prefix to read. |
| `opts` | `object` | — |  |

- Returns: [`Readable`](https://nodejs.org/api/stream.html#class-streamreadable) — a stream of all subpaths of entries in drive stored at paths prefixed by `folder`.

| Option | Default | Description |
| --- | --- | --- |
| `wait` | `true` | Wait for block to be downloaded |

#### `const stream = await drive.entries([range], [options])`

[API definition on GitHub](https://github.com/holepunchto/hyperdrive/blob/v13.3.2/index.js#L807)

`options` are the same as `Hyperbee().createReadStream([range], [options])`.

| Parameter | Type | Default | Description |
| --- | --- | --- | --- |
| `range` | `object` | — | Hyperbee range bounds (`gt`/`gte`/`lt`/`lte`). |
| `opts` | `object` | — |  |

- Returns: [`Readable`](https://nodejs.org/api/stream.html#class-streamreadable) — a read stream of entries in the drive.

```js
for await (const entry of drive.entries()) console.log(entry.key)
```

#### `const comparison = drive.compare(entryA, entryB)`

[API definition on GitHub](https://github.com/holepunchto/hyperdrive/blob/v13.3.2/index.js#L559)

Compares two entries by their sequence number.

| Parameter | Type | Default | Description |
| --- | --- | --- | --- |
| `a` | `object` | — | The first entry (as returned by `drive.entry()`). |
| `b` | `object` | — | The second entry (as returned by `drive.entry()`). |

- Returns: `number` — `0` if entries are the same, `1` if `entryA` is older, and `-1` if `entryB` is older.

```js
const comparison = drive.compare(entryA, entryB)
```

#### `const watcher = drive.watch([folder])`

[API definition on GitHub](https://github.com/holepunchto/hyperdrive/blob/v13.3.2/index.js#L716)

Usage example:

| Parameter | Type | Default | Description |
| --- | --- | --- | --- |
| `folder` | `string` | — | to watch. Defaults to `/`. |

- Returns: [`Watcher`](/reference/building-blocks/hyperbee) — an iterator that listens on `folder` to yield changes, by default on `/`.

```js
for await (const [current, previous] of watcher) {
  console.log(current.version)
  console.log(previous.version)
}
```

### Versions and batched mutations

#### `const batch = drive.batch()`

[API definition on GitHub](https://github.com/holepunchto/hyperdrive/blob/v13.3.2/index.js#L314)

Useful for atomically mutate the drive, has the same interface as Hyperdrive.

- Returns: [`Hyperdrive`](/reference/building-blocks/hyperdrive) — A batch with the same interface as Hyperdrive; call `batch.flush()` to commit.

```js
const batch = drive.batch()
await batch.put('/a.txt', Buffer.from('a'))
await batch.flush()
```

#### `await batch.flush()`

[API definition on GitHub](https://github.com/holepunchto/hyperdrive/blob/v13.3.2/index.js#L338)

Commit a batch of mutations to the underlying drive.

- Returns: [`Promise<void>`](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Promise) — Resolves once the batch is committed and closed.

```js
await batch.flush()
```

#### `const snapshot = drive.checkout(version)`

[API definition on GitHub](https://github.com/holepunchto/hyperdrive/blob/v13.3.2/index.js#L301)

Get a read-only snapshot of a previous version.

| Parameter | Type | Default | Description |
| --- | --- | --- | --- |
| `version` | `number` | — | Drive version to snapshot. |

- Returns: [`Hyperdrive`](/reference/building-blocks/hyperdrive) — A read-only Hyperdrive at the requested version.

```js
const snapshot = drive.checkout(4)
```

#### `const stream = drive.diff(version, folder, [options])`

[API definition on GitHub](https://github.com/holepunchto/hyperdrive/blob/v13.3.2/index.js#L733)

Efficiently create a stream of the shallow changes to `folder` between `version` and `drive.version`.

| Parameter | Type | Default | Description |
| --- | --- | --- | --- |
| `length` | `number` | — | Drive version to diff against the current `drive.version`. |
| `folder` | `string` | — | Path prefix to scope the diff to. Omit for the whole drive. |
| `opts` | `object` | — | Diff options, forwarded to the underlying Hyperbee diff stream. |

- Returns: [`Readable`](https://nodejs.org/api/stream.html#class-streamreadable) — A stream of `{ left, right }` diff entries.

| Option | Default | Description |
| --- | --- | --- |
| `left` | `Object` | Entry in folder at drive.version for some path |
| `right` | `Object` | Entry in folder at drive.checkout(version) for some path |

#### `await drive.truncate(version, [options] })`

[API definition on GitHub](https://github.com/holepunchto/hyperdrive/blob/v13.3.2/index.js#L209)

Truncates the Hyperdrive to a previous version (both the file-structure reference and the blobs).

| Parameter | Type | Default | Description |
| --- | --- | --- | --- |
| `version` | `number` | — | Drive version to truncate to. |
| `options` | [`TruncateOptions`](#truncateoptions) | `{}` | Truncate options. |

- Returns: [`Promise<void>`](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Promise) — Resolves once the drive and its blobs are truncated.

```js
await drive.truncate(drive.version - 1)
```

### Replication, downloads, and blob access

#### `const mirror = drive.mirror(out, [options])`

[API definition on GitHub](https://github.com/holepunchto/hyperdrive/blob/v13.3.2/index.js#L898)

Efficiently mirror this drive into another. Returns a [`MirrorDrive`](https://github.com/holepunchto/mirror-drive#api) instance constructed with `options`.

| Parameter | Type | Default | Description |
| --- | --- | --- | --- |
| `out` | [`Hyperdrive\|Localdrive`](/reference/building-blocks/hyperdrive) | — | Destination drive to mirror into. |
| `opts` | `object` | — | Mirror options, forwarded to `MirrorDrive`. |

- Returns: [`MirrorDrive`](/reference/helpers/mirrordrive) — A `MirrorDrive` instance; await it or `mirror.done()` to finish.

```js
const mirror = drive.mirror(out)
await mirror.done()
```

#### `const download = drive.download(folder, [options])`

[API definition on GitHub](https://github.com/holepunchto/hyperdrive/blob/v13.3.2/index.js#L824)

Downloads the blobs corresponding to all entries in the drive at paths prefixed with `folder`. Returns a `Download` object that resolves once all data has been downloaded:

| Parameter | Type | Default | Description |
| --- | --- | --- | --- |
| `folder` | `string` | `'/'` | Path prefix to download. Defaults to the whole drive. |
| `opts` | `object` | — | `options` are the same as those for `drive.list(folder, [options])`. |

- Returns: `Download` — A Download object; await `download.done()` to know when complete.

```js
const download = await drive.download(key)
await download.done()
```

```js
download.destroy()
```

#### `await drive.downloadDiff(version, folder, [options])`

[API definition on GitHub](https://github.com/holepunchto/hyperdrive/blob/v13.3.2/index.js#L756)

Downloads all the blobs in `folder` corresponding to entries in `drive.checkout(version)` that are not in `drive.version`. Returns a `Download` object that resolves once all data has been downloaded:

| Parameter | Type | Default | Description |
| --- | --- | --- | --- |
| `length` | `number` | — | Drive version to diff against the current `drive.version`. |
| `folder` | `string` | — | Path prefix to scope the download to. Omit for the whole drive. |
| `opts` | `object` | — | Diff options, forwarded to `drive.diff()`. |

- Returns: `Download` — A Download object; await `download.done()` to know when complete.

```js
const download = await drive.downloadDiff(version, folder)
await download.done()
```

```js
download.destroy()
```

#### `await drive.downloadRange(dbRanges, blobRanges)`

[API definition on GitHub](https://github.com/holepunchto/hyperdrive/blob/v13.3.2/index.js#L781)

Downloads the entries and blobs stored in the ranges `dbRanges` and `blobRanges`. Returns a `Download` object that resolves once all data has been downloaded:

| Parameter | Type | Default | Description |
| --- | --- | --- | --- |
| `dbRanges` | [`Array<object>`](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Array) | — | Block ranges to download from the db (metadata) core. |
| `blobRanges` | [`Array<object>`](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Array) | — | Block ranges to download from the blobs core. |

- Returns: `Download` — A Download object; await `download.done()` to know when complete.

```js
const download = await drive.downloadRange(dbRanges, blobRanges)
await download.done()
```

```js
download.destroy()
```

#### `const done = drive.findingPeers()`

[API definition on GitHub](https://github.com/holepunchto/hyperdrive/blob/v13.3.2/index.js#L190)

Indicate to Hyperdrive that you're finding peers in the background, requests will be on hold until this is done.

- Returns: `Function` — A `done` callback to call once peer discovery has finished.

```js
const done = drive.findingPeers()
swarm.flush().then(done, done)
```

#### `const stream = drive.replicate(isInitiatorOrStream)`

[API definition on GitHub](https://github.com/holepunchto/hyperdrive/blob/v13.3.2/index.js#L262)

Usage example:

| Parameter | Type | Default | Description |
| --- | --- | --- | --- |
| `isInitiator` | [`boolean\|Stream`](https://nodejs.org/api/stream.html) | — | `true`/`false` to initiate replication, or an existing stream/socket to replicate over. |
| `opts` | `object` | — | Replication options, forwarded to `corestore.replicate()`. |

- Returns: [`Stream`](https://nodejs.org/api/stream.html) — A replication stream.

```js
const swarm = new Hyperswarm()
const done = drive.findingPeers()
swarm.on('connection', (socket) => drive.replicate(socket))
swarm.join(drive.discoveryKey)
swarm.flush().then(done, done)
```

#### `const updated = await drive.update([options])`

[API definition on GitHub](https://github.com/holepunchto/hyperdrive/blob/v13.3.2/index.js#L280)

Waits for initial proof of the new drive version until all `findingPeers` are done.

| Parameter | Type | Default | Description |
| --- | --- | --- | --- |
| `opts` | [`UpdateOptions`](#updateoptions) | — | `options` |

- Returns: [`Promise<boolean>`](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Promise) — `true` if the drive advanced to a new version, otherwise `false`.

| Option | Default | Description |
| --- | --- | --- |
| `wait` | `false` |  |

#### `const blobs = await drive.getBlobs()`

[API definition on GitHub](https://github.com/holepunchto/hyperdrive/blob/v13.3.2/index.js#L457)

Returns the [Hyperblobs](https://github.com/holepunchto/hyperblobs)
instance storing the blobs indexed by drive entries.

- Returns: [`Promise<Hyperblobs>`](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Promise) — the [Hyperblobs](https://github.com/holepunchto/hyperblobs) instance storing the blobs indexed by drive entries.

```js
await drive.put('/file.txt', Buffer.from('hi'))

const buffer1 = await drive.get('/file.txt')

const blobs = await drive.getBlobs()
const entry = await drive.entry('/file.txt')
const buffer2 = await blobs.get(entry.value.blob)

// => buffer1 and buffer2 are equals
```

#### `const blobsLength = await drive.getBlobsLength(checkout)`

[API definition on GitHub](https://github.com/holepunchto/hyperdrive/blob/v13.3.2/index.js#L235)

Returns the length of the Hyperblobs instance at the time of the specified
Hyperdrive version (defaults to the current version).

| Parameter | Type | Default | Description |
| --- | --- | --- | --- |
| `checkout` | `number` | — | Drive version to measure. Defaults to the current version. |

- Returns: [`Promise<number>`](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Promise) — the length of the Hyperblobs instance at the time of the specified Hyperdrive version (defaults to the current version).

```js
const blobsLength = await drive.getBlobsLength()
```

## Types

### DriveOptions

Options for creating a Hyperdrive.

| Property | Type | Default | Description |
| --- | --- | --- | --- |
| `encryptionKey` | [`Buffer`](https://nodejs.org/api/buffer.html#class-buffer) | — | Encryption key used to encrypt and decrypt the underlying cores. |
| `onwait` | `Function` | — | Hook called when a block has to be downloaded before it can be read. |
| `active` | `boolean` | `true` | Whether the drive's cores actively maintain peer connections. |

### TruncateOptions

Options for `drive.truncate()`.

| Property | Type | Default | Description |
| --- | --- | --- | --- |
| `blobs` | `number` | — | Corresponding blobs length, if known. Recommended to let the method figure it out. |

### UpdateOptions

Options for `drive.update()`.

| Property | Type | Default | Description |
| --- | --- | --- | --- |
| `wait` | `boolean` | `false` | Whether to wait for peers before resolving. Use this or `drive.findingPeers()` to make `await drive.update()` blocking. |

### GetOptions

Options for `drive.get()`.

| Property | Type | Default | Description |
| --- | --- | --- | --- |
| `wait` | `boolean` | `true` | Wait for the block to be downloaded. |
| `timeout` | `number` | `0` | Max milliseconds to wait (0 means no timeout). |

### WriteOptions

Options for writing a file (`drive.put()` and `drive.createWriteStream()`).

| Property | Type | Default | Description |
| --- | --- | --- | --- |
| `executable` | `boolean` | `false` | Whether the blob at the path is an executable. |
| `metadata` | `*` | `null` | Extended file information, i.e. an arbitrary JSON value. |
| `dedup` | `boolean` | `false` | Reuse the existing blob if the content is unchanged (createWriteStream only). |

### ClearOptions

Options for clearing blobs (`drive.clear()` and `drive.clearAll()`).

| Property | Type | Default | Description |
| --- | --- | --- | --- |
| `diff` | `boolean` | `false` | When enabled, returns a `{ blocks }` object reporting the bytes cleared; otherwise the result is `null`. |

### SymlinkOptions

Options for `drive.symlink()`.

| Property | Type | Default | Description |
| --- | --- | --- | --- |
| `metadata` | `*` | `null` | Extended file information, i.e. an arbitrary JSON value. |

### EntryOptions

Options for `drive.entry()`.

| Property | Type | Default | Description |
| --- | --- | --- | --- |
| `follow` | `boolean` | `false` | Follow symlinks, up to 16 deep or it throws an error. |
| `wait` | `boolean` | `true` | Wait for the block to be downloaded. |
| `timeout` | `number` | `0` | Max milliseconds to wait (0 means no timeout). |

## See also

- [Create a full peer-to-peer filesystem with Hyperdrive](/how-to/stream-and-share-media/create-a-full-peer-to-peer-filesystem-with-hyperdrive)—end-to-end filesystem replication walkthrough.
- [Work with many Hypercores using Corestore](/how-to/store-and-replicate/work-with-many-hypercores-using-corestore)—recommended pattern when one app manages multiple drives or related cores.
- [Localdrive](/reference/helpers/localdrive)—map a Hyperdrive-like API onto the local filesystem.
- [Mirrordrive](/reference/helpers/mirrordrive)—sync between Hyperdrive and other drive-like destinations.
- [Corestore](/reference/helpers/corestore)—shared storage and replication manager for drive metadata and content.
- [Hyperbee](/reference/building-blocks/hyperbee)—Hyperdrive uses a Hyperbee internally for metadata indexing.
- [Drives](/reference/tools/drives)—CLI tool for creating, mirroring, and seeding Hyperdrives.
