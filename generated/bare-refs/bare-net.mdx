---
title: "bare-net"
description: "TCP and IPC servers and clients for JavaScript"
docType: reference
schemaType: APIReference
---

<mark style={{ backgroundColor: '#7dde9a', padding: '2px 8px', borderRadius: '4px', fontSize: '0.85em', fontWeight: 500 }}>stable</mark>

[`bare-net`](https://github.com/holepunchto/bare-net) — TCP and IPC servers and clients for JavaScript.

```sh
npm i bare-net
```

## Usage

```js
const net = require('bare-net')

const server = net.createServer()
server.on('connection', (socket) => socket.on('data', console.log))
server.listen(() => console.log('server is up'))

const { port } = server.address()
const socket = net.createConnection(port)
socket.write('hello world')
```

## API

### NetSocket

#### `new NetSocket(opts?: NetOptions)`

<sub>[Source](https://github.com/holepunchto/bare-net/blob/v2.3.2/index.d.ts#L59)</sub>

Create a `NetSocket`.

**Parameters**

- `opts` ([`NetOptions`](#netoptions), optional)

#### `connect(path: string, opts?: PipeConnectOptions, onconnect?: () => void): this`

<sub>[Source](https://github.com/holepunchto/bare-net/blob/v2.3.2/index.d.ts#L40)</sub>

Connect the socket to a `path` over IPC, or to a `port`/`host` over TCP.

Overloads:

```ts
connect(path: string, opts?: PipeConnectOptions, onconnect?: () => void): this
connect(path: string, onconnect: () => void): this
connect(port: number, host?: string, opts?: TCPSocketConnectOptions, onconnect?: () => void): this
connect(port: number, host: string, onconnect: () => void): this
connect(port: number, onconnect: () => void): this
connect(opts: NetSocketConnectOptions, onconnect?: () => void): this
```

**Parameters**

- `path` (`string`)
- `opts` (`PipeConnectOptions`, optional)
- `onconnect` (`() => void`, optional)

#### `connecting: boolean`

<sub>[Source](https://github.com/holepunchto/bare-net/blob/v2.3.2/index.d.ts#L29)</sub>

`true` while the underlying socket is in the process of connecting.

#### `localAddress: string`

<sub>[Source](https://github.com/holepunchto/bare-net/blob/v2.3.2/index.d.ts#L33)</sub>

Local IP address the socket is connected on, if connected over TCP.

#### `localFamily: string`

<sub>[Source](https://github.com/holepunchto/bare-net/blob/v2.3.2/index.d.ts#L35)</sub>

Local IP address family, if connected over TCP.

#### `localPort: number`

<sub>[Source](https://github.com/holepunchto/bare-net/blob/v2.3.2/index.d.ts#L34)</sub>

Local port the socket is connected on, if connected over TCP.

#### `pending: boolean`

<sub>[Source](https://github.com/holepunchto/bare-net/blob/v2.3.2/index.d.ts#L30)</sub>

`true` if the socket hasn't been assigned an underlying TCP or IPC socket yet.

#### `readyState: 'open' | 'readOnly' | 'writeOnly' | 'opening'`

<sub>[Source](https://github.com/holepunchto/bare-net/blob/v2.3.2/index.d.ts#L32)</sub>

Current connection state of the socket.

#### `NetSocket.ref(): this`

<sub>[Source](https://github.com/holepunchto/bare-net/blob/v2.3.2/index.d.ts#L54)</sub>

Reference the socket, keeping the event loop alive while it is open.

#### `remoteAddress: string`

<sub>[Source](https://github.com/holepunchto/bare-net/blob/v2.3.2/index.d.ts#L36)</sub>

Remote IP address the socket is connected to, if connected over TCP.

#### `remoteFamily: string`

<sub>[Source](https://github.com/holepunchto/bare-net/blob/v2.3.2/index.d.ts#L38)</sub>

Remote IP address family, if connected over TCP.

#### `remotePort: number`

<sub>[Source](https://github.com/holepunchto/bare-net/blob/v2.3.2/index.d.ts#L37)</sub>

Remote port the socket is connected to, if connected over TCP.

#### `setKeepAlive(enable?: boolean, delay?: number): this`

<sub>[Source](https://github.com/holepunchto/bare-net/blob/v2.3.2/index.d.ts#L47)</sub>

Enable or disable TCP keep-alive on the underlying socket.

Overloads:

```ts
setKeepAlive(enable?: boolean, delay?: number): this
setKeepAlive(delay: number): this
```

**Parameters**

- `enable` (`boolean`, optional)
- `delay` (`number`, optional)

#### `setNoDelay(enable?: boolean): this`

<sub>[Source](https://github.com/holepunchto/bare-net/blob/v2.3.2/index.d.ts#L50)</sub>

Enable or disable Nagle's algorithm on the underlying socket.

**Parameters**

- `enable` (`boolean`, optional)

#### `setTimeout(ms: number, ontimeout?: () => void): this`

<sub>[Source](https://github.com/holepunchto/bare-net/blob/v2.3.2/index.d.ts#L52)</sub>

Set the socket's inactivity timeout.

**Parameters**

- `ms` (`number`)
- `ontimeout` (`() => void`, optional)

#### `timeout: number`

<sub>[Source](https://github.com/holepunchto/bare-net/blob/v2.3.2/index.d.ts#L31)</sub>

The socket's current inactivity timeout, in milliseconds.

#### `NetSocket.unref(): this`

<sub>[Source](https://github.com/holepunchto/bare-net/blob/v2.3.2/index.d.ts#L55)</sub>

Unreference the socket, allowing the event loop to exit while it is open.

### NetServer

#### `new NetServer(opts?: NetOptions, onconnection?: (socket: NetSocket) => void)`

<sub>[Source](https://github.com/holepunchto/bare-net/blob/v2.3.2/index.d.ts#L106)</sub>

Create a `NetServer`, optionally registering a `connection` listener.

Overloads:

```ts
new NetServer(opts?: NetOptions, onconnection?: (socket: NetSocket) => void)
new NetServer(onconnection: (socket: NetSocket) => void)
```

**Parameters**

- `opts` ([`NetOptions`](#netoptions), optional)
- `onconnection` (`(socket: NetSocket) => void`, optional)

#### `address(): string | TCPSocketAddress | null`

<sub>[Source](https://github.com/holepunchto/bare-net/blob/v2.3.2/index.d.ts#L76)</sub>

Returns the address the server is listening on, or `null` if it isn't listening.

#### `close(onclose: (err?: Error) => void): this`

<sub>[Source](https://github.com/holepunchto/bare-net/blob/v2.3.2/index.d.ts#L99)</sub>

Stop the server from accepting new connections.

**Parameters**

- `onclose` (`(err?: Error) => void`)

#### `listen`

```ts
listen(path: string, backlog?: number, opts?: PipeServerListenOptions, onlistening?: () => void): this
```

<sub>[Source](https://github.com/holepunchto/bare-net/blob/v2.3.2/index.d.ts#L78)</sub>

Start the server listening on a `path` over IPC, or a `port`/`host` over TCP.

**Parameters**

- `path` (`string`)
- `backlog` (`number`, optional)
- `opts` (`PipeServerListenOptions`, optional)
- `onlistening` (`() => void`, optional)

#### `listening: boolean`

<sub>[Source](https://github.com/holepunchto/bare-net/blob/v2.3.2/index.d.ts#L74)</sub>

`true` if the server is currently listening for connections.

#### `NetServer.ref(): this`

<sub>[Source](https://github.com/holepunchto/bare-net/blob/v2.3.2/index.d.ts#L101)</sub>

Reference the server, keeping the event loop alive while it is listening.

#### `NetServer.unref(): this`

<sub>[Source](https://github.com/holepunchto/bare-net/blob/v2.3.2/index.d.ts#L102)</sub>

Unreference the server, allowing the event loop to exit while it is listening.

### Functions

#### `createConnection`

```ts
createConnection(path: string, opts?: NetOptions & PipeConnectOptions, onconnect?: () => void): NetSocket
```

<sub>[Source](https://github.com/holepunchto/bare-net/blob/v2.3.2/index.d.ts#L112)</sub>

Create a `NetSocket` and connect it over IPC if a `path` is given, otherwise over TCP.

**Parameters**

- `path` (`string`)
- `opts` (`NetOptions & PipeConnectOptions`, optional)
- `onconnect` (`() => void`, optional)

#### `createServer(opts?: NetOptions, onconnection?: (socket: NetSocket) => void): NetServer`

<sub>[Source](https://github.com/holepunchto/bare-net/blob/v2.3.2/index.d.ts#L138)</sub>

Create a `NetServer`, optionally registering a `connection` listener.

Overloads:

```ts
createServer(opts?: NetOptions, onconnection?: (socket: NetSocket) => void): NetServer
createServer(onconnection: (socket: NetSocket) => void): NetServer
```

**Parameters**

- `opts` ([`NetOptions`](#netoptions), optional)
- `onconnection` (`(socket: NetSocket) => void`, optional)

### Constants and variables

#### `constants`

```ts
constants: {
  type: { TCP: 1; IPC: 2 }
  state: { UNREFED: number }
}
```

<sub>[Source](https://github.com/holepunchto/bare-net/blob/v2.3.2/lib/constants.d.ts#L1)</sub>

Connection type and internal state flags used by sockets and servers.

### Types

#### `NetOptions`

```ts
interface NetOptions {
  allowHalfOpen?: boolean
  eagerOpen?: boolean
  readBufferSize?: number
}
```

<sub>[Source](https://github.com/holepunchto/bare-net/blob/v2.3.2/index.d.ts#L16)</sub>

Options accepted when constructing a `NetSocket` or `NetServer`.

#### `NetSocketEvents`

```ts
interface NetSocketEvents extends DuplexEvents {
  connect: []
}
```

<sub>[Source](https://github.com/holepunchto/bare-net/blob/v2.3.2/index.d.ts#L22)</sub>

Events emitted by a `NetSocket`, extending the underlying duplex stream's events.

#### `NetSocketConnectOptions`

```ts
interface NetSocketConnectOptions extends PipeConnectOptions, TCPSocketConnectOptions {}
```

<sub>[Source](https://github.com/holepunchto/bare-net/blob/v2.3.2/index.d.ts#L26)</sub>

#### `NetServerEvents`

```ts
interface NetServerEvents extends EventMap {
  close: []
  connection: [socket: NetSocket]
  error: [err: Error]
  listening: []
}
```

<sub>[Source](https://github.com/holepunchto/bare-net/blob/v2.3.2/index.d.ts#L64)</sub>

Events emitted by a `NetServer`.

#### `NetServerListenOptions`

```ts
interface NetServerListenOptions extends PipeServerListenOptions, TCPServerListenOptions {}
```

<sub>[Source](https://github.com/holepunchto/bare-net/blob/v2.3.2/index.d.ts#L71)</sub>

## `bare-net/constants`

### Constants and variables

#### `constants.constants`

```ts
constants: {
  type: { TCP: 1; IPC: 2 }
  state: { UNREFED: number }
}
```

<sub>[Source](https://github.com/holepunchto/bare-net/blob/v2.3.2/lib/constants.d.ts#L1)</sub>

Connection type and internal state flags used by sockets and servers.

## See also

- [Bare modules](/reference/modules/bare-modules) — the full `bare-*` catalog.
- [Bare runtime API](/reference/bare/runtime) — the runtime these modules extend.
