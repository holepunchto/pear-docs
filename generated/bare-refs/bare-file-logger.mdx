---
title: "bare-file-logger"
description: "File system logger for Bare"
docType: reference
schemaType: APIReference
---

<mark style={{ backgroundColor: '#7dde9a', padding: '2px 8px', borderRadius: '4px', fontSize: '0.85em', fontWeight: 500 }}>stable</mark>

[`bare-file-logger`](https://github.com/holepunchto/bare-file-logger) — File system logger for Bare.

```sh
npm i bare-file-logger
```

## Usage

```js
const FileLog = require('bare-file-logger')

const log = new FileLog('my-logs.txt')

log.info('Hello %s', 'world!')
```

## API

### FileLog

#### `new FileLog(path: string, options?: FileLogOptions)`

<sub>[Source](https://github.com/holepunchto/bare-file-logger/blob/v1.2.1/index.d.ts#L20)</sub>

Options include:

**Parameters**

- `path` (`string`)
- `options` ([`FileLogOptions`](#filelogoptions), optional)

#### `append(label: string, ...data: unknown[]): void`

<sub>[Source](https://github.com/holepunchto/bare-file-logger/blob/v1.2.1/index.d.ts#L15)</sub>

**Parameters**

- `label` (`string`)
- `data` (`unknown[]`)

#### `close(): void`

<sub>[Source](https://github.com/holepunchto/bare-file-logger/blob/v1.2.1/index.d.ts#L16)</sub>

### Types

#### `FileLogOptions`

```ts
interface FileLogOptions {
  maxSize?: number
  rotate?: (path: string) => string
  rotateInterval?: number
}
```

<sub>[Source](https://github.com/holepunchto/bare-file-logger/blob/v1.2.1/index.d.ts#L4)</sub>

#### `FileLogEvents`

```ts
interface FileLogEvents extends EventMap {
  rotate: [path: string, archived: string | null]
}
```

<sub>[Source](https://github.com/holepunchto/bare-file-logger/blob/v1.2.1/index.d.ts#L10)</sub>

## See also

- [Bare modules](/reference/modules/bare-modules) — the full `bare-*` catalog.
- [Bare runtime API](/reference/bare/runtime) — the runtime these modules extend.
