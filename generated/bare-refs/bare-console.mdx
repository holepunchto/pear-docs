---
title: "bare-console"
description: "WHATWG debugging console for JavaScript"
docType: reference
schemaType: APIReference
---

<mark style={{ backgroundColor: '#7dde9a', padding: '2px 8px', borderRadius: '4px', fontSize: '0.85em', fontWeight: 500 }}>stable</mark>

[`bare-console`](https://github.com/holepunchto/bare-console) — WHATWG debugging console for JavaScript.

Mirrors the Node.js [`console`](https://nodejs.org/api/console.html) module.

```sh
npm i bare-console
```

## Usage

```js
const Console = require('bare-console')

const console = new Console()

console.log('Hello')
console.error(new Error('Something happened'))

console.time()
for (let i = 0; i < 1000000000; i++) {}
console.timeEnd()

console.trace('Show me')
```

To install `console` as a global, require the `bare-console/global` subpath:

```js
require('bare-console/global')

console.log('Hello')
```

## API

### Console

#### `new Console(log?: Log)`

<sub>[Source](https://github.com/holepunchto/bare-console/blob/v6.2.0/index.d.ts#L26)</sub>

Construct a new `Console`. By default, output is written through `bare-logger` (\<https://github.com/holepunchto/bare-logger>), or `bare-system-logger` (\<https://github.com/holepunchto/bare-system-logger>) on Android.

**Parameters**

- `log` (`Log`, optional)

#### `assert(condition: unknown, ...data: unknown[]): void`

<sub>[Source](https://github.com/holepunchto/bare-console/blob/v6.2.0/index.d.ts#L18)</sub>

If `condition` is falsy, log `data` prefixed with `'Assertion failed'`. Otherwise do nothing.

**Parameters**

- `condition` (`unknown`)
- `data` (`unknown[]`)

#### `clear(): void`

<sub>[Source](https://github.com/holepunchto/bare-console/blob/v6.2.0/index.d.ts#L12)</sub>

Forward to `log.clear()`.

#### `Console: ConsoleConstructor`

<sub>[Source](https://github.com/holepunchto/bare-console/blob/v6.2.0/index.d.ts#L4)</sub>

Construct a new `Console`. By default, output is written through `bare-logger` (\<https://github.com/holepunchto/bare-logger>), or `bare-system-logger` (\<https://github.com/holepunchto/bare-system-logger>) on Android.

#### `count(label?: string): void`

<sub>[Source](https://github.com/holepunchto/bare-console/blob/v6.2.0/index.d.ts#L19)</sub>

Increment and log the counter identified by `label` (default `'default'`).

**Parameters**

- `label` (`string`, optional)

#### `countReset(label?: string): void`

<sub>[Source](https://github.com/holepunchto/bare-console/blob/v6.2.0/index.d.ts#L20)</sub>

Remove the counter identified by `label` (default `'default'`).

**Parameters**

- `label` (`string`, optional)

#### `debug(...data: unknown[]): void`

<sub>[Source](https://github.com/holepunchto/bare-console/blob/v6.2.0/index.d.ts#L6)</sub>

**Parameters**

- `data` (`unknown[]`)

#### `error(...data: unknown[]): void`

<sub>[Source](https://github.com/holepunchto/bare-console/blob/v6.2.0/index.d.ts#L10)</sub>

Forward `data` to the corresponding method on the backend. `console.log()` is an alias for `console.info()`.

**Parameters**

- `data` (`unknown[]`)

#### `info(...data: unknown[]): void`

<sub>[Source](https://github.com/holepunchto/bare-console/blob/v6.2.0/index.d.ts#L8)</sub>

**Parameters**

- `data` (`unknown[]`)

#### `log(...data: unknown[]): void`

<sub>[Source](https://github.com/holepunchto/bare-console/blob/v6.2.0/index.d.ts#L7)</sub>

**Parameters**

- `data` (`unknown[]`)

#### `table(tabularData: unknown, properties?: readonly string[]): void`

<sub>[Source](https://github.com/holepunchto/bare-console/blob/v6.2.0/index.d.ts#L22)</sub>

Render `tabularData` as a table. Arrays and plain objects use an `(index)` column; `Map` and `Set` use an `(iteration index)` column with `key` and `values` (or `values` only) headers. Pass `properties` to restrict which object keys are shown as columns; the argument is ignored for `Map` and `Set`. Values that aren't tabular (primitives, `null`, functions) fall back to `console.log(tabularData)`.

**Parameters**

- `tabularData` (`unknown`)
- `properties` (`readonly string[]`, optional)

#### `time(label?: string): void`

<sub>[Source](https://github.com/holepunchto/bare-console/blob/v6.2.0/index.d.ts#L14)</sub>

Start a timer identified by `label` (default `'default'`). Warns if a timer with the same label is already running.

**Parameters**

- `label` (`string`, optional)

#### `timeEnd(label?: string): void`

<sub>[Source](https://github.com/holepunchto/bare-console/blob/v6.2.0/index.d.ts#L15)</sub>

Log the elapsed time and remove the timer identified by `label` (default `'default'`).

**Parameters**

- `label` (`string`, optional)

#### `timeLog(label?: string, ...data: unknown[]): void`

<sub>[Source](https://github.com/holepunchto/bare-console/blob/v6.2.0/index.d.ts#L16)</sub>

Log the elapsed time for the timer identified by `label` (default `'default'`), along with any additional `data`. Output uses milliseconds for short durations and seconds beyond one second.

**Parameters**

- `label` (`string`, optional)
- `data` (`unknown[]`)

#### `trace(...data: unknown[]): void`

<sub>[Source](https://github.com/holepunchto/bare-console/blob/v6.2.0/index.d.ts#L21)</sub>

Log a stack trace prefixed with the formatted `data`.

**Parameters**

- `data` (`unknown[]`)

#### `warn(...data: unknown[]): void`

<sub>[Source](https://github.com/holepunchto/bare-console/blob/v6.2.0/index.d.ts#L9)</sub>

**Parameters**

- `data` (`unknown[]`)

## See also

- [Bare modules](/reference/modules/bare-modules) — the full `bare-*` catalog.
- [Bare runtime API](/reference/bare/runtime) — the runtime these modules extend.
