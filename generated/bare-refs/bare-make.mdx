---
title: "bare-make"
description: "Opinionated build system generator based on CMake"
docType: reference
schemaType: APIReference
---

<mark style={{ backgroundColor: '#7dde9a', padding: '2px 8px', borderRadius: '4px', fontSize: '0.85em', fontWeight: 500 }}>stable</mark>

[`bare-make`](https://github.com/holepunchto/bare-make) — Opinionated build system generator based on CMake.

```sh
npm i bare-make
```

## Usage

Like CMake, builds happen in three steps: You first generate a build system, then run the build system, and finally install the built artefacts. To perform the steps programmatically from JavaScript, do:

```js
const make = require('bare-make')

await make.generate()
await make.build()
await make.install()
```

The steps can also be performed interactively from the command line using the included CLI:

```console
bare-make generate
bare-make build
bare-make install
```

### Testing

To run tests for projects that use [`enable_testing()`](https://cmake.org/cmake/help/latest/command/enable_testing.html#command:enable_testing) and [`add_test()`](https://cmake.org/cmake/help/latest/command/add_test.html#command:add_test), do:

```js
await make.test()
```

Tests can also be run from the command line:

```console
bare-make test
```

## API

### errors

#### `new errors(msg: string, code: string, fn?: MakeError)`

<sub>[Source](https://github.com/holepunchto/bare-make/blob/v1.8.0/lib/errors.d.ts#L2)</sub>

**Parameters**

- `msg` (`string`)
- `code` (`string`)
- `fn` (`MakeError`, optional)

#### `errors.BUILD_FAILED(msg: string): MakeError`

<sub>[Source](https://github.com/holepunchto/bare-make/blob/v1.8.0/lib/errors.d.ts#L6)</sub>

**Parameters**

- `msg` (`string`)

#### `errors.GENERATE_FAILED(msg: string): MakeError`

<sub>[Source](https://github.com/holepunchto/bare-make/blob/v1.8.0/lib/errors.d.ts#L5)</sub>

**Parameters**

- `msg` (`string`)

#### `errors.INSTALL_FAILED(msg: string): MakeError`

<sub>[Source](https://github.com/holepunchto/bare-make/blob/v1.8.0/lib/errors.d.ts#L7)</sub>

**Parameters**

- `msg` (`string`)

#### `errors.TEST_FAILED(msg: string): MakeError`

<sub>[Source](https://github.com/holepunchto/bare-make/blob/v1.8.0/lib/errors.d.ts#L8)</sub>

**Parameters**

- `msg` (`string`)

#### `errors.UNKNOWN_TOOLCHAIN(msg: string): MakeError`

<sub>[Source](https://github.com/holepunchto/bare-make/blob/v1.8.0/lib/errors.d.ts#L4)</sub>

**Parameters**

- `msg` (`string`)

### Functions

#### `build(opts?: BuildOptions): Promise<void>`

<sub>[Source](https://github.com/holepunchto/bare-make/blob/v1.8.0/lib/build.d.ts#L14)</sub>

**Parameters**

- `opts` (`BuildOptions`, optional)

#### `generate(opts?: GenerateOptions): Promise<void>`

<sub>[Source](https://github.com/holepunchto/bare-make/blob/v1.8.0/lib/generate.d.ts#L23)</sub>

**Parameters**

- `opts` (`GenerateOptions`, optional)

#### `install(opts?: InstallOptions): Promise<void>`

<sub>[Source](https://github.com/holepunchto/bare-make/blob/v1.8.0/lib/install.d.ts#L15)</sub>

**Parameters**

- `opts` (`InstallOptions`, optional)

#### `test(opts?: TestOptions): Promise<void>`

<sub>[Source](https://github.com/holepunchto/bare-make/blob/v1.8.0/lib/test.d.ts#L13)</sub>

**Parameters**

- `opts` (`TestOptions`, optional)

## `bare-make/errors`

### MakeError

#### `new MakeError(msg: string, code: string, fn?: MakeError)`

<sub>[Source](https://github.com/holepunchto/bare-make/blob/v1.8.0/lib/errors.d.ts#L2)</sub>

**Parameters**

- `msg` (`string`)
- `code` (`string`)
- `fn` (`MakeError`, optional)

#### `MakeError.BUILD_FAILED(msg: string): MakeError`

<sub>[Source](https://github.com/holepunchto/bare-make/blob/v1.8.0/lib/errors.d.ts#L6)</sub>

**Parameters**

- `msg` (`string`)

#### `MakeError.GENERATE_FAILED(msg: string): MakeError`

<sub>[Source](https://github.com/holepunchto/bare-make/blob/v1.8.0/lib/errors.d.ts#L5)</sub>

**Parameters**

- `msg` (`string`)

#### `MakeError.INSTALL_FAILED(msg: string): MakeError`

<sub>[Source](https://github.com/holepunchto/bare-make/blob/v1.8.0/lib/errors.d.ts#L7)</sub>

**Parameters**

- `msg` (`string`)

#### `MakeError.TEST_FAILED(msg: string): MakeError`

<sub>[Source](https://github.com/holepunchto/bare-make/blob/v1.8.0/lib/errors.d.ts#L8)</sub>

**Parameters**

- `msg` (`string`)

#### `MakeError.UNKNOWN_TOOLCHAIN(msg: string): MakeError`

<sub>[Source](https://github.com/holepunchto/bare-make/blob/v1.8.0/lib/errors.d.ts#L4)</sub>

**Parameters**

- `msg` (`string`)

## `bare-make/build`

### Functions

#### `build.build(opts?: BuildOptions): Promise<void>`

<sub>[Source](https://github.com/holepunchto/bare-make/blob/v1.8.0/lib/build.d.ts#L14)</sub>

**Parameters**

- `opts` (`BuildOptions`, optional)

### Types

#### `BuildOptions`

```ts
interface BuildOptions {
  build?: string
  clean?: boolean
  cwd?: string
  parallel?: number
  preset?: string
  stdio?: Pipe
  target?: string
  verbose?: boolean
}
```

<sub>[Source](https://github.com/holepunchto/bare-make/blob/v1.8.0/lib/build.d.ts#L3)</sub>

## `bare-make/generate`

### Functions

#### `generate.generate(opts?: GenerateOptions): Promise<void>`

<sub>[Source](https://github.com/holepunchto/bare-make/blob/v1.8.0/lib/generate.d.ts#L23)</sub>

**Parameters**

- `opts` (`GenerateOptions`, optional)

### Types

#### `GenerateOptions`

```ts
interface GenerateOptions {
  arch?: string
  build?: string
  cache?: boolean
  cwd?: string
  debug?: boolean
  define?: string[]
  environment?: string
  fuzz?: boolean
  platform?: string
  preset?: string
  sanitize?: string
  simulator?: boolean
  source?: string
  stdio?: Pipe
  verbose?: boolean
  withDebugSymbols?: boolean
  withMinimalSize?: boolean
}
```

<sub>[Source](https://github.com/holepunchto/bare-make/blob/v1.8.0/lib/generate.d.ts#L3)</sub>

## `bare-make/install`

### Functions

#### `install.install(opts?: InstallOptions): Promise<void>`

<sub>[Source](https://github.com/holepunchto/bare-make/blob/v1.8.0/lib/install.d.ts#L15)</sub>

**Parameters**

- `opts` (`InstallOptions`, optional)

### Types

#### `InstallOptions`

```ts
interface InstallOptions {
  build?: string
  component?: string
  cwd?: string
  link?: boolean
  parallel?: boolean
  prefix?: string
  stdio?: Pipe
  strip?: boolean
  verbose?: boolean
}
```

<sub>[Source](https://github.com/holepunchto/bare-make/blob/v1.8.0/lib/install.d.ts#L3)</sub>

## `bare-make/test`

### Functions

#### `test.test(opts?: TestOptions): Promise<void>`

<sub>[Source](https://github.com/holepunchto/bare-make/blob/v1.8.0/lib/test.d.ts#L13)</sub>

**Parameters**

- `opts` (`TestOptions`, optional)

### Types

#### `TestOptions`

```ts
interface TestOptions {
  build?: string
  cwd?: string
  parallel?: number
  preset?: string
  stdio?: Pipe
  timeout?: number
  verbose?: boolean
}
```

<sub>[Source](https://github.com/holepunchto/bare-make/blob/v1.8.0/lib/test.d.ts#L3)</sub>

## See also

- [Bare modules](/reference/modules/bare-modules) — the full `bare-*` catalog.
- [Bare runtime API](/reference/bare/runtime) — the runtime these modules extend.
