---
title: "bare-fs"
description: "Native file system operations for Bare"
docType: reference
schemaType: APIReference
---

<mark style={{ backgroundColor: '#7dde9a', padding: '2px 8px', borderRadius: '4px', fontSize: '0.85em', fontWeight: 500 }}>stable</mark>

[`bare-fs`](https://github.com/holepunchto/bare-fs) — Native file system operations for Bare. It is a native addon and requires Bare `>=1.16.0`.

Mirrors the Node.js [`fs`](https://nodejs.org/api/fs.html) module.

```sh
npm i bare-fs
```

## Usage

```js
const fs = require('bare-fs')

const fd = await fs.open('hello.txt')

const buffer = Buffer.alloc(1024)

try {
  const length = await fs.read(fd, buffer)

  console.log('Read', length, 'bytes')
} finally {
  await fs.close(fd)
}
```

## API

### Opening, reading, and writing

#### `open(filepath: Path, flags?: Flag | number, mode?: string | number): Promise<number>`

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/index.d.ts#L423)</sub>

Open a file, returning a file descriptor. `flags` defaults to `'r'` and `mode` defaults to `0o666`. `flags` may be a string such as `'r'`, `'w'`, `'a'`, `'r+'`, etc., or a numeric combination of `fs.constants` flags.

Overloads:

```ts
open(filepath: Path, flags?: Flag | number, mode?: string | number): Promise<number>
open(filepath: Path, flags: Flag | number, mode: string | number, cb: Callback<[fd: number]>): void
open(filepath: Path, flags: Flag | number, cb: Callback<[fd: number]>): void
open(filepath: Path, cb: Callback<[fd: number]>): void
```

Synchronous form: `openSync(filepath: Path, flags?: Flag | number, mode?: string | number): number`

**Parameters**

- `filepath` ([`Path`](#path))
- `flags` ([`Flag`](#flag) | `number`, optional)
- `mode` (`string` | `number`, optional)

#### `close(fd: number): Promise<void>`

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/index.d.ts#L282)</sub>

Close a file descriptor.

Overloads:

```ts
close(fd: number): Promise<void>
close(fd: number, cb: Callback): void
```

Synchronous form: `closeSync(fd: number): void`

**Parameters**

- `fd` (`number`)

#### `closeSync(): void`

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/index.d.ts#L50)</sub>

#### `read`

```ts
read(fd: number, buffer: Buffer | ArrayBufferView, offset?: number, len?: number, pos?: number): Promise<number>
```

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/index.d.ts#L517)</sub>

Read from a file descriptor into `buffer`. `offset` defaults to `0`, `len` defaults to `buffer.byteLength - offset`, and `pos` defaults to `-1` (current position). Returns the number of bytes read.

Synchronous form: `readSync(fd: number, buffer: Buffer | ArrayBufferView, offset?: number, len?: number, pos?: number): number`

**Parameters**

- `fd` (`number`)
- `buffer` (`Buffer` | `ArrayBufferView`)
- `offset` (`number`, optional)
- `len` (`number`, optional)
- `pos` (`number`, optional)

#### `readSync(): Dirent<T> | null`

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/index.d.ts#L46)</sub>

#### `readv(fd: number, buffers: ArrayBufferView[], position?: number): Promise<number>`

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/index.d.ts#L913)</sub>

Read from a file descriptor into an array of `buffers`. `pos` defaults to `-1`.

Overloads:

```ts
readv(fd: number, buffers: ArrayBufferView[], position?: number): Promise<number>
readv(fd: number, buffers: ArrayBufferView[], position: number, cb: Callback<[len: number]>): void
readv(fd: number, buffers: ArrayBufferView[], cb: Callback<[len: number]>): void
```

Synchronous form: `readvSync(fd: number, buffers: ArrayBufferView[], position?: number): number`

**Parameters**

- `fd` (`number`)
- `buffers` (`ArrayBufferView[]`)
- `position` (`number`, optional)

#### `write`

```ts
write(fd: number, data: Buffer | ArrayBufferView, offset?: number, len?: number, pos?: number): Promise<number>
```

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/index.d.ts#L1108)</sub>

Write `data` to a file descriptor. When `data` is a string, the signature is `fs.write(fd, data[, pos[, encoding]])` where `encoding` defaults to `'utf8'`. Returns the number of bytes written.

Synchronous form: `writeSync(fd: number, data: Buffer | ArrayBufferView, offset?: number, len?: number, pos?: number): number`

**Parameters**

- `fd` (`number`)
- `data` (`Buffer` | `ArrayBufferView`)
- `offset` (`number`, optional)
- `len` (`number`, optional)
- `pos` (`number`, optional)

#### `writev(fd: number, buffers: ArrayBufferView[], pos?: number): Promise<number>`

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/index.d.ts#L1221)</sub>

Write an array of `buffers` to a file descriptor. `pos` defaults to `-1`.

Overloads:

```ts
writev(fd: number, buffers: ArrayBufferView[], pos?: number): Promise<number>
writev(fd: number, buffers: ArrayBufferView[], pos: number, cb: Callback<[len: number]>): void
writev(fd: number, buffers: ArrayBufferView[], cb: Callback<[len: number]>): void
```

Synchronous form: `writevSync(fd: number, buffers: ArrayBufferView[], pos?: number): number`

**Parameters**

- `fd` (`number`)
- `buffers` (`ArrayBufferView[]`)
- `pos` (`number`, optional)

#### `fsync(fd: number): Promise<void>`

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/index.d.ts#L338)</sub>

Flush all modified in-core data of the file referred by its file descriptor to the disk device.

Overloads:

```ts
fsync(fd: number): Promise<void>
fsync(fd: number, cb: Callback): void
```

Synchronous form: `fsyncSync(fd: number): void`

**Parameters**

- `fd` (`number`)

#### `fdatasync(fd: number): Promise<void>`

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/index.d.ts#L326)</sub>

Similar to `fsync`, but does not flush modified metadata unless necessary.

Overloads:

```ts
fdatasync(fd: number): Promise<void>
fdatasync(fd: number, cb: Callback): void
```

Synchronous form: `fdatasyncSync(fd: number): void`

**Parameters**

- `fd` (`number`)

### Whole-file helpers

#### `readFile(filepath: Path, opts: ReadFileOptions & { encoding: BufferEncoding }): Promise<string>`

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/index.d.ts#L568)</sub>

Read the entire contents of a file. Returns a `Buffer` by default, or a string if an `encoding` is specified.

Overloads:

```ts
readFile(filepath: Path, opts: ReadFileOptions & { encoding: BufferEncoding }): Promise<string>
readFile(filepath: Path, opts: ReadFileOptions & { encoding?: 'buffer' }): Promise<Buffer>
readFile(filepath: Path, opts: ReadFileOptions): Promise<string | Buffer>
readFile(filepath: Path, encoding: BufferEncoding): Promise<string>
readFile(filepath: Path, encoding: 'buffer'): Promise<Buffer>
readFile(filepath: Path, encoding?: BufferEncoding | 'buffer'): Promise<string | Buffer>
readFile(filepath: Path): Promise<Buffer>
readFile(filepath: Path, opts: ReadFileOptions & { encoding: BufferEncoding }, cb: Callback<[buffer?: string]>): void
readFile(filepath: Path, opts: ReadFileOptions & { encoding?: 'buffer' }, cb: Callback<[buffer?: Buffer]>): void
readFile(filepath: Path, opts: ReadFileOptions, cb: Callback<[buffer?: string | Buffer]>): void
readFile(filepath: Path, encoding: BufferEncoding, cb: Callback<[buffer?: string]>): void
readFile(filepath: Path, encoding: 'buffer', cb: Callback<[buffer?: Buffer]>): void
readFile(filepath: Path, encoding: BufferEncoding | 'buffer', cb: Callback<[buffer?: string | Buffer]>): void
readFile(filepath: Path, cb: Callback<[buffer?: Buffer]>): void
```

Synchronous form: `readFileSync(filepath: Path, opts: ReadFileOptions & { encoding: BufferEncoding }): string`

**Parameters**

- `filepath` ([`Path`](#path))
- `opts` (`ReadFileOptions & { encoding: BufferEncoding }`)

#### `writeFile`

```ts
writeFile(filepath: Path, data: string | Buffer | ArrayBufferView, opts?: WriteFileOptions): Promise<void>
```

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/index.d.ts#L1177)</sub>

Write `data` to a file, replacing it if it already exists.

Synchronous form: `writeFileSync(filepath: Path, data: string | Buffer | ArrayBufferView, opts?: WriteFileOptions): void`

**Parameters**

- `filepath` ([`Path`](#path))
- `data` (`string` | `Buffer` | `ArrayBufferView`)
- `opts` ([`WriteFileOptions`](#writefileoptions), optional)

#### `appendFile`

```ts
appendFile(filepath: Path, data: string | Buffer | ArrayBufferView, opts?: AppendFileOptions): Promise<void>
```

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/index.d.ts#L226)</sub>

Append `data` to a file, creating it if it does not exist. Accepts the same options as `fs.writeFile()` but defaults to the `'a'` flag.

Synchronous form: `appendFileSync(filepath: Path, data: string | Buffer | ArrayBufferView, opts?: AppendFileOptions): void`

**Parameters**

- `filepath` ([`Path`](#path))
- `data` (`string` | `Buffer` | `ArrayBufferView`)
- `opts` ([`AppendFileOptions`](#appendfileoptions), optional)

#### `access(filepath: Path, mode?: number): Promise<void>`

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/index.d.ts#L212)</sub>

Check whether the file at `filepath` is accessible. `mode` defaults to `fs.constants.F_OK`.

Overloads:

```ts
access(filepath: Path, mode?: number): Promise<void>
access(filepath: Path, mode: number, cb: Callback): void
access(filepath: Path, cb: Callback): void
```

Synchronous form: `accessSync(filepath: Path, mode?: number): void`

**Parameters**

- `filepath` ([`Path`](#path))
- `mode` (`number`, optional)

#### `exists(filepath: Path): Promise<boolean>`

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/index.d.ts#L308)</sub>

Check whether a file exists at `filepath`. Returns `true` if the file is accessible, `false` otherwise.

Overloads:

```ts
exists(filepath: Path): Promise<boolean>
exists(filepath: Path, cb: (exists: boolean) => void): void
```

Synchronous form: `existsSync(filepath: Path): boolean`

**Parameters**

- `filepath` ([`Path`](#path))

### Metadata and size

#### `stat(filepath: Path): Promise<Stats>`

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/index.d.ts#L1033)</sub>

Get the status of a file. Returns a `Stats` object.

Overloads:

```ts
stat(filepath: Path): Promise<Stats>
stat(filepath: Path, cb: Callback<[stats: Stats | null]>): void
```

Synchronous form: `statSync(filepath: Path): Stats`

**Parameters**

- `filepath` ([`Path`](#path))

#### `lstat(filepath: Path): Promise<Stats>`

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/index.d.ts#L358)</sub>

Like `fs.stat()`, but if `filepath` is a symbolic link, the link itself is statted, not the file it refers to.

Overloads:

```ts
lstat(filepath: Path): Promise<Stats>
lstat(filepath: Path, cb: Callback<[stats: Stats | null]>): void
```

Synchronous form: `lstatSync(filepath: Path): Stats`

**Parameters**

- `filepath` ([`Path`](#path))

#### `fstat(fd: number): Promise<Stats>`

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/index.d.ts#L332)</sub>

Get the status of a file by its file descriptor. Returns a `Stats` object.

Overloads:

```ts
fstat(fd: number): Promise<Stats>
fstat(fd: number, cb: Callback<[stats: Stats | null]>): void
```

Synchronous form: `fstatSync(fd: number): Stats`

**Parameters**

- `fd` (`number`)

#### `statfs(filepath: Path): Promise<StatFs>`

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/index.d.ts#L1039)</sub>

Get filesystem statistics. Returns a `StatFs` object.

Overloads:

```ts
statfs(filepath: Path): Promise<StatFs>
statfs(filepath: Path, cb: Callback<[stats: StatFs | null]>): void
```

Synchronous form: `statfsSync(filepath: Path): StatFs`

**Parameters**

- `filepath` ([`Path`](#path))

#### `truncate(filepath: Path, len?: number): Promise<void>`

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/index.d.ts#L1053)</sub>

Truncate the file at `filename` to `len` bytes. `len` defaults to `0`.

Overloads:

```ts
truncate(filepath: Path, len?: number): Promise<void>
truncate(filepath: Path, len: number, cb: Callback): void
truncate(filepath: Path, cb: Callback): void
```

Synchronous form: `truncateSync(filepath: Path, len?: number): void`

**Parameters**

- `filepath` ([`Path`](#path))
- `len` (`number`, optional)

#### `ftruncate(fd: number, len?: number): Promise<void>`

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/index.d.ts#L344)</sub>

Truncate a file to `len` bytes. `len` defaults to `0`.

Overloads:

```ts
ftruncate(fd: number, len?: number): Promise<void>
ftruncate(fd: number, len: number, cb: Callback): void
ftruncate(fd: number, cb: Callback): void
```

Synchronous form: `ftruncateSync(fd: number, len?: number): void`

**Parameters**

- `fd` (`number`)
- `len` (`number`, optional)

### Permissions, ownership, and times

#### `chmod(filepath: Path, mode: string | number): Promise<void>`

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/index.d.ts#L270)</sub>

Change the permissions of a file. `mode` may be a numeric mode or a string that will be parsed as octal.

Overloads:

```ts
chmod(filepath: Path, mode: string | number): Promise<void>
chmod(filepath: Path, mode: string | number, cb: Callback): void
```

Synchronous form: `chmodSync(filepath: Path, mode: string | number): void`

**Parameters**

- `filepath` ([`Path`](#path))
- `mode` (`string` | `number`)

#### `fchmod(fd: number, mode: string | number): Promise<void>`

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/index.d.ts#L314)</sub>

Change the permissions of a file by its file descriptor.

Overloads:

```ts
fchmod(fd: number, mode: string | number): Promise<void>
fchmod(fd: number, mode: string | number, cb: Callback): void
```

Synchronous form: `fchmodSync(fd: number, mode: string | number): void`

**Parameters**

- `fd` (`number`)
- `mode` (`string` | `number`)

#### `chown(filepath: Path, uid: number, gid: number): Promise<void>`

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/index.d.ts#L276)</sub>

Change the owner and group of a file.

Overloads:

```ts
chown(filepath: Path, uid: number, gid: number): Promise<void>
chown(filepath: Path, uid: number, gid: number, cb: Callback): void
```

Synchronous form: `chownSync(filepath: Path, uid: number, gid: number): void`

**Parameters**

- `filepath` ([`Path`](#path))
- `uid` (`number`)
- `gid` (`number`)

#### `fchown(fd: number, uid: number, gid: number): Promise<void>`

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/index.d.ts#L320)</sub>

Change the owner and group of a file by its file descriptor.

Overloads:

```ts
fchown(fd: number, uid: number, gid: number): Promise<void>
fchown(fd: number, uid: number, gid: number, cb: Callback): void
```

Synchronous form: `fchownSync(fd: number, uid: number, gid: number): void`

**Parameters**

- `fd` (`number`)
- `uid` (`number`)
- `gid` (`number`)

#### `lchown(filepath: Path, uid: number, gid: number): Promise<void>`

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/index.d.ts#L352)</sub>

Change the owner and group of a file, but if `filepath` is a symbolic link, the changes are applied only to the link, not the file it refers to.

Overloads:

```ts
lchown(filepath: Path, uid: number, gid: number): Promise<void>
lchown(filepath: Path, uid: number, gid: number, cb: Callback): void
```

Synchronous form: `lchownSync(filepath: Path, uid: number, gid: number): void`

**Parameters**

- `filepath` ([`Path`](#path))
- `uid` (`number`)
- `gid` (`number`)

#### `utimes(filepath: Path, atime: number | Date, mtime: number | Date): Promise<void>`

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/index.d.ts#L364)</sub>

Change the access and modification times of a file. Times may be numbers (seconds since epoch) or `Date` objects.

Overloads:

```ts
utimes(filepath: Path, atime: number | Date, mtime: number | Date): Promise<void>
utimes(filepath: Path, atime: number | Date, mtime: number | Date, cb: Callback): void
```

Synchronous form: `utimesSync(filepath: Path, atime: number | Date, mtime: number | Date): void`

**Parameters**

- `filepath` ([`Path`](#path))
- `atime` (`number` | `Date`)
- `mtime` (`number` | `Date`)

#### `lutimes(filepath: Path, atime: number | Date, mtime: number | Date): Promise<void>`

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/index.d.ts#L375)</sub>

Like `fs.utimes()`, but if `filepath` is a symbolic link, the timestamps of the link is changed, not the file it refers to.

Overloads:

```ts
lutimes(filepath: Path, atime: number | Date, mtime: number | Date): Promise<void>
lutimes(filepath: Path, atime: number | Date, mtime: number | Date, cb: Callback): void
```

Synchronous form: `lutimesSync(filepath: Path, atime: number | Date, mtime: number | Date): void`

**Parameters**

- `filepath` ([`Path`](#path))
- `atime` (`number` | `Date`)
- `mtime` (`number` | `Date`)

#### `futimes(fd: number, atime: number | Date, mtime: number | Date): Promise<void>`

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/index.d.ts#L386)</sub>

Change the access and modification times of a file by its file descriptor. Times may be numbers (seconds since epoch) or `Date` objects.

Overloads:

```ts
futimes(fd: number, atime: number | Date, mtime: number | Date): Promise<void>
futimes(fd: number, atime: number | Date, mtime: number | Date, cb: Callback): void
```

Synchronous form: `futimesSync(fd: number, atime: number | Date, mtime: number | Date): void`

**Parameters**

- `fd` (`number`)
- `atime` (`number` | `Date`)
- `mtime` (`number` | `Date`)

### Directories

#### `mkdir(filepath: Path, opts?: MkdirOptions): Promise<void>`

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/index.d.ts#L403)</sub>

Create a directory at `filepath`.

Overloads:

```ts
mkdir(filepath: Path, opts?: MkdirOptions): Promise<void>
mkdir(filepath: Path, mode: number): Promise<void>
mkdir(filepath: Path, opts: MkdirOptions, cb: Callback): void
mkdir(filepath: Path, mode: number, cb: Callback): void
mkdir(filepath: Path, cb: Callback): void
```

Synchronous form: `mkdirSync(filepath: Path, opts?: MkdirOptions): void`

**Parameters**

- `filepath` ([`Path`](#path))
- `opts` ([`MkdirOptions`](#mkdiroptions), optional)

#### `mkdtemp(prefix: Path): Promise<string>`

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/index.d.ts#L417)</sub>

Create a unique temporary directory.

Overloads:

```ts
mkdtemp(prefix: Path): Promise<string>
mkdtemp(prefix: Path, cb: Callback<[path: string | null]>): void
```

Synchronous form: `mkdtempSync(prefix: Path): string`

**Parameters**

- `prefix` ([`Path`](#path))

#### `rmdir(filepath: Path): Promise<void>`

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/index.d.ts#L1027)</sub>

Remove an empty directory.

Overloads:

```ts
rmdir(filepath: Path): Promise<void>
rmdir(filepath: Path, cb: Callback): void
```

Synchronous form: `rmdirSync(filepath: Path): void`

**Parameters**

- `filepath` ([`Path`](#path))

#### `readdir`

```ts
readdir(filepath: Path, opts: ReaddirOptions & { encoding?: BufferEncoding }): Promise<Dirent<string>[] | string[]>
```

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/index.d.ts#L648)</sub>

Read the contents of a directory. Returns an array of filenames or, if `withFileTypes` is `true`, an array of `Dirent` objects.

Synchronous form: `readdirSync(filepath: Path, opts: ReaddirOptions & { encoding?: BufferEncoding }): Dirent<string>[] | string[]`

**Parameters**

- `filepath` ([`Path`](#path))
- `opts` (`ReaddirOptions & { encoding?: BufferEncoding }`)

#### `opendir`

```ts
opendir(filepath: Path, opts: OpendirOptions & { encoding?: BufferEncoding }): Promise<Dir<string>>
```

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/index.d.ts#L443)</sub>

Open a directory for iteration. Returns a `Dir` object.

Synchronous form: `opendirSync(filepath: Path, opts: OpendirOptions & { encoding?: BufferEncoding }): Dir<string>`

**Parameters**

- `filepath` ([`Path`](#path))
- `opts` (`OpendirOptions & { encoding?: BufferEncoding }`)

### Links, moving, copying, and removing

#### `link(src: Path, dst: Path): Promise<void>`

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/index.d.ts#L392)</sub>

Creates a new link (also known as a hard link) to an existing file.

Overloads:

```ts
link(src: Path, dst: Path): Promise<void>
link(src: Path, dst: Path, cb: Callback): void
```

Synchronous form: `linkSync(src: Path, dst: Path): void`

**Parameters**

- `src` ([`Path`](#path))
- `dst` ([`Path`](#path))

#### `symlink(target: Path, filepath: Path, type?: string | number): Promise<void>`

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/index.d.ts#L1045)</sub>

Create a symbolic link at `filepath` pointing to `target`. `type` may be `'file'`, `'dir'`, or `'junction'` (Windows only) or a numeric flag. On Windows, if `type` is not provided, it is inferred from the target.

Overloads:

```ts
symlink(target: Path, filepath: Path, type?: string | number): Promise<void>
symlink(target: Path, filepath: Path, type: string | number, cb: Callback): void
symlink(target: Path, filepath: Path, cb: Callback): void
```

Synchronous form: `symlinkSync(target: Path, filepath: Path, type?: string | number): void`

**Parameters**

- `target` ([`Path`](#path))
- `filepath` ([`Path`](#path))
- `type` (`string` | `number`, optional)

#### `readlink(filepath: Path, opts: ReadlinkOptions & { encoding?: BufferEncoding }): Promise<string>`

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/index.d.ts#L835)</sub>

Read the target of a symbolic link.

Overloads:

```ts
readlink(filepath: Path, opts: ReadlinkOptions & { encoding?: BufferEncoding }): Promise<string>
readlink(filepath: Path, opts: ReadlinkOptions & { encoding: 'buffer' }): Promise<Buffer>
readlink(filepath: Path, opts: ReadlinkOptions): Promise<string | Buffer>
readlink(filepath: Path, encoding: BufferEncoding): Promise<string>
readlink(filepath: Path, encoding: 'buffer'): Promise<Buffer>
readlink(filepath: Path, encoding: BufferEncoding | 'buffer'): Promise<string | Buffer>
readlink(filepath: Path): Promise<string>
readlink(filepath: Path, opts: ReadlinkOptions & { encoding?: BufferEncoding }, cb: Callback<[link: string | null]>): void
readlink(filepath: Path, opts: ReadlinkOptions & { encoding: 'buffer' }, cb: Callback<[link: Buffer | null]>): void
readlink(filepath: Path, opts: ReadlinkOptions, cb: Callback<[link: string | Buffer | null]>): void
readlink(filepath: Path, encoding: BufferEncoding, cb: Callback<[link: string | null]>): void
readlink(filepath: Path, encoding: 'buffer', cb: Callback<[link: Buffer | null]>): void
readlink(filepath: Path, encoding: BufferEncoding | 'buffer', cb: Callback<[link: string | Buffer | null]>): void
readlink(filepath: Path, cb: Callback<[link: string | null]>): void
```

Synchronous form: `readlinkSync(filepath: Path, opts: ReadlinkOptions & { encoding?: BufferEncoding }): string`

**Parameters**

- `filepath` ([`Path`](#path))
- `opts` (`ReadlinkOptions & { encoding?: BufferEncoding }`)

#### `realpath(filepath: Path, opts: RealpathOptions & { encoding?: BufferEncoding }): Promise<string>`

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/index.d.ts#L930)</sub>

Resolve the real path of `filepath`, expanding all symbolic links.

Overloads:

```ts
realpath(filepath: Path, opts: RealpathOptions & { encoding?: BufferEncoding }): Promise<string>
realpath(filepath: Path, opts: RealpathOptions & { encoding: 'buffer' }): Promise<Buffer>
realpath(filepath: Path, opts: RealpathOptions): Promise<string | Buffer>
realpath(filepath: Path, encoding: BufferEncoding): Promise<string>
realpath(filepath: Path, encoding: 'buffer'): Promise<Buffer>
realpath(filepath: Path, encoding: BufferEncoding | 'buffer'): Promise<string | Buffer>
realpath(filepath: Path): Promise<string>
realpath(filepath: Path, opts: RealpathOptions & { encoding?: BufferEncoding }, cb: Callback<[path: string | null]>): void
realpath(filepath: Path, opts: RealpathOptions & { encoding: 'buffer' }, cb: Callback<[path: Buffer | null]>): void
realpath(filepath: Path, opts: RealpathOptions, cb: Callback<[path: string | Buffer | null]>): void
realpath(filepath: Path, encoding: BufferEncoding, cb: Callback<[path: string | null]>): void
realpath(filepath: Path, encoding: 'buffer', cb: Callback<[path: Buffer | null]>): void
realpath(filepath: Path, encoding: BufferEncoding | 'buffer', cb: Callback<[path: string | Buffer | null]>): void
realpath(filepath: Path, cb: Callback<[path: string | null]>): void
```

Synchronous form: `realpathSync(filepath: Path, opts: RealpathOptions & { encoding?: BufferEncoding }): string`

**Parameters**

- `filepath` ([`Path`](#path))
- `opts` (`RealpathOptions & { encoding?: BufferEncoding }`)

#### `rename(src: Path, dst: Path): Promise<void>`

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/index.d.ts#L1008)</sub>

Rename a file from `src` to `dst`.

Overloads:

```ts
rename(src: Path, dst: Path): Promise<void>
rename(src: Path, dst: Path, cb: Callback): void
```

Synchronous form: `renameSync(src: Path, dst: Path): void`

**Parameters**

- `src` ([`Path`](#path))
- `dst` ([`Path`](#path))

#### `unlink(filepath: Path): Promise<void>`

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/index.d.ts#L1061)</sub>

Remove a file.

Overloads:

```ts
unlink(filepath: Path): Promise<void>
unlink(filepath: Path, cb: Callback): void
```

Synchronous form: `unlinkSync(filepath: Path): void`

**Parameters**

- `filepath` ([`Path`](#path))

#### `rm(filepath: Path, opts?: RmOptions): Promise<void>`

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/index.d.ts#L1019)</sub>

Remove a file or directory at `filepath`.

Overloads:

```ts
rm(filepath: Path, opts?: RmOptions): Promise<void>
rm(filepath: Path, opts: RmOptions, cb: Callback): void
rm(filepath: Path, cb: Callback): void
```

Synchronous form: `rmSync(filepath: Path, opts?: RmOptions): void`

**Parameters**

- `filepath` ([`Path`](#path))
- `opts` ([`RmOptions`](#rmoptions), optional)

#### `copyFile(src: Path, dst: Path, mode?: number): Promise<void>`

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/index.d.ts#L288)</sub>

Copy a file from `src` to `dst`. `mode` is an optional bitmask created from `fs.constants.COPYFILE_EXCL`, `fs.constants.COPYFILE_FICLONE`, or `fs.constants.COPYFILE_FICLONE_FORCE`.

Overloads:

```ts
copyFile(src: Path, dst: Path, mode?: number): Promise<void>
copyFile(src: Path, dst: Path, mode: number, cb: Callback): void
copyFile(src: Path, dst: Path, cb: Callback): void
```

Synchronous form: `copyFileSync(src: Path, dst: Path, mode?: number): void`

**Parameters**

- `src` ([`Path`](#path))
- `dst` ([`Path`](#path))
- `mode` (`number`, optional)

#### `cp(src: Path, dst: Path, opts?: CpOptions): Promise<void>`

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/index.d.ts#L300)</sub>

Copy a file or directory from `src` to `dst`.

Overloads:

```ts
cp(src: Path, dst: Path, opts?: CpOptions): Promise<void>
cp(src: Path, dst: Path, opts: CpOptions, cb: Callback): void
cp(src: Path, dst: Path, cb: Callback): void
```

Synchronous form: `cpSync(src: Path, dst: Path, opts?: CpOptions): void`

**Parameters**

- `src` ([`Path`](#path))
- `dst` ([`Path`](#path))
- `opts` ([`CpOptions`](#cpoptions), optional)

### Streams and watching

#### `createReadStream(path: Path | null, opts?: ReadStreamOptions): ReadStream`

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/index.d.ts#L164)</sub>

Create a readable stream for a file. Returns a `ReadStream`.

**Parameters**

- `path` ([`Path`](#path) | `null`)
- `opts` ([`ReadStreamOptions`](#readstreamoptions), optional)

#### `createWriteStream(path: Path | null, opts?: WriteStreamOptions): WriteStream`

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/index.d.ts#L183)</sub>

Create a writable stream for a file. Returns a `WriteStream`.

**Parameters**

- `path` ([`Path`](#path) | `null`)
- `opts` ([`WriteStreamOptions`](#writestreamoptions), optional)

#### `watch`

```ts
watch(filepath: Path, opts: WatcherOptions & { encoding?: BufferEncoding }, cb: (eventType: WatcherEventType, filename: string) => void): Watcher<string>
```

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/index.d.ts#L1067)</sub>

Watch a file or directory for changes. Returns a `Watcher` object. The `callback`, if provided, is called with `(eventType, filename)` on each change.

**Parameters**

- `filepath` ([`Path`](#path))
- `opts` (`WatcherOptions & { encoding?: BufferEncoding }`)
- `cb` (`(eventType: WatcherEventType, filename: string) => void`)

### Modules

#### `promises`

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/promises.d.ts#L1)</sub>

#### `constants`

```ts
constants: {
  O_RDWR: number
  O_RDONLY: number
  O_WRONLY: number
  O_CREAT: number
  O_TRUNC: number
  O_APPEND: number

  F_OK: number
  R_OK: number
  W_OK: number
  X_OK: number

  S_IFMT: number
  S_IFREG: number
  S_IFDIR: number
  S_IFCHR: number
  S_IFLNK: number
  S_IFBLK: number
  S_IFIFO: number
  S_IFSOCK: number

  S_IRUSR: number
  S_IWUSR: number
  S_IXUSR: number
  S_IRGRP: number
  S_IWGRP: number
  S_IXGRP: number
  S_IROTH: number
  S_IWOTH: number
  S_IXOTH: number

  UV_DIRENT_UNKNOWN: number
  UV_DIRENT_FILE: number
  UV_DIRENT_DIR: number
  UV_DIRENT_LINK: number
  UV_DIRENT_FIFO: number
  UV_DIRENT_SOCKET: number
  UV_DIRENT_CHAR: number
  UV_DIRENT_BLOCK: number

  COPYFILE_EXCL: number
  COPYFILE_FICLONE: number
  COPYFILE_FICLONE_FORCE: number
  UV_FS_SYMLINK_DIR: number
  UV_FS_SYMLINK_JUNCTION: number
}
```

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/lib/constants.d.ts#L1)</sub>

An object containing file system constants, such as file access modes and file type flags. See `fs/constants` for the full list.

### Dir

#### `close(): Promise<void>`

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/index.d.ts#L48)</sub>

Close a file descriptor.

Overloads:

```ts
close(): Promise<void>
close(cb: Callback): void
```

#### `path: string`

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/index.d.ts#L42)</sub>

The path of the directory.

#### `read(): Promise<Dirent<T> | null>`

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/index.d.ts#L44)</sub>

Read from a file descriptor into `buffer`. `offset` defaults to `0`, `len` defaults to `buffer.byteLength - offset`, and `pos` defaults to `-1` (current position). Returns the number of bytes read.

Overloads:

```ts
read(): Promise<Dirent<T> | null>
read(cb: Callback<[dirent: Dirent<T> | null]>): void
```

### Dirent

#### `Dirent.isBlockDevice(): boolean`

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/index.d.ts#L68)</sub>

Returns `true` if the file is a block device.

#### `Dirent.isCharacterDevice(): boolean`

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/index.d.ts#L67)</sub>

Returns `true` if the file is a character device.

#### `Dirent.isDirectory(): boolean`

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/index.d.ts#L63)</sub>

Returns `true` if the file is a directory.

#### `Dirent.isFIFO(): boolean`

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/index.d.ts#L65)</sub>

Returns `true` if the file is a FIFO (named pipe).

#### `Dirent.isFile(): boolean`

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/index.d.ts#L62)</sub>

Returns `true` if the file is a regular file.

#### `Dirent.isSocket(): boolean`

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/index.d.ts#L66)</sub>

Returns `true` if the file is a socket.

#### `Dirent.isSymbolicLink(): boolean`

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/index.d.ts#L64)</sub>

Returns `true` if the file is a symbolic link. Only meaningful when using `fs.lstat()`.

#### `name: T`

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/index.d.ts#L59)</sub>

The name of the directory entry, as a string or `Buffer` depending on the encoding.

#### `parentPath: string`

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/index.d.ts#L58)</sub>

The path of the parent directory.

#### `type: number`

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/index.d.ts#L60)</sub>

The numeric type of the directory entry.

### Stats

#### `atime: Date`

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/index.d.ts#L90)</sub>

The access time as a `Date` object.

#### `atimeMs: number`

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/index.d.ts#L86)</sub>

The access time in milliseconds since the epoch.

#### `birthtime: Date`

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/index.d.ts#L93)</sub>

The creation time as a `Date` object.

#### `birthtimeMs: number`

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/index.d.ts#L89)</sub>

The creation time in milliseconds since the epoch.

#### `blksize: number`

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/index.d.ts#L82)</sub>

The file system block size for I/O operations.

#### `blocks: number`

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/index.d.ts#L85)</sub>

The number of 512-byte blocks allocated.

#### `ctime: Date`

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/index.d.ts#L92)</sub>

The change time as a `Date` object.

#### `ctimeMs: number`

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/index.d.ts#L88)</sub>

The change time in milliseconds since the epoch.

#### `dev: number`

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/index.d.ts#L76)</sub>

The device identifier.

#### `gid: number`

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/index.d.ts#L80)</sub>

The group identifier of the file owner.

#### `ino: number`

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/index.d.ts#L83)</sub>

The inode number.

#### `Stats.isBlockDevice(): boolean`

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/index.d.ts#L97)</sub>

Returns `true` if the file is a block device.

#### `Stats.isCharacterDevice(): boolean`

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/index.d.ts#L98)</sub>

Returns `true` if the file is a character device.

#### `Stats.isDirectory(): boolean`

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/index.d.ts#L95)</sub>

Returns `true` if the file is a directory.

#### `Stats.isFIFO(): boolean`

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/index.d.ts#L99)</sub>

Returns `true` if the file is a FIFO (named pipe).

#### `Stats.isFile(): boolean`

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/index.d.ts#L96)</sub>

Returns `true` if the file is a regular file.

#### `Stats.isSocket(): boolean`

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/index.d.ts#L101)</sub>

Returns `true` if the file is a socket.

#### `Stats.isSymbolicLink(): boolean`

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/index.d.ts#L100)</sub>

Returns `true` if the file is a symbolic link. Only meaningful when using `fs.lstat()`.

#### `mode: number`

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/index.d.ts#L77)</sub>

The file mode (type and permissions).

#### `mtime: Date`

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/index.d.ts#L91)</sub>

The modification time as a `Date` object.

#### `mtimeMs: number`

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/index.d.ts#L87)</sub>

The modification time in milliseconds since the epoch.

#### `nlink: number`

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/index.d.ts#L78)</sub>

The number of hard links.

#### `rdev: number`

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/index.d.ts#L81)</sub>

The device identifier for special files.

#### `size: number`

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/index.d.ts#L84)</sub>

The size of the file in bytes.

#### `uid: number`

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/index.d.ts#L79)</sub>

The user identifier of the file owner.

### Watcher

#### `close(): void`

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/index.d.ts#L203)</sub>

Close a file descriptor.

#### `ref(): void`

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/index.d.ts#L204)</sub>

Prevent the event loop from exiting while the watcher is active.

#### `unref(): void`

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/index.d.ts#L205)</sub>

Allow the event loop to exit even if the watcher is still active.

### Types

#### `Path`

```ts
type Path = string | Buffer | URL
```

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/index.d.ts#L10)</sub>

#### `Flag`

```ts
type Flag = | 'a'
  | 'a+'
  | 'as'
  | 'as+'
  | 'ax'
  | 'ax+'
  | 'r'
  | 'r+'
  | 'rs'
  | 'rs+'
  | 'sa'
  | 'sa+'
  | 'sr'
  | 'sr+'
  | 'w'
  | 'w+'
  | 'wx'
  | 'wx+'
  | 'xa'
  | 'xa+'
  | 'xw'
  | 'xw+'
```

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/index.d.ts#L12)</sub>

#### `ReadStreamOptions`

```ts
interface ReadStreamOptions {
  fd?: number
  flags?: Flag
  mode?: number
  start?: number
  end?: number
}
```

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/index.d.ts#L145)</sub>

#### `WriteStreamOptions`

```ts
interface WriteStreamOptions {
  fd?: number
  flags?: Flag
  mode?: number
}
```

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/index.d.ts#L166)</sub>

#### `WatcherOptions`

```ts
interface WatcherOptions {
  persistent?: boolean
  recursive?: boolean
  encoding?: BufferEncoding | 'buffer'
}
```

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/index.d.ts#L185)</sub>

#### `WatcherEventType`

```ts
type WatcherEventType = 'rename' | 'change'
```

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/index.d.ts#L191)</sub>

#### `WatcherEvents`

```ts
interface WatcherEvents<T extends string | Buffer = string | Buffer> extends EventMap {
  error: [err: Error]
  change: [eventType: WatcherEventType, filename: T]
  close: []
}
```

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/index.d.ts#L193)</sub>

#### `AppendFileOptions`

```ts
interface AppendFileOptions {
  encoding?: BufferEncoding
  flag?: string
  mode?: number
}
```

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/index.d.ts#L220)</sub>

#### `CpOptions`

```ts
interface CpOptions {
  recursive?: boolean
}
```

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/index.d.ts#L296)</sub>

#### `MkdirOptions`

```ts
interface MkdirOptions {
  mode?: number
  recursive?: boolean
}
```

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/index.d.ts#L398)</sub>

#### `OpendirOptions`

```ts
interface OpendirOptions {
  encoding?: BufferEncoding | 'buffer'
  bufferSize?: number
}
```

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/index.d.ts#L438)</sub>

#### `ReadFileOptions`

```ts
interface ReadFileOptions {
  encoding?: BufferEncoding | 'buffer'
  flag?: Flag
}
```

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/index.d.ts#L563)</sub>

#### `ReaddirOptions`

```ts
interface ReaddirOptions extends OpendirOptions {
  withFileTypes?: boolean
}
```

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/index.d.ts#L645)</sub>

#### `ReadlinkOptions`

```ts
interface ReadlinkOptions {
  encoding?: BufferEncoding | 'buffer'
}
```

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/index.d.ts#L831)</sub>

#### `RealpathOptions`

```ts
interface RealpathOptions {
  encoding?: BufferEncoding | 'buffer'
}
```

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/index.d.ts#L926)</sub>

#### `RmOptions`

```ts
interface RmOptions {
  force?: boolean
  recursive?: boolean
}
```

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/index.d.ts#L1014)</sub>

#### `WriteFileOptions`

```ts
interface WriteFileOptions {
  encoding?: BufferEncoding
  flag?: Flag
  mode?: number
}
```

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/index.d.ts#L1171)</sub>

### Classes

#### `StatFs`

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/index.d.ts#L123)</sub>

```ts
class StatFs {
  bavail: number
  bfree: number
  blocks: number
  bsize: number
  ffree: number
  files: number
  type: number
}
```

#### `ReadStream`

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/index.d.ts#L153)</sub>

```ts
class ReadStream {
  fd: number
  flags: Flag
  mode: number
  path: string | null
}
```

#### `WriteStream`

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/index.d.ts#L172)</sub>

```ts
class WriteStream {
  fd: number
  flags: Flag
  mode: number
  path: string | null
}
```

## `bare-fs/promises`

### Functions

#### `open(filepath: Path, flags?: Flag | number, mode?: string | number): Promise<FileHandle>`

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/promises.d.ts#L81)</sub>

Open a file, returning a file descriptor. `flags` defaults to `'r'` and `mode` defaults to `0o666`. `flags` may be a string such as `'r'`, `'w'`, `'a'`, `'r+'`, etc., or a numeric combination of `fs.constants` flags.

**Parameters**

- `filepath` ([`Path`](#path))
- `flags` ([`Flag`](#flag) | `number`, optional)
- `mode` (`string` | `number`, optional)

#### `promises.access(filepath: Path, mode?: number): Promise<void>`

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/promises.d.ts#L87)</sub>

Check whether the file at `filepath` is accessible. `mode` defaults to `fs.constants.F_OK`.

**Parameters**

- `filepath` ([`Path`](#path))
- `mode` (`number`, optional)

#### `promises.appendFile`

```ts
appendFile(filepath: Path, data: string | Buffer | ArrayBufferView, opts?: AppendFileOptions): Promise<void>
```

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/promises.d.ts#L89)</sub>

Append `data` to a file, creating it if it does not exist. Accepts the same options as `fs.writeFile()` but defaults to the `'a'` flag.

**Parameters**

- `filepath` ([`Path`](#path))
- `data` (`string` | `Buffer` | `ArrayBufferView`)
- `opts` ([`AppendFileOptions`](#appendfileoptions), optional)

#### `promises.chmod(filepath: Path, mode: string | number): Promise<void>`

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/promises.d.ts#L101)</sub>

Change the permissions of a file. `mode` may be a numeric mode or a string that will be parsed as octal.

**Parameters**

- `filepath` ([`Path`](#path))
- `mode` (`string` | `number`)

#### `promises.chown(filepath: Path, uid: number, gid: number): Promise<void>`

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/promises.d.ts#L103)</sub>

Change the owner and group of a file.

**Parameters**

- `filepath` ([`Path`](#path))
- `uid` (`number`)
- `gid` (`number`)

#### `promises.copyFile(src: Path, dst: Path, mode?: number): Promise<void>`

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/promises.d.ts#L105)</sub>

Copy a file from `src` to `dst`. `mode` is an optional bitmask created from `fs.constants.COPYFILE_EXCL`, `fs.constants.COPYFILE_FICLONE`, or `fs.constants.COPYFILE_FICLONE_FORCE`.

**Parameters**

- `src` ([`Path`](#path))
- `dst` ([`Path`](#path))
- `mode` (`number`, optional)

#### `promises.cp(src: Path, dst: Path, opts?: CpOptions): Promise<void>`

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/promises.d.ts#L107)</sub>

Copy a file or directory from `src` to `dst`.

**Parameters**

- `src` ([`Path`](#path))
- `dst` ([`Path`](#path))
- `opts` ([`CpOptions`](#cpoptions), optional)

#### `promises.lchown(filepath: Path, uid: number, gid: number): Promise<void>`

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/promises.d.ts#L109)</sub>

Change the owner and group of a file, but if `filepath` is a symbolic link, the changes are applied only to the link, not the file it refers to.

**Parameters**

- `filepath` ([`Path`](#path))
- `uid` (`number`)
- `gid` (`number`)

#### `promises.lutimes(filepath: Path, atime: number | Date, mtime: number | Date): Promise<void>`

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/promises.d.ts#L111)</sub>

Like `fs.utimes()`, but if `filepath` is a symbolic link, the timestamps of the link is changed, not the file it refers to.

**Parameters**

- `filepath` ([`Path`](#path))
- `atime` (`number` | `Date`)
- `mtime` (`number` | `Date`)

#### `promises.link(src: Path, dst: Path): Promise<void>`

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/promises.d.ts#L113)</sub>

Creates a new link (also known as a hard link) to an existing file.

**Parameters**

- `src` ([`Path`](#path))
- `dst` ([`Path`](#path))

#### `promises.lstat(filepath: Path): Promise<Stats>`

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/promises.d.ts#L115)</sub>

Like `fs.stat()`, but if `filepath` is a symbolic link, the link itself is statted, not the file it refers to.

**Parameters**

- `filepath` ([`Path`](#path))

#### `promises.mkdir(filepath: Path, opts?: MkdirOptions): Promise<void>`

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/promises.d.ts#L117)</sub>

Create a directory at `filepath`.

Overloads:

```ts
mkdir(filepath: Path, opts?: MkdirOptions): Promise<void>
mkdir(filepath: Path, mode: number): Promise<void>
```

**Parameters**

- `filepath` ([`Path`](#path))
- `opts` ([`MkdirOptions`](#mkdiroptions), optional)

#### `promises.mkdtemp(prefix: Path): Promise<string>`

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/promises.d.ts#L121)</sub>

Create a unique temporary directory.

**Parameters**

- `prefix` ([`Path`](#path))

#### `promises.opendir`

```ts
opendir(filepath: Path, opts: OpendirOptions & { encoding?: BufferEncoding }): Promise<Dir<string>>
```

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/promises.d.ts#L123)</sub>

Open a directory for iteration. Returns a `Dir` object.

**Parameters**

- `filepath` ([`Path`](#path))
- `opts` (`OpendirOptions & { encoding?: BufferEncoding }`)

#### `promises.readFile(filepath: Path, opts: ReadFileOptions & { encoding: BufferEncoding }): Promise<string>`

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/promises.d.ts#L143)</sub>

Read the entire contents of a file. Returns a `Buffer` by default, or a string if an `encoding` is specified.

Overloads:

```ts
readFile(filepath: Path, opts: ReadFileOptions & { encoding: BufferEncoding }): Promise<string>
readFile(filepath: Path, opts: ReadFileOptions & { encoding?: 'buffer' }): Promise<Buffer>
readFile(filepath: Path, opts: ReadFileOptions): Promise<string | Buffer>
readFile(filepath: Path, encoding: BufferEncoding): Promise<string>
readFile(filepath: Path, encoding: 'buffer'): Promise<Buffer>
readFile(filepath: Path, encoding?: BufferEncoding | 'buffer'): Promise<string | Buffer>
readFile(filepath: Path): Promise<Buffer>
```

**Parameters**

- `filepath` ([`Path`](#path))
- `opts` (`ReadFileOptions & { encoding: BufferEncoding }`)

#### `promises.readdir`

```ts
readdir(filepath: Path, opts: ReaddirOptions & { encoding?: BufferEncoding }): Promise<Dir<string>[] | string[]>
```

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/promises.d.ts#L166)</sub>

Read the contents of a directory. Returns an array of filenames or, if `withFileTypes` is `true`, an array of `Dirent` objects.

**Parameters**

- `filepath` ([`Path`](#path))
- `opts` (`ReaddirOptions & { encoding?: BufferEncoding }`)

#### `promises.readlink(filepath: Path, opts: ReadlinkOptions & { encoding?: BufferEncoding }): Promise<string>`

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/promises.d.ts#L219)</sub>

Read the target of a symbolic link.

Overloads:

```ts
readlink(filepath: Path, opts: ReadlinkOptions & { encoding?: BufferEncoding }): Promise<string>
readlink(filepath: Path, opts: ReadlinkOptions & { encoding: 'buffer' }): Promise<Buffer>
readlink(filepath: Path, opts: ReadlinkOptions): Promise<string | Buffer>
readlink(filepath: Path, encoding: BufferEncoding): Promise<string>
readlink(filepath: Path, encoding: 'buffer'): Promise<Buffer>
readlink(filepath: Path, encoding: BufferEncoding | 'buffer'): Promise<string | Buffer>
readlink(filepath: Path): Promise<string>
```

**Parameters**

- `filepath` ([`Path`](#path))
- `opts` (`ReadlinkOptions & { encoding?: BufferEncoding }`)

#### `promises.realpath(filepath: Path, opts: RealpathOptions & { encoding?: BufferEncoding }): Promise<string>`

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/promises.d.ts#L242)</sub>

Resolve the real path of `filepath`, expanding all symbolic links.

Overloads:

```ts
realpath(filepath: Path, opts: RealpathOptions & { encoding?: BufferEncoding }): Promise<string>
realpath(filepath: Path, opts: RealpathOptions & { encoding: 'buffer' }): Promise<Buffer>
realpath(filepath: Path, opts: RealpathOptions): Promise<string | Buffer>
realpath(filepath: Path, encoding: BufferEncoding): Promise<string>
realpath(filepath: Path, encoding: 'buffer'): Promise<Buffer>
realpath(filepath: Path, encoding: BufferEncoding | 'buffer'): Promise<string | Buffer>
realpath(filepath: Path): Promise<string>
```

**Parameters**

- `filepath` ([`Path`](#path))
- `opts` (`RealpathOptions & { encoding?: BufferEncoding }`)

#### `promises.rename(src: Path, dst: Path): Promise<void>`

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/promises.d.ts#L265)</sub>

Rename a file from `src` to `dst`.

**Parameters**

- `src` ([`Path`](#path))
- `dst` ([`Path`](#path))

#### `promises.rm(filepath: Path, opts?: RmOptions): Promise<void>`

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/promises.d.ts#L267)</sub>

Remove a file or directory at `filepath`.

**Parameters**

- `filepath` ([`Path`](#path))
- `opts` ([`RmOptions`](#rmoptions), optional)

#### `promises.rmdir(filepath: Path): Promise<void>`

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/promises.d.ts#L269)</sub>

Remove an empty directory.

**Parameters**

- `filepath` ([`Path`](#path))

#### `promises.stat(filepath: Path): Promise<Stats>`

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/promises.d.ts#L271)</sub>

Get the status of a file. Returns a `Stats` object.

**Parameters**

- `filepath` ([`Path`](#path))

#### `promises.statfs(filepath: Path): Promise<StatFs>`

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/promises.d.ts#L273)</sub>

Get filesystem statistics. Returns a `StatFs` object.

**Parameters**

- `filepath` ([`Path`](#path))

#### `promises.truncate(filepath: Path, len?: number): Promise<void>`

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/promises.d.ts#L275)</sub>

Truncate the file at `filename` to `len` bytes. `len` defaults to `0`.

**Parameters**

- `filepath` ([`Path`](#path))
- `len` (`number`, optional)

#### `promises.symlink(target: Path, filepath: Path, type?: string | number): Promise<void>`

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/promises.d.ts#L277)</sub>

Create a symbolic link at `filepath` pointing to `target`. `type` may be `'file'`, `'dir'`, or `'junction'` (Windows only) or a numeric flag. On Windows, if `type` is not provided, it is inferred from the target.

**Parameters**

- `target` ([`Path`](#path))
- `filepath` ([`Path`](#path))
- `type` (`string` | `number`, optional)

#### `promises.unlink(filepath: Path): Promise<void>`

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/promises.d.ts#L279)</sub>

Remove a file.

**Parameters**

- `filepath` ([`Path`](#path))

#### `promises.utimes(filepath: Path, atime: number | Date, mtime: number | Date): Promise<void>`

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/promises.d.ts#L281)</sub>

Change the access and modification times of a file. Times may be numbers (seconds since epoch) or `Date` objects.

**Parameters**

- `filepath` ([`Path`](#path))
- `atime` (`number` | `Date`)
- `mtime` (`number` | `Date`)

#### `watch(filepath: Path, opts: WatcherOptions & { encoding?: BufferEncoding }): Watcher<string>`

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/promises.d.ts#L283)</sub>

Watch a file or directory for changes. Returns a `Watcher` object. The `callback`, if provided, is called with `(eventType, filename)` on each change.

Overloads:

```ts
watch(filepath: Path, opts: WatcherOptions & { encoding?: BufferEncoding }): Watcher<string>
watch(filepath: Path, opts: WatcherOptions & { encoding: 'buffer' }): Watcher<Buffer>
watch(filepath: Path, opts: WatcherOptions): Watcher
watch(filepath: Path, encoding: BufferEncoding): Watcher<string>
watch(filepath: Path, encoding: 'buffer'): Watcher<Buffer>
watch(filepath: Path, encoding: BufferEncoding | 'buffer'): Watcher
watch(filepath: Path): Watcher<string>
```

**Parameters**

- `filepath` ([`Path`](#path))
- `opts` (`WatcherOptions & { encoding?: BufferEncoding }`)

#### `promises.writeFile`

```ts
writeFile(filepath: Path, data: string | Buffer | ArrayBufferView, opts?: WriteFileOptions): Promise<void>
```

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/promises.d.ts#L303)</sub>

Write `data` to a file, replacing it if it already exists.

**Parameters**

- `filepath` ([`Path`](#path))
- `data` (`string` | `Buffer` | `ArrayBufferView`)
- `opts` ([`WriteFileOptions`](#writefileoptions), optional)

### Constants and variables

#### `promises.constants`

```ts
constants: {
  O_RDWR: number
  O_RDONLY: number
  O_WRONLY: number
  O_CREAT: number
  O_TRUNC: number
  O_APPEND: number

  F_OK: number
  R_OK: number
  W_OK: number
  X_OK: number

  S_IFMT: number
  S_IFREG: number
  S_IFDIR: number
  S_IFCHR: number
  S_IFLNK: number
  S_IFBLK: number
  S_IFIFO: number
  S_IFSOCK: number

  S_IRUSR: number
  S_IWUSR: number
  S_IXUSR: number
  S_IRGRP: number
  S_IWGRP: number
  S_IXGRP: number
  S_IROTH: number
  S_IWOTH: number
  S_IXOTH: number

  UV_DIRENT_UNKNOWN: number
  UV_DIRENT_FILE: number
  UV_DIRENT_DIR: number
  UV_DIRENT_LINK: number
  UV_DIRENT_FIFO: number
  UV_DIRENT_SOCKET: number
  UV_DIRENT_CHAR: number
  UV_DIRENT_BLOCK: number

  COPYFILE_EXCL: number
  COPYFILE_FICLONE: number
  COPYFILE_FICLONE_FORCE: number
  UV_FS_SYMLINK_DIR: number
  UV_FS_SYMLINK_JUNCTION: number
}
```

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/lib/constants.d.ts#L1)</sub>

An object containing file system constants, such as file access modes and file type flags. See `fs/constants` for the full list.

## `bare-fs/constants`

### Constants and variables

#### `constants.constants`

```ts
constants: {
  O_RDWR: number
  O_RDONLY: number
  O_WRONLY: number
  O_CREAT: number
  O_TRUNC: number
  O_APPEND: number

  F_OK: number
  R_OK: number
  W_OK: number
  X_OK: number

  S_IFMT: number
  S_IFREG: number
  S_IFDIR: number
  S_IFCHR: number
  S_IFLNK: number
  S_IFBLK: number
  S_IFIFO: number
  S_IFSOCK: number

  S_IRUSR: number
  S_IWUSR: number
  S_IXUSR: number
  S_IRGRP: number
  S_IWGRP: number
  S_IXGRP: number
  S_IROTH: number
  S_IWOTH: number
  S_IXOTH: number

  UV_DIRENT_UNKNOWN: number
  UV_DIRENT_FILE: number
  UV_DIRENT_DIR: number
  UV_DIRENT_LINK: number
  UV_DIRENT_FIFO: number
  UV_DIRENT_SOCKET: number
  UV_DIRENT_CHAR: number
  UV_DIRENT_BLOCK: number

  COPYFILE_EXCL: number
  COPYFILE_FICLONE: number
  COPYFILE_FICLONE_FORCE: number
  UV_FS_SYMLINK_DIR: number
  UV_FS_SYMLINK_JUNCTION: number
}
```

<sub>[Source](https://github.com/holepunchto/bare-fs/blob/v4.7.4/lib/constants.d.ts#L1)</sub>

An object containing file system constants, such as file access modes and file type flags. See `fs/constants` for the full list.

## See also

- [Bare modules](/reference/modules/bare-modules) — the full `bare-*` catalog.
- [Bare runtime API](/reference/bare/runtime) — the runtime these modules extend.
