---
title: "bare-module-resolve"
description: "Low-level module resolution algorithm for Bare"
docType: reference
schemaType: APIReference
---

<mark style={{ backgroundColor: '#7dde9a', padding: '2px 8px', borderRadius: '4px', fontSize: '0.85em', fontWeight: 500 }}>stable</mark>

[`bare-module-resolve`](https://github.com/holepunchto/bare-module-resolve) — Low-level module resolution algorithm for Bare.

```sh
npm i bare-module-resolve
```

## Usage

For synchronous resolution:

```js
const resolve = require('bare-module-resolve')

function readPackage(url) {
  // Read and parse `url` if it exists, otherwise `null`
}

for (const resolution of resolve('./file.js', new URL('file:///directory/'), readPackage)) {
  console.log(resolution)
}
```

For asynchronous resolution:

```js
const resolve = require('bare-module-resolve')

async function readPackage(url) {
  // Read and parse `url` if it exists, otherwise `null`
}

for await (const resolution of resolve('./file.js', new URL('file:///directory/'), readPackage)) {
  console.log(resolution)
}
```

## API

### Functions

#### `resolve`

```ts
resolve(specifier: string, parentURL: URL, readPackage?: (url: URL) => JSON | null): Iterable<URL>
```

<sub>[Source](https://github.com/holepunchto/bare-module-resolve/blob/v1.12.2/index.d.ts#L35)</sub>

Resolve `specifier` relative to `parentURL`, which must be a WHATWG `URL` instance. `readPackage` is called with a `URL` instance for every package manifest to be read and must either return the parsed JSON package manifest, if it exists, or `null`. If `readPackage` returns a promise, synchronous iteration is not supported.

**Parameters**

- `specifier` (`string`)
- `parentURL` (`URL`)
- `readPackage` (`(url: URL) => JSON | null`, optional)

### Types

#### `ResolveOptions`

```ts
interface ResolveOptions {
  builtinProtocol?: string
  builtins?: Builtins
  conditions?: Conditions
  defer?: string[]
  deferredProtocol?: string
  engines?: Engines
  extensions?: string[]
  imports?: ImportsMap
  matchedConditions?: string[]
  resolutions?: ResolutionsMap
}
```

<sub>[Source](https://github.com/holepunchto/bare-module-resolve/blob/v1.12.2/index.d.ts#L22)</sub>

## `bare-module-resolve/errors`

### ModuleResolveError

#### `code: string`

<sub>[Source](https://github.com/holepunchto/bare-module-resolve/blob/v1.12.2/lib/errors.d.ts#L2)</sub>

#### `ModuleResolveError.INVALID_MODULE_SPECIFIER(msg: string): ModuleResolveError`

<sub>[Source](https://github.com/holepunchto/bare-module-resolve/blob/v1.12.2/lib/errors.d.ts#L4)</sub>

**Parameters**

- `msg` (`string`)

#### `ModuleResolveError.INVALID_PACKAGE_TARGET(msg: string): ModuleResolveError`

<sub>[Source](https://github.com/holepunchto/bare-module-resolve/blob/v1.12.2/lib/errors.d.ts#L5)</sub>

**Parameters**

- `msg` (`string`)

#### `ModuleResolveError.PACKAGE_IMPORT_NOT_DEFINED(msg: string): ModuleResolveError`

<sub>[Source](https://github.com/holepunchto/bare-module-resolve/blob/v1.12.2/lib/errors.d.ts#L7)</sub>

**Parameters**

- `msg` (`string`)

#### `ModuleResolveError.PACKAGE_PATH_NOT_EXPORTED(msg: string): ModuleResolveError`

<sub>[Source](https://github.com/holepunchto/bare-module-resolve/blob/v1.12.2/lib/errors.d.ts#L6)</sub>

**Parameters**

- `msg` (`string`)

#### `ModuleResolveError.UNSUPPORTED_ENGINE(msg: string): ModuleResolveError`

<sub>[Source](https://github.com/holepunchto/bare-module-resolve/blob/v1.12.2/lib/errors.d.ts#L8)</sub>

**Parameters**

- `msg` (`string`)

## See also

- [Bare modules](/reference/modules/bare-modules) — the full `bare-*` catalog.
- [Bare runtime API](/reference/bare/runtime) — the runtime these modules extend.
