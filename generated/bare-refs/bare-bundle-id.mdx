---
title: "bare-bundle-id"
description: "Construct a unique ID for a bundle"
docType: reference
schemaType: APIReference
upstreamVersion: "1.0.2"
---

<mark style={{ backgroundColor: '#7dde9a', padding: '2px 8px', borderRadius: '4px', fontSize: '0.85em', fontWeight: 500 }}>stable</mark>

[`bare-bundle-id`](https://github.com/holepunchto/bare-bundle-id) — Construct a unique ID for a bundle.

```sh
npm i bare-bundle-id
```

## Usage

```js
const Bundle = require('bare-bundle')
const id = require('bare-bundle-id')

const bundle = new Bundle()
  .write('/foo.js', "module.exports = require('./bar')", {
    main: true,
    imports: {
      './bar': '/bar.js'
    }
  })
  .write('/bar.js', 'module.exports = 42')

bundle.id = id(bundle).toString('hex')
// 33824862...
```

## API

### Functions

#### `id(bundle: Bundle): Buffer`

Returns a 32-byte BLAKE2b hash of `bundle`'s sorted file entries (path, contents, and mode), uniquely identifying the bundle's contents.

**Parameters**

| Parameter | Type | Default | Description |
| --- | --- | --- | --- |
| `bundle` | `Bundle` | — | The [`bare-bundle`](https://github.com/holepunchto/bare-bundle) instance to hash; its file entries are sorted by path before hashing, so the ID is stable regardless of the order they were written in. |

## See also

- [Bare modules](/reference/modules/bare-modules) — the full `bare-*` catalog.
- [Bare runtime API](/reference/bare/runtime) — the runtime these modules extend.
