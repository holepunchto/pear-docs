---
title: "bare-tty"
description: "Native TTY streams for JavaScript"
docType: reference
schemaType: APIReference
---

<mark style={{ backgroundColor: '#7dde9a', padding: '2px 8px', borderRadius: '4px', fontSize: '0.85em', fontWeight: 500 }}>stable</mark>

[`bare-tty`](https://github.com/holepunchto/bare-tty) — Native TTY streams for JavaScript. It is a native addon and requires Bare `>=1.16.0`.

Mirrors the Node.js [`tty`](https://nodejs.org/api/tty.html) module.

```sh
npm i bare-tty
```

## Usage

```js
const tty = require('bare-tty')

const stdout = new tty.WriteStream(1)

stdout.write('Hello world!\n')
```

## API

### ReadStream

#### `new ReadStream(fd: number, opts?: ReadableOptions)`

<sub>[Source](https://github.com/holepunchto/bare-tty/blob/v5.1.1/index.d.ts#L13)</sub>

**Parameters**

- `fd` (`number`)
- `opts` (`ReadableOptions`, optional)

#### `ReadStream.fd: number`

<sub>[Source](https://github.com/holepunchto/bare-tty/blob/v5.1.1/index.d.ts#L5)</sub>

#### `ReadStream.isTTY: true`

<sub>[Source](https://github.com/holepunchto/bare-tty/blob/v5.1.1/index.d.ts#L6)</sub>

#### `setMode(mode: number): this`

<sub>[Source](https://github.com/holepunchto/bare-tty/blob/v5.1.1/index.d.ts#L8)</sub>

**Parameters**

- `mode` (`number`)

#### `setRawMode(mode: boolean): this`

<sub>[Source](https://github.com/holepunchto/bare-tty/blob/v5.1.1/index.d.ts#L9)</sub>

**Parameters**

- `mode` (`boolean`)

### WriteStream

#### `new WriteStream(fd: number, opts?: WritableOptions)`

<sub>[Source](https://github.com/holepunchto/bare-tty/blob/v5.1.1/index.d.ts#L30)</sub>

**Parameters**

- `fd` (`number`)
- `opts` (`WritableOptions`, optional)

#### `columns: number`

<sub>[Source](https://github.com/holepunchto/bare-tty/blob/v5.1.1/index.d.ts#L23)</sub>

#### `WriteStream.fd: number`

<sub>[Source](https://github.com/holepunchto/bare-tty/blob/v5.1.1/index.d.ts#L21)</sub>

#### `getWindowSize(): [width: number, height: number]`

<sub>[Source](https://github.com/holepunchto/bare-tty/blob/v5.1.1/index.d.ts#L26)</sub>

#### `WriteStream.isTTY: true`

<sub>[Source](https://github.com/holepunchto/bare-tty/blob/v5.1.1/index.d.ts#L22)</sub>

#### `rows: number`

<sub>[Source](https://github.com/holepunchto/bare-tty/blob/v5.1.1/index.d.ts#L24)</sub>

### Functions

#### `isTTY(fd: number): boolean`

<sub>[Source](https://github.com/holepunchto/bare-tty/blob/v5.1.1/index.d.ts#L33)</sub>

**Parameters**

- `fd` (`number`)

### Constants and variables

#### `constants`

```ts
constants: {
  mode: { NORMAL: number; RAW: number; IO: number }
  state: { READING: number; CLOSING: number }
}
```

<sub>[Source](https://github.com/holepunchto/bare-tty/blob/v5.1.1/lib/constants.d.ts#L1)</sub>

## See also

- [Bare modules](/reference/modules/bare-modules) — the full `bare-*` catalog.
- [Bare runtime API](/reference/bare/runtime) — the runtime these modules extend.
