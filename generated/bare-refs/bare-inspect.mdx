---
title: "bare-inspect"
description: "Inspect objects as strings for debugging"
docType: reference
schemaType: APIReference
---

<mark style={{ backgroundColor: '#7dde9a', padding: '2px 8px', borderRadius: '4px', fontSize: '0.85em', fontWeight: 500 }}>stable</mark>

[`bare-inspect`](https://github.com/holepunchto/bare-inspect) — Inspect objects as strings for debugging. It is a native addon and requires Bare `>=1.18.0`.

```sh
npm i bare-inspect
```

## Usage

```js
const inspect = require('bare-inspect')

console.log(inspect(globalThis))
```

## API

### Functions

#### `inspect(value: unknown, opts?: InspectOptions): string`

<sub>[Source](https://github.com/holepunchto/bare-inspect/blob/v3.1.4/index.d.ts#L12)</sub>

Returns a string representation of `value` for debugging, similar to Node's `util.inspect()`. If `value` has a `Symbol.for('bare.inspect')` or `Symbol.for('nodejs.util.inspect.custom')` method, it is used to produce the output.

**Parameters**

- `value` (`unknown`)
- `opts` ([`InspectOptions`](#inspectoptions), optional)

### Types

#### `InspectOptions`

```ts
interface InspectOptions {
  colors?: boolean
  depth?: number
  breakLength?: number
  stylize?: InspectStylize
}
```

<sub>[Source](https://github.com/holepunchto/bare-inspect/blob/v3.1.4/index.d.ts#L5)</sub>

Options for `inspect()`. `colors` enables ANSI styling, `depth` limits how many levels of nested objects are inspected (defaults to `2`), `breakLength` sets the line-wrap width (defaults to `80`), and `stylize` is a custom function for applying styles to values.

#### `InspectStylize`

```ts
interface InspectStylize {
  (value: string, style: keyof typeof inspect.styles): string
}
```

<sub>[Source](https://github.com/holepunchto/bare-inspect/blob/v3.1.4/index.d.ts#L1)</sub>

A function that applies a named style (such as `'string'` or `'number'`) to a value before it's included in the output.

## See also

- [Bare modules](/reference/modules/bare-modules) — the full `bare-*` catalog.
- [Bare runtime API](/reference/bare/runtime) — the runtime these modules extend.
