---
title: "bare-rpc"
description: "librpc ABI compatible RPC for Bare"
docType: reference
schemaType: APIReference
---

<mark style={{ backgroundColor: '#7dde9a', padding: '2px 8px', borderRadius: '4px', fontSize: '0.85em', fontWeight: 500 }}>stable</mark>

[`bare-rpc`](https://github.com/holepunchto/bare-rpc) — librpc ABI compatible RPC for Bare.

```sh
npm i bare-rpc
```

## Usage

```js
import RPC from 'bare-rpc'

// On one end
const rpc = new RPC(stream, (req) => {
  if (req.command === 42) {
    console.log(req.data.toString()) // ping
    req.reply('pong')
  }
})

// On the other end
const req = rpc.request(42)
req.send('ping')

const replyBuffer = await req.reply()
console.log(replyBuffer.toString()) // pong
```

## API

### RPC

#### `new RPC(stream: Duplex, onrequest: (req: RPCIncomingRequest) => void | Promise<void>)`

<sub>[Source](https://github.com/holepunchto/bare-rpc/blob/v1.3.6/index.d.ts#L105)</sub>

Create an RPC instance using a duplex `stream`. `onrequest` is an optional callback run when a remote request is received. This is where processing and responding to an RPC request happens. `onrequest` receives a `RPCIncomingRequest` as an argument, for example:

**Parameters**

- `stream` (`Duplex`)
- `onrequest` (`(req: RPCIncomingRequest) => void | Promise<void>`)

#### `event(command: number): RPCOutgoingEvent`

<sub>[Source](https://github.com/holepunchto/bare-rpc/blob/v1.3.6/index.d.ts#L100)</sub>

**Parameters**

- `command` (`number`)

#### `idle: boolean`

<sub>[Source](https://github.com/holepunchto/bare-rpc/blob/v1.3.6/index.d.ts#L98)</sub>

#### `request(command: number): RPCOutgoingRequest`

<sub>[Source](https://github.com/holepunchto/bare-rpc/blob/v1.3.6/index.d.ts#L101)</sub>

Create a request for `command`. `command` is a unique number that should be used to differentiate different requests on the remote end. Returns a `RPCOutgoingRequest`.

**Parameters**

- `command` (`number`)

### RPCIncomingEvent

#### `new RPCIncomingEvent(rpc: RPC, command: number, data: Buffer)`

<sub>[Source](https://github.com/holepunchto/bare-rpc/blob/v1.3.6/index.d.ts#L27)</sub>

**Parameters**

- `rpc` (`RPC`)
- `command` (`number`)
- `data` (`Buffer`)

#### `RPCIncomingEvent.command: number`

<sub>[Source](https://github.com/holepunchto/bare-rpc/blob/v1.3.6/index.d.ts#L22)</sub>

The command that the request was created with. A command is a unique number.

#### `RPCIncomingEvent.data: Buffer | null`

<sub>[Source](https://github.com/holepunchto/bare-rpc/blob/v1.3.6/index.d.ts#L23)</sub>

The data buffer sent with the request.

#### `RPCIncomingEvent.rpc: RPC`

<sub>[Source](https://github.com/holepunchto/bare-rpc/blob/v1.3.6/index.d.ts#L21)</sub>

### RPCOutgoingEvent

#### `new RPCOutgoingEvent(rpc: RPC, command: number)`

<sub>[Source](https://github.com/holepunchto/bare-rpc/blob/v1.3.6/index.d.ts#L57)</sub>

**Parameters**

- `rpc` (`RPC`)
- `command` (`number`)

#### `RPCOutgoingEvent.command: number`

<sub>[Source](https://github.com/holepunchto/bare-rpc/blob/v1.3.6/index.d.ts#L50)</sub>

The command that the request was created with. A command is a unique number.

#### `RPCOutgoingEvent.rpc: RPC`

<sub>[Source](https://github.com/holepunchto/bare-rpc/blob/v1.3.6/index.d.ts#L49)</sub>

#### `RPCOutgoingEvent.send(data?: Buffer | string | null, encoding?: BufferEncoding): void`

<sub>[Source](https://github.com/holepunchto/bare-rpc/blob/v1.3.6/index.d.ts#L53)</sub>

Send the request with the provided `data`. `data` can be a buffer or a string which will be encoded using `encoding`.

**Parameters**

- `data` (`Buffer` | `string` | `null`, optional)
- `encoding` (`BufferEncoding`, optional)

#### `RPCOutgoingEvent.sent: boolean`

<sub>[Source](https://github.com/holepunchto/bare-rpc/blob/v1.3.6/index.d.ts#L51)</sub>

A boolean for whether the request has been sent.

### RPCIncomingRequest

#### `new RPCIncomingRequest(rpc: RPC, id: number, command: number, data: Buffer)`

<sub>[Source](https://github.com/holepunchto/bare-rpc/blob/v1.3.6/index.d.ts#L45)</sub>

**Parameters**

- `rpc` (`RPC`)
- `id` (`number`)
- `command` (`number`)
- `data` (`Buffer`)

#### `RPCIncomingRequest.command: number`

<sub>[Source](https://github.com/holepunchto/bare-rpc/blob/v1.3.6/index.d.ts#L33)</sub>

The command that the request was created with. A command is a unique number.

#### `createRequestStream(opts?: ReadableOptions): RPCIncomingStream`

<sub>[Source](https://github.com/holepunchto/bare-rpc/blob/v1.3.6/index.d.ts#L41)</sub>

Create a [`Writable`](https://github.com/mafintosh/streamx#writable-stream) stream for sending data with the request.

**Parameters**

- `opts` (`ReadableOptions`, optional)

#### `createResponseStream(opts?: WritableOptions): RPCOutgoingStream`

<sub>[Source](https://github.com/holepunchto/bare-rpc/blob/v1.3.6/index.d.ts#L40)</sub>

Create a [`Readable`](https://github.com/mafintosh/streamx#readable-stream) stream for receiving data in reply to the request.

**Parameters**

- `opts` (`WritableOptions`, optional)

#### `RPCIncomingRequest.data: Buffer | null`

<sub>[Source](https://github.com/holepunchto/bare-rpc/blob/v1.3.6/index.d.ts#L34)</sub>

The data buffer sent with the request.

#### `RPCIncomingRequest.id: number`

<sub>[Source](https://github.com/holepunchto/bare-rpc/blob/v1.3.6/index.d.ts#L32)</sub>

#### `RPCIncomingRequest.received: boolean`

<sub>[Source](https://github.com/holepunchto/bare-rpc/blob/v1.3.6/index.d.ts#L36)</sub>

A boolean for whether the request has received a reply.

#### `reply(data?: Buffer | string | null, encoding?: BufferEncoding): void`

<sub>[Source](https://github.com/holepunchto/bare-rpc/blob/v1.3.6/index.d.ts#L38)</sub>

Await the reply from the remote end to the request. `encoding` can be defined for decoding the response `data` buffer back into a string.

**Parameters**

- `data` (`Buffer` | `string` | `null`, optional)
- `encoding` (`BufferEncoding`, optional)

#### `RPCIncomingRequest.rpc: RPC`

<sub>[Source](https://github.com/holepunchto/bare-rpc/blob/v1.3.6/index.d.ts#L31)</sub>

#### `RPCIncomingRequest.sent: boolean`

<sub>[Source](https://github.com/holepunchto/bare-rpc/blob/v1.3.6/index.d.ts#L35)</sub>

A boolean for whether the request has been sent.

### RPCOutgoingRequest

#### `new RPCOutgoingRequest(rpc: RPC, id: number, command: number)`

<sub>[Source](https://github.com/holepunchto/bare-rpc/blob/v1.3.6/index.d.ts#L76)</sub>

**Parameters**

- `rpc` (`RPC`)
- `id` (`number`)
- `command` (`number`)

#### `RPCOutgoingRequest.command: number`

<sub>[Source](https://github.com/holepunchto/bare-rpc/blob/v1.3.6/index.d.ts#L63)</sub>

The command that the request was created with. A command is a unique number.

#### `createRequestStream(opts?: WritableOptions): RPCOutgoingStream`

<sub>[Source](https://github.com/holepunchto/bare-rpc/blob/v1.3.6/index.d.ts#L71)</sub>

Create a [`Writable`](https://github.com/mafintosh/streamx#writable-stream) stream for sending data with the request.

**Parameters**

- `opts` (`WritableOptions`, optional)

#### `createResponseStream(opts?: ReadableOptions): RPCIncomingStream`

<sub>[Source](https://github.com/holepunchto/bare-rpc/blob/v1.3.6/index.d.ts#L72)</sub>

Create a [`Readable`](https://github.com/mafintosh/streamx#readable-stream) stream for receiving data in reply to the request.

**Parameters**

- `opts` (`ReadableOptions`, optional)

#### `RPCOutgoingRequest.id: number`

<sub>[Source](https://github.com/holepunchto/bare-rpc/blob/v1.3.6/index.d.ts#L62)</sub>

#### `RPCOutgoingRequest.received: boolean`

<sub>[Source](https://github.com/holepunchto/bare-rpc/blob/v1.3.6/index.d.ts#L65)</sub>

A boolean for whether the request has received a reply.

#### `reply(encoding?: BufferEncoding): Promise<Buffer | string | null>`

<sub>[Source](https://github.com/holepunchto/bare-rpc/blob/v1.3.6/index.d.ts#L69)</sub>

Await the reply from the remote end to the request. `encoding` can be defined for decoding the response `data` buffer back into a string.

**Parameters**

- `encoding` (`BufferEncoding`, optional)

#### `RPCOutgoingRequest.rpc: RPC`

<sub>[Source](https://github.com/holepunchto/bare-rpc/blob/v1.3.6/index.d.ts#L61)</sub>

#### `RPCOutgoingRequest.send(data?: Buffer | string | null, encoding?: BufferEncoding): void`

<sub>[Source](https://github.com/holepunchto/bare-rpc/blob/v1.3.6/index.d.ts#L67)</sub>

Send the request with the provided `data`. `data` can be a buffer or a string which will be encoded using `encoding`.

**Parameters**

- `data` (`Buffer` | `string` | `null`, optional)
- `encoding` (`BufferEncoding`, optional)

#### `RPCOutgoingRequest.sent: boolean`

<sub>[Source](https://github.com/holepunchto/bare-rpc/blob/v1.3.6/index.d.ts#L64)</sub>

A boolean for whether the request has been sent.

### RPCIncomingStream

#### `RPCIncomingStream`

```ts
new RPCIncomingStream(rpc: RPC, request: RPCIncomingRequest | RPCOutgoingRequest, type: typeof constants.type.REQUEST | typeof constants.type.RESPONSE, opts?: ReadableOptions)
```

<sub>[Source](https://github.com/holepunchto/bare-rpc/blob/v1.3.6/index.d.ts#L80)</sub>

**Parameters**

- `rpc` (`RPC`)
- `request` (`RPCIncomingRequest` | `RPCOutgoingRequest`)
- `type` (`typeof constants.type.REQUEST` | `typeof constants.type.RESPONSE`)
- `opts` (`ReadableOptions`, optional)

### RPCOutgoingStream

#### `RPCOutgoingStream`

```ts
new RPCOutgoingStream(rpc: RPC, request: RPCIncomingRequest | RPCOutgoingRequest, type: typeof constants.type.REQUEST | typeof constants.type.RESPONSE, opts?: WritableOptions)
```

<sub>[Source](https://github.com/holepunchto/bare-rpc/blob/v1.3.6/index.d.ts#L89)</sub>

**Parameters**

- `rpc` (`RPC`)
- `request` (`RPCIncomingRequest` | `RPCOutgoingRequest`)
- `type` (`typeof constants.type.REQUEST` | `typeof constants.type.RESPONSE`)
- `opts` (`WritableOptions`, optional)

## `bare-rpc/errors`

### RPCError

#### `RPCError.ALREADY_SENT(msg: string): RPCError`

<sub>[Source](https://github.com/holepunchto/bare-rpc/blob/v1.3.6/lib/errors.d.ts#L3)</sub>

**Parameters**

- `msg` (`string`)

#### `RPCError.UNKNOWN_MESSAGE(msg: string): RPCError`

<sub>[Source](https://github.com/holepunchto/bare-rpc/blob/v1.3.6/lib/errors.d.ts#L2)</sub>

**Parameters**

- `msg` (`string`)

## See also

- [Bare modules](/reference/modules/bare-modules) — the full `bare-*` catalog.
- [Bare runtime API](/reference/bare/runtime) — the runtime these modules extend.
