---
title: "bare-tcp"
description: "Native TCP sockets for JavaScript"
docType: reference
schemaType: APIReference
---

<mark style={{ backgroundColor: '#7dde9a', padding: '2px 8px', borderRadius: '4px', fontSize: '0.85em', fontWeight: 500 }}>stable</mark>

[`bare-tcp`](https://github.com/holepunchto/bare-tcp) — Native TCP sockets for JavaScript. It is a native addon and requires Bare `>=1.16.0`.

Mirrors the Node.js [`net`](https://nodejs.org/api/net.html) module.

```sh
npm i bare-tcp
```

## Usage

```js
const tcp = require('bare-tcp')

const server = tcp.createServer()
server.on('connection', (socket) => socket.on('data', console.log))
server.listen(() => console.log('server is up'))

const { port } = server.address()
const socket = tcp.createConnection(port)
socket.write('hello world')
```

## API

### TCPSocket

#### `new TCPSocket(opts?: TCPSocketOptions)`

<sub>[Source](https://github.com/holepunchto/bare-tcp/blob/v2.5.1/index.d.ts#L77)</sub>

**Parameters**

- `opts` ([`TCPSocketOptions`](#tcpsocketoptions), optional)

#### `connect`

```ts
connect(port: number, host?: string, opts?: TCPSocketConnectOptions, onconnect?: () => void): this
```

<sub>[Source](https://github.com/holepunchto/bare-tcp/blob/v2.5.1/index.d.ts#L56)</sub>

Connect the socket to `port` on `host`. If `host` is not provided, it defaults to `'localhost'`. `onconnect` is called when the connection is established.

**Parameters**

- `port` (`number`)
- `host` (`string`, optional)
- `opts` ([`TCPSocketConnectOptions`](#tcpsocketconnectoptions), optional)
- `onconnect` (`() => void`, optional)

#### `connecting: boolean`

<sub>[Source](https://github.com/holepunchto/bare-tcp/blob/v2.5.1/index.d.ts#L45)</sub>

Whether the socket is currently connecting.

#### `localAddress: string`

<sub>[Source](https://github.com/holepunchto/bare-tcp/blob/v2.5.1/index.d.ts#L49)</sub>

The local IP address of the socket, if connected.

#### `localFamily: string`

<sub>[Source](https://github.com/holepunchto/bare-tcp/blob/v2.5.1/index.d.ts#L50)</sub>

The local IP family (`'IPv4'` or `'IPv6'`), if connected.

#### `localPort: number`

<sub>[Source](https://github.com/holepunchto/bare-tcp/blob/v2.5.1/index.d.ts#L51)</sub>

The local port of the socket, if connected.

#### `open(fd: number, opts?: { fd?: number }, onconnect?: () => void): this`

<sub>[Source](https://github.com/holepunchto/bare-tcp/blob/v2.5.1/index.d.ts#L61)</sub>

Overloads:

```ts
open(fd: number, opts?: { fd?: number }, onconnect?: () => void): this
open(fd: number, onconnect: () => void): this
open(opts: { fd: number }, onconnect?: () => void): this
```

**Parameters**

- `fd` (`number`)
- `opts` (`{ fd?: number }`, optional)
- `onconnect` (`() => void`, optional)

#### `pending: boolean`

<sub>[Source](https://github.com/holepunchto/bare-tcp/blob/v2.5.1/index.d.ts#L46)</sub>

Whether the socket has not yet connected.

#### `readyState: 'open' | 'opening'`

<sub>[Source](https://github.com/holepunchto/bare-tcp/blob/v2.5.1/index.d.ts#L48)</sub>

The current state of the socket. Either `'open'` or `'opening'`.

#### `TCPSocket.ref(): this`

<sub>[Source](https://github.com/holepunchto/bare-tcp/blob/v2.5.1/index.d.ts#L72)</sub>

Ref the socket, preventing the process from exiting.

#### `remoteAddress: string`

<sub>[Source](https://github.com/holepunchto/bare-tcp/blob/v2.5.1/index.d.ts#L52)</sub>

The remote IP address of the socket, if connected.

#### `remoteFamily: string`

<sub>[Source](https://github.com/holepunchto/bare-tcp/blob/v2.5.1/index.d.ts#L53)</sub>

The remote IP family (`'IPv4'` or `'IPv6'`), if connected.

#### `remotePort: number`

<sub>[Source](https://github.com/holepunchto/bare-tcp/blob/v2.5.1/index.d.ts#L54)</sub>

The remote port of the socket, if connected.

#### `setKeepAlive(enable?: boolean, delay?: number): this`

<sub>[Source](https://github.com/holepunchto/bare-tcp/blob/v2.5.1/index.d.ts#L65)</sub>

Enable or disable keep-alive. `delay` is the initial delay in milliseconds before the first keep-alive probe is sent.

Overloads:

```ts
setKeepAlive(enable?: boolean, delay?: number): this
setKeepAlive(delay: number): this
```

**Parameters**

- `enable` (`boolean`, optional)
- `delay` (`number`, optional)

#### `setNoDelay(enable?: boolean): this`

<sub>[Source](https://github.com/holepunchto/bare-tcp/blob/v2.5.1/index.d.ts#L68)</sub>

Enable or disable Nagle's algorithm. When `enable` is `true` (the default), data is sent immediately without buffering.

**Parameters**

- `enable` (`boolean`, optional)

#### `setTimeout(ms: number, ontimeout?: () => void): this`

<sub>[Source](https://github.com/holepunchto/bare-tcp/blob/v2.5.1/index.d.ts#L70)</sub>

Set a timeout in milliseconds. When the socket is idle for `ms` milliseconds, a `timeout` event is emitted. Pass `0` to disable the timeout.

**Parameters**

- `ms` (`number`)
- `ontimeout` (`() => void`, optional)

#### `timeout: number`

<sub>[Source](https://github.com/holepunchto/bare-tcp/blob/v2.5.1/index.d.ts#L47)</sub>

The timeout in milliseconds, or `undefined` if no timeout is set.

#### `TCPSocket.unref(): this`

<sub>[Source](https://github.com/holepunchto/bare-tcp/blob/v2.5.1/index.d.ts#L73)</sub>

Unref the socket, allowing the process to exit.

### TCPServer

#### `new TCPServer(opts?: TCPServerOptions, onconnection?: () => void)`

<sub>[Source](https://github.com/holepunchto/bare-tcp/blob/v2.5.1/index.d.ts#L142)</sub>

Overloads:

```ts
new TCPServer(opts?: TCPServerOptions, onconnection?: () => void)
new TCPServer(onconnection: () => void)
```

**Parameters**

- `opts` ([`TCPServerOptions`](#tcpserveroptions), optional)
- `onconnection` (`() => void`, optional)

#### `address(): TCPSocketAddress`

<sub>[Source](https://github.com/holepunchto/bare-tcp/blob/v2.5.1/index.d.ts#L121)</sub>

#### `close(onclose?: (err?: Error) => void): this`

<sub>[Source](https://github.com/holepunchto/bare-tcp/blob/v2.5.1/index.d.ts#L135)</sub>

Close the server. No new connections will be accepted. The server emits `close` after all existing connections have ended.

**Parameters**

- `onclose` (`(err?: Error) => void`, optional)

#### `closing: boolean`

<sub>[Source](https://github.com/holepunchto/bare-tcp/blob/v2.5.1/index.d.ts#L117)</sub>

Whether the server is closing.

#### `connections: Set<TCPSocket>`

<sub>[Source](https://github.com/holepunchto/bare-tcp/blob/v2.5.1/index.d.ts#L118)</sub>

A `Set` of active connections.

#### `listen`

```ts
listen(port?: number, host?: string, backlog?: number, opts?: TCPServerListenOptions, onlistening?: () => void): this
```

<sub>[Source](https://github.com/holepunchto/bare-tcp/blob/v2.5.1/index.d.ts#L123)</sub>

Start listening for connections on `port` and `host`. If `port` is `0`, an available port is assigned. If `host` is not provided, it defaults to `'localhost'`. `backlog` defaults to `511`.

**Parameters**

- `port` (`number`, optional)
- `host` (`string`, optional)
- `backlog` (`number`, optional)
- `opts` ([`TCPServerListenOptions`](#tcpserverlistenoptions), optional)
- `onlistening` (`() => void`, optional)

#### `listening: boolean`

<sub>[Source](https://github.com/holepunchto/bare-tcp/blob/v2.5.1/index.d.ts#L116)</sub>

Whether the server is listening.

#### `maxConnections: number`

<sub>[Source](https://github.com/holepunchto/bare-tcp/blob/v2.5.1/index.d.ts#L119)</sub>

#### `TCPServer.ref(): this`

<sub>[Source](https://github.com/holepunchto/bare-tcp/blob/v2.5.1/index.d.ts#L137)</sub>

Ref the socket, preventing the process from exiting.

#### `TCPServer.unref(): this`

<sub>[Source](https://github.com/holepunchto/bare-tcp/blob/v2.5.1/index.d.ts#L138)</sub>

Unref the socket, allowing the process to exit.

### Functions

#### `createConnection`

```ts
createConnection(port: number, host?: string, opts?: TCPSocketOptions & TCPSocketConnectOptions, onconnect?: () => void): TCPSocket
```

<sub>[Source](https://github.com/holepunchto/bare-tcp/blob/v2.5.1/index.d.ts#L146)</sub>

Create a new socket and connect it to `port` on `host`. Shorthand for `new tcp.Socket(options).connect(port, host, options, onconnect)`.

**Parameters**

- `port` (`number`)
- `host` (`string`, optional)
- `opts` (`TCPSocketOptions & TCPSocketConnectOptions`, optional)
- `onconnect` (`() => void`, optional)

#### `createServer(opts?: TCPServerOptions, onconnection?: () => void): TCPServer`

<sub>[Source](https://github.com/holepunchto/bare-tcp/blob/v2.5.1/index.d.ts#L164)</sub>

Create a new TCP server. `server` extends \<https://github.com/holepunchto/bare-events>.

Overloads:

```ts
createServer(opts?: TCPServerOptions, onconnection?: () => void): TCPServer
createServer(onconnection: () => void): TCPServer
```

**Parameters**

- `opts` ([`TCPServerOptions`](#tcpserveroptions), optional)
- `onconnection` (`() => void`, optional)

#### `socketpair(): [first: number, second: number]`

<sub>[Source](https://github.com/holepunchto/bare-tcp/blob/v2.5.1/index.d.ts#L174)</sub>

#### `isIP(host: string): IPFamily | 0`

<sub>[Source](https://github.com/holepunchto/bare-tcp/blob/v2.5.1/index.d.ts#L168)</sub>

Returns `4` if `host` is an IPv4 address, `6` if it is an IPv6 address, or `0` otherwise.

**Parameters**

- `host` (`string`)

#### `isIPv4(host: string): boolean`

<sub>[Source](https://github.com/holepunchto/bare-tcp/blob/v2.5.1/index.d.ts#L170)</sub>

Returns `true` if `host` is an IPv4 address.

**Parameters**

- `host` (`string`)

#### `isIPv6(host: string): boolean`

<sub>[Source](https://github.com/holepunchto/bare-tcp/blob/v2.5.1/index.d.ts#L172)</sub>

Returns `true` if `host` is an IPv6 address.

**Parameters**

- `host` (`string`)

### Constants and variables

#### `constants`

```ts
constants: {
  state: {
    CONNECTING: number
    CONNECTED: number
    BINDING: number
    BOUND: number
    READING: number
    CLOSING: number
    UNREFED: number
  }
}
```

<sub>[Source](https://github.com/holepunchto/bare-tcp/blob/v2.5.1/lib/constants.d.ts#L1)</sub>

Object containing internal state constants.

### Types

#### `TCPSocketAddress`

```ts
interface TCPSocketAddress {
  address: string
  family: `IPv${IPFamily}`
  port: number
}
```

<sub>[Source](https://github.com/holepunchto/bare-tcp/blob/v2.5.1/index.d.ts#L16)</sub>

#### `TCPSocketEvents`

```ts
interface TCPSocketEvents extends DuplexEvents {
  connect: []
  lookup: [err: Error | null, address: string | null, family: IPFamily | 0, host: string]
  timeout: [ms: number]
}
```

<sub>[Source](https://github.com/holepunchto/bare-tcp/blob/v2.5.1/index.d.ts#L22)</sub>

#### `TCPSocketOptions`

```ts
interface TCPSocketOptions {
  allowHalfOpen?: boolean
  eagerOpen?: boolean
  readBufferSize?: number
}
```

<sub>[Source](https://github.com/holepunchto/bare-tcp/blob/v2.5.1/index.d.ts#L28)</sub>

#### `TCPSocketConnectOptions`

```ts
interface TCPSocketConnectOptions extends LookupOptions {
  lookup?: DNSLookup
  host?: string
  keepAlive?: boolean
  keepAliveInitialDelay?: boolean
  noDelay?: boolean
  port?: number
  timeout?: number
}
```

<sub>[Source](https://github.com/holepunchto/bare-tcp/blob/v2.5.1/index.d.ts#L34)</sub>

#### `TCPServerDropInfo`

```ts
interface TCPServerDropInfo {
  localAddress?: string
  localPort?: number
  localFamily?: string
  remoteAddress?: string
  remotePort?: number
  remoteFamily?: string
}
```

<sub>[Source](https://github.com/holepunchto/bare-tcp/blob/v2.5.1/index.d.ts#L80)</sub>

#### `TCPServerEvents`

```ts
interface TCPServerEvents extends EventMap {
  close: []
  connection: [socket: TCPSocket]
  drop: [info: TCPServerDropInfo]
  error: [err: Error]
  listening: []
  lookup: [err: Error | null, address: string | null, family: IPFamily | 0, host: string]
}
```

<sub>[Source](https://github.com/holepunchto/bare-tcp/blob/v2.5.1/index.d.ts#L89)</sub>

#### `TCPServerOptions`

```ts
interface TCPServerOptions {
  allowHalfOpen?: number
  keepAlive?: boolean
  keepAliveInitialDelay?: boolean
  maxConnections?: number
  noDelay?: boolean
  pauseOnConnect?: boolean
  readBufferSize?: number
}
```

<sub>[Source](https://github.com/holepunchto/bare-tcp/blob/v2.5.1/index.d.ts#L98)</sub>

#### `TCPServerListenOptions`

```ts
interface TCPServerListenOptions extends LookupOptions {
  lookup?: DNSLookup
  backlog?: number
  host?: string
  port?: number
}
```

<sub>[Source](https://github.com/holepunchto/bare-tcp/blob/v2.5.1/index.d.ts#L108)</sub>

### Classes

#### `TCPError`

<sub>[Source](https://github.com/holepunchto/bare-tcp/blob/v2.5.1/lib/errors.d.ts#L1)</sub>

```ts
class TCPError {
  code: string
}
```

## See also

- [Bare modules](/reference/modules/bare-modules) — the full `bare-*` catalog.
- [Bare runtime API](/reference/bare/runtime) — the runtime these modules extend.
