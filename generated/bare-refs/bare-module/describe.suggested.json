{
  "describe": {
    "Module.protocol": "The default `ModuleProtocol` class for resolving, reading and loading modules. See [Protocols](#protocols) for usage.",
    "cache": "The global cache of loaded modules.",
    "Module.resolve": "Resolve the module `specifier` relative to the `parentURL`. `specifier` is a string and `parentURL` is a WHATWG `URL`.",
    "Module.asset": "Get the asset URL by resolving `specifier` relative to `parentURL`. `specifier` is a string and `parentURL` is a WHATWG `URL`.",
    "Module.load": "Load a module with the provided `url`. `url` is a WHATWG `URL`. If provided, the `source` will be passed to the matching `extension` for the `url`.",
    "url": "The WHATWG `URL` identifier of the module.",
    "filename": "The file portion of `module.url`.",
    "dirname": "The directory portion of `module.url`.",
    "type": "The type of the module. See [`Module.constants.types`](#module.constants.types) for possible values.",
    "defaultType": "The assumed type of a module without a `type` using an ambiguous extension, such as `.js`. See [`Module.constants.types`](#module.constants.types) for possible values.",
    "main": "The module representing the entry script where the program was launched.",
    "exports": "The exports from the module.",
    "imports": "The import map when the module was loaded.",
    "resolutions": "A map of preresolved imports with keys being serialized parent URLs and values being `\"imports\"` maps.",
    "builtins": "A map of builtin module specifiers mapped to the loaded module.",
    "conditions": "An array of conditions used to resolve dependencies while loading the module. See [Conditional Exports](#conditional-exports) for possible values.",
    "addon": "Also used to import modules but specifically loads only addon modules. `specifier` is resolved relative to `parentURL` using the [addon resolution](https://github.com/holepunchto/bare-addon-resolve#algorithm) algorithm.",
    "host": "Returns the string representation of the platform and architecture used when resolving addons with the pattern `<platform>-<arch>[-<environment>]`. Returns the same value as `Bare.Addon.host`.",
    "Module.createRequire": "Options include:"
  },
  "unmatchedHeadings": [
    "states",
    "types",
    "require",
    "Module.Protocol"
  ],
  "undocumentedSymbols": [
    "Module",
    "Attributes",
    "Cache",
    "Options",
    "LoadOptions",
    "ResolveOptions",
    "ModuleProtocol",
    "CreateRequireOptions",
    "RequireOptions",
    "RequireAddon",
    "Require"
  ]
}
