---
title: "bare-abort-controller"
description: "Abort controller support for Bare"
docType: reference
schemaType: APIReference
---

<mark style={{ backgroundColor: '#7dde9a', padding: '2px 8px', borderRadius: '4px', fontSize: '0.85em', fontWeight: 500 }}>stable</mark>

[`bare-abort-controller`](https://github.com/holepunchto/bare-abort-controller) — Abort controller support for Bare.

```sh
npm i bare-abort-controller
```

## Usage

```js
const controller = new AbortController()

const signal = controller.signal

signal.addEventListener('abort', (event) => {
  console.log(event)
})

controller.abort(new Error('Operation aborted'))
```

## API

### AbortController

#### `new AbortController()`

<sub>[Source](https://github.com/holepunchto/bare-abort-controller/blob/v1.1.2/index.d.ts#L27)</sub>

#### `abort(reason: any): void`

<sub>[Source](https://github.com/holepunchto/bare-abort-controller/blob/v1.1.2/index.d.ts#L23)</sub>

**Parameters**

- `reason` (`any`)

#### `signal: AbortSignal`

<sub>[Source](https://github.com/holepunchto/bare-abort-controller/blob/v1.1.2/index.d.ts#L21)</sub>

### AbortSignal

#### `aborted: boolean`

<sub>[Source](https://github.com/holepunchto/bare-abort-controller/blob/v1.1.2/index.d.ts#L4)</sub>

#### `AbortSignal.abort(reason: any): AbortSignal`

<sub>[Source](https://github.com/holepunchto/bare-abort-controller/blob/v1.1.2/index.d.ts#L15)</sub>

**Parameters**

- `reason` (`any`)

#### `AbortSignal.any(signals: AbortSignal[]): AbortSignal`

<sub>[Source](https://github.com/holepunchto/bare-abort-controller/blob/v1.1.2/index.d.ts#L17)</sub>

**Parameters**

- `signals` (`AbortSignal[]`)

#### `AbortSignal.timeout(ms: number): AbortSignal`

<sub>[Source](https://github.com/holepunchto/bare-abort-controller/blob/v1.1.2/index.d.ts#L16)</sub>

**Parameters**

- `ms` (`number`)

#### `reason: any`

<sub>[Source](https://github.com/holepunchto/bare-abort-controller/blob/v1.1.2/index.d.ts#L5)</sub>

#### `throwIfAborted(): void`

<sub>[Source](https://github.com/holepunchto/bare-abort-controller/blob/v1.1.2/index.d.ts#L7)</sub>

#### `toJSON(): { aborted: boolean; reason: any }`

<sub>[Source](https://github.com/holepunchto/bare-abort-controller/blob/v1.1.2/index.d.ts#L9)</sub>

## `bare-abort-controller/global`

### Types

#### `AbortControllerConstructor`

```ts
type AbortControllerConstructor = typeof abort.AbortController
```

<sub>[Source](https://github.com/holepunchto/bare-abort-controller/blob/v1.1.2/global.d.ts#L3)</sub>

#### `AbortSignalConstructor`

```ts
type AbortSignalConstructor = typeof abort.AbortSignal
```

<sub>[Source](https://github.com/holepunchto/bare-abort-controller/blob/v1.1.2/global.d.ts#L4)</sub>

## See also

- [Bare modules](/reference/modules/bare-modules) — the full `bare-*` catalog.
- [Bare runtime API](/reference/bare/runtime) — the runtime these modules extend.
