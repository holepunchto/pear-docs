---
title: "bare-tls"
description: "Transport Layer Security (TLS) streams for JavaScript"
docType: reference
schemaType: APIReference
---

<mark style={{ backgroundColor: '#7dde9a', padding: '2px 8px', borderRadius: '4px', fontSize: '0.85em', fontWeight: 500 }}>stable</mark>

[`bare-tls`](https://github.com/holepunchto/bare-tls) — Transport Layer Security (TLS) streams for JavaScript. It is a native addon and requires Bare `>=1.7.0`.

Mirrors the Node.js [`tls`](https://nodejs.org/api/tls.html) module.

```sh
npm i bare-tls
```

## Usage

```js
const tls = require('bare-tls')
const fs = require('bare-fs')

const server = tls.createServer(
  {
    cert: fs.readFileSync('cert.pem'),
    key: fs.readFileSync('key.pem')
  },
  (socket) => {
    socket.on('data', (data) => socket.end('pong')).on('close', () => server.close())
  }
)

server.listen(8443)

const client = tls.connect({ port: 8443, host: 'localhost' })

client.on('data', (data) => console.log(data)).end('ping')
```

## API

### errors

#### `errors.from(err: Error): TLSError`

<sub>[Source](https://github.com/holepunchto/bare-tls/blob/v3.1.7/lib/errors.d.ts#L2)</sub>

**Parameters**

| Parameter | Type | Default | Description |
| --- | --- | --- | --- |
| `err` | `Error` | — | — |

### TLSSocket

#### `new TLSSocket(socket: Duplex, opts?: TLSSocketOptions)`

<sub>[Source](https://github.com/holepunchto/bare-tls/blob/v3.1.7/index.d.ts#L32)</sub>

**Parameters**

| Parameter | Type | Default | Description |
| --- | --- | --- | --- |
| `socket` | `Duplex` | — | — |
| `opts?` | [`TLSSocketOptions`](#tlssocketoptions) | — | — |

#### `alpnProtocol: string | null`

<sub>[Source](https://github.com/holepunchto/bare-tls/blob/v3.1.7/index.d.ts#L28)</sub>

The negotiated ALPN protocol as a string, or `null` if no protocol was negotiated.

#### `encrypted: true`

<sub>[Source](https://github.com/holepunchto/bare-tls/blob/v3.1.7/index.d.ts#L27)</sub>

Always `true`.

#### `socket: Duplex`

<sub>[Source](https://github.com/holepunchto/bare-tls/blob/v3.1.7/index.d.ts#L26)</sub>

The underlying duplex stream.

### Functions

#### `createServer(opts?: TLSSocketOptions, onconnection?: (socket: TLSSocket) => void): TLSNetServer`

<sub>[Source](https://github.com/holepunchto/bare-tls/blob/v3.1.7/index.d.ts#L48)</sub>

Creates a TLS server that listens for TCP connections and wraps them with TLS. Incoming connections are emitted as `'connection'` events with a `tls.Socket` instance. Options are the same as `tls.Socket`, plus any options supported by \<https://github.com/holepunchto/bare-net>.

**Parameters**

| Parameter | Type | Default | Description |
| --- | --- | --- | --- |
| `opts?` | [`TLSSocketOptions`](#tlssocketoptions) | — | — |
| `onconnection?` | `(socket: TLSSocket) => void` | — | — |

#### `createConnection`

```ts
createConnection(opts: TLSSocketOptions & { port: number; host?: string }, onconnect?: () => void): TLSSocket
```

<sub>[Source](https://github.com/holepunchto/bare-tls/blob/v3.1.7/index.d.ts#L53)</sub>

Creates a TCP connection and wraps it with TLS. `opts` are passed to both the underlying TCP socket and `TLSSocket`. At minimum, `port` must be specified.

**Parameters**

| Parameter | Type | Default | Description |
| --- | --- | --- | --- |
| `opts` | `TLSSocketOptions & { port: number; host?: string }` | — | — |
| `onconnect?` | `() => void` | — | — |

### Constants and variables

#### `constants`

```ts
constants: {
  state: {
    CONNECTED: number
    ATTACHED: number
  }
}
```

<sub>[Source](https://github.com/holepunchto/bare-tls/blob/v3.1.7/lib/constants.d.ts#L1)</sub>

### Types

#### `TLSSocketEvents`

```ts
interface TLSSocketEvents extends DuplexEvents {
  connect: []
}
```

<sub>[Source](https://github.com/holepunchto/bare-tls/blob/v3.1.7/index.d.ts#L8)</sub>

#### `TLSSocketOptions`

```ts
interface TLSSocketOptions {
  isServer?: boolean
  cert?: ArrayBufferView
  key?: ArrayBufferView
  host?: string
  rejectUnauthorized?: boolean
  ca?: ArrayBufferView
  alpnProtocols?: string[]
  eagerOpen?: boolean
  allowHalfOpen?: boolean
  readBufferSize?: number
}
```

<sub>[Source](https://github.com/holepunchto/bare-tls/blob/v3.1.7/index.d.ts#L12)</sub>

#### `TLSNetServerEvents`

```ts
interface TLSNetServerEvents {
  listening: []
  connection: [socket: TLSSocket]
  error: [err: Error]
  close: []
}
```

<sub>[Source](https://github.com/holepunchto/bare-tls/blob/v3.1.7/index.d.ts#L37)</sub>

#### `TLSNetServer`

```ts
interface TLSNetServer extends EventEmitter<TLSNetServerEvents> {
  readonly listening: boolean
}
```

<sub>[Source](https://github.com/holepunchto/bare-tls/blob/v3.1.7/index.d.ts#L44)</sub>

## `bare-tls/constants`

### Constants and variables

#### `constants.constants`

```ts
constants: {
  state: {
    CONNECTED: number
    ATTACHED: number
  }
}
```

<sub>[Source](https://github.com/holepunchto/bare-tls/blob/v3.1.7/lib/constants.d.ts#L1)</sub>

## `bare-tls/errors`

### TLSError

#### `TLSError.from(err: Error): TLSError`

<sub>[Source](https://github.com/holepunchto/bare-tls/blob/v3.1.7/lib/errors.d.ts#L2)</sub>

**Parameters**

| Parameter | Type | Default | Description |
| --- | --- | --- | --- |
| `err` | `Error` | — | — |

## See also

- [Bare modules](/reference/modules/bare-modules) — the full `bare-*` catalog.
- [Bare runtime API](/reference/bare/runtime) — the runtime these modules extend.
