---
title: "bare-https"
description: "HTTPS library for JavaScript"
docType: reference
schemaType: APIReference
---

<mark style={{ backgroundColor: '#7dde9a', padding: '2px 8px', borderRadius: '4px', fontSize: '0.85em', fontWeight: 500 }}>stable</mark>

[`bare-https`](https://github.com/holepunchto/bare-https) — HTTPS library for JavaScript.

Mirrors the Node.js [`https`](https://nodejs.org/api/https.html) module.

```sh
npm i bare-https
```

## Usage

```js
const https = require('bare-https')

const options = {
  cert: fs.readFileSync('test/fixtures/cert.crt'),
  key: fs.readFileSync('test/fixtures/cert.key')
}

const server = https.createServer(options, (req, res) => {
  res.statusCode = 200
  res.setHeader('Content-Length', 10)
  res.write('hello world!')
  res.end()
})

server.listen(0, () => {
  const { port } = server.address()
  console.log('server is bound on', port)

  const client = https.request({ port }, (res) => {
    res.on('data', (data) => console.log(data.toString()))
  })
  client.end()
})
```

## API

### HTTPSAgent

#### `createConnection(opts?: HTTPSSocketOptions): HTTPSSocket`

<sub>[Source](https://github.com/holepunchto/bare-https/blob/v3.0.0/index.d.ts#L26)</sub>

**Parameters**

- `opts` ([`HTTPSSocketOptions`](#httpssocketoptions), optional)

#### `HTTPSAgent.global: HTTPSAgent`

<sub>[Source](https://github.com/holepunchto/bare-https/blob/v3.0.0/index.d.ts#L32)</sub>

### HTTPSServer

#### `HTTPSServer`

```ts
new HTTPSServer(opts?: HTTPSServerOptions, onrequest?: (req: HTTPIncomingMessage, res: HTTPServerResponse) => void)
```

<sub>[Source](https://github.com/holepunchto/bare-https/blob/v3.0.0/index.d.ts#L42)</sub>

**Parameters**

- `opts` (`HTTPSServerOptions`, optional)
- `onrequest` (`(req: HTTPIncomingMessage, res: HTTPServerResponse) => void`, optional)

### HTTPSClientRequest

#### `new HTTPSClientRequest(opts?: HTTPSClientRequestOptions, onresponse?: () => void)`

<sub>[Source](https://github.com/holepunchto/bare-https/blob/v3.0.0/index.d.ts#L59)</sub>

Overloads:

```ts
new HTTPSClientRequest(opts?: HTTPSClientRequestOptions, onresponse?: () => void)
new HTTPSClientRequest(onresponse: () => void)
```

**Parameters**

- `opts` (`HTTPSClientRequestOptions`, optional)
- `onresponse` (`() => void`, optional)

### Functions

#### `createServer`

```ts
createServer(opts?: HTTPSServerOptions, onrequest?: (req: HTTPIncomingMessage, res: HTTPServerResponse) => void): HTTPSServer
```

<sub>[Source](https://github.com/holepunchto/bare-https/blob/v3.0.0/index.d.ts#L66)</sub>

**Parameters**

- `opts` (`HTTPSServerOptions`, optional)
- `onrequest` (`(req: HTTPIncomingMessage, res: HTTPServerResponse) => void`, optional)

#### `request`

```ts
request(url: URL | string, opts?: HTTPSClientRequestOptions, onresponse?: (res: HTTPIncomingMessage) => void): HTTPSClientRequest
```

<sub>[Source](https://github.com/holepunchto/bare-https/blob/v3.0.0/index.d.ts#L75)</sub>

**Parameters**

- `url` (`URL` | `string`)
- `opts` (`HTTPSClientRequestOptions`, optional)
- `onresponse` (`(res: HTTPIncomingMessage) => void`, optional)

### Constants and variables

#### `globalAgent: HTTPSAgent`

<sub>[Source](https://github.com/holepunchto/bare-https/blob/v3.0.0/index.d.ts#L35)</sub>

### Types

#### `HTTPSSocketEvents`

```ts
interface HTTPSSocketEvents extends TLSSocketEvents, TCPSocketEvents {}
```

<sub>[Source](https://github.com/holepunchto/bare-https/blob/v3.0.0/index.d.ts#L13)</sub>

#### `HTTPSSocketOptions`

```ts
interface HTTPSSocketOptions
  extends TLSSocketOptions, TCPSocketOptions, TCPSocketConnectOptions {}
```

<sub>[Source](https://github.com/holepunchto/bare-https/blob/v3.0.0/index.d.ts#L15)</sub>

### Classes

#### `HTTPSSocket`

<sub>[Source](https://github.com/holepunchto/bare-https/blob/v3.0.0/index.d.ts#L18)</sub>

```ts
class HTTPSSocket {
}
```

## See also

- [Bare modules](/reference/modules/bare-modules) — the full `bare-*` catalog.
- [Bare runtime API](/reference/bare/runtime) — the runtime these modules extend.
