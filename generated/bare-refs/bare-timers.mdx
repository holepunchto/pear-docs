---
title: "bare-timers"
description: "Native timers for Javascript"
docType: reference
schemaType: APIReference
---

<mark style={{ backgroundColor: '#7dde9a', padding: '2px 8px', borderRadius: '4px', fontSize: '0.85em', fontWeight: 500 }}>stable</mark>

[`bare-timers`](https://github.com/holepunchto/bare-timers) — Native timers for Javascript. It is a native addon and requires Bare `>=1.7.0`.

Mirrors the Node.js [`timers`](https://nodejs.org/api/timers.html) module.

```sh
npm i bare-timers
```

## Usage

```js
const { setTimeout, clearTimeout } = require('bare-timers')
```

## API

### Functions

#### `setTimeout`

```ts
setTimeout<T extends unknown[]>(callback: (...args: T) => unknown, delay: number, ...args: T): Timeout
```

<sub>[Source](https://github.com/holepunchto/bare-timers/blob/v3.2.1/index.d.ts#L13)</sub>

**Parameters**

- `callback` (`(...args: T) => unknown`)
- `delay` (`number`)
- `args` (`T`)

#### `clearTimeout(timer: Timeout): void`

<sub>[Source](https://github.com/holepunchto/bare-timers/blob/v3.2.1/index.d.ts#L19)</sub>

**Parameters**

- `timer` ([`Timeout`](#timeout))

#### `setInterval`

```ts
setInterval<T extends unknown[]>(callback: (...args: T) => unknown, delay: number, ...args: T): Timeout
```

<sub>[Source](https://github.com/holepunchto/bare-timers/blob/v3.2.1/index.d.ts#L21)</sub>

**Parameters**

- `callback` (`(...args: T) => unknown`)
- `delay` (`number`)
- `args` (`T`)

#### `clearInterval(timer: Timeout): void`

<sub>[Source](https://github.com/holepunchto/bare-timers/blob/v3.2.1/index.d.ts#L27)</sub>

**Parameters**

- `timer` ([`Timeout`](#timeout))

#### `setImmediate<T extends unknown[]>(callback: (...args: T) => unknown, ...args: T): Immediate`

<sub>[Source](https://github.com/holepunchto/bare-timers/blob/v3.2.1/index.d.ts#L29)</sub>

**Parameters**

- `callback` (`(...args: T) => unknown`)
- `args` (`T`)

#### `clearImmediate(immediate: Immediate): void`

<sub>[Source](https://github.com/holepunchto/bare-timers/blob/v3.2.1/index.d.ts#L34)</sub>

**Parameters**

- `immediate` ([`Immediate`](#immediate))

### Types

#### `Task`

```ts
interface Task {
  ref(): this
  unref(): this
  hasRef(): boolean
}
```

<sub>[Source](https://github.com/holepunchto/bare-timers/blob/v3.2.1/index.d.ts#L1)</sub>

#### `Timeout`

```ts
interface Timeout extends Task {
  refresh(): this
}
```

<sub>[Source](https://github.com/holepunchto/bare-timers/blob/v3.2.1/index.d.ts#L7)</sub>

#### `Immediate`

```ts
interface Immediate extends Task {}
```

<sub>[Source](https://github.com/holepunchto/bare-timers/blob/v3.2.1/index.d.ts#L11)</sub>

## `bare-timers/promises`

### Functions

#### `setTimeout<T>(delay?: number, value?: T, options?: TimeoutOptions): Promise<T>`

<sub>[Source](https://github.com/holepunchto/bare-timers/blob/v3.2.1/promises.d.ts#L12)</sub>

**Parameters**

- `delay` (`number`, optional)
- `value` (`T`, optional)
- `options` (`TimeoutOptions`, optional)

#### `setInterval<T>(delay?: number, value?: T, options?: TimeoutOptions): AsyncGenerator<T>`

<sub>[Source](https://github.com/holepunchto/bare-timers/blob/v3.2.1/promises.d.ts#L14)</sub>

**Parameters**

- `delay` (`number`, optional)
- `value` (`T`, optional)
- `options` (`TimeoutOptions`, optional)

#### `setImmediate<T>(value?: T, options?: ImmediateOptions): Promsie<T>`

<sub>[Source](https://github.com/holepunchto/bare-timers/blob/v3.2.1/promises.d.ts#L20)</sub>

**Parameters**

- `value` (`T`, optional)
- `options` (`ImmediateOptions`, optional)

### Types

#### `TaskOptions`

```ts
interface TaskOptions {
  ref?: boolean
  signal?: AbortSignal
}
```

<sub>[Source](https://github.com/holepunchto/bare-timers/blob/v3.2.1/promises.d.ts#L3)</sub>

#### `TimeoutOptions`

```ts
interface TimeoutOptions extends TaskOptions {}
```

<sub>[Source](https://github.com/holepunchto/bare-timers/blob/v3.2.1/promises.d.ts#L8)</sub>

#### `ImmediateOptions`

```ts
interface ImmediateOptions extends TaskOptions {}
```

<sub>[Source](https://github.com/holepunchto/bare-timers/blob/v3.2.1/promises.d.ts#L10)</sub>

## See also

- [Bare modules](/reference/modules/bare-modules) — the full `bare-*` catalog.
- [Bare runtime API](/reference/bare/runtime) — the runtime these modules extend.
