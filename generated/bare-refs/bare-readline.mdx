---
title: "bare-readline"
description: "Line editing for interactive CLIs with command history"
docType: reference
schemaType: APIReference
---

<mark style={{ backgroundColor: '#7dde9a', padding: '2px 8px', borderRadius: '4px', fontSize: '0.85em', fontWeight: 500 }}>stable</mark>

[`bare-readline`](https://github.com/holepunchto/bare-readline) — Line editing for interactive CLIs with command history.

Mirrors the Node.js [`readline`](https://nodejs.org/api/readline.html) module.

```sh
npm i bare-readline
```

## Usage

```js
const readline = require('bare-readline')

const rl = readline.createInterface({
  input: stream,
  output: stream
})

rl.on('data', (line) => {
  console.log(line)
  rl.prompt()
}).prompt()
```

## API

### Readline

#### `new Readline(opts?: ReadlineOptions)`

<sub>[Source](https://github.com/holepunchto/bare-readline/blob/v1.3.1/index.d.ts#L31)</sub>

Create a `Readline` interface over an input and output stream.

**Parameters**

- `opts` ([`ReadlineOptions`](#readlineoptions), optional)

#### `clearLine(): string`

<sub>[Source](https://github.com/holepunchto/bare-readline/blob/v1.3.1/index.d.ts#L27)</sub>

Clear the current input line from the output and reset the line buffer and cursor, returning the cleared line.

#### `close(): void`

<sub>[Source](https://github.com/holepunchto/bare-readline/blob/v1.3.1/index.d.ts#L25)</sub>

Stop listening for input and end the stream.

#### `cursor: number`

<sub>[Source](https://github.com/holepunchto/bare-readline/blob/v1.3.1/index.d.ts#L17)</sub>

The current cursor position within the input line.

#### `getPrompt(): string`

<sub>[Source](https://github.com/holepunchto/bare-readline/blob/v1.3.1/index.d.ts#L24)</sub>

Returns the current prompt string.

#### `input: Readable`

<sub>[Source](https://github.com/holepunchto/bare-readline/blob/v1.3.1/index.d.ts#L19)</sub>

The readable stream that keystrokes are read from.

#### `line: string`

<sub>[Source](https://github.com/holepunchto/bare-readline/blob/v1.3.1/index.d.ts#L18)</sub>

The current, in-progress input line.

#### `output: Writable`

<sub>[Source](https://github.com/holepunchto/bare-readline/blob/v1.3.1/index.d.ts#L20)</sub>

The writable stream that the prompt and line edits are rendered to.

#### `prompt(): void`

<sub>[Source](https://github.com/holepunchto/bare-readline/blob/v1.3.1/index.d.ts#L22)</sub>

Render the prompt and current input line to the output.

#### `Readline.constants: { EOL: string }`

<sub>[Source](https://github.com/holepunchto/bare-readline/blob/v1.3.1/index.d.ts#L37)</sub>

Constants used by `Readline`, including the end-of-line sequence.

#### `Readline.createInterface(opts?: ReadlineOptions): Readline`

<sub>[Source](https://github.com/holepunchto/bare-readline/blob/v1.3.1/index.d.ts#L35)</sub>

Create a `Readline` interface over an input and output stream.

**Parameters**

- `opts` ([`ReadlineOptions`](#readlineoptions), optional)

#### `setPrompt(prompt: string): void`

<sub>[Source](https://github.com/holepunchto/bare-readline/blob/v1.3.1/index.d.ts#L23)</sub>

Set the prompt string written before the input line.

**Parameters**

- `prompt` (`string`)

#### `write(data: string): void`

<sub>[Source](https://github.com/holepunchto/bare-readline/blob/v1.3.1/index.d.ts#L26)</sub>

Write data directly to the output stream.

**Parameters**

- `data` (`string`)

### Types

#### `ReadlineOptions`

```ts
interface ReadlineOptions {
  crlfDelay?: number
  input?: Readable
  output?: Writable
  prompt?: string
}
```

<sub>[Source](https://github.com/holepunchto/bare-readline/blob/v1.3.1/index.d.ts#L3)</sub>

Options accepted when constructing a `Readline` interface.

#### `ReadlineEvents`

```ts
interface ReadlineEvents extends ReadableEvents {
  data: [line: string]
  history: [history: string[]]
  line: [line: string]
}
```

<sub>[Source](https://github.com/holepunchto/bare-readline/blob/v1.3.1/index.d.ts#L10)</sub>

Events emitted by a `Readline` interface.

## See also

- [Bare modules](/reference/modules/bare-modules) — the full `bare-*` catalog.
- [Bare runtime API](/reference/bare/runtime) — the runtime these modules extend.
