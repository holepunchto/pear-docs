---
title: "bare-semver"
description: "Minimal semantic versioning library for Bare"
docType: reference
schemaType: APIReference
---

<mark style={{ backgroundColor: '#7dde9a', padding: '2px 8px', borderRadius: '4px', fontSize: '0.85em', fontWeight: 500 }}>stable</mark>

[`bare-semver`](https://github.com/holepunchto/bare-semver) — Minimal semantic versioning library for Bare.

```sh
npm i bare-semver
```

## Usage

```js
const semver = require('bare-semver')

const version = semver.Version.parse('1.2.3-alpha.1+build.42')

console.log(version.major) // 1
console.log(version.minor) // 2
console.log(version.patch) // 3

const satisfied = semver.satisfies('1.2.3', '>=1.0.0 <2.0.0')

console.log(satisfied) // true
```

## API

### errors

#### `errors.INVALID_RANGE(msg: string, fn?: Function): SemVerError`

<sub>[Source](https://github.com/holepunchto/bare-semver/blob/v1.1.0/index.d.ts#L6)</sub>

**Parameters**

| Parameter | Type | Default | Description |
| --- | --- | --- | --- |
| `msg` | `string` | — | — |
| `fn?` | `Function` | — | — |

#### `errors.INVALID_VERSION(msg: string, fn?: Function): SemVerError`

<sub>[Source](https://github.com/holepunchto/bare-semver/blob/v1.1.0/index.d.ts#L4)</sub>

**Parameters**

| Parameter | Type | Default | Description |
| --- | --- | --- | --- |
| `msg` | `string` | — | — |
| `fn?` | `Function` | — | — |

### Version

#### `Version`

```ts
new Version(major: number, minor: number, patch: number, opts?: { prerelease?: string[]; build?: string[] })
```

<sub>[Source](https://github.com/holepunchto/bare-semver/blob/v1.1.0/index.d.ts#L14)</sub>

Create a new version with the given `major`, `minor`, and `patch` components.

**Parameters**

| Parameter | Type | Default | Description |
| --- | --- | --- | --- |
| `major` | `number` | — | — |
| `minor` | `number` | — | — |
| `patch` | `number` | — | — |
| `opts?` | `{ prerelease?: string[]; build?: string[] }` | — | — |

#### `build: string[]`

<sub>[Source](https://github.com/holepunchto/bare-semver/blob/v1.1.0/index.d.ts#L25)</sub>

An array of build metadata tags.

#### `compare(version: Version): boolean`

<sub>[Source](https://github.com/holepunchto/bare-semver/blob/v1.1.0/index.d.ts#L27)</sub>

Compare `version` with `other`, returning `1` if `version` is greater, `-1` if less, or `0` if equal. Comparison follows the Semantic Versioning 2.0.0 specification, including prerelease precedence rules.

**Parameters**

| Parameter | Type | Default | Description |
| --- | --- | --- | --- |
| `version` | `Version` | — | — |

#### `major: number`

<sub>[Source](https://github.com/holepunchto/bare-semver/blob/v1.1.0/index.d.ts#L21)</sub>

The major version number.

#### `minor: number`

<sub>[Source](https://github.com/holepunchto/bare-semver/blob/v1.1.0/index.d.ts#L22)</sub>

The minor version number.

#### `patch: number`

<sub>[Source](https://github.com/holepunchto/bare-semver/blob/v1.1.0/index.d.ts#L23)</sub>

The patch version number.

#### `prerelease: string[]`

<sub>[Source](https://github.com/holepunchto/bare-semver/blob/v1.1.0/index.d.ts#L24)</sub>

An array of prerelease tags.

#### `Version.toString(): string`

<sub>[Source](https://github.com/holepunchto/bare-semver/blob/v1.1.0/index.d.ts#L29)</sub>

Return the string representation of `version`.

#### `Version.compare(a: Version, b: Version): number`

<sub>[Source](https://github.com/holepunchto/bare-semver/blob/v1.1.0/index.d.ts#L35)</sub>

**Parameters**

| Parameter | Type | Default | Description |
| --- | --- | --- | --- |
| `a` | `Version` | — | — |
| `b` | `Version` | — | — |

#### `Version.parse(input: string): Version`

<sub>[Source](https://github.com/holepunchto/bare-semver/blob/v1.1.0/index.d.ts#L33)</sub>

Parse a semantic version string into a `Version` instance. Throws a `SemVerError` with code `INVALID_VERSION` if `input` is not a valid version string.

**Parameters**

| Parameter | Type | Default | Description |
| --- | --- | --- | --- |
| `input` | `string` | — | — |

### Comparator

#### `new Comparator(operator: number, version: Version)`

<sub>[Source](https://github.com/holepunchto/bare-semver/blob/v1.1.0/index.d.ts#L39)</sub>

Create a new comparator with the given `operator` constant and `version`.

**Parameters**

| Parameter | Type | Default | Description |
| --- | --- | --- | --- |
| `operator` | `number` | — | — |
| `version` | `Version` | — | — |

#### `operator: number`

<sub>[Source](https://github.com/holepunchto/bare-semver/blob/v1.1.0/index.d.ts#L41)</sub>

The comparison operator constant.

#### `Comparator.test(version: Version): boolean`

<sub>[Source](https://github.com/holepunchto/bare-semver/blob/v1.1.0/index.d.ts#L44)</sub>

Test whether `version` satisfies the comparator.

**Parameters**

| Parameter | Type | Default | Description |
| --- | --- | --- | --- |
| `version` | `Version` | — | — |

#### `Comparator.toString(): string`

<sub>[Source](https://github.com/holepunchto/bare-semver/blob/v1.1.0/index.d.ts#L46)</sub>

Return the string representation of `version`.

#### `version: Version`

<sub>[Source](https://github.com/holepunchto/bare-semver/blob/v1.1.0/index.d.ts#L42)</sub>

The `Version` instance to compare against.

### Range

#### `new Range(comparators?: Comparator[][])`

<sub>[Source](https://github.com/holepunchto/bare-semver/blob/v1.1.0/index.d.ts#L50)</sub>

Create a new range from a two-dimensional array of `Comparator` instances. Each inner array represents a set of comparators joined by intersection, and the outer array represents the union of those sets.

**Parameters**

| Parameter | Type | Default | Description |
| --- | --- | --- | --- |
| `comparators?` | `Comparator[][]` | — | — |

#### `comparators: Comparator[][]`

<sub>[Source](https://github.com/holepunchto/bare-semver/blob/v1.1.0/index.d.ts#L52)</sub>

The two-dimensional array of `Comparator` instances.

#### `Range.parse(input: string): Range`

<sub>[Source](https://github.com/holepunchto/bare-semver/blob/v1.1.0/index.d.ts#L60)</sub>

Parse a range string into a `Range` instance. Supports comparison operators (`<`, `<=`, `>`, `>=`, `=`), partial versions, and logical OR (`||`).

**Parameters**

| Parameter | Type | Default | Description |
| --- | --- | --- | --- |
| `input` | `string` | — | — |

#### `Range.test(version: Version): boolean`

<sub>[Source](https://github.com/holepunchto/bare-semver/blob/v1.1.0/index.d.ts#L54)</sub>

Test whether `version` satisfies the comparator.

**Parameters**

| Parameter | Type | Default | Description |
| --- | --- | --- | --- |
| `version` | `Version` | — | — |

#### `Range.toString(): string`

<sub>[Source](https://github.com/holepunchto/bare-semver/blob/v1.1.0/index.d.ts#L56)</sub>

Return the string representation of `version`.

### Functions

#### `satisfies(version: Version, range: Range): boolean`

<sub>[Source](https://github.com/holepunchto/bare-semver/blob/v1.1.0/index.d.ts#L11)</sub>

Test whether `version` satisfies `range`. Both `version` and `range` may be strings, in which case they will be parsed.

**Parameters**

| Parameter | Type | Default | Description |
| --- | --- | --- | --- |
| `version` | `Version` | — | — |
| `range` | `Range` | — | — |

### Constants and variables

#### `constants: { EQ: 1; LT: 2; LTE: 3; GT: 4; GTE: 5 }`

<sub>[Source](https://github.com/holepunchto/bare-semver/blob/v1.1.0/index.d.ts#L1)</sub>

An object containing the comparison operator constants.

## See also

- [Bare modules](/reference/modules/bare-modules) — the full `bare-*` catalog.
- [Bare runtime API](/reference/bare/runtime) — the runtime these modules extend.
