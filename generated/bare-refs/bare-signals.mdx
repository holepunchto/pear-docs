---
title: "bare-signals"
description: "Native signal handling for JavaScript"
docType: reference
schemaType: APIReference
---

<mark style={{ backgroundColor: '#7dde9a', padding: '2px 8px', borderRadius: '4px', fontSize: '0.85em', fontWeight: 500 }}>stable</mark>

[`bare-signals`](https://github.com/holepunchto/bare-signals) — Native signal handling for JavaScript. It is a native addon and requires Bare `>=1.7.0`.

```sh
npm i bare-signals
```

## Usage

```js
const Signal = require('bare-signals')

const sigint = new Signal('SIGINT')

sigint.on('signal', () => console.log('SIGINT caught')).start()
```

## API

### Signal

#### `new Signal(signum: number | string)`

<sub>[Source](https://github.com/holepunchto/bare-signals/blob/v4.2.0/index.d.ts#L27)</sub>

**Parameters**

- `signum` (`number` | `string`)

#### `close(): Promise<void>`

<sub>[Source](https://github.com/holepunchto/bare-signals/blob/v4.2.0/index.d.ts#L21)</sub>

#### `Signal.ref(): this`

<sub>[Source](https://github.com/holepunchto/bare-signals/blob/v4.2.0/index.d.ts#L19)</sub>

#### `Signal.constants: typeof os.constants.signals`

<sub>[Source](https://github.com/holepunchto/bare-signals/blob/v4.2.0/index.d.ts#L33)</sub>

#### `Signal.send(signum: number | string, pid?: number): void`

<sub>[Source](https://github.com/holepunchto/bare-signals/blob/v4.2.0/index.d.ts#L25)</sub>

**Parameters**

- `signum` (`number` | `string`)
- `pid` (`number`, optional)

#### `start(): this`

<sub>[Source](https://github.com/holepunchto/bare-signals/blob/v4.2.0/index.d.ts#L17)</sub>

#### `stop(): this`

<sub>[Source](https://github.com/holepunchto/bare-signals/blob/v4.2.0/index.d.ts#L18)</sub>

#### `Signal.unref(): this`

<sub>[Source](https://github.com/holepunchto/bare-signals/blob/v4.2.0/index.d.ts#L20)</sub>

### SignalEmitter

#### `SignalEmitter.ref(): this`

<sub>[Source](https://github.com/holepunchto/bare-signals/blob/v4.2.0/index.d.ts#L6)</sub>

#### `SignalEmitter.unref(): this`

<sub>[Source](https://github.com/holepunchto/bare-signals/blob/v4.2.0/index.d.ts#L7)</sub>

### Classes

#### `SignalError`

<sub>[Source](https://github.com/holepunchto/bare-signals/blob/v4.2.0/index.d.ts#L12)</sub>

```ts
class SignalError {
  code: string
}
```

## See also

- [Bare modules](/reference/modules/bare-modules) — the full `bare-*` catalog.
- [Bare runtime API](/reference/bare/runtime) — the runtime these modules extend.
