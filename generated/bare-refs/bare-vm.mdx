---
title: "bare-vm"
description: "Isolated JavaScript contexts for Bare"
docType: reference
schemaType: APIReference
---

<mark style={{ backgroundColor: '#7dde9a', padding: '2px 8px', borderRadius: '4px', fontSize: '0.85em', fontWeight: 500 }}>stable</mark>

[`bare-vm`](https://github.com/holepunchto/bare-vm) — Isolated JavaScript contexts for Bare.

```sh
npm i bare-vm
```

## Usage

```js
const vm = require('bare-vm')

const context = vm.createContext()
vm.runInContext('x = 40; x += 2', context) // 42

vm.runInNewContext('x = 40; x += 2') // 42
```

## API

### Functions

#### `createContext(): Record<string | number | symbol, unknown>`

<sub>[Source](https://github.com/holepunchto/bare-vm/blob/v1.0.1/index.d.ts#L1)</sub>

#### `runInContext`

```ts
runInContext(code: string, context: Record<string | number | symbol, unknown>, opts?: {
    filename?: string
    offset?: number
  }): unknown
```

<sub>[Source](https://github.com/holepunchto/bare-vm/blob/v1.0.1/index.d.ts#L3)</sub>

**Parameters**

- `code` (`string`)
- `context` (`Record<string | number | symbol, unknown>`)
- `opts` (`{ filename?: string offset?: number }`, optional)

#### `runInNewContext`

```ts
runInNewContext(code: string, opts?: {
    filename?: string
    offset?: number
  }): unknown
```

<sub>[Source](https://github.com/holepunchto/bare-vm/blob/v1.0.1/index.d.ts#L12)</sub>

**Parameters**

- `code` (`string`)
- `opts` (`{ filename?: string offset?: number }`, optional)

## See also

- [Bare modules](/reference/modules/bare-modules) — the full `bare-*` catalog.
- [Bare runtime API](/reference/bare/runtime) — the runtime these modules extend.
