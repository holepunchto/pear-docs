# pear-* references — human review TODO

_Auto-generated by the refgen todo script. Everything below needs a person._

## Selected but not documented — ship no usable `.d.ts`

- [ ] **pear-aliases** — in the top-N by downloads but the published package ships no `.d.ts` (or declares one it doesn't ship). Needs upstream types before it can be generated.
- [ ] **pear-api** — in the top-N by downloads but the published package ships no `.d.ts` (or declares one it doesn't ship). Needs upstream types before it can be generated.
- [ ] **pear-appdrive** — in the top-N by downloads but the published package ships no `.d.ts` (or declares one it doesn't ship). Needs upstream types before it can be generated.
- [ ] **pear-bridge** — in the top-N by downloads but the published package ships no `.d.ts` (or declares one it doesn't ship). Needs upstream types before it can be generated.
- [ ] **pear-changelog** — in the top-N by downloads but the published package ships no `.d.ts` (or declares one it doesn't ship). Needs upstream types before it can be generated.
- [ ] **pear-cmd** — in the top-N by downloads but the published package ships no `.d.ts` (or declares one it doesn't ship). Needs upstream types before it can be generated.
- [ ] **pear-constants** — in the top-N by downloads but the published package ships no `.d.ts` (or declares one it doesn't ship). Needs upstream types before it can be generated.
- [ ] **pear-crasher** — in the top-N by downloads but the published package ships no `.d.ts` (or declares one it doesn't ship). Needs upstream types before it can be generated.
- [ ] **pear-drop** — in the top-N by downloads but the published package ships no `.d.ts` (or declares one it doesn't ship). Needs upstream types before it can be generated.
- [ ] **pear-dump** — in the top-N by downloads but the published package ships no `.d.ts` (or declares one it doesn't ship). Needs upstream types before it can be generated.
- [ ] **pear-electron** — in the top-N by downloads but the published package ships no `.d.ts` (or declares one it doesn't ship). Needs upstream types before it can be generated.
- [ ] **pear-errors** — in the top-N by downloads but the published package ships no `.d.ts` (or declares one it doesn't ship). Needs upstream types before it can be generated.
- [ ] **pear-gracedown** — in the top-N by downloads but the published package ships no `.d.ts` (or declares one it doesn't ship). Needs upstream types before it can be generated.
- [ ] **pear-gunk** — in the top-N by downloads but the published package ships no `.d.ts` (or declares one it doesn't ship). Needs upstream types before it can be generated.
- [ ] **pear-hotmods** — in the top-N by downloads but the published package ships no `.d.ts` (or declares one it doesn't ship). Needs upstream types before it can be generated.
- [ ] **pear-info** — in the top-N by downloads but the published package ships no `.d.ts` (or declares one it doesn't ship). Needs upstream types before it can be generated.
- [ ] **pear-inspect** — in the top-N by downloads but the published package ships no `.d.ts` (or declares one it doesn't ship). Needs upstream types before it can be generated.
- [ ] **pear-ipc** — in the top-N by downloads but the published package ships no `.d.ts` (or declares one it doesn't ship). Needs upstream types before it can be generated.
- [ ] **pear-link** — in the top-N by downloads but the published package ships no `.d.ts` (or declares one it doesn't ship). Needs upstream types before it can be generated.
- [ ] **pear-message** — in the top-N by downloads but the published package ships no `.d.ts` (or declares one it doesn't ship). Needs upstream types before it can be generated.
- [ ] **pear-messages** — in the top-N by downloads but the published package ships no `.d.ts` (or declares one it doesn't ship). Needs upstream types before it can be generated.
- [ ] **pear-opwait** — in the top-N by downloads but the published package ships no `.d.ts` (or declares one it doesn't ship). Needs upstream types before it can be generated.
- [ ] **pear-pipe** — in the top-N by downloads but the published package ships no `.d.ts` (or declares one it doesn't ship). Needs upstream types before it can be generated.
- [ ] **pear-ref** — in the top-N by downloads but the published package ships no `.d.ts` (or declares one it doesn't ship). Needs upstream types before it can be generated.
- [ ] **pear-release** — in the top-N by downloads but the published package ships no `.d.ts` (or declares one it doesn't ship). Needs upstream types before it can be generated.
- [ ] **pear-rti** — in the top-N by downloads but the published package ships no `.d.ts` (or declares one it doesn't ship). Needs upstream types before it can be generated.
- [ ] **pear-run** — in the top-N by downloads but the published package ships no `.d.ts` (or declares one it doesn't ship). Needs upstream types before it can be generated.
- [ ] **pear-seed** — in the top-N by downloads but the published package ships no `.d.ts` (or declares one it doesn't ship). Needs upstream types before it can be generated.
- [ ] **pear-stage** — in the top-N by downloads but the published package ships no `.d.ts` (or declares one it doesn't ship). Needs upstream types before it can be generated.
- [ ] **pear-stamp** — in the top-N by downloads but the published package ships no `.d.ts` (or declares one it doesn't ship). Needs upstream types before it can be generated.
- [ ] **pear-state** — in the top-N by downloads but the published package ships no `.d.ts` (or declares one it doesn't ship). Needs upstream types before it can be generated.
- [ ] **pear-terminal** — in the top-N by downloads but the published package ships no `.d.ts` (or declares one it doesn't ship). Needs upstream types before it can be generated.
- [ ] **pear-tryboot** — in the top-N by downloads but the published package ships no `.d.ts` (or declares one it doesn't ship). Needs upstream types before it can be generated.
- [ ] **pear-updates** — in the top-N by downloads but the published package ships no `.d.ts` (or declares one it doesn't ship). Needs upstream types before it can be generated.
- [ ] **pear-user-dirs** — in the top-N by downloads but the published package ships no `.d.ts` (or declares one it doesn't ship). Needs upstream types before it can be generated.
- [ ] **pear-wakeups** — in the top-N by downloads but the published package ships no `.d.ts` (or declares one it doesn't ship). Needs upstream types before it can be generated.

## Modules with no author-written descriptions (mandate: do not fabricate)

- _none_

## Auto-transcribed descriptions to review before going live

- _none_

## Coverage gaps surfaced by the transcriber

- _none_

## Catalog rows auto-added — verify section placement

- _none_

## Stability to confirm

- _all set_

## Before committing / going live

- [ ] Render the generated pages in the Fumadocs dev server (compile-checked only so far).
- [ ] Side-by-side a few against the existing hand-written pages.
- [ ] Spot-check a source link resolves at its pinned version tag.
