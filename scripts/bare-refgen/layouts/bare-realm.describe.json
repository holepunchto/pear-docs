{
  "Realm.evaluate": "Evaluate `code` in the realm's isolated global scope and return its completion value. The realm has its own `globalThis`, so assignments to globals made by `code` do not leak into the caller's environment."
}
