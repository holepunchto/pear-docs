{
  "createContext": "Create and return a new isolated global context that code can be run in with `runInContext()`.",
  "runInContext": "Run `code` inside `context`, a context previously created with `createContext()`, and return the result.",
  "runInNewContext": "Create a new context and run `code` inside it in one step, equivalent to calling `createContext()` followed by `runInContext()`."
}
