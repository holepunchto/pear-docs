{
  "Mutex.from": "Options are the same as `new Mutex()`.",
  "handle": "A `SharedArrayBuffer` with the underlying mutex handle.",
  "held": "Whether or not the current thread currently holds the mutex."
}
