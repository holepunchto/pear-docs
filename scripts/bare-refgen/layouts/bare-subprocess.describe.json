{
  "spawn": "Spawn `file` as a new subprocess with the given `args`. Returns a `Subprocess` instance. `args` may be `null` or omitted to spawn with no arguments. If `args` is omitted, the second argument is treated as `options`.",
  "pid": "The process ID of the child.",
  "spawnfile": "The file that was spawned.",
  "spawnargs": "The arguments the child was spawned with.",
  "stdio": "An array of `bare-pipe` instances corresponding to the configured stdio slots. Slots configured as `'inherit'`, `'ignore'`, or backed by an inherited fd are `null`.",
  "stdin": "Convenience accessor for `subprocess.stdio[0]`.",
  "stdout": "Convenience accessor for `subprocess.stdio[1]`.",
  "stderr": "Convenience accessor for `subprocess.stdio[2]`.",
  "exitCode": "The exit code of the child, or `null` if the child has not exited or was terminated by a signal.",
  "signalCode": "The name of the signal the child was terminated with, or `null`.",
  "killed": "`true` if `subprocess.kill()` has been called, otherwise `false`.",
  "connected": "`true` while an IPC channel exists between parent and child.",
  "channel": "The `SubprocessChannel` instance backing the IPC channel, or `undefined` when no channel exists. For `serialization: 'binary'`, this is always `undefined`.",
  "unref": "Unreference the subprocess and its stdio pipes against the event loop.",
  "kill": "Send a signal to the child. `signum` may be a signal number or a name (for example `'SIGTERM'`). Defaults to `SIGTERM`.",
  "send": "Send `message` to the child over the IPC channel. `handle` may be a `bare-pipe` `Pipe` or a `bare-tcp` `Socket` to transfer ownership of along with the message. `callback` is invoked with `(err)` after the message has been written.",
  "disconnect": "Close the IPC channel. A `'disconnect'` event is emitted once the channel is fully closed.",
  "ref": "Reference the subprocess and its stdio pipes against the event loop."
}
