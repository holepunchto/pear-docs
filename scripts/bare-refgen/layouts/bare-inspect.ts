import type { Layout } from '../layout';

const layout: Layout = {
  groups: [],
  params: {
    inspect: {
      value: 'The value to produce a string representation of.',
      opts: 'Formatting options; see `InspectOptions`.',
    },
  },
};

export default layout;
