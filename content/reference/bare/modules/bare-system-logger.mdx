---
title: "bare-system-logger"
description: "System logger for Bare"
docType: reference
schemaType: APIReference
---

<mark style={{ backgroundColor: '#7dde9a', padding: '2px 8px', borderRadius: '4px', fontSize: '0.85em', fontWeight: 500 }}>stable</mark>

[`bare-system-logger`](https://github.com/holepunchto/bare-system-logger) — System logger for Bare. It is a native addon.

```sh
npm i bare-system-logger
```

## Usage

```js
const SystemLog = require('bare-system-logger')

const log = new SystemLog()

log.info('Hello %s', 'world!')
```

## API

### Classes

#### `SystemLog`

```ts
class SystemLog {
}
```

A logger that writes formatted messages to the native OS system log, at `debug`, `info`, `warn`, `error`, and `fatal` severity levels.

## See also

- Builds on `bare-logger`.
- [Bare modules](/reference/modules/bare-modules) — the full `bare-*` catalog.
- [Bare runtime API](/reference/bare/runtime) — the runtime these modules extend.
