---
title: "bare-addon-resolve"
description: "Reference for bare-addon-resolve: the low-level native-addon resolution algorithm for Bare, exposed as a generator."
docType: reference
schemaType: APIReference
---

[`bare-addon-resolve`](https://github.com/holepunchto/bare-addon-resolve) implements Bare's low-level resolution algorithm for **native addons**—the counterpart to [`bare-module-resolve`](/reference/bare/modules/bare-module-resolve) for `.bare` / prebuilt binaries. It's a pure-JavaScript generator that yields candidate addon URLs for a specifier.

<Callout type="info">
A low-level building block. Addons normally resolve through the [runtime](/reference/bare/runtime#bareaddon) or are bundled by [`bare-pack`](/reference/modules/bare-modules); use this directly only in tooling.
</Callout>

```sh
npm i bare-addon-resolve
```

## Usage

```js
const resolve = require('bare-addon-resolve')

function readPackage (url) {
  // Read and parse `url` if it exists, otherwise return null
}

for (const resolution of resolve('./addon', new URL('file:///directory/'), readPackage)) {
  console.log(resolution)
}
```

An asynchronous form is supported by iterating with `for await`.

## API

#### `const resolver = resolve(specifier, parentURL[, options][, readPackage])`

Create an addon resolver. Iterate synchronously or with `for await`; each yielded value is a candidate addon `URL`.

### Sub-generators

`resolve.addon`, `resolve.url`, `resolve.package`, `resolve.packageSelf`, `resolve.preresolved`, `resolve.file`, `resolve.directory`, and `resolve.linked` expose the algorithm's steps—including `linked`, which resolves to `linked:` specifiers for runtimes that link addons ahead of time (iOS and Android). See the [repository README](https://github.com/holepunchto/bare-addon-resolve).

## Related modules

Builds on [`bare-module-resolve`](/reference/bare/modules/bare-module-resolve) and `bare-semver` (see [Bare modules](/reference/modules/bare-modules)).

## See also

- [`bare-module-traverse`](/reference/bare/modules/bare-module-traverse)—uses this to find a graph's addons.
- [Bare runtime API](/reference/bare/runtime#bareaddon)—`Bare.Addon`, the runtime addon loader.
- [Bare modules](/reference/modules/bare-modules)—the full `bare-*` catalog.
