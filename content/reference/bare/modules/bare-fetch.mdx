---
title: "bare-fetch"
description: "Reference for bare-fetch: a WHATWG Fetch implementation for Bare, with Request, Response, and Headers."
docType: reference
schemaType: APIReference
---

<mark style={{ backgroundColor: '#7dde9a', padding: '2px 8px', borderRadius: '4px', fontSize: '0.85em', fontWeight: 500 }}>stable</mark>

[`bare-fetch`](https://github.com/holepunchto/bare-fetch) is a WHATWG [Fetch](https://fetch.spec.whatwg.org/) implementation for [Bare](/reference/bare/runtime)—the same `fetch` API the web exposes. It's pure JavaScript, built on [`bare-http1`](/reference/modules/bare-modules) and `bare-https`.

```sh
npm i bare-fetch
```

## Usage

```js
const fetch = require('bare-fetch')

const res = await fetch('https://example.com/data')
console.log(await res.json())
```

## API

#### `const response = await fetch(input[, init])`

Fetch a resource. `input` may be a URL string, a `URL` object, or a `Request` object; `init` carries `method`, `headers`, `body`, `signal`, `agent` (default `null`), etc. Returns a `Response`. The `fetch.Request`, `fetch.Response`, and `fetch.Headers` constructors are also attached to the function.

### Request

#### `const request = new Request(input[, init])`

`input` may be a URL string, a `URL` object, or another `Request` object. Properties: `request.url`, `request.method`, `request.headers`, `request.signal`, `request.body`, `request.bodyUsed`. Read the body with `request.buffer()`, `request.bytes()`, `request.arrayBuffer()`, `request.text()`, `request.json()`, or `request.formData()`; copy with `request.clone()`.

### Response

A `Response` exposes `url`, `redirected`, `status`, `ok`, `statusText`, `headers`, `body`, and `bodyUsed`, with the same body readers as `Request` (`buffer()`, `bytes()`, `arrayBuffer()`, `text()`, `json()`, `formData()`) and `clone()`.

### Headers

#### `const headers = new Headers([init])`

A case-insensitive header map: `append(name, value)`, `delete(name)`, `get(name)`, `has(name)`, `set(name, value)`, and the iterators `entries()`, `keys()`, `values()`, plus `forEach(callback[, thisArg])`.

## Related modules

Builds on `bare-form-data`, [`bare-http1`](/reference/modules/bare-modules), `bare-https`, `bare-mime`, [`bare-stream`](/reference/bare/modules/bare-stream), `bare-url`, and `bare-zlib` (see [Bare modules](/reference/modules/bare-modules)).

## See also

- [Bare modules](/reference/modules/bare-modules)—the full `bare-*` catalog.
- [`bare-tcp`](/reference/bare/modules/bare-tcp)—the lower-level socket API beneath HTTP.
- [Bare runtime API](/reference/bare/runtime)—the runtime this client runs on.
