---
title: "bare-zlib"
description: "Stream-based zlib bindings for JavaScript"
docType: reference
schemaType: APIReference
---

<mark style={{ backgroundColor: '#7dde9a', padding: '2px 8px', borderRadius: '4px', fontSize: '0.85em', fontWeight: 500 }}>stable</mark>

[`bare-zlib`](https://github.com/holepunchto/bare-zlib) — Stream-based zlib bindings for JavaScript. It is a native addon.

Mirrors the Node.js [`zlib`](https://nodejs.org/api/zlib.html) module.

```sh
npm i bare-zlib
```

## API

### Zlib

#### `Zlib.constants`

```ts
Zlib.constants: {
  Z_NO_FLUSH: number
  Z_PARTIAL_FLUSH: number
  Z_SYNC_FLUSH: number
  Z_FULL_FLUSH: number
  Z_FINISH: number
  Z_BLOCK: number
  Z_TREES: number

  Z_FILTERED: number
  Z_HUFFMAN_ONLY: number
  Z_RLE: number
  Z_FIXED: number
  Z_DEFAULT_STRATEGY: number

  Z_NO_COMPRESSION: number
  Z_BEST_SPEED: number
  Z_BEST_COMPRESSION: number
  Z_DEFAULT_COMPRESSION: number

  Z_MIN_CHUNK: number
  Z_MAX_CHUNK: number
  Z_DEFAULT_CHUNK: number

  Z_MIN_MEMLEVEL: number
  Z_MAX_MEMLEVEL: number
  Z_DEFAULT_MEMLEVEL: number

  Z_MIN_LEVEL: number
  Z_MAX_LEVEL: number
  Z_DEFAULT_LEVEL: number

  Z_MIN_WINDOWBITS: number
  Z_MAX_WINDOWBITS: number
  Z_DEFAULT_WINDOWBITS: number

  Z_OK: number
  Z_STREAM_END: number
  Z_NEED_DICT: number
  Z_ERRNO: number
  Z_STREAM_ERROR: number
  Z_DATA_ERROR: number
  Z_MEM_ERROR: number
  Z_BUF_ERROR: number
  Z_VERSION_ERROR: number
}
```

<sub>[Source](https://github.com/holepunchto/bare-zlib/blob/v1.4.1/lib/constants.d.ts#L1)</sub>

Zlib-related constants such as flush modes, compression levels, strategies, and chunk/window/memory-level bounds.

#### `Zlib.createDeflate(opts?: ZlibOptions): Deflate`

<sub>[Source](https://github.com/holepunchto/bare-zlib/blob/v1.4.1/index.d.ts#L81)</sub>

Create and return a new `Deflate` stream for streaming zlib compression.

**Parameters**

| Parameter | Type | Default | Description |
| --- | --- | --- | --- |
| `opts?` | [`ZlibOptions`](#zliboptions) | — | Options for the stream and the underlying zlib state. |

#### `Zlib.createDeflateRaw(opts?: ZlibOptions): DeflateRaw`

<sub>[Source](https://github.com/holepunchto/bare-zlib/blob/v1.4.1/index.d.ts#L83)</sub>

Create and return a new `DeflateRaw` stream for streaming raw deflate compression, without a zlib header.

**Parameters**

| Parameter | Type | Default | Description |
| --- | --- | --- | --- |
| `opts?` | [`ZlibOptions`](#zliboptions) | — | Options for the stream and the underlying zlib state. |

#### `Zlib.createGunzip(opts?: ZlibOptions): Gunzip`

<sub>[Source](https://github.com/holepunchto/bare-zlib/blob/v1.4.1/index.d.ts#L86)</sub>

Create and return a new `Gunzip` stream for streaming gzip decompression.

**Parameters**

| Parameter | Type | Default | Description |
| --- | --- | --- | --- |
| `opts?` | [`ZlibOptions`](#zliboptions) | — | Options for the stream and the underlying zlib state. |

#### `Zlib.createGzip(opts?: ZlibOptions): Gzip`

<sub>[Source](https://github.com/holepunchto/bare-zlib/blob/v1.4.1/index.d.ts#L85)</sub>

Create and return a new `Gzip` stream for streaming gzip compression.

**Parameters**

| Parameter | Type | Default | Description |
| --- | --- | --- | --- |
| `opts?` | [`ZlibOptions`](#zliboptions) | — | Options for the stream and the underlying zlib state. |

#### `Zlib.createInflate(opts?: ZlibOptions): Inflate`

<sub>[Source](https://github.com/holepunchto/bare-zlib/blob/v1.4.1/index.d.ts#L82)</sub>

Create and return a new `Inflate` stream for streaming zlib decompression.

**Parameters**

| Parameter | Type | Default | Description |
| --- | --- | --- | --- |
| `opts?` | [`ZlibOptions`](#zliboptions) | — | Options for the stream and the underlying zlib state. |

#### `Zlib.createInflateRaw(opts?: ZlibOptions): InflateRaw`

<sub>[Source](https://github.com/holepunchto/bare-zlib/blob/v1.4.1/index.d.ts#L84)</sub>

Create and return a new `InflateRaw` stream for streaming raw deflate decompression, without a zlib header.

**Parameters**

| Parameter | Type | Default | Description |
| --- | --- | --- | --- |
| `opts?` | [`ZlibOptions`](#zliboptions) | — | Options for the stream and the underlying zlib state. |

#### `Zlib.deflate(buffer: ZlibInput, opts: ZlibOptions, cb: ZlibCallback): void`

<sub>[Source](https://github.com/holepunchto/bare-zlib/blob/v1.4.1/index.d.ts#L88)</sub>

Compress `buffer` with deflate, calling `cb` with the resulting `Buffer`.

Overloads:

```ts
Zlib.deflate(buffer: ZlibInput, opts: ZlibOptions, cb: ZlibCallback): void
Zlib.deflate(buffer: ZlibInput, cb: ZlibCallback): void
```

**Parameters**

| Parameter | Type | Default | Description |
| --- | --- | --- | --- |
| `buffer` | `ZlibInput` | — | The data to compress. |
| `opts` | [`ZlibOptions`](#zliboptions) | — | The zlib options to apply for this operation. |
| `cb` | [`ZlibCallback`](#zlibcallback) | — | Called with the resulting `Buffer`, or with an error if the operation fails. |

#### `Zlib.deflateRaw(buffer: ZlibInput, opts: ZlibOptions, cb: ZlibCallback): void`

<sub>[Source](https://github.com/holepunchto/bare-zlib/blob/v1.4.1/index.d.ts#L94)</sub>

Compress `buffer` with raw deflate, without a zlib header, calling `cb` with the resulting `Buffer`.

Overloads:

```ts
Zlib.deflateRaw(buffer: ZlibInput, opts: ZlibOptions, cb: ZlibCallback): void
Zlib.deflateRaw(buffer: ZlibInput, cb: ZlibCallback): void
```

**Parameters**

| Parameter | Type | Default | Description |
| --- | --- | --- | --- |
| `buffer` | `ZlibInput` | — | The data to compress. |
| `opts` | [`ZlibOptions`](#zliboptions) | — | The zlib options to apply for this operation. |
| `cb` | [`ZlibCallback`](#zlibcallback) | — | Called with the resulting `Buffer`, or with an error if the operation fails. |

#### `Zlib.deflateRawSync(buffer: ZlibInput, opts?: ZlibOptions): Buffer`

<sub>[Source](https://github.com/holepunchto/bare-zlib/blob/v1.4.1/index.d.ts#L108)</sub>

Synchronously compress `buffer` with raw deflate, without a zlib header, and return the resulting `Buffer`.

**Parameters**

| Parameter | Type | Default | Description |
| --- | --- | --- | --- |
| `buffer` | `ZlibInput` | — | The data to compress; a string is converted to a `Buffer`. |
| `opts?` | [`ZlibOptions`](#zliboptions) | — | The zlib options to apply for this operation. |

**Throws**

- `LIMIT_EXCEEDED` — the output exceeded `maxOutputLength`.
- `ZlibError` — the underlying zlib operation failed; `code` identifies the failure.

#### `Zlib.deflateSync(buffer: ZlibInput, opts?: ZlibOptions): Buffer`

<sub>[Source](https://github.com/holepunchto/bare-zlib/blob/v1.4.1/index.d.ts#L106)</sub>

Synchronously compress `buffer` with deflate and return the resulting `Buffer`.

**Parameters**

| Parameter | Type | Default | Description |
| --- | --- | --- | --- |
| `buffer` | `ZlibInput` | — | The data to compress; a string is converted to a `Buffer`. |
| `opts?` | [`ZlibOptions`](#zliboptions) | — | The zlib options to apply for this operation. |

**Throws**

- `LIMIT_EXCEEDED` — the output exceeded `maxOutputLength`.
- `ZlibError` — the underlying zlib operation failed; `code` identifies the failure.

#### `Zlib.gunzip(buffer: ZlibInput, opts: ZlibOptions, cb: ZlibCallback): void`

<sub>[Source](https://github.com/holepunchto/bare-zlib/blob/v1.4.1/index.d.ts#L103)</sub>

Decompress `buffer` as gzip, calling `cb` with the resulting `Buffer`.

Overloads:

```ts
Zlib.gunzip(buffer: ZlibInput, opts: ZlibOptions, cb: ZlibCallback): void
Zlib.gunzip(buffer: ZlibInput, cb: ZlibCallback): void
```

**Parameters**

| Parameter | Type | Default | Description |
| --- | --- | --- | --- |
| `buffer` | `ZlibInput` | — | The data to decompress. |
| `opts` | [`ZlibOptions`](#zliboptions) | — | The zlib options to apply for this operation. |
| `cb` | [`ZlibCallback`](#zlibcallback) | — | Called with the resulting `Buffer`, or with an error if the operation fails. |

#### `Zlib.gunzipSync(buffer: ZlibInput, opts?: ZlibOptions): Buffer`

<sub>[Source](https://github.com/holepunchto/bare-zlib/blob/v1.4.1/index.d.ts#L111)</sub>

Synchronously decompress `buffer` as gzip and return the resulting `Buffer`.

**Parameters**

| Parameter | Type | Default | Description |
| --- | --- | --- | --- |
| `buffer` | `ZlibInput` | — | The data to decompress; a string is converted to a `Buffer`. |
| `opts?` | [`ZlibOptions`](#zliboptions) | — | The zlib options to apply for this operation. |

**Throws**

- `LIMIT_EXCEEDED` — the output exceeded `maxOutputLength`.
- `ZlibError` — the underlying zlib operation failed; `code` (such as `DATA_ERROR`) identifies the failure.

#### `Zlib.gzip(buffer: ZlibInput, opts: ZlibOptions, cb: ZlibCallback): void`

<sub>[Source](https://github.com/holepunchto/bare-zlib/blob/v1.4.1/index.d.ts#L100)</sub>

Compress `buffer` as gzip, calling `cb` with the resulting `Buffer`.

Overloads:

```ts
Zlib.gzip(buffer: ZlibInput, opts: ZlibOptions, cb: ZlibCallback): void
Zlib.gzip(buffer: ZlibInput, cb: ZlibCallback): void
```

**Parameters**

| Parameter | Type | Default | Description |
| --- | --- | --- | --- |
| `buffer` | `ZlibInput` | — | The data to compress. |
| `opts` | [`ZlibOptions`](#zliboptions) | — | The zlib options to apply for this operation. |
| `cb` | [`ZlibCallback`](#zlibcallback) | — | Called with the resulting `Buffer`, or with an error if the operation fails. |

#### `Zlib.gzipSync(buffer: ZlibInput, opts?: ZlibOptions): Buffer`

<sub>[Source](https://github.com/holepunchto/bare-zlib/blob/v1.4.1/index.d.ts#L110)</sub>

Synchronously compress `buffer` as gzip and return the resulting `Buffer`.

**Parameters**

| Parameter | Type | Default | Description |
| --- | --- | --- | --- |
| `buffer` | `ZlibInput` | — | The data to compress; a string is converted to a `Buffer`. |
| `opts?` | [`ZlibOptions`](#zliboptions) | — | The zlib options to apply for this operation. |

**Throws**

- `LIMIT_EXCEEDED` — the output exceeded `maxOutputLength`.
- `ZlibError` — the underlying zlib operation failed; `code` identifies the failure.

#### `Zlib.inflate(buffer: ZlibInput, opts: ZlibOptions, cb: ZlibCallback): void`

<sub>[Source](https://github.com/holepunchto/bare-zlib/blob/v1.4.1/index.d.ts#L91)</sub>

Decompress `buffer` with inflate, calling `cb` with the resulting `Buffer`.

Overloads:

```ts
Zlib.inflate(buffer: ZlibInput, opts: ZlibOptions, cb: ZlibCallback): void
Zlib.inflate(buffer: ZlibInput, cb: ZlibCallback): void
```

**Parameters**

| Parameter | Type | Default | Description |
| --- | --- | --- | --- |
| `buffer` | `ZlibInput` | — | The data to decompress. |
| `opts` | [`ZlibOptions`](#zliboptions) | — | The zlib options to apply for this operation. |
| `cb` | [`ZlibCallback`](#zlibcallback) | — | Called with the resulting `Buffer`, or with an error if the operation fails. |

#### `Zlib.inflateRaw(buffer: ZlibInput, opts: ZlibOptions, cb: ZlibCallback): void`

<sub>[Source](https://github.com/holepunchto/bare-zlib/blob/v1.4.1/index.d.ts#L97)</sub>

Decompress `buffer` with raw inflate, without a zlib header, calling `cb` with the resulting `Buffer`.

Overloads:

```ts
Zlib.inflateRaw(buffer: ZlibInput, opts: ZlibOptions, cb: ZlibCallback): void
Zlib.inflateRaw(buffer: ZlibInput, cb: ZlibCallback): void
```

**Parameters**

| Parameter | Type | Default | Description |
| --- | --- | --- | --- |
| `buffer` | `ZlibInput` | — | The data to decompress. |
| `opts` | [`ZlibOptions`](#zliboptions) | — | The zlib options to apply for this operation. |
| `cb` | [`ZlibCallback`](#zlibcallback) | — | Called with the resulting `Buffer`, or with an error if the operation fails. |

#### `Zlib.inflateRawSync(buffer: ZlibInput, opts?: ZlibOptions): Buffer`

<sub>[Source](https://github.com/holepunchto/bare-zlib/blob/v1.4.1/index.d.ts#L109)</sub>

Synchronously decompress `buffer` with raw inflate, without a zlib header, and return the resulting `Buffer`.

**Parameters**

| Parameter | Type | Default | Description |
| --- | --- | --- | --- |
| `buffer` | `ZlibInput` | — | The data to decompress; a string is converted to a `Buffer`. |
| `opts?` | [`ZlibOptions`](#zliboptions) | — | The zlib options to apply for this operation. |

**Throws**

- `LIMIT_EXCEEDED` — the output exceeded `maxOutputLength`.
- `ZlibError` — the underlying zlib operation failed; `code` (such as `DATA_ERROR`) identifies the failure.

#### `Zlib.inflateSync(buffer: ZlibInput, opts?: ZlibOptions): Buffer`

<sub>[Source](https://github.com/holepunchto/bare-zlib/blob/v1.4.1/index.d.ts#L107)</sub>

Synchronously decompress `buffer` with inflate and return the resulting `Buffer`.

**Parameters**

| Parameter | Type | Default | Description |
| --- | --- | --- | --- |
| `buffer` | `ZlibInput` | — | The data to decompress; a string is converted to a `Buffer`. |
| `opts?` | [`ZlibOptions`](#zliboptions) | — | The zlib options to apply for this operation. |

**Throws**

- `LIMIT_EXCEEDED` — the output exceeded `maxOutputLength`.
- `ZlibError` — the underlying zlib operation failed; `code` (such as `DATA_ERROR`) identifies the failure.

#### `Zlib.ZlibInput`

```ts
type ZlibInput = string | Buffer | Uint8Array
```

<sub>[Source](https://github.com/holepunchto/bare-zlib/blob/v1.4.1/index.d.ts#L7)</sub>

The input accepted by the one-shot functions: a string, `Buffer`, or `Uint8Array`.

### ZlibStream

#### `flush(mode?: number, cb?: ZlibFlushCallback): Promise<void>`

<sub>[Source](https://github.com/holepunchto/bare-zlib/blob/v1.4.1/index.d.ts#L32)</sub>

Flush queued data through the stream immediately using the given flush `mode`, resolving once it has drained.

Overloads:

```ts
flush(mode?: number, cb?: ZlibFlushCallback): Promise<void>
flush(cb: ZlibFlushCallback): Promise<void>
```

**Parameters**

| Parameter | Type | Default | Description |
| --- | --- | --- | --- |
| `mode?` | `number` | — | The flush mode, from `constants` (default `Z_FULL_FLUSH`). |
| `cb?` | [`ZlibFlushCallback`](#zlibflushcallback) | — | Called once the flush completes, for Node.js compatibility; the returned promise resolves as well. |

#### `reset(): void`

<sub>[Source](https://github.com/holepunchto/bare-zlib/blob/v1.4.1/index.d.ts#L35)</sub>

Reset the underlying compressor or decompressor to its initial state.

**Throws**

- `STREAM_CLOSED` — the stream has already closed.

### Deflate

#### `new Deflate(opts?: ZlibOptions)`

<sub>[Source](https://github.com/holepunchto/bare-zlib/blob/v1.4.1/index.d.ts#L41)</sub>

Create a `Deflate` stream with the given `opts`.

**Parameters**

| Parameter | Type | Default | Description |
| --- | --- | --- | --- |
| `opts?` | [`ZlibOptions`](#zliboptions) | — | Options for the stream and the underlying zlib state. |

### Inflate

#### `new Inflate(opts?: ZlibOptions)`

<sub>[Source](https://github.com/holepunchto/bare-zlib/blob/v1.4.1/index.d.ts#L45)</sub>

Create an `Inflate` stream with the given `opts`.

**Parameters**

| Parameter | Type | Default | Description |
| --- | --- | --- | --- |
| `opts?` | [`ZlibOptions`](#zliboptions) | — | Options for the stream and the underlying zlib state. |

### DeflateRaw

#### `new DeflateRaw(opts?: ZlibOptions)`

<sub>[Source](https://github.com/holepunchto/bare-zlib/blob/v1.4.1/index.d.ts#L49)</sub>

Create a `DeflateRaw` stream with the given `opts`.

**Parameters**

| Parameter | Type | Default | Description |
| --- | --- | --- | --- |
| `opts?` | [`ZlibOptions`](#zliboptions) | — | Options for the stream and the underlying zlib state. |

### InflateRaw

#### `new InflateRaw(opts?: ZlibOptions)`

<sub>[Source](https://github.com/holepunchto/bare-zlib/blob/v1.4.1/index.d.ts#L53)</sub>

Create an `InflateRaw` stream with the given `opts`.

**Parameters**

| Parameter | Type | Default | Description |
| --- | --- | --- | --- |
| `opts?` | [`ZlibOptions`](#zliboptions) | — | Options for the stream and the underlying zlib state. |

### Gzip

#### `new Gzip(opts?: ZlibOptions)`

<sub>[Source](https://github.com/holepunchto/bare-zlib/blob/v1.4.1/index.d.ts#L57)</sub>

Create a `Gzip` stream with the given `opts`.

**Parameters**

| Parameter | Type | Default | Description |
| --- | --- | --- | --- |
| `opts?` | [`ZlibOptions`](#zliboptions) | — | Options for the stream and the underlying zlib state. |

### Gunzip

#### `new Gunzip(opts?: ZlibOptions)`

<sub>[Source](https://github.com/holepunchto/bare-zlib/blob/v1.4.1/index.d.ts#L61)</sub>

Create a `Gunzip` stream with the given `opts`.

**Parameters**

| Parameter | Type | Default | Description |
| --- | --- | --- | --- |
| `opts?` | [`ZlibOptions`](#zliboptions) | — | Options for the stream and the underlying zlib state. |

### Types

#### `ZlibOptions`

```ts
interface ZlibOptions<S extends ZlibStream = ZlibStream> extends Omit<
  TransformOptions<S>,
  'flush'
> {
  flush?: number
  finishFlush?: number
  chunkSize?: number
  level?: number
  windowBits?: number
  memLevel?: number
  strategy?: number
  maxOutputLength?: number
}
```

<sub>[Source](https://github.com/holepunchto/bare-zlib/blob/v1.4.1/index.d.ts#L17)</sub>

#### `ZlibCallback`

```ts
interface ZlibCallback {
  (err: Error | null, result: Buffer): void
}
```

<sub>[Source](https://github.com/holepunchto/bare-zlib/blob/v1.4.1/index.d.ts#L9)</sub>

Callback for the one-shot asynchronous functions, called with an error or the resulting `Buffer`.

#### `ZlibFlushCallback`

```ts
interface ZlibFlushCallback {
  (err: Error | null): void
}
```

<sub>[Source](https://github.com/holepunchto/bare-zlib/blob/v1.4.1/index.d.ts#L13)</sub>

Callback invoked once a `flush()` completes.

### Classes

#### `ZlibError`

<sub>[Source](https://github.com/holepunchto/bare-zlib/blob/v1.4.1/lib/errors.d.ts#L11)</sub>

```ts
class ZlibError {
  code: ZlibErrorCode
  name: 'ZlibError'
}
```

## `bare-zlib/constants`

### Constants and variables

#### `constants`

```ts
constants: {
  Z_NO_FLUSH: number
  Z_PARTIAL_FLUSH: number
  Z_SYNC_FLUSH: number
  Z_FULL_FLUSH: number
  Z_FINISH: number
  Z_BLOCK: number
  Z_TREES: number

  Z_FILTERED: number
  Z_HUFFMAN_ONLY: number
  Z_RLE: number
  Z_FIXED: number
  Z_DEFAULT_STRATEGY: number

  Z_NO_COMPRESSION: number
  Z_BEST_SPEED: number
  Z_BEST_COMPRESSION: number
  Z_DEFAULT_COMPRESSION: number

  Z_MIN_CHUNK: number
  Z_MAX_CHUNK: number
  Z_DEFAULT_CHUNK: number

  Z_MIN_MEMLEVEL: number
  Z_MAX_MEMLEVEL: number
  Z_DEFAULT_MEMLEVEL: number

  Z_MIN_LEVEL: number
  Z_MAX_LEVEL: number
  Z_DEFAULT_LEVEL: number

  Z_MIN_WINDOWBITS: number
  Z_MAX_WINDOWBITS: number
  Z_DEFAULT_WINDOWBITS: number

  Z_OK: number
  Z_STREAM_END: number
  Z_NEED_DICT: number
  Z_ERRNO: number
  Z_STREAM_ERROR: number
  Z_DATA_ERROR: number
  Z_MEM_ERROR: number
  Z_BUF_ERROR: number
  Z_VERSION_ERROR: number
}
```

<sub>[Source](https://github.com/holepunchto/bare-zlib/blob/v1.4.1/lib/constants.d.ts#L1)</sub>

Zlib-related constants such as flush modes, compression levels, strategies, and chunk/window/memory-level bounds.

## `bare-zlib/errors`

### Classes

#### `errors.ZlibError`

<sub>[Source](https://github.com/holepunchto/bare-zlib/blob/v1.4.1/lib/errors.d.ts#L11)</sub>

An error thrown by a zlib operation, carrying a `code` identifying the underlying zlib failure (such as `DATA_ERROR` or `MEM_ERROR`).

## See also

- [Bare modules](/reference/modules/bare-modules) — the full `bare-*` catalog.
- [Bare runtime API](/reference/bare/runtime) — the runtime these modules extend.
