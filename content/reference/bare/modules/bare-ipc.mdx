---
title: "bare-ipc"
description: "Reference for bare-ipc: lightweight pipe-based inter-process communication for Bare, with structured-clone message ports."
docType: reference
schemaType: APIReference
---

<mark style={{ backgroundColor: '#7dde9a', padding: '2px 8px', borderRadius: '4px', fontSize: '0.85em', fontWeight: 500 }}>stable</mark>

[`bare-ipc`](https://github.com/holepunchto/bare-ipc) provides lightweight pipe-based inter-process communication for [Bare](/reference/bare/runtime). It pairs two ports over [`bare-pipe`](/reference/bare/modules/bare-pipe) and exchanges structured-cloned messages between them. It's pure JavaScript. (For the host↔worklet channel in native apps, see [`bare-kit`](/reference/bare/bare-kit) instead.)

```sh
npm i bare-ipc
```

## Usage

```js
const IPC = require('bare-ipc')

const [portA, portB] = IPC.open()

const a = portA.connect()
const b = portB.connect()

a.on('data', (data) => { /* received from b */ }).end('hello b')
```

## API

#### `const [portA, portB] = IPC.open()`

Create a connected pair of `IPCPort`s.

#### `const ipc = new IPC(port)`

Wrap a single transport `port`. Exposes `ipc.incoming`, `ipc.outgoing`, `ipc.ref()`, `ipc.unref()`.

#### `const port = new IPCPort(incoming, outgoing)`

A port over explicit `incoming`/`outgoing` transports: `port.connect()` returns the message stream; `port.incoming`, `port.outgoing`, `port.detached`.

## Related modules

Builds on [`bare-pipe`](/reference/bare/modules/bare-pipe), [`bare-stream`](/reference/bare/modules/bare-stream), and `bare-structured-clone` (see [Bare modules](/reference/modules/bare-modules)).

## See also

- [`bare-channel`](/reference/bare/modules/bare-channel)—inter-*thread* messaging (same idea, within a process).
- [`bare-kit`](/reference/bare/bare-kit)—the host↔worklet IPC channel in native apps.
- [Bare modules](/reference/modules/bare-modules)—the full `bare-*` catalog.
