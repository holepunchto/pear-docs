---
title: "bare-tls"
description: "Reference for bare-tls: Transport Layer Security (TLS) streams for Bare, layered over a duplex socket, with a server and connect helper."
docType: reference
schemaType: APIReference
---

<mark style={{ backgroundColor: '#7dde9a', padding: '2px 8px', borderRadius: '4px', fontSize: '0.85em', fontWeight: 500 }}>stable</mark>

[`bare-tls`](https://github.com/holepunchto/bare-tls) provides TLS streams for [Bare](/reference/bare/runtime), wrapping an underlying duplex socket (typically [`bare-tcp`](/reference/bare/modules/bare-tcp)) in an encrypted [`bare-stream`](/reference/bare/modules/bare-stream). The shape follows the Node.js [`tls`](https://nodejs.org/api/tls.html) module. It's a native addon and requires Bare `>=1.7.0`.

```sh
npm i bare-tls
```

## Usage

```js
const tls = require('bare-tls')
const fs = require('bare-fs')

const server = tls.createServer(
  { cert: fs.readFileSync('cert.pem'), key: fs.readFileSync('key.pem') },
  (socket) => socket.end('secure hello')
)
```

## API

### Socket

#### `const socket = new tls.Socket(stream[, options])`

Wrap an existing duplex `stream` in TLS. Exposes `socket.socket` (the underlying stream), `socket.encrypted`, and `socket.alpnProtocol`; emits `connect`.

### Server

#### `const server = tls.createServer([options][, onconnection])`

Create a TLS server: `server.listen(...args)`, `server.address()`, `server.close([onclose])`, `server.ref()` / `server.unref()`, and `server.listening`. Emits `listening`, `connection`, `close`, `error`.

### connect

#### `const socket = tls.connect(options[, onconnect])` · `tls.connect(port[, host][, onconnect])`

Open a TLS client connection.

## Related modules

Builds on [`bare-net`](/reference/modules/bare-modules) and [`bare-stream`](/reference/bare/modules/bare-stream) (see [Bare modules](/reference/modules/bare-modules)).

## See also

- [`bare-tcp`](/reference/bare/modules/bare-tcp)—the unencrypted socket TLS wraps.
- [Bare modules](/reference/modules/bare-modules)—the full `bare-*` catalog.
- [Bare runtime API](/reference/bare/runtime)—the runtime this runs on.
