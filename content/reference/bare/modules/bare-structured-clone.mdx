---
title: "bare-structured-clone"
description: "Reference for bare-structured-clone: the HTML structured-clone algorithm for Bare—structuredClone plus low-level serialize/deserialize with transfer."
docType: reference
schemaType: APIReference
---

<mark style={{ backgroundColor: '#7dde9a', padding: '2px 8px', borderRadius: '4px', fontSize: '0.85em', fontWeight: 500 }}>stable</mark>

[`bare-structured-clone`](https://github.com/holepunchto/bare-structured-clone) implements the HTML [structured-clone](https://html.spec.whatwg.org/multipage/structured-data.html) algorithm for [Bare](/reference/bare/runtime): the `structuredClone` function plus the low-level serialize/deserialize primitives that inter-thread messaging ([`bare-channel`](/reference/bare/modules/bare-channel)) is built on. It's a native addon and requires Bare `>=1.2.0`.

```sh
npm i bare-structured-clone
```

## Usage

```js
const structuredClone = require('bare-structured-clone')

const copy = structuredClone({ hello: 'world' })

const buffer = new ArrayBuffer(4)
const transferred = structuredClone(buffer, { transfer: [buffer] })
```

Require `bare-structured-clone/global` to install `structuredClone` as a global.

## API

#### `const copy = structuredClone(value[, options])`

Deep-clone a value, optionally transferring objects via `options.transfer`.

### Low-level serialization

#### `serialize(value[, forStorage[, interfaces]])` · `serializeWithTransfer(value[, transferList[, interfaces]])`

Produce a serialized representation, optionally with a transfer list.

#### `deserialize(serialized[, interfaces])` · `deserializeWithTransfer(serialized[, interfaces])`

Reconstruct a value from its serialized form.

#### `preencode(state, serialized)` · `encode(state, serialized)` · `const serialized = decode(state)`

The wire codec used to move serialized values across a boundary, plus `constants`, `symbols`, and the `Serializable` / `Transferable` base classes for custom types.

## Related modules

Builds on [`bare-buffer`](/reference/modules/bare-modules), `bare-type`, and [`bare-url`](/reference/bare/modules/bare-url) (see [Bare modules](/reference/modules/bare-modules)).

## See also

- [`bare-channel`](/reference/bare/modules/bare-channel) and [`bare-broadcast-channel`](/reference/bare/modules/bare-broadcast-channel)—inter-thread messaging built on this.
- [Bare modules](/reference/modules/bare-modules)—the full `bare-*` catalog.
- [Bare runtime API](/reference/bare/runtime#barethread)—`Bare.Thread`.
