---
title: "bare-console"
description: "Reference for bare-console: a WHATWG console implementation for Bare—logging, timers, counters, assertions, and tables."
docType: reference
schemaType: APIReference
---

<mark style={{ backgroundColor: '#7dde9a', padding: '2px 8px', borderRadius: '4px', fontSize: '0.85em', fontWeight: 500 }}>stable</mark>

[`bare-console`](https://github.com/holepunchto/bare-console) is a WHATWG [console](https://console.spec.whatwg.org/) implementation for [Bare](/reference/bare/runtime)—the `console.*` API. The global `console` in a Bare program is an instance of it; you can also construct your own bound to a custom log function. It's pure JavaScript.

```sh
npm i bare-console
```

## Usage

```js
const Console = require('bare-console')

const console = new Console()
console.log('Hello')
console.error(new Error('Something happened'))
```

## API

#### `const console = new Console([log])`

Create a console, optionally backed by a custom `log` object. The object must implement the methods used by console operations: `{ debug(...data), info(...data), warn(...data), error(...data), clear(), format(...data) }`.

### Methods

- **Logging:** `log(...data)`, `info(...data)`, `debug(...data)`, `warn(...data)`, `error(...data)`, `trace(...data)`, `clear()`.
- **Timers:** `time([label])`, `timeLog([label[, ...data]])`, `timeEnd([label])`.
- **Counters:** `count([label])`, `countReset([label])`.
- **Other:** `assert(condition, ...data)`, `table(tabularData[, properties])`.

## Related modules

Builds on [`bare-format`](/reference/modules/bare-modules), `bare-hrtime`, `bare-logger`, `bare-system-logger`, and `bare-type` (see [Bare modules](/reference/modules/bare-modules)).

## See also

- [Bare modules](/reference/modules/bare-modules)—the full `bare-*` catalog.
- [`bare-inspector`](/reference/bare/modules/bare-inspector)—deeper debugging via the V8 inspector.
- [Bare runtime API](/reference/bare/runtime)—the runtime this runs on.
