---
title: "Hypercore"
description: "Secure distributed append-only log for large datasets and realtime streams."
docType: reference
schemaType: APIReference
---

<mark style={{ backgroundColor: '#7dde9a', padding: '2px 8px', borderRadius: '4px', fontSize: '0.85em', fontWeight: 500 }}>stable</mark>

Hypercore is a secure, distributed append-only log for sharing large datasets and realtime streams. It supports sparse replication, verified reads, optional block encryption, and session-based workflows for building higher-level local-first and peer-to-peer data structures.

## Install

```sh
npm i hypercore
```

## Quickstart

```js
import Hypercore from 'hypercore'

const core = new Hypercore('./my-first-core', { valueEncoding: 'utf-8' })
await core.ready()

await core.append('hello')
await core.append('from hypercore')

console.log('length:', core.length)
console.log('first block:', await core.get(0))
console.log('second block:', await core.get(1))

for await (const block of core.createReadStream()) {
  console.log('streamed:', block)
}

await core.close()
```

## API Reference

### Constructors and Sessions

#### `new Hypercore(storage, [key], [options])`

[API definition on GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L45)
- Signature: `new Hypercore(storage, [key], [options])` ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L45))
- Parameters ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L45)):
  - `storage`: a directory path, a `hypercore-storage` instance, or a storage value supplied through `options.storage`.
  - `key`: optional public key used to reopen an existing core. If omitted, Hypercore loads the key from storage or creates a new writable core.
  - `options`: optional settings for creation and runtime behavior, including:
    - `createIfMissing`, `overwrite`, `force`
    - `valueEncoding`, `encodeBatch`
    - `keyPair`, `writable`
    - `encryption`, `onwait`, `timeout`, `inflightRange`, `notDownloadingLinger`, `allowFork`
    - `userData`, `manifest`, `preload`, `storage`, `key`, `ongc`, `onseq`
- Returns: a `Hypercore` instance. Call `await core.ready()` before relying on synchronous properties such as `core.key`, `core.discoveryKey`, or `core.length`. ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L214))
- Example: `const core = new Hypercore('./data', { valueEncoding: 'json' })`

User Data is a local-only key/value store with string keys and string or `Buffer` values. It is not replicated and is useful for peer-specific metadata such as encryption keys or app state.

The manifest controls how a core is authenticated, including `version`, `hash`, `quorum`, `signers`, `prologue`, `linked`, and manifest-level `userData`. Changing the manifest changes the resulting Hypercore key.

#### `core.session([options])`

[API definition on GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L217)
- Signature: `core.session([options])` ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L217))
- Parameters ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L217)):
  - `options`: inherited constructor options plus session-specific fields:
    - `weak`: close automatically when all non-weak sessions are gone.
    - `exclusive`: wait for exclusive access before the session becomes ready.
    - `checkout`: open the core at a specific length.
    - `atom`: attach the session to a storage atom for atomic changes.
    - `name`: create a named persisted branch.
- Returns: a new `Hypercore` session that shares the same underlying core state. ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L249))
- Example: `const checkout = core.session({ checkout: 10 })`

#### `core.commit(session, opts = {})`

[API definition on GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L544)
- Signature: `core.commit(session, opts = {})` ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L544))
- Parameters ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L544)):
  - `session`: a session whose blocks should be committed into the default core.
  - `opts`: optional commit controls:
    - `length`: expected length after commit.
    - `treeLength`: expected Merkle tree length before commit.
    - `keyPair`: key pair used to sign the commit.
    - `signature`: explicit signature for the committed blocks.
- Returns: `{ byteLength, length }` when the commit succeeds, or `null` if it fails. ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L548))
- Example: `const committed = core.commit(session)`

#### `core.snapshot([options])`

[API definition on GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L209)
- Signature: `core.snapshot([options])` ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L209))
- Parameters ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L209)):
  - `options`: the same options accepted by `core.session([options])`.
- Returns: a snapshot-backed session that does not append or truncate as the underlying core changes. ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L210))
- Example: `const snap = core.snapshot()`

### Writing and Local Mutation

#### `await core.append(block, options = {})`

[API definition on GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L1083)
- Signature: `await core.append(block, options = {})` ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L1083))
- Parameters ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L1090)):
  - `block`: a single block or an array of blocks.
  - `options`: append controls:
    - `writable`: ignore the writable guard without changing whether the core is actually writable.
    - `maxLength`: cap the resulting core length.
    - `keyPair`: key pair used to sign the appended block or batch.
    - `signature`: explicit signature for the append.
- Returns: `{ length, byteLength }` for the resulting core after the append. ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L1118))
- Example: `await core.append(['block 1', 'block 2'])`

#### `core.createWriteStream()`

[API definition on GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L1039)
- Signature: `core.createWriteStream()` ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L1039))
- Parameters ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L1039)):
  - none.
- Returns: a writable stream that appends each written chunk as its own block. ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L1040))
- Example: `core.createWriteStream().end('hello')`

#### `await core.clear(start, [end], [options])`

[API definition on GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L866)
- Signature: `await core.clear(start, [end], [options])` ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L866))
- Parameters ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L866)):
  - `start`: first block index to clear from local storage.
  - `end`: optional non-inclusive end index.
  - `options`: optional flags, currently `diff` to return cleared-byte information.
- Returns: metadata about cleared bytes when `diff: true`, otherwise `null`. ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L888))
- Example: `await core.clear(0, 10, { diff: true })`

#### `await core.truncate(newLength, [options])`

[API definition on GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L1061)
- Signature: `await core.truncate(newLength, [options])` ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L1061))
- Parameters ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L1090)):
  - `newLength`: the shorter length to truncate to.
  - `options`: a fork number or an options object containing:
    - `fork`: fork id after truncation.
    - `keyPair`: key pair used to sign the truncation.
    - `signature`: explicit truncation signature.
- Returns: a promise that resolves when the truncation is persisted. ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L1061))
- Example: `await core.truncate(4, { fork: core.fork + 1 })`

### Reading, Streams, and Proofs

#### `await core.get(index, [options])`

[API definition on GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L835)
- Signature: `await core.get(index, [options])` ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L835))
- Parameters ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L835)):
  - `index`: block index to read.
  - `options`: read controls:
    - `wait`: wait for download if the block is missing locally.
    - `onwait`: callback fired when the read starts waiting.
    - `timeout`: maximum wait time in milliseconds.
    - `activeRequests`: advanced replication request handle.
    - `valueEncoding`: override the core's default encoding.
    - `decrypt`: automatically decrypt encrypted blocks.
    - `raw`: return undecoded data.
- Returns: the decoded block, or raw bytes when `raw: true`. ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L863))
- Example: `const first = await core.get(0, { wait: false })`

#### `await core.has(start, [end])`

[API definition on GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L803)
- Signature: `await core.has(start, [end])` ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L803))
- Parameters ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L803)):
  - `start`: first block index to check.
  - `end`: optional non-inclusive end index.
- Returns: `true` when all requested blocks are available locally. ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L832))
- Example: `const cached = await core.has(0, core.length)`

#### `await core.update([options])`

[API definition on GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L728)
- Signature: `await core.update([options])` ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L728))
- Parameters ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L728)):
  - `options`: optional update controls:
    - `wait`: wait for a merkle-tree update after peer discovery.
    - `activeRequests`: advanced replication request handles.
    - `force`: allow updates even when the core is writable.
- Returns: `true` if the core length changed and `false` otherwise. ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L758))
- Example: `await core.update({ wait: true })`

`core.update()` works especially well with `core.findingPeers()` or a swarm flush so Hypercore knows when peer discovery is still in progress.

#### `await core.seek(byteOffset, [options])`

[API definition on GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L761)
- Signature: `await core.seek(byteOffset, [options])` ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L761))
- Parameters ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L761)):
  - `byteOffset`: absolute byte offset in the logical stream.
  - `options`: optional seek controls:
    - `wait`: wait for missing data to download.
    - `timeout`: maximum wait time in milliseconds.
    - `activeRequests`: advanced replication request handles.
- Returns: `[index, relativeOffset]`, where `index` is the containing block and `relativeOffset` is the byte offset inside that block. ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L798))
- Example: `const [index, offset] = await core.seek(1024)`

#### `core.createReadStream([options])`

[API definition on GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L1035)
- Signature: `core.createReadStream([options])` ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L1035))
- Parameters ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L1035)):
  - `options`: stream controls:
    - `start`: first block index, default `0`.
    - `end`: non-inclusive end index, default `core.length`.
    - `wait`: whether to wait for missing blocks.
    - `timeout`: maximum wait time.
    - `live`: keep the stream open for future appends.
    - `snapshot`: freeze `end` at open time when `true`.
- Returns: an async-iterable readable stream of decoded blocks. ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L1036))
- Example: `for await (const block of core.createReadStream({ live: true })) {}`

#### `core.createByteStream([options])`

[API definition on GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L1043)
- Signature: `core.createByteStream([options])` ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L1043))
- Parameters ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L1090)):
  - `options`: byte-range controls:
    - `byteOffset`: first byte to read.
    - `byteLength`: number of bytes to read.
    - `prefetch`: number of bytes to download ahead.
- Returns: an async-iterable readable stream over a byte range. ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L1044))
- Example: `for await (const chunk of core.createByteStream({ byteOffset: 0, byteLength: 64 })) {}`

#### `await core.treeHash([length])`

[API definition on GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L1129)
- Signature: `await core.treeHash([length])` ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L1129))
- Parameters ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L1129)):
  - `length`: optional logical length to hash. Defaults to the current core length.
- Returns: the Merkle tree hash for the requested length. ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L1135))
- Example: `const hash = await core.treeHash()`

#### `await core.proof(opts)`

[API definition on GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L1143)
- Signature: `await core.proof(opts)` ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L1143))
- Parameters ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L1143)):
  - `opts`: proof request options:
    - `block`: `{ index, nodes }` for a block proof.
    - `hash`: `{ index, nodes }` for a hash proof.
    - `seek`: `{ bytes, padding }` for a seek proof.
    - `upgrade`: `{ start, length }` for an upgrade proof.
- Returns: a `TreeProof` instance for the requested data. ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L1152))
- Example: `const proof = await core.proof({ block: { index: 0, nodes: 0 } })`

#### `await core.verifyFullyRemote(proof)`

[API definition on GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L1160)
- Signature: `await core.verifyFullyRemote(proof)` ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L1160))
- Parameters ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L1160)):
  - `proof`: a proof returned by a remote peer.
- Returns: the verified Merkle tree batch, or throws if verification fails. ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L1164))
- Example: `const batch = await core.verifyFullyRemote(proof)`

#### `await core.signable([length], [fork])`

[API definition on GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L1121)
- Signature: `await core.signable([length], [fork])` ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L1121))
- Parameters ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L1121)):
  - `length`: optional logical length to encode.
  - `fork`: optional fork id to encode.
- Returns: a `Buffer` containing the signable payload for the current key, tree hash, length, and fork. ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L1126))
- Example: `const payload = await core.signable(core.length, core.fork)`

#### `core.download([range])`

[API definition on GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L1047)
- Signature: `core.download([range])` ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L1047))
- Parameters ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L1090)):
  - `range`: optional download request:
    - `start`: first block index.
    - `end`: non-inclusive end index, or `-1` for continuous downloading.
    - `blocks`: explicit list of block indices.
    - `linear`: download in order instead of randomly.
    - `activeRequests`: advanced replication request handles.
- Returns: a download handle with `.done()` and `.destroy()` methods. ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L1048))
- Example: `await core.download({ start: 0, end: 10 }).done()`

### Extensions and Replication

#### `core.registerExtension(name, handlers = {})`

[API definition on GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L1218)
- Signature: `core.registerExtension(name, handlers = {})` ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L1218))
- Parameters ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L1218)):
  - `name`: extension name.
  - `handlers`: optional extension handlers:
    - `encoding`: compact encoding for messages.
    - `onmessage`: callback for messages from peers.
- Returns: an extension handle. ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L1263))
- Example: `const ext = core.registerExtension('chat', { encoding: 'json', onmessage })`

This extension API is documented as legacy. For new protocol work, the upstream README recommends creating a `Protomux` protocol instead.

#### `ext.send(message, peer)`

[API definition on GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L1232)
- Signature: `ext.send(message, peer)` ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L1232))
- Parameters ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L1232)):
  - `message`: encoded payload for the extension.
  - `peer`: a specific peer to send to.
- Returns: sends a message over the registered extension channel. ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L1232))
- Example: `ext.send({ type: 'ping' }, peer)`

#### `ext.broadcast(message)`

[API definition on GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L1236)
- Signature: `ext.broadcast(message)` ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L1236))
- Parameters ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L1236)):
  - `message`: encoded payload for all connected peers.
- Returns: broadcasts the message to every peer on the extension. ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L1236))
- Example: `ext.broadcast({ type: 'invalidate' })`

#### `ext.destroy()`

[API definition on GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L1242)
- Signature: `ext.destroy()` ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L1242))
- Parameters ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L1242)):
  - none.
- Returns: unregisters the extension from the core. ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L1242))
- Example: `ext.destroy()`

#### `core.replicate(isInitiatorOrReplicationStream, opts = {})`

[API definition on GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L551)
- Signature: `core.replicate(isInitiatorOrReplicationStream, opts = {})` ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L551))
- Parameters ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L551)):
  - `isInitiatorOrReplicationStream`: either a boolean initiator flag or an existing Hypercore replication stream to multiplex onto.
  - `opts`: the same options accepted by `Hypercore.createProtocolStream()`, including `ondiscoverykey`.
- Returns: a replication stream you can pipe to a transport. ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L565))
- Example: `socket.pipe(core.replicate(true)).pipe(socket)`

#### `core.findingPeers()`

[API definition on GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L706)
- Signature: `core.findingPeers()` ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L706))
- Parameters ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L706)):
  - none.
- Returns: a `done` callback that should be called when the current peer-discovery pass finishes. ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L713))
- Example: `const done = core.findingPeers()`

### Storage Inspection and Mark-and-Sweep

#### `await core.info([options])`

[API definition on GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L722)
- Signature: `await core.info([options])` ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L722))
- Parameters ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L722)):
  - `options`: optional info flags. Set `storage: true` to include storage byte estimates.
- Returns: an info object with fields such as `key`, `discoveryKey`, `length`, `contiguousLength`, `byteLength`, `fork`, `padding`, and `storage`. ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L725))
- Example: `const info = await core.info({ storage: true })`

#### `await core.startMarking()`

[API definition on GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L989)
- Signature: `await core.startMarking()` ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L989))
- Parameters ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L989)):
  - none.
- Returns: enables mark-and-sweep mode and clears any previous markings. ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L989))
- Example: `await core.startMarking()`

#### `await core.markBlock(start, end = start + 1)`

[API definition on GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L966)
- Signature: `await core.markBlock(start, end = start + 1)` ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L966))
- Parameters ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L966)):
  - `start`: first block index to mark.
  - `end`: optional non-inclusive end index.
- Returns: marks the requested block range so `core.sweep()` retains it. ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L977))
- Example: `await core.markBlock(10, 20)`

#### `await core.clearMarkings()`

[API definition on GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L980)
- Signature: `await core.clearMarkings()` ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L980))
- Parameters ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L980)):
  - none.
- Returns: removes all current mark-and-sweep markings. ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L980))
- Example: `await core.clearMarkings()`

#### `await core.sweep(opts)`

[API definition on GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L1005)
- Signature: `await core.sweep(opts)` ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L1005))
- Parameters ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L1005)):
  - `opts`: optional sweep controls, currently `batchSize` to control how often clears flush to storage.
- Returns: clears all unmarked local blocks from storage. ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L1005))
- Example: `await core.sweep({ batchSize: 1000 })`

The README describes the full mark-and-sweep flow as `startMarking()`, read or manually mark the blocks you want to keep, then `sweep()` to clear everything else.

### Lifecycle and Local Configuration

#### `await core.close([{ error }])`

[API definition on GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L339)
- Signature: `await core.close([{ error }])` ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L339))
- Parameters ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L339)):
  - `error`: optional error object used to reject pending replication requests.
- Returns: fully closes the core and its active session state. ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L343))
- Example: `await core.close()`

#### `await core.ready()`

[API definition on GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L545)
- Signature: `await core.ready()` ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L545))
- Parameters ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L545)):
  - none.
- Returns: resolves when the core has opened and synchronous properties are populated. ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L548))
- Example: `await core.ready()`

#### `await core.setEncryption(encryption)`

[API definition on GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L258)
- Signature: `await core.setEncryption(encryption)` ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L258))
- Parameters ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L258)):
  - `encryption`: an object that satisfies the Hypercore encryption interface.
- Returns: updates the encryption implementation used by the core. ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L263))
- Example: `await core.setEncryption(encryption)`

#### `await core.setEncryptionKey(key, [opts])`

[API definition on GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L252)
- Signature: `await core.setEncryptionKey(key, [opts])` ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L252))
- Parameters ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L252)):
  - `key`: encryption key material.
  - `opts`: optional flags, including `block` to indicate a block-encryption key.
- Returns: updates the active encryption key. ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L255))
- Example: `await core.setEncryptionKey(key, { block: true })`

#### `core.setKeyPair(keyPair)`

[API definition on GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L273)
- Signature: `core.setKeyPair(keyPair)` ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L273))
- Parameters ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L273)):
  - `keyPair`: `{ publicKey, secretKey }` buffers for the core's signer.
- Returns: updates the key pair used internally by the core. ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L273))
- Example: `core.setKeyPair(keyPair)`

#### `core.setActive(active)`

[API definition on GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L277)
- Signature: `core.setActive(active)` ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L277))
- Parameters ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L277)):
  - `active`: whether the core should stay active for download tracking and lingering replication.
- Returns: toggles activity tracking for the core. ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L281))
- Example: `core.setActive(false)`

#### `await core.setUserData(key, value)`

[API definition on GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L669)
- Signature: `await core.setUserData(key, value)` ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L669))
- Parameters ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L669)):
  - `key`: string key.
  - `value`: string or `Buffer` value.
- Returns: stores a local-only user-data entry. ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L672))
- Example: `await core.setUserData('encryption-key', secret)`

#### `await core.getUserData(key)`

[API definition on GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L676)
- Signature: `await core.getUserData(key)` ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L676))
- Parameters ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L676)):
  - `key`: string key to read.
- Returns: the stored user-data value for that key. ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L681))
- Example: `const secret = await core.getUserData('encryption-key')`

### Properties

#### `core.writable`

[API definition on GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L76)
- Signature: `core.writable` ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L76))
- Parameters ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L76)):
  - none.
- Returns: `true` when the core can append or truncate. Populated after `ready`. ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L76))
- Example: `if (core.writable) await core.append('next block')`

#### `core.readable`

[API definition on GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L75)
- Signature: `core.readable` ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L75))
- Parameters ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L75)):
  - none.
- Returns: `true` when the core can be read from. Becomes `false` after close. ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L75))
- Example: `if (!core.readable) throw new Error('core is closed')`

#### `core.id`

[API definition on GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L587)
- Signature: `core.id` ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L587))
- Parameters ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L587)):
  - none.
- Returns: the z-base-32 string form of the public key. ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L587))
- Example: `console.log(core.id)`

#### `core.key`

[API definition on GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L131)
- Signature: `core.key` ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L131))
- Parameters ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L131)):
  - none.
- Returns: the public key `Buffer` for the core. ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L591))
- Example: `console.log(core.key)`

#### `core.keyPair`

[API definition on GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L74)
- Signature: `core.keyPair` ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L74))
- Parameters ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L74)):
  - none.
- Returns: `{ publicKey, secretKey }` for writable cores after `ready`. ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L74))
- Example: `console.log(core.keyPair?.publicKey)`

#### `core.discoveryKey`

[API definition on GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L138)
- Signature: `core.discoveryKey` ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L138))
- Parameters ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L138)):
  - none.
- Returns: a derived key used to discover peers without revealing the read capability. ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L595))
- Example: `swarm.join(core.discoveryKey)`

#### `core.length`

[API definition on GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L603)
- Signature: `core.length` ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L603))
- Parameters ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L603)):
  - none.
- Returns: the number of blocks currently available on the core. ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L603))
- Example: `console.log(core.length)`

#### `core.signedLength`

[API definition on GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L608)
- Signature: `core.signedLength` ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L608))
- Parameters ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L608)):
  - none.
- Returns: the number of blocks signed by a quorum. For single-signer cores, this equals `core.length`. ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L608))
- Example: `console.log(core.signedLength)`

#### `core.contiguousLength`

[API definition on GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L626)
- Signature: `core.contiguousLength` ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L626))
- Parameters ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L626)):
  - none.
- Returns: how many blocks are available contiguously from index `0`. ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L626))
- Example: `console.log(core.contiguousLength)`

#### `core.remoteContiguousLength`

[API definition on GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L621)
- Signature: `core.remoteContiguousLength` ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L621))
- Parameters ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L621)):
  - none.
- Returns: the largest contiguous length reported by any known remote. ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L621))
- Example: `console.log(core.remoteContiguousLength)`

#### `core.fork`

[API definition on GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L635)
- Signature: `core.fork` ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L635))
- Parameters ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L635)):
  - none.
- Returns: the current fork id. ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L635))
- Example: `console.log(core.fork)`

#### `core.padding`

[API definition on GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L640)
- Signature: `core.padding` ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L640))
- Parameters ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L640)):
  - none.
- Returns: the per-block padding value, usually `0` unless block encryption is enabled. ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L640))
- Example: `console.log(core.padding)`

#### `core.peers`

[API definition on GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L648)
- Signature: `core.peers` ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L648))
- Parameters ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L648)):
  - none.
- Returns: the array of peers currently replicating with this core. ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L648))
- Example: `console.log(core.peers.length)`

### Events

#### `core.on('close')`

[API definition on GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L323)
- Signature: `core.on('close', listener)` ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L323))
- Parameters ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L323)):
  - `listener`: callback fired once the core has fully closed.
- Returns: registers a close listener on the core. ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L323))
- Example: `core.on('close', () => console.log('closed'))`

#### `core.on('ready')`

[API definition on GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L336)
- Signature: `core.on('ready', listener)` ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L336))
- Parameters ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L336)):
  - `listener`: callback fired after the core opens its internal state.
- Returns: registers a ready listener on the core. ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L343))
- Example: `core.on('ready', () => console.log(core.key))`

#### `core.on('append')`

[API definition on GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L1371)
- Signature: `core.on('append', listener)` ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L1371))
- Parameters ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L1371)):
  - `listener`: callback fired when local or remote appends change `length` or `byteLength`.
- Returns: registers an append listener on the core. ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L1371))
- Example: `core.on('append', () => console.log(core.length))`

#### `core.on('truncate', ancestors, forkId)`

[API definition on GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L1371)
- Signature: `core.on('truncate', listener)` ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L1371))
- Parameters ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L1371)):
  - `listener`: callback that receives `ancestors` and `forkId` when the core is truncated.
- Returns: registers a truncate listener on the core. ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L1371))
- Example: `core.on('truncate', (ancestors, forkId) => console.log(ancestors, forkId))`

#### `core.on('peer-add')`

[API definition on GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L331)
- Signature: `core.on('peer-add', listener)` ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L331))
- Parameters ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L331)):
  - `listener`: callback fired when a peer connection is established.
- Returns: registers a peer-add listener on the core. ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L343))
- Example: `core.on('peer-add', () => console.log(core.peers.length))`

#### `core.on('peer-remove')`

[API definition on GitHub](https://github.com/holepunchto/hypercore/blob/main/lib/replicator.js#L3174)
- Signature: `core.on('peer-remove', listener)` ([GitHub](https://github.com/holepunchto/hypercore/blob/main/lib/replicator.js#L3174))
- Parameters ([GitHub](https://github.com/holepunchto/hypercore/blob/main/lib/replicator.js#L3174)):
  - `listener`: callback fired when a peer connection closes.
- Returns: registers a peer-remove listener on the core. ([GitHub](https://github.com/holepunchto/hypercore/blob/main/lib/replicator.js#L3174))
- Example: `core.on('peer-remove', () => console.log(core.peers.length))`

#### `core.on('upload', index, byteLength, peer)`

[API definition on GitHub](https://github.com/holepunchto/hypercore/blob/main/lib/replicator.js#L3202)
- Signature: `core.on('upload', listener)` ([GitHub](https://github.com/holepunchto/hypercore/blob/main/lib/replicator.js#L3202))
- Parameters ([GitHub](https://github.com/holepunchto/hypercore/blob/main/lib/replicator.js#L3202)):
  - `listener`: callback receiving `index`, `byteLength`, and `peer` for each uploaded block.
- Returns: registers an upload listener on the core. ([GitHub](https://github.com/holepunchto/hypercore/blob/main/lib/replicator.js#L3202))
- Example: `core.on('upload', (index) => console.log('uploaded', index))`

#### `core.on('download', index, byteLength, peer)`

[API definition on GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L1047)
- Signature: `core.on('download', listener)` ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L1047))
- Parameters ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L1090)):
  - `listener`: callback receiving `index`, `byteLength`, and `peer` for each downloaded block.
- Returns: registers a download listener on the core. ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L1048))
- Example: `core.on('download', (index) => console.log('downloaded', index))`

#### `core.on('remote-contiguous-length', length)`

[API definition on GitHub](https://github.com/holepunchto/hypercore/blob/main/lib/core.js#L97)
- Signature: `core.on('remote-contiguous-length', listener)` ([GitHub](https://github.com/holepunchto/hypercore/blob/main/lib/core.js#L97))
- Parameters ([GitHub](https://github.com/holepunchto/hypercore/blob/main/lib/core.js#L97)):
  - `listener`: callback receiving the updated remote contiguous length.
- Returns: registers a listener for `core.remoteContiguousLength` changes. ([GitHub](https://github.com/holepunchto/hypercore/blob/main/lib/core.js#L97))
- Example: `core.on('remote-contiguous-length', (length) => console.log(length))`

### Static Helpers

#### `Hypercore.MAX_SUGGESTED_BLOCK_SIZE`

[API definition on GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L116)
- Signature: `Hypercore.MAX_SUGGESTED_BLOCK_SIZE` ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L116))
- Parameters ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L116)):
  - none.
- Returns: the recommended 15 MB maximum block size for smooth replication. ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L116))
- Example: `if (buf.length > Hypercore.MAX_SUGGESTED_BLOCK_SIZE) throw new Error('split the block')`

#### `Hypercore.key(manifest, options = {})`

[API definition on GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L131)
- Signature: `Hypercore.key(manifest, options = {})` ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L131))
- Parameters ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L131)):
  - `manifest`: a manifest object or a single signer's public key.
  - `options`: optional derivation flags:
    - `compat`: treat a single signer public key as the core key.
    - `version`: manifest version for single-signer input.
    - `namespace`: signer namespace for single-signer input.
- Returns: the Hypercore key for the supplied manifest. ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L135))
- Example: `const key = Hypercore.key(manifest)`

#### `Hypercore.discoveryKey(key)`

[API definition on GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L138)
- Signature: `Hypercore.discoveryKey(key)` ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L138))
- Parameters ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L138)):
  - `key`: Hypercore public key.
- Returns: the discovery key derived from `key`. ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L139))
- Example: `const discoveryKey = Hypercore.discoveryKey(key)`

#### `Hypercore.blockEncryptionKey(key, encryptionKey)`

[API definition on GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L142)
- Signature: `Hypercore.blockEncryptionKey(key, encryptionKey)` ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L142))
- Parameters ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L142)):
  - `key`: Hypercore public key.
  - `encryptionKey`: base encryption key material.
- Returns: a block-encryption key derived from the two inputs. ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L143))
- Example: `const blockKey = Hypercore.blockEncryptionKey(key, encryptionKey)`

#### `Hypercore.getProtocolMuxer(stream)`

[API definition on GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L146)
- Signature: `Hypercore.getProtocolMuxer(stream)` ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L146))
- Parameters ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L146)):
  - `stream`: a Hypercore protocol stream.
- Returns: the attached Protomux instance. ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L147))
- Example: `const mux = Hypercore.getProtocolMuxer(stream)`

#### `Hypercore.createCore(storage, opts)`

[API definition on GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L150)
- Signature: `Hypercore.createCore(storage, opts)` ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L150))
- Parameters ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L150)):
  - `storage`: path or storage instance for the internal core.
  - `opts`: internal core options.
- Returns: the internal core object without wrapping it in a full `Hypercore` instance. ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L151))
- Example: `const internal = Hypercore.createCore('./data', {})`

#### `Hypercore.createProtocolStream(isInitiator, opts = {})`

[API definition on GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L154)
- Signature: `Hypercore.createProtocolStream(isInitiator, opts = {})` ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L154))
- Parameters ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L154)):
  - `isInitiator`: a boolean initiator flag, a framed stream, or an existing Protomux.
  - `opts`: protocol options, including `ondiscoverykey` for Corestore-style key management.
- Returns: an encrypted Noise protocol stream with Protomux attached. ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L185))
- Example: `const stream = Hypercore.createProtocolStream(true)`

#### `Hypercore.defaultStorage(storage, opts = {})`

[API definition on GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L188)
- Signature: `Hypercore.defaultStorage(storage, opts = {})` ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L188))
- Parameters ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L188)):
  - `storage`: path or existing `hypercore-storage` instance.
  - `opts`: optional storage configuration passed to the default storage implementation.
- Returns: the existing storage instance or a new default Hypercore storage wrapper. ([GitHub](https://github.com/holepunchto/hypercore/blob/main/index.js#L192))
- Example: `const storage = Hypercore.defaultStorage('./data')`

## See also

- [Replicate and persist with Hypercore](/how-to/store-and-replicate/replicate-and-persist-with-hypercore) — pair a single core with peer discovery and replication.
- [Work with many Hypercores using Corestore](/how-to/store-and-replicate/work-with-many-hypercores-using-corestore) — recommended pattern when one process manages multiple cores.
- [Corestore](/reference/helpers/corestore) — helper reference for creating, naming, and co-replicating groups of Hypercores.
- [Hyperbee](/reference/building-blocks/hyperbee) — sorted key/value B-tree built directly on top of a Hypercore.
- [Hyperdrive](/reference/building-blocks/hyperdrive) — filesystem abstraction whose metadata and blob stores are Hypercore-backed.
- [Compact encoding](/reference/helpers/compact-encoding) — binary encoding toolkit used to structure Hypercore block payloads.
