{/*
  Shared snippet included via <include> by Pear's quadrant landings, so every
  quadrant points across to Bare in the same words. Reciprocal:
  _pear-cross-callout.mdx — change the two together.

  NOTE: links inside _snippets/ are NOT covered by check:internal-links and do
  NOT count toward check:cross-links inbound totals (scripts/helpers.ts
  `getFiles` skips underscore-prefixed files). Verify hrefs here by hand.
*/}

<Callout type="info">
**Pear runs on Bare.** Bare is the zero-core JavaScript runtime underneath the platform, and it has its own docs: the peer-to-peer building blocks, embedding in native and mobile apps, and compiling to a standalone binary all live on the [Bare tab](/bare). [How Pear and Bare fit together](/pear/explanation/pear-and-bare) maps the layers.
</Callout>
