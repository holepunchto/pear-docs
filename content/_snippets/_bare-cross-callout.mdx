{/*
  Shared snippet included via <include> by Pear's and P2P's quadrant
  landings, so every quadrant points across to Bare in the same words.
  Host-agnostic on purpose — it's embedded from both product contexts, so it
  doesn't say "Pear runs on Bare" or similar host-specific framing.
  Siblings: _pear-cross-callout.mdx, _p2p-cross-callout.mdx — keep all three
  in sync when one changes.

  NOTE: links inside _snippets/ are NOT covered by check:internal-links and do
  NOT count toward check:cross-links inbound totals (scripts/helpers.ts
  `getFiles` skips underscore-prefixed files). Verify hrefs here by hand.
*/}

<Callout type="info">
**Bare is the runtime underneath.** A small, zero-core JavaScript runtime with its own docs: the module system, native addons, embedding in native and mobile apps, and compiling to a standalone binary all live on the [Bare tab](/bare). [How the stack fits together](/p2p/explanation/how-the-stack-fits-together) maps the layers.
</Callout>
