{/*
  Shared snippet included via <include> by Bare's and P2P's quadrant
  landings, so every quadrant points across to Pear in the same words.
  Host-agnostic on purpose — it's embedded from both product contexts, so it
  doesn't say "everything here runs on Bare" or similar host-specific
  framing. Siblings: _bare-cross-callout.mdx, _p2p-cross-callout.mdx — keep
  all three in sync when one changes.
  Same caveat as the others: links in _snippets/ are not link-checked.
*/}

<Callout type="info">
**Pear is the platform on top.** Install the `pear` CLI when you want peer-to-peer app distribution, over-the-air updates, and one command to stage and release—the core you write here moves across unchanged. Start on the [Pear tab](/), or read [How the stack fits together](/p2p/explanation/how-the-stack-fits-together).
</Callout>
