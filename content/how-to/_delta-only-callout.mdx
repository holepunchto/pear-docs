{/*
  Partial: rendered inline via Fumadocs <include> in the chat-app delta how-tos (../<section>/*.mdx).
  Flags the guide as delta-only on top of the pear-chat scaffold built in the getting-started path.
  See: https://www.fumadocs.dev/docs/markdown#include
*/}

This is a **delta-only** how-to. The shared Electron + PearRuntime + Bare worker scaffold — with plain-JSON messages over a [`framed-stream`](https://www.npmjs.com/package/framed-stream) pipe and a vanilla HTML renderer — is built step by step in [Reshape into a production app](/getting-started/build-a-peer-to-peer-chat/reshape-into-a-production-app) — read that first.
