---
title: "Application Programming Interface (API)"
description: "Reference for the removed global.Pear direct-access API."
docType: reference
product: pear
schemaType: APIReference
---

<Callout type="warning">
**Deprecated in Pear v2.6.5, removed as of Pear v3.** The `global.Pear` direct-access API documented here was injected by the removed `pear run` command and is superseded by **[Pear OTA](/pear/reference/pear/runtime)** (the `pear-runtime` module)—instantiate `Pear` and use its API instead. This page is retained for reference.
</Callout>

Pear is built on [Bare](/bare/reference/modules/bare-modules). Pear applications have a `global.Pear` object. For background on the runtime split, see [Runtime and languages](/pear/explanation/runtime-and-languages); for the surrounding `pear-*` library catalog, see [Modules](/pear/reference/modules/pear-modules). For the `global.Bare` object, see the [Bare runtime API](/bare/reference/bare/runtime).

## `global.Pear`
<a name="globalpear"></a>

The Pear Platform API is made available globally as `Pear`.

The `Pear` API is designed to be as minimal as possible.

The majority of capabilities are supplied via [Pear Modules](/pear/reference/modules/pear-modules).

### `Pear.app <Object>`
<a name="pear-app"></a>

**REMOVED**—superseded by the [Pear OTA](/pear/reference/pear/runtime) (`pear-runtime`) API (instantiate `Pear` and use its instance instead). Applies to all `Pear.app.*` properties below.

Contains application information. Supersedes `Pear.config`.

#### `Pear.app.key <Buffer|null>`
<a name="pear-app-key"></a>

**REMOVED**

A buffer of the application key. If running from disk, `Pear.app.key` is null.

#### `Pear.app.length <Integer>`
<a name="pear-app-length"></a>

**REMOVED**

The application drive length. The application version consists of `Pear.app.key`, `Pear.app.length` and `Pear.app.fork`.

#### `Pear.app.fork <Integer>`
<a name="pear-app-fork"></a>

**REMOVED**

The application drive fork count. A fork is where the append-only log of the drive is truncated.

The application version consists of `Pear.app.key`, `Pear.app.length` and `Pear.app.fork`.

#### `Pear.app.alias <String|null>`
<a name="pear-app-alias"></a>

**REMOVED**

Given an application that is run from a pear:// link with an alias it, contains that alias.
For example the `Pear.app.alias` for `pear run pear://myapp` would be `myapp`.

#### `Pear.app.dev <Boolean>`
<a name="pear-app-dev"></a>

**REMOVED**

Whether the application is in development mode.

Note that development mode means, the application has been run with the `--dev` flag.

Development mode is not the same as development environment. Development environment equates to running from disk.

For example, to detect both development mode & environment:

```js
const fromDisk = Pear.app.key === null
const isDev = fromDisk && Pear.app.dev
```

#### `Pear.app.name <String>`
<a name="pear-app-name"></a>

**REMOVED**

Application name.

#### `Pear.app.main <String>`
<a name="pear-app-main"></a>

**REMOVED**

Application entry file.

#### `Pear.app.channel <String|null>`
<a name="pear-app-channel"></a>

**REMOVED**

Application release/staging channel.

#### `Pear.app.storage <String>`
<a name="pear-app-storage"></a>

**REMOVED**

Application storage path

#### `Pear.app.options <Object>`
<a name="pear-app-options"></a>

**REMOVED**

Configuration options.
The `pear` configuration object as supplied via an applications `package.json` file.

**References**

* [Configuration](/pear/reference/pear/configuration)

#### `Pear.app.env <Object>`
<a name="pear-app-env"></a>

**REMOVED**

The environment variables that an application was started with, as key-value pairs in an object.

#### `Pear.app.flags <Object>`
<a name="pear-app-flags"></a>

**REMOVED**

Parsed command-line flag values as supplied when an application was started.

#### `Pear.app.checkout <String>`
<a name="pear-app-checkout"></a>

**REMOVED**

The value of the [`pear run --checkout`](/pear/reference/pear/cli#pear-run-flags-linkdir-app-args) flag. Same as [`Pear.app.flags.checkout`](#pear-app-checkout).

**References**

* [`pear run`](/pear/reference/pear/cli#pear-run-flags-linkdir-app-args)
* [`Pear.app.flags`](#pear-app-flags)

#### `Pear.app.storage <String>`
<a name="pear-app-storage-dir"></a>

**REMOVED**

Application storage path.

#### `Pear.app.args <Array>`
<a name="pear-app-args"></a>

**REMOVED**

Command-line application arguments passed like `pear run --dev . --some arg`.

#### `Pear.app.release <Number>`
<a name="pear-app-release"></a>

**REMOVED**

The current release length as marked by the `pear release` command.

**References**

* [`pear release`](/pear/reference/pear/cli#pear-release-link-dir)

#### `Pear.app.link <String>`
<a name="pear-app-link"></a>

**REMOVED**

The link that was passed to `pear run` with alias resolved to key.

Includes any potential pathname, query or fragment.

**References**

* [`pear run`](/pear/reference/pear/cli#pear-run-flags-linkdir-app-args)

#### `Pear.app.links <Object|Array>`
<a name="pear-app-links"></a>

**REMOVED**

Holds trusted Pear application links and domains as specified in the `links` field inside `package.json`.

**References**
* [`pear.links`](/pear/reference/pear/configuration#pearlinks-objectarray)
* [`pear run`](/pear/reference/pear/cli#pear-run-flags-linkdir-app-args)

#### `Pear.app.routes <String>`
<a name="pear-app-routes"></a>

**REMOVED**

The configuration provided [`pear.routes`](/pear/reference/pear/configuration#pearroutes-objectstring) mapping object.

#### `Pear.app.entrypoint <String>`
<a name="pear-app-entrypoint"></a>

**REMOVED**

The link pathname (`pear://<key>/<pathname>`), after any route mappings have been applied per [`pear.routes`](/pear/reference/pear/configuration#pearroutes-objectstring).

Includes the leading `/`, e.g given `pear://foo/bar/baz`, `Pear.app.entrypoint` would be `/bar/baz`.

Only `Pear.app.entrypoint` supports route-mapping via the [`pear.routes`](/pear/reference/pear/configuration#pearroutes-objectstring) mapping object. `Pear.app.route` is provides access the raw pathname, including the leading slash (`/`), while `Pear.app.linkData` is legacy, excludes the slash, but is still supported.

* [`Pear.app.route`](#pear-app-route)
* [`Pear.app.routes`](#pear-app-routes)
* [`pear.routes`](/pear/reference/pear/configuration#pearroutes-objectstring)

#### `Pear.app.fragment <String>`
<a name="pear-app-fragment"></a>

**REMOVED**

The link hash, without the leading `#`.

Given `pear://<key>/<pathname>#frag` `Pear.app.fragment` would be `frag`.

#### `Pear.app.query <String>`
<a name="pear-app-query"></a>

**REMOVED**

The link query string, without the leading `?`.

Given `pear://<key>/<pathname>?qs` `Pear.app.query` would be `qs`.

#### `Pear.app.route <String>`
<a name="pear-app-route"></a>

**REMOVED**

The link pathname (`pear://<key>/<pathname>`), prior to any route mappings being applied.

Includes the leading `/`, e.g given `pear://foo/bar/baz`, `Pear.app.route` would be `/bar/baz`.

**References**

* [`Pear.app.entrypoint`](#pear-app-entrypoint)
* [`Pear.app.routes`](#pear-app-routes)
* [`pear.routes`](/pear/reference/pear/configuration#pearroutes-objectstring)

#### `Pear.app.linkData <String>`
<a name="pear-app-linkData"></a>

**REMOVED**

Holds just the data portion of a Pear link.

The Pear link of an application. Takes the form `pear://<key>/<data>`.

In development, `pear://dev/<data>`.

Unlike `Pear.app.route` and `Pear.app.entrypoint` *does not* include the leading slash (`/`).

Legacy but still supported. Prefer `Pear.app.entrypoint` or `Pear.app.route`.

**References**

* [`Pear.app.entrypoint`](#pear-app-entrypoint)
* [`Pear.app.route`](#pear-app-route)
* [`Pear.app.link`](#pear-app-link)
* [`pear run`](/pear/reference/pear/cli#pear-run-flags-linkdir-app-args)

#### `Pear.app.checkpoint <Any>`
<a name="pear-app-checkpoint"></a>

**REMOVED**

Holds state as set by `Pear.checkpoint()`. When an application restarts it will hold the most recent value passed to `Pear.checkpoint()`.

Stores state that will be available as `Pear.app.checkpoint` next time the application starts.

The `Pear.app.checkpoint` property immediately reflects the latest checkpoint.

The returned `Promise` will resolve once the checkpoint has been successfully stored.

**References**

* [Pear.checkpoint()](#pear-checkpoint)

#### `Pear.app.release <Integer>`
<a name="pear-app-release-sequence"></a>

**REMOVED**

Application release sequence integer.

#### `Pear.app.flags <Object>`
<a name="pear-app-run-flags"></a>

**REMOVED**

Parsed `pear run` flags.

#### `Pear.app.applink <String>`
<a name="pear-app-applink"></a>

**REMOVED**

Pear application link. May be a `pear://` link or a local directory.

#### `Pear.app.dir <String>`
<a name="pear-app-dir"></a>

**REMOVED**

The current working directory of `pear run` when the application was started.

#### `Pear.app.dht.nodes <Array<Object>>`
<a name="pear-app-dht-nodes"></a>

**REMOVED**

A list of known [DHT](https://github.com/holepunchto/hyperdht) nodes of the form `{ host: <String>, port: <Number> }`. The nodes are set when the Pear application is started.

Unless started with a custom set of bootstrap nodes, Pear caches known nodes to speed up connecting to the swarm and to make it more resilient.

#### `Pear.app.dht.bootstrap <Array<Object>>`
<a name="pear-app-dht-bootstrap"></a>

**REMOVED**

A list of custom bootstrap nodes Pear is started with of the form `{ host: <String>, port: <Number> }`.

#### `Pear.app.assets  <String>`
<a name="pear-app-assets"></a>

**REMOVED**

Advanced / integration purposes.

Per [`pear-assets`](/pear/reference/pear/configuration#pear-assets) configuration assets are fetched and stored to disk. Use `Pear.app.assets[namespace].path` to get the path that given asset is stored to.

Takes the form `{ [namespace]: { link <String>, ns <String>, path <String>, name <String>, only <Array<String>>, bytes <Integer> } }`. `namespace` is per the property name on the assets object. By convention, should be describe the asset type, for example: `ui`.

* `link` - Configuration supplied `pear://<fork>.<length>.<key>`
* `ns` - Namespace - same as the property name
* `path` - The path that Pear stored the asset to on-disk
* `name` - Configuration supplied name
* `only` - Configuration supplied only array
* `bytes` - The total bytes used by asset on-disk

Example:

```js
{
  "ui": {
    "link": "pear://0.940.cktxzetiwt6un3ado5kgqedge6ya4nfazjckzq76zcapefwxakdy",
    "ns": "ui",
    "path": "/Users/xxx/Library/Application Support/pear/assets/0c82035d08c00ae4da2f307eb5d2c1fd",
    "name": "Pear Runtime",
    "only": [
      "/boot.bundle",
      "/by-arch/%HOST%",
      "/prebuilds/%HOST%"
    ],
    "bytes": 259158801
  }
}
```

#### `Pear.app.prerunning <Boolean>`
<a name="pear-app-prerunning"></a>

**REMOVED**

Whether the current application is a [`pre`](/pear/reference/pear/configuration#pear-pre) script.

#### `Pear.app.startId <String>`
<a name="pear-app-startid"></a>

**REMOVED**

Advanced. Integration purposes.

The application start identifier. Can be used to register a [`pear-ipc`](https://github.com/holepunchto/pear-ipc) client to an applicaiton, with
`ipc.identify()`

#### `Pear.app.swapDir <String>`
<a name="pear-app-swapdir"></a>

**REMOVED**

Advanced. Integration purposes.

The active swap directory with Pear platform directory.

#### `Pear.app.pearDir <String>`
<a name="pear-app-peardir"></a>

**REMOVED**

Advanced. Integration purposes.

Pear platform directory.

### `Pear.checkpoint(<Any>) => Promise`
<a name="pear-checkpoint"></a>

**REMOVED**

Stores state that will be available as `Pear.app.checkpoint` next time the application starts.

The `Pear.app.checkpoint` property immediately reflects the latest checkpoint.

The returned `Promise` will resolve once the checkpoint has been successfully stored.

**References**

* [`Pear.app.checkpoint`](#pear-app-checkpoint)

### `Pear.teardown(fn <Async Function|Function>)`
<a name="pear-teardown"></a>

**REMOVED**

Register application clean-up handlers to be called when an application begins to unload.

May be called multiple times to register multiple teardown handlers.

Functions supplied to teardown will be executed in order of registration when
an application begins to unload. Any promise returned from each supplied function
will be waited upon until resolution before calling the next teardown handler.

### `Pear.argv`
<a name="pear-argv"></a>

**REMOVED**—use [`Bare.argv`](/bare/reference/bare/runtime#bareargv).

The command line arguments passed to the process when launched.

### `Pear.pid`
<a name="pear-pid"></a>

**REMOVED**—use [`Bare.pid`](/bare/reference/bare/runtime#barepid).

The ID of the current process.

### `Pear.exitCode`
<a name="pear-exitcode"></a>

**REMOVED**—use [`Bare.exitCode`](/bare/reference/bare/runtime#bareexitcode).

The code that will be returned once the process exits. If the process is exited using `Bare.exit()` without specifying a code, `Bare.exitCode` is used.

### `Pear.exit(code)`
<a name="pear-exit"></a>

**REMOVED**—use [`Bare.exit()`](/bare/reference/bare/runtime#bareexitcode-1).

Exits the process with the provided exit code. Follows Pear teardown flow, whereas [`Bare.exit()`](/bare/reference/bare/runtime#bareexitcode-1) does not.

### `Pear.constructor.CUTOVER` (Integration)
<a name="pear-constructor-cutover"></a>

**REMOVED**

<include>../../../_snippets/_integration-apis-unstable-callout.mdx</include>

For auto-cutover opt-out do `Pear.constructor.CUTOVER = false` in the first-tick.

Cutover signals the end of the application init phase and instructs sidecar to stop buffering critical streams, such as updates.

This stops an internal `ipc.cutover` call to the sidecar, indicating that a manual call will be made later which in turn allows child processes to listen to critical streams without any data loss but the onus is then on child process to call cutover in order free resources in the sidecar.

### `Pear.constructor.COMPAT` (Integration)
<a name="pear-constructor-compat"></a>

**REMOVED**

<include>../../../_snippets/_integration-apis-unstable-callout.mdx</include>

Compat-mode opt-in for legacy applications. For JavaScript projects, use the [`pear-runtime` reference](/pear/reference/pear/runtime).

### `Pear.constructor.RTI` (Integration)
<a name="pear-constructor-rit"></a>

**REMOVED**

<include>../../../_snippets/_integration-apis-unstable-callout.mdx</include>

Runtime Information. Used by additional runtimes to bootstrap runtime state at boot. Used by [`pear-run`](https://github.com/holepunchto/pear-run).

### `Pear.constructor.IPC` (Integration)
<a name="pear-constructor-ipc"></a>

**REMOVED**

<include>../../../_snippets/_integration-apis-unstable-callout.mdx</include>

Symbol for accessing built-in IPC client. Used by libraries and other integrations.

```js
const ipc = Pear[Pear.constructor.IPC]
```

### `Pear.constructor.RUNTIME` (Integration)
<a name="pear-constructor-runtime"></a>

**REMOVED**

<include>../../../_snippets/_integration-apis-unstable-callout.mdx</include>

The runtime binary to spawn when running. Used by [`pear-run`](https://github.com/holepunchto/pear-run). Useful for certain testing scenarios.

### `Pear.constructor.RUNTIME_ARGV` (Integration)
<a name="pear-constructor-runtime_argv"></a>

**REMOVED**

<include>../../../_snippets/_integration-apis-unstable-callout.mdx</include>

Used to modify argv passed spawn when running. Used by [`pear-run`](https://github.com/holepunchto/pear-run). Useful for certain testing scenarios.


### `Pear.restart()`
<a name="pear-restart"></a>

**REMOVED**

**REMOVED** Use [`pear-restart`](https://github.com/holepunchto/pear-restart).

### `Pear.config <Object>`
<a name="pear-config"></a>

**REMOVED**

**REMOVED** Use [`Pear.app`](#pear-app).

### `Pear.messages([ pattern ], [ listener ]) -> Iterable`
<a name="pear-messages"></a>

**REMOVED**

**REMOVED** Use [`pear-messages`](https://github.com/holepunchto/pear-messages).

### `await Pear.message(<Object>)`
<a name="pear-message"></a>

**REMOVED**

**REMOVED** Use [`pear-message`](https://github.com/holepunchto/pear-message).

### `Pear.worker <Object>`
<a name="pear-worker"></a>

**REMOVED**

**REMOVED** Use [`pear-run`](https://github.com/holepunchto/pear-run) & [`pear-pipe`](https://github.com/holepunchto/pear-pipe).

### `Pear.media <Object>`
<a name="pear-media"></a>

**REMOVED**

**REMOVED** Use [`pear-electron ui.media`](https://github.com/holepunchto/pear-electron#uimedia-object-).

### `Pear.versions <Async Function>`
<a name="pear-versions"></a>

**REMOVED**

Outputs version information.

Example:

```js
{
  platform: {
    key: 'pzcjqmpoo6szkoc4bpkw65ib9ctnrq7b6mneeinbhbheihaq6p6o',
    length: 5949,
    fork: 0
  },
  app: { key: null, length: 0, fork: 0 },
  runtimes: { bare: '1.24.3', pear: '2.2.6' }
}
```

### `Pear.reload()`
<a name="pear-reload"></a>

**REMOVED**

**REMOVED** Use `location.reload()` in Desktop apps. No reload in terminal apps.

### `Pear.updates(listener <Async Function|Function>) =>streamx.Readable`
<a name="pear-updates"></a>

**REMOVED**

**REMOVED** Use [`pear-updates`](https://github.com/holepunchto/pear-updates).

### `const update = await Pear.updated()`
<a name="pear-updated"></a>

**REMOVED**

**REMOVED** No option. Do not use.

### `Pear.wakeups(listener <Async Function|Function>) =>streamx.Readable`
<a name="pear-wakeups"></a>

**REMOVED**

**REMOVED** Use [`pear-wakeups`](https://github.com/holepunchto/pear-wakeups).

### `Pear.badge(count <Integer|null>) =>Promise<Boolean>`
<a name="pear-badge"></a>

**REMOVED**

**REMOVED** Use [`pear-electron ui.app.badge()`](https://github.com/holepunchto/pear-electron#const-success--await-uiappbadgecount-integernull-).

### `Pear.tray(options <Object>, listener <AsyncFunction|Function>) => Promise<untray()>`
<a name="pear-tray"></a>

**REMOVED**

**REMOVED** Use [`pear-electron ui.app.tray()`](https://github.com/holepunchto/pear-electron#const-untray--await-uiapptrayoptions-object-listener-function-).

### `const win = new Pear.Window(entry <String>, options<Object>)`
<a name="pear-window"></a>

**REMOVED**

**REMOVED** Use [`pear-electron ui.Window`](https://github.com/holepunchto/pear-electron#const-win--new-uiwindowentry-string-options-object-).

### `const view = new Pear.View(options <Object>)`
<a name="pear-view"></a>

**REMOVED**

**REMOVED** Use [`pear-electron ui.View`](https://github.com/holepunchto/pear-electron#const-view--new-uiviewoptions-object-).

## See also

- [`pear-runtime` reference](/pear/reference/pear/runtime)—the supported, non-deprecated way to access these capabilities from a Pear app.
- [Configuration](/pear/reference/pear/configuration)—the `package.json` fields read at the values surfaced in `Pear.config` and `Pear.app.*`.
- [Pear CLI](/pear/reference/pear/cli)—commands that drive the lifecycle these APIs reflect (`pear stage`, `pear seed`, `pear build`).
- [Modules](/pear/reference/modules/pear-modules)—the `pear-*` libraries that supply most application capabilities.
- [Bare runtime API](/bare/reference/bare/runtime)—the `global.Bare` object: process properties, lifecycle, addons, threads, IPC.
- [Bare modules](/bare/reference/modules/bare-modules)—runtime primitives the API builds on.
