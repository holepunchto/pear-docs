---
title: Getting started
description: Install the pear CLI, then ship an app you already have—stage, seed, and release it over the air.
docType: getting-started
product: pear
schemaType: TechArticle
---

import { Tabs, Tab } from 'fumadocs-ui/components/tabs'

This page gets the `pear` CLI onto your machine and points you at how to stage, seed, and release an app you already have. Don't have an app yet? [Build one on P2P](/p2p/getting-started)—a type-along tutorial that builds a peer-to-peer chat app from scratch, then reshapes it into the production `hello-pear-electron` scaffold this page's release steps assume.

## Install Pear

Get the [`pear`](/pear/reference/pear/cli) CLI from **[install.pears.com](https://install.pears.com)**:

<Tabs items={['curl (macOS / Linux)', 'irm (Windows)', 'npx', 'docker']}>
<Tab value="curl (macOS / Linux)">
```sh
curl https://install.pears.com/pear.sh | sh
```
</Tab>
<Tab value="irm (Windows)">
```powershell
irm https://install.pears.com/pear.ps1 | iex
```
</Tab>
<Tab value="npx">
```sh
npx pear
```
</Tab>
<Tab value="docker">
```sh
docker run -it --rm tetherto/pear
```
</Tab>
</Tabs>

Follow the `PATH` instructions the installer prints, then run `pear` directly. See [Install & upgrade](/pear/reference/pear/cli#install) for the npm-global alternative and upgrade details. The [`tetherto/pear`](https://hub.docker.com/r/tetherto/pear) image runs Pear inside Ubuntu with `pear` and `pear-install` ready to go—handy for trying the CLI without installing anything locally.

<include>../../_snippets/_interactive-menu-callout.mdx</include>

## Already have a built app? Ship it

The deep, step-by-step guides for every part of the release pipeline already exist as how-tos—this section signposts into them rather than repeating them:

- [Deploy your application](/pear/how-to/operate-an-app/manual-deployment/deployment)—`pear touch`, `pear stage`, `pear provision`, and `pear seed`, the core stage → provision → seed flow.
- [Build & package](/pear/how-to/operate-an-app/build-and-package)—desktop distributables, binaries, and app-store submissions.
- [CI/CD with GitHub Actions](/pear/how-to/operate-an-app/github-actions)—automate the same flow from CI.
- [Multisig](/pear/how-to/operate-an-app/multisig)—require multiple signers before a production release goes live.

For the concepts behind that flow—what staging, provisioning, and multisig actually do and why—see [Release pipeline](/pear/explanation/deployment-releasing-apps-p2p).

## Where to go next

- [Build a peer-to-peer chat](/p2p/getting-started)—the full type-along tutorial, if you're starting from nothing.
- [Command Line Interface (CLI)](/pear/reference/pear/cli)—every `pear` command and flag.
- [Manage installed applications](/pear/how-to/manage-installed-applications)—list installed Pear apps and reset their storage.
- [Troubleshoot common issues](/pear/how-to/troubleshooting)—fixes for common Pear and Bare development issues.
