---
title: "Build a multiwriter app with Autobase"
description: "Let several peers write to one shared, eventually consistent view with Autobase."
docType: how-to
product: p2p
schemaType: TechArticle
---

A [Hypercore](/p2p/reference/building-blocks/hypercore) has exactly one writer: the peer that holds its key pair. [Autobase](/p2p/reference/building-blocks/autobase) lifts that restriction. Several writers each append to their own local core, Autobase linearizes those cores into one causal order, and an `apply` handler replays that order into a **view**—a derived Hypercore every peer rebuilds identically.

This guide uses [Corestore](/p2p/reference/helpers/corestore) to hold the cores and [Hyperswarm](/p2p/reference/building-blocks/hyperswarm) to replicate them; see [Work with many Hypercores using Corestore](/p2p/how-to/store-and-replicate/work-with-many-hypercores-using-corestore) if those concepts are unfamiliar.

<include>../../../_snippets/_from-a-template-callout.mdx</include>

This guide consists of two applications:
* [`base-writer-app`](#create-the-base-writer-app) - creates the Autobase, appends messages, and grants write access to other peers.
* [`base-peer-app`](#create-the-base-peer-app) - joins the same Autobase by key, reads the view, and appends once it has been made a writer.

## How Autobase differs from a plain Hypercore

Three ideas carry the rest of this guide:

* **Writers, not one writer.** Every peer appends to its own local core. `base.local.key` identifies that core, and an existing writer grants access by naming it.
* **Linearization.** Nodes reference the nodes they saw, forming a causal graph. Autobase orders that graph so no node precedes a node it references, and so all peers converge on the same order.
* **The view is derived, never written directly.** You never append to `base.view`. You append to the base, and `apply` decides what lands in the view.

<Callout type="warn">
Autobase reorders nodes when new causal information arrives, undoing and reapplying the view. `open` and `apply` must therefore be deterministic and derive state only from the arguments they are given. A handler that reads a clock, a global, or the network will make peers diverge, and the undo path cannot roll those effects back.
</Callout>

## Create the base writer app

The `base-writer-app` creates a new Autobase, announces it on Hyperswarm, and appends messages to it. It also accepts an `add <writer-key>` command that grants another peer write access.

### Create the `base-writer-app` directory and add dependencies

Start the `base-writer-app` project with the following commands:

```sh example=autobase-multiwriter step=setup
mkdir base-writer-app
cd base-writer-app
npm init -y
npm pkg set type="module"
npm install autobase corestore hyperswarm b4a bare-pipe bare-process
```

This will install the following dependencies:

- [`autobase`](/p2p/reference/building-blocks/autobase): A module for building multiwriter views over Hypercores.
- [`bare-pipe`](https://www.npmjs.com/package/bare-pipe): A module for working with pipes, used here to read stdin.
- [`bare-process`](https://www.npmjs.com/package/bare-process): A module for working with processes.
- [`hyperswarm`](/p2p/reference/building-blocks/hyperswarm): A module for working with Hyperswarm.
- [`corestore`](/p2p/reference/helpers/corestore): A module for working with Corestore.
- [`b4a`](https://www.npmjs.com/package/b4a): A module for working with buffers.

### Add the `base-writer-app` logic

Create the `base-writer-app/index.js` file with the following content:

`open` builds the view from the store Autobase passes in (L13–L15), and `apply` reduces each batch of linearized nodes into it (L18–L33): a node carrying `addWriter` calls [`host.addWriter`](/p2p/reference/building-blocks/autobase#await-hostaddwriterkey--indexer--true-) instead of being appended (L24–L27), every other node is appended verbatim (L28). The whole body runs inside a `try`/`catch` (L23,L29–L31): `apply` replays this node on every peer forever, so a malformed one—a typo, or a node from a hostile peer—must be skipped, never thrown. Passing `null` as the bootstrap argument creates a new base rather than loading one (L36). A [Corestore](/p2p/reference/helpers/corestore) replication stream is attached to every [Hyperswarm](/p2p/reference/building-blocks/hyperswarm) connection (L39), and the key is printed only once the topic has been announced to the DHT (L43–L46)—advertising it earlier races a peer that looks the topic up immediately. The `update` event fires after each `apply` run, which is where the view is printed (L48). Reading stdin (L55–L64) splits input two ways: `add <writer-key>` appends an `addWriter` node (L58–L62), anything else appends a message (L63).

```javascript file=<rootDir>/examples/how-to/store-and-replicate/build-a-multiwriter-app-with-autobase/base-writer-app/index.js title="base-writer-app/index.js" lineNumbers {13-15,18-33,24-27,36,39,43-46,48,55-64} skip="example-import"
```

<Callout type="info">
`{ indexer: false }` adds the peer as a plain writer. Indexers additionally sign checkpoints, which is what advances [`base.signedLength`](/p2p/reference/building-blocks/autobase#basesignedlength) and lets peers who are behind catch up without replaying everything. Promote a writer to indexer only once you trust it to stay online—a quorum of indexers must be reachable for the signed length to advance.
</Callout>

{/* @harness example=autobase-multiwriter step=copy from=examples/how-to/store-and-replicate/build-a-multiwriter-app-with-autobase/base-writer-app/index.js to=base-writer-app/index.js */}

### Run the `base-writer-app`

In one terminal, run `base-writer-app` with `bare`.

```sh example=autobase-multiwriter step=run process=writer expect="base key:" capture-key="base key: ([0-9a-f]+)" timeout=45000
bare base-writer-app
```

It prints the base key and then the view, which starts with the one message the app appends on startup:

```text skip="illustrative-output"
base key: be92e2a474c62c9b910d0849b592229971ba1b30d6bfa02fa6f4d819919eab7c
view (1 entries):
  0 writer: first message
```

## Create the base peer app

The `base-peer-app` takes the base key as a command-line argument and joins the same Autobase. It has no write access at first, so it reads the view and waits.

### Create the `base-peer-app` directory and add dependencies

Create the `base-peer-app` project with the following commands:

```sh example=autobase-multiwriter step=setup
mkdir base-peer-app
cd base-peer-app
npm init -y
npm pkg set type="module"
npm install autobase corestore hyperswarm b4a bare-process
```

This will install the following dependencies:

- [`autobase`](/p2p/reference/building-blocks/autobase): A module for building multiwriter views over Hypercores.
- [`bare-process`](https://www.npmjs.com/package/bare-process): A module for working with processes.
- [`hyperswarm`](/p2p/reference/building-blocks/hyperswarm): A module for working with Hyperswarm.
- [`corestore`](/p2p/reference/helpers/corestore): A module for working with Corestore.
- [`b4a`](https://www.npmjs.com/package/b4a): A module for working with buffers.

### Add the `base-peer-app` logic

Create the `base-peer-app/index.js` file with the following content:

`open` and `apply` are copied unchanged from the writer app (L17–L37)—both peers must derive the same view from the same nodes, and skip the same malformed ones rather than crashing on them. The base key arrives as a command-line argument (L7–L9) and is passed as the bootstrap argument, which loads the existing base instead of creating one (L40). `base.local` is this peer's own writer core; its key is what the writer app has to add (L44). After joining the swarm (L46–L47), the peer waits for the topic lookup to settle before drawing any conclusion from an empty swarm (L52), pulls what is available with [`base.update()`](/p2p/reference/building-blocks/autobase#await-baseupdate) (L53), then checks [`base.writable`](/p2p/reference/building-blocks/autobase#basewritable) and, if it is still read-only, waits for the `writable` event before appending (L55–L62).

```javascript file=<rootDir>/examples/how-to/store-and-replicate/build-a-multiwriter-app-with-autobase/base-peer-app/index.js title="base-peer-app/index.js" lineNumbers {7-9,17-37,40,44,46-47,52,53,55-62} skip="example-import"
```

{/* @harness example=autobase-multiwriter step=copy from=examples/how-to/store-and-replicate/build-a-multiwriter-app-with-autobase/base-peer-app/index.js to=base-peer-app/index.js */}

### Run the `base-peer-app`

In another terminal, run the `base-peer-app` with `bare` and pass it the base key from the [`base-writer-app`](#run-the-base-writer-app).

```sh example=autobase-multiwriter step=run process=peer cmd="bare base-peer-app ${key}" expect="writer key:" capture-writer="writer key: ([0-9a-f]+)" timeout=45000
bare base-peer-app <SUPPLY THE BASE KEY HERE>
```

It prints its own writer key, replicates the view read-only, and waits:

```text skip="illustrative-output"
writer key: 8def73b0a3e7382cef887fabec4b5d5332cf3c28da640c912a90be447075e7dc
read-only, waiting to be added as a writer
```

<Callout type="info">
A peer that has not been added is a full reader: it replicates the base and rebuilds the view locally. Only appending is gated. If you want unknown peers to be able to write without a manual grant, use [optimistic appends](/p2p/reference/building-blocks/autobase#new-autobasestore-bootstrap-opts) instead—but validate every optimistic block inside `apply` before calling `host.ackWriter`.
</Callout>

## Grant write access

Copy the peer's writer key and, in the `base-writer-app` terminal, type:

```sh skip="manual-walkthrough"
add <SUPPLY THE WRITER KEY HERE>
```

{/* @harness example=autobase-multiwriter step=expect process=peer contains="read-only, waiting to be added as a writer" timeout=45000 */}
{/* @harness example=autobase-multiwriter step=send process=writer data="add ${writer}\n" delay=3000 */}

The writer app appends an `addWriter` node. That node replicates to the peer, whose own `apply` runs the same [`host.addWriter`](/p2p/reference/building-blocks/autobase#await-hostaddwriterkey--indexer--true-) call and reaches the same conclusion—the grant is part of the shared, replayable history, not a side channel. The peer's `writable` event then fires and it appends its message:

```text skip="illustrative-output"
now writable
view (2 entries):
  0 writer: first message
  1 peer: hello from the second writer
```

{/* @harness example=autobase-multiwriter step=expect process=peer contains="now writable" timeout=60000 */}

Within a moment the same two entries, in the same order, appear in the `base-writer-app` terminal. Both peers have converged on one view built from two writers.

{/* @harness example=autobase-multiwriter step=expect process=writer contains="hello from the second writer" timeout=60000 */}

<Callout type="tip">
Type more messages into either terminal and watch both views update. Entries settle into an order that both peers agree on—if the two peers append while disconnected, the causal graph forks and Autobase merges it once they reconnect, which can move an entry that had already been displayed.
</Callout>

## Where to go next

The view here is a raw Hypercore, which is the smallest thing that demonstrates linearization. Real apps usually open a [Hyperbee](/p2p/reference/building-blocks/hyperbee) in `open` instead, so `apply` can write keyed records and readers can query rather than scan. Key exchange is the other production gap: this guide passes keys through the terminal, whereas apps that pair over an invite use [`blind-pairing`](/p2p/how-to/connect-to-peers/host-multiple-rooms-in-one-chat-app) to hand the base key and the writer key over automatically.

## See also

- [Autobase reference](/p2p/reference/building-blocks/autobase)—full API, including optimistic appends, indexers, and fast-forward.
- [Build a multiwriter database with Autobee](/p2p/how-to/store-and-replicate/build-a-multiwriter-database-with-autobee)—the same flow with the view fixed to a Hyperbee, so peers can query instead of scan.
- [Host multiple rooms in one chat app](/p2p/how-to/connect-to-peers/host-multiple-rooms-in-one-chat-app)—Autobase with a Hyperbee-style view and invite-based pairing.
- [Share append-only databases with Hyperbee](/p2p/how-to/store-and-replicate/share-append-only-databases-with-hyperbee)—the single-writer database this guide's view can be upgraded to.
- [Work with many Hypercores using Corestore](/p2p/how-to/store-and-replicate/work-with-many-hypercores-using-corestore)—the Corestore patterns this guide builds on.
