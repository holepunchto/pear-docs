{
  "name": "pear-docs-qvac-service",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "QVAC-backed search + RAG + MCP service for the Pear docs site (PoC)",
  "scripts": {
    "build:index": "node --import tsx src/index-build.ts",
    "start": "node --import tsx src/server.ts",
    "dev": "node --import tsx --watch src/server.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "@qvac/sdk": "^0.14.0",
    "zod": "^3.25.76"
  }
}
