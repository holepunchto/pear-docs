node_modules/
.next/
next-env.d.ts
*.tsbuildinfo

# Vendored ONNX weights — fetched at build time by `npm run fetch:model`,
# ~34 MB of binary that would otherwise churn the repo.
models/

# The repo root ignores `data/` (the full QVAC service regenerates its index on
# the deploy host). Here the index is a COMMITTED build artifact: building it
# needs a docs checkout, and the container has none. Re-include it.
!data/
!data/index.json
!data/pages.json
